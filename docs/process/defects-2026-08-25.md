# The defects of 2026-08-25: eight hunts, eleven defects, every one in the day's own code

Written the night they were found, per the rule that a day passing
roughly ten defects gets a ledger and that the OWES column is the
record. This day did not reach ten, and the ledger exists anyway
because of the DISTRIBUTION: eight hunts in three rounds, ELEVEN
confirmed defects, and every one of them inside code written within
hours — NINE of the eleven inside repairs for defects the same day
had found, and four of those inside repairs made in the hour before.

THE CURVE DID NOT FLATTEN. Round one: four hunts, four defects. Round
two: four hunts, three defects, all created by round one's repairs.
Round three: two hunts, four defects, three of them created by round
two's repairs. A round aimed at last round's fixes has found
something every single time, which is the strongest argument this
project has yet produced for not shipping while the hunts are still
running.

THE SECOND ROUND IS THE ARGUMENT FOR THE SECOND ROUND. Four hunts
found four defects; the three hunts aimed at those four repairs found
three more, and one of them had been examined and ruled BENIGN by the
first round — it went live because a fix removed the accident that
was hiding it.

The ratio is the finding, and it matches this project's own history
exactly (2026-08-16: eleven of fourteen within hours; 2026-08-20:
four of five in that day's code). Aiming a round of hunts at fresh
work is not a precaution. It is where the defects are.

## The ledger

| # | what a user lost | where it lives | found by | owes |
|---|---|---|---|---|
| 1 | Remove the region layer from the project, choose another, Generate: the GeoPackage holding the previous dataset's map is OVERWRITTEN and its map replaced in the project, with nothing said. Every change-of-dataset protection skipped | `dialog._on_layer_changed`: `switched` asked whether the WATCHED LAYER OBJECT had changed, and `_layers_removed` nulls it for C++ hygiene. Second fault under it: removing the layer empties the combo, so `_swap_dataset_memory` ran with None, banked the outgoing dataset and NULLED the identity before the next one arrived | TWO hunts independently — the boundary hunt (asymmetry) and the switch-acts hunt (one-boundary) | **FIXED** — `switched` asks about the DATASET IN FORCE (`_memory_layer_id`); the swap returns on an empty chooser. Test + 2 entries |
| 2 | A user who opens the plugin BEFORE loading data has that dataset's hand-picked colours — keyed by its own value strings — applied to the NEXT dataset they choose, drawn on its map and written into its saved project, while the dataset they were chosen for keeps nothing | `dialog._settle_layer_choice` rebuilt for a late-landing layer and never swapped banks, so `_memory_layer_id` stayed None and the pre-identity MERGE branch filed the first dataset's records under the second | the banks hunt (two-stores) | **FIXED** — the settle binds the dataset. Test + 1 entry |
| 3 | A column deleted in QGIS takes its element to a surviving column and the RAMP and CLASS COUNT ride along, so a colour scale chosen for one variable's semantics paints an unrelated one — against ruling 3's "a dropped column takes its whole scheme" | `dialog._adapt_to_the_layer` (door three) edits the row IN PLACE and never rebuilds, because the race-among-choosers rule forbids it. Shelving the RECORDS was not enough: `_assignments` reads the WIDGETS | the shelf hunt (write-only) | **FIXED** — door three moves the ramp combo, the class spinner and the Reverse switch too. Test + 1 entry |
| 4 | Import a QML for an element on dataset A, work on B, return to A: the element comes home to its own column wearing B's file, and A's is gone from the session — the map draws the wrong colours | `dialog._refresh_table` restored the class source from the shelf and then wrote `prev["class_choice"]` straight back over it, unguarded by the reset flag | the shelf hunt (same hunt, second finding) | **FIXED** — a restored class source outranks the previous table. Test + 1 entry |

| 5 | Reopen a project holding maps of TWO datasets, choose the first, Generate: the SECOND dataset's finished map is deleted and replaced by a second copy of the first. Two maps become one dataset twice | `_newest_output_group` takes the newest group and adoption runs at CONSTRUCTION, before the user has chosen anything — harmless while a session held one group, a loss the moment ruling 2 made a second group the ordinary result of a demo. Nothing on a landed layer said which dataset made it | the losses hunt (asymmetry), which started from what a user loses rather than from the code | **FIXED** — every output layer carries `weavingspace_region`, and a landing refuses to write into a group whose stamps name another dataset. Test + 2 entries |
| 6 | A user who picks a RAMP and leaves the derived style alone loses it twice when their column is deleted in QGIS: it paints the replacement column, then flips to a default at the next unrelated rebuild | the door-three widget reset written an hour earlier sat inside the `elif ... touched` branch, while `_shelve_scheme` pops the records for EVERY dropped row | the reset-set hunt (write-only) | **FIXED** — the reset runs for every row whose column has gone. Test + 1 entry |
| 7 | A scheme set aside in the project you CLOSED reappears in the project you OPEN: an element redraws a column from the old project, wearing its ramp, and writes it into the GeoPackage | `_forget_the_last_project`'s clear list omitted `_scheme_memory`. It was harmless only by ACCIDENT — the bank swap used to run with None on a replacement and empty the view in passing — and tonight's empty-chooser fix removed the accident | the identity hunt (two-stores) | **FIXED** — the shelf joins the clear list. Test + 1 entry |

| 8 | A derived categorical style rode its deleted column onto the numeric column its element landed on, and Generate drew 37 categories — one legend line per value | `_adapt_to_the_layer`: the mode re-derivation was gated on `touched` while the widget reset beside it was not. Two halves of one block asking different questions | the door-three hunt (one-boundary), reading the MAP | **FIXED** — both halves ask about the COLUMN. Covered by the ramp test + entry |
| 9 | An element coming home to a column that returned was stripped a line after the shelf handed its scheme back: nine classes of YlOrBr came home as five of Reds, then read YlOrBr at nine one rebuild later — three maps from one set of choices | the widget reset widened an hour earlier ran even when `restored` was not None, and the restore branch moved the MODE without the ramp, count and Reverse controls | the door-three hunt (same hunt, second finding) | **FIXED** — the reset skips a restored row, and the restore moves the controls too. Covered by the shelf round-trip test |
| 10 | Keeping a previous result protected its GROUP and not its FILE: a run wrote into the kept result's GeoPackage, rewriting tables with the new dataset's columns and DROPPING others, leaving the kept group drawing dead layers under its own names | the guard asked only `opt_new_group.isChecked()`, while three things now keep a result | the output-group hunt (two-stores), reading the GeoPackage with OGR | **FIXED** — the guard asks the question the group asks. Test + 1 entry |
| 11 | Reopening a project under an OPEN panel made every hand-picked colour and pinned bound in it unreachable: the editor offered the ramp default and the next Generate painted over them and wiped the stamp from the .qgz and the GeoPackage | on a project read the chooser binds for an instant to one of the plugin's OWN OUTPUT LAYERS before adoption runs, so the identity bound to that and the adopted stamps were banked where nothing could reach them | the save-and-reopen hunt (asymmetry), with a freshly built dialog as its control | **FIXED** — our own output never binds the identity, nor does a layer touched mid-read. Test + 1 entry |

Nothing owes. Eleven tests registered, thirteen catalogue entries judged,
and every fix verified by driving the product — three of the four by
a SECOND route as well: the GeoPackage read with OGR rather than
QGIS, the saved project read as raw bytes, and the map's rendered
colours.

## What the shape of these eleven says

**THREE OF THE ELEVEN ARE THE SAME QUESTION ASKED IN THREE PLACES**,
and it is the night's most useful pattern: when a branch keys on the
ABSENCE of a record, ask what else can produce that absence, and what
else can BIND the thing you are keyed on. Row 2 was "the plugin
opened before the data". Row 11 was "the chooser touches our own
output during a project read". Row 1 was "a removal nulls the watched
layer". Same shape, three doors, found by three different hunts.

**A CLEANUP THAT WORKS BY SIDE EFFECT IS A CLEANUP NOBODY HAS
WRITTEN DOWN.** Row 7 is the night's best lesson. The clear list was
missing the shelf from the moment the shelf existed, and the first
round LOOKED at that omission and ruled it benign, correctly, because
something else happened to empty the record. The fix that removed the
accident was three lines long and nowhere near the clear list. So: a
list that is wrong but masked is still wrong, and the day the mask
moves is the day it costs something. When you find a record emptied
by something other than the code that owns it, write the entry
anyway.

**A FIX MADE UNDER A RULING CAN VIOLATE THE RULING BESIDE IT.** Row 5
is ruling 2 -- keep the previous dataset's map -- creating the very
loss it exists to prevent, because adoption elsewhere assumed one
group. When a ruling starts producing a SECOND artefact by default,
re-drive every path that used to assume one.

**THREE OF THE FIRST FOUR WERE INSIDE THE DAY'S OWN REPAIRS**, and the
fourth was inside the day's own feature. Defect 2 lived inside the
orphan-merge fix written a few hours earlier — a branch keyed on "no
identity yet", written for ONE route and firing for TWO. Defects 3
and 4 lived inside the whole-scheme reset. Defect 1 was in the
boundary the full suite had drawn that afternoon.

**A FIX THAT KEYS ON THE ABSENCE OF A RECORD MUST ENUMERATE EVERY
ROUTE TO THAT ABSENCE.** That is defect 2 stated generally, and it is
the most transferable line here. `old_id is None` meant "adoption
wrote these before the chooser settled". It also meant "the plugin
opened before the data", which nobody had listed.

**TWO HUNTS CONVERGING IS WORTH MORE THAN ONE HUNT AGREEING WITH
ITSELF.** Defect 1 was found from two directions with different
briefs, different probes and different second routes, and the second
hunt added what the first could not see: the layer COUNT decides
which of two code paths runs, so the same act answered differently in
a project with two polygon layers and one with three.

**A RECORD IS NOT THE THING A USER SEES.** Defect 3 was invisible to
every record-level check because `_assignments` reads widgets. The
matrix, the tests and the entries all agreed the records were right;
the map disagreed. Where a path edits the table IN PLACE, the widgets
ARE the state.

## What was ruled out, so nobody spends the time again

The banks hunt ruled out `_scheme_memory` missing from
`_forget_the_last_project` (benign: the swap-to-None empties the view
first, dump-verified) and `_quant_colours` being unbanked (an
asymmetry with no harm statable under ruling 8's value-laden
wording). The shelf hunt ruled out cross-element bleed, rows left on
"---", and one dirty oracle of its own (a second dialog adopting an
already-generated layer). The switch-acts hunt ruled out an
unofferable element count — the catalogue's extension loop fills
2..26 — and notices being swallowed.

The SECOND round ruled out more, and the list matters because these
are the places three careful hunts looked and found nothing: the
mirror risk of the reset flag (a style, ramp, count or Reverse chosen
AFTER a switch all survive it), double-arming that flag (idempotent),
door three firing on a dataset switch (it does not), the settle route
skipping `_begin_new_dataset` (measured, correct), plain project
replacement, and A-B-A and A-B-C shelf journeys.

## AND WHAT I GOT WRONG WHILE FIXING THEM, since the pattern is the
## useful part

**MY FIRST FIX FOR ROW 5 HAD A FAULT OF ITS OWN**, caught by driving
rather than by reading: the mismatch check referenced `region_source`
eighteen lines before it was assigned — an UnboundLocalError inside a
Qt slot, swallowed, which showed up only as a landing quietly reusing
the previous group. The rule this project already carries would have
caught it: assert the postcondition, not just the anchor.

**AND THE FIRST FIX FOR ROW 1 DID NOT TAKE**, because a second fault
sat under it. Correcting `switched` to ask about the dataset in force
left the route broken, since removing the layer empties the chooser
and the bank swap then banked the outgoing dataset and nulled the
identity before the next one arrived. Two faults, one route, and the
second hid the first — which is why the probe, not the reasoning, is
what confirmed each fix.

---

# The same day, later: building the group-unit rulings

Eight more defects came out of building what the grilling settled,
and the distribution is the same one this file already argues about.
THREE WERE MINE, made that evening and found by the tests written
beside them within the hour. THREE WERE PRE-EXISTING REDS on this
unpushed branch, which nothing had run since they arrived. TWO WERE
HOLES IN THE CATALOGUE, found by re-judging entries the day's changes
had moved under.

## The ledger

| # | What a user would have lost | Where | Arrived | Found by | OWES |
| --- | --- | --- | --- | --- | --- |
| 1 | A landing was followed by the dialog undoing whatever had changed since: the combo re-emits `layerChanged` on every project change, so the group's record was re-applied over the session. A test swapping two elements' variables found the swap gone and one element drawing a single symbol. | `_bind_group_to_dataset` | that evening | `test_metamorphic_variable_permutation` | test + entry |
| 2 | Removing the region layer made the dialog let go of the map it had already made, leaving the layers unclaimed in the project so the next Generate built a rival beside them. An empty chooser is not a dataset — a rule this dialog had already settled once for the memory banks. | `_bind_group_to_dataset` | that evening | `test_the_region_layer_goes_while_the_quant_editor_is_open` and one other | test + entry |
| 3 | Six reopen journeys went red at once: a MEMORY layer's `source()` carries a uid that changes across a save, so a map made from one comes back stamped with a string matching nothing, and the dialog let go of the group it had just adopted. `_landed_this_session` also outlived the project it was about. | `_bind_group_to_dataset`, `_forget_the_last_project` | that evening | the full suite | test + entry |
| 4 | `weavingspace_region` had been shipping undocumented since 2026-08-24, so the test policing what output layers carry was RED on a branch nothing had run. | `test_the_output_carries_no_working_state` | 2026-08-24 | this sweep, confirmed against the commit before it | prose |
| 5 | The success branch of the case-pair test read its layers AFTER `_temp_dir` had released them — harmless only because the branch was never taken, and taken the moment trimming let both spellings through. | `test_attribute_names_survive_the_round_trip` | long-standing | this sweep | test |
| 6 | A recovery — a project whose region file has moved, then pointing at live data — built a rival group instead of replacing the one it had adopted, contradicting a ruling of 2026-08-21 that the guard of the 24th did not know about. | `_add_output_layers`'s `theirs` term | 2026-08-24 | the full suite, confirmed red at `7965916` | test + entry |
| 7 | A region read back out of one of our own GeoPackages offered `weavingspace_fid`, the plugin's own primary key, as a mappable variable: the default picker took it, the run named a table for it, and the write failed outright — 16 of 61 features and the whole landing lost. | `_layer_fields` | long-standing, reachable once a map could be resumed | the resume test's fifth leg | test + entry |
| 8 | Trimming made a variable PERMUTATION a geometry change, and the signature carried only the SET of mapped variables — invariant under a permutation. Re-seeding element a onto b's column found no field and QGIS handed back a single symbol. | `_geometry_signature` | that evening, with ruling 6 | `test_metamorphic_variable_permutation` | test + entry |

## The two catalogue holes, which are the more interesting half

**`icon-mode-counts-its-icons` COULD NEVER HAVE CAUGHT ITS MUTATION.**
Its test ran the live-update leg first, which drew the map; the
explicit Generate that followed found nothing changed, took its fast
path, and never reached the size gate at all. A live run also skips
the band question by design. So a guard that mistook icon mode for a
tiling was invisible to the test written to catch exactly that. Found
by re-judging the entry after the ceiling became a question — the
legs are the other way round now, and it catches.

**`the-spacing-advice-keeps-its-magnitude` NAMED THE WRONG TEST.** It
mutates what the DIALOG prints; the test it named asks
`spacing_in_words` directly and reads no message at all. It could
only ever survive. Re-aimed at the boundary test, which parses the
number out of the sentence a user is shown.

THE SHAPE BOTH SHARE is one this file's earlier half already names:
an entry reports `caught` or `SURVIVED` about a PAIRING, and a
pairing can be wrong in two ways — the test cannot reach the
behaviour, or the test never looks at it. Neither shows up while the
entry is green, and both showed up the moment the code underneath
moved. When a change lands near a catalogue entry, re-judge it rather
than re-anchoring it and moving on: re-anchoring keeps an OLD claim
true and never states the new one.

## And one survivor that was neither

`keeping-a-result-keeps-its-file` SURVIVED once the output-group
chooser existed, and it was not a weak test. "Create new" is a FOURTH
way to keep a previous result, and the only one where the group's own
region stamps cannot answer — they name the current dataset, so the
arming is all that stands between the run and the kept file. The test
gained that route, and the entry catches again.

`a-switch-detaches-the-group` went the other way. Re-aimed at the
binding's own answer for a dataset with no group, it still survived,
so the redundancy question was put the way this project's rules ask:
break every route AT ONCE. THREE mechanisms now keep a change of
dataset out of the previous dataset's group, each separately guarded
and separately caught, and no single line owns the rule they jointly
implement. The entry was deleted with its reasoning rather than kept
as one that can only ever be red.

---

# And the hunt round that closed the day: eleven CLAIMS, none reproduced

Four hunts were pointed at the group-unit build's own fresh code, in
the four directions this project's record says pay: asymmetry and
twins, two stores of one fact, one boundary but not another, and
backwards from harm. They returned ELEVEN claims between them.

**NOT ONE IS IN THIS LEDGER'S TABLE, and that is deliberate.** A
hunt's claim is believed only once it has been reproduced by a route
the hunt did not use; the reproduction is the expensive half and it
has not been done. They are recorded in `dev/state-of-play.md` and in
the work list, where they are owed work rather than findings. Writing
them here as defects would be this file claiming a certainty nobody
has earned — the same error as a site named by reading rather than by
measuring.

WHAT THE ROUND DID CONFIRM, without reproducing anything: the
distribution is again what this file has argued twice already. Every
claim is against code written the same day, and at least two are
against REPAIRS made hours earlier — the in-flight guard added to one
door of three, and the record whose two halves were split for good
reasons that nobody then checked field by field.

**TWO HUNTS FOUND THE SAME THING BY DIFFERENT ROUTES**, which is the
strongest signal this method produces: embedding the source into a
GeoPackage may defeat the very gate that would let its pins and
hand-picked colours back out, because the embedded region loads under
a source string that can never equal the recorded one. One hunt
reached it from "one boundary but not another" and the other from
"asymmetry and twins".

**AND ONE DIRECTION CAME BACK CLEAN**, which is worth recording
because a hunt that finds nothing is a result: both size-guard
questions default to No, so a stray Return cannot start a run that
may take the machine.

THE SEQUEL THIS FILE ALREADY PREDICTS: a round aimed at the repairs
for these will find more. It has every time — four rounds, four
times. Nothing here should ship while the hunts are still returning.
