# Defects found on 2026-09-02

The 24-bug campaign's ledger. The campaign's rules are in
`HUNT-RECORD.md` under "THE 24-BUG CAMPAIGN"; what matters here is
what a person would have lost, where it lives, when it arrived, and
what each row still OWES. A row is CLOSED only when the claim was
reproduced BY A ROUTE THE HUNT DID NOT USE, repaired, guarded by a
registered test, proved by a catalogue entry judged `caught`, and
committed. `--` in the OWES column would mean the fix rests on its
hunt's own reproduction, which is not what this project calls fixed.

FOUR OF THE EIGHT HUNTS FOUND NOTHING, and that is recorded here
rather than left out: the consistency sweep (19 acts, 3,903
comparisons, 0 disagreements, with all five negative controls firing),
the re-vendor differential (56 designs, identical class labels and
identical ground per label), and the two directions whose findings
were their own instruments. A round that can only look good is a round
nobody should read.

## Closed

| # | What a person loses | Where | Since | Guarded by |
| --- | --- | --- | ---: | --- |
| 1 | A Cancel pressed while the map is being WRITTEN does nothing at all: every table is written, the layers are repointed at the new file, and the person is told "Saved" about the save they had just stopped. | `dialog._hold_until_the_save_lands`, the clearing of `_save_cancelled` | 2026-09-01, `2390d81`, the commit that gave the writer its stop callback | `test_a_cancel_during_the_write_stops_the_write`, entry `a-cancel-mid-write-is-left-for-the-writer-to-read` |
| 2 | A drag on the push rail -- the tab's own handle for `push_vertex` -- previews under the pointer and is thrown away on release, with nothing said and nothing in the change list. | `topology_tab._drag_moved`, which had no branch for it | 2026-08-31, `e80f10d`, the commit that gave the push a rail on the drawing | `test_every_handle_a_drag_offers_commits_what_it_previewed`, entry `a-push-drag-reaches-the-record` |
| 3 | A Cancel arriving too late to stop anything -- after the tables are in, during the repointing or the styling -- destroys the person's NEXT save instead: it writes nothing, leaves an unreadable stub at the path they named, and says the save was stopped. | `dialog._save_the_map`, the `finally` that ends the act | 2026-09-02, `e765039`, this same day's repair for row 1 | `test_a_cancel_after_the_tables_does_not_poison_the_next_save`, entry `a-cancelled-save-does-not-outlive-its-own-act` |
| 4 | A stub left by a cancelled or failed first save makes the file nobody's for the rest of the session, so every remover scoped to our own files is off: shrink the design and the dropped elements' tables, columns and values travel in the file a colleague receives. | `dialog._save_the_map`, `existed` asking the file's SIZE | 2026-09-01, `2390d81`, with the mid-write cancel that can leave a stub | `test_a_stub_from_a_stopped_save_is_not_somebody_elses_file`, entry `an-empty-file-is-nobodys` |
| 5 | A Save pressed while a topology build is still coming writes no motif and records `topology_written: False`; the same press a second later writes the unit and its dual. One act, two files, on a race nobody can see. | `dialog._a_topology_is_owed`, which asked the file and not the work already running | 2026-09-01, `6537978`, when the build moved off the thread that paints | `test_a_save_waits_for_a_build_already_coming`, entry — the outcome is a differential rather than a line |
| 6 | Closing the window or quitting QGIS during a write freezes the waiting window for the whole 180-second ceiling with its bar on "preparing to save", and the only control on it throws the map away. | `dialog._hold_until_the_save_lands`, waiting on a flag an OUTER frame owns | 2026-09-01, `2390d81`, the hold's own commit | `test_a_close_during_a_write_does_not_wait_for_the_write`, entry `a-hold-declines-a-write-it-cannot-serve` |
| 7 | A save whose COMMIT is refused reports "Saved", writes nothing, and repoints every element layer at tables the failed commit never created -- so the map on screen empties and the tiles in memory go with it. | `bridge.write_gpkg_layers`, the commit's return value never read | 2026-09-01, `e804be9`, the single-OGR-session rewrite | `test_a_save_whose_commit_fails_says_so_and_keeps_the_map`, entry `a-refused-commit-is-not-a-save` |
| 8 | Answering "Close" to "Save it before closing?" while the map is being WRITTEN saves it anyway, over the file the person has just declined to write, and tells them nothing was saved. Every element layer is repointed at that file, so the map in their project is backed by it. | `dialog.closeEvent`, whose Close arm answered a promise where a WRITE was outstanding | 2026-09-01, `2390d81`, the commit that added the question | `test_a_close_that_declines_the_save_stops_the_write`, entry `a-close-that-declines-the-save-stops-it` |
| 9 | "Cancel the save" pressed once the tables are in -- during the styling or the repointing, 13.0s of a 256-element save -- cannot be served, so the map is written and the person is told it was not, beside the save's own report that it was. | `dialog._hold_until_the_save_lands`, reading `_saving_now` after the wait rather than at the press | 2026-09-01, `2390d81`, the waiting window's own commit | `test_a_cancel_the_writer_could_not_serve_says_nothing`, entry `the-hold-does-not-report-a-cancel-it-could-not-serve` |
| 10 | Pressing Load on a saved map whose own layers are still in the project -- the plugin or the project reopened -- leaves the output path aimed at that file, so pointing the region chooser at other data and pressing Generate and Save replaces the opened map inside its own file, with no question and nothing said. | `dialog._resume_from_gpkg`, the already-open branch, which never recorded the map as this session's work | 2026-08-28, `8db74ea`, the repair that gave the fresh branch that record and not this one | `test_both_load_doors_count_the_map_as_this_sessions_work`, entry `both-load-doors-count-the-map-as-work` |
| 11 | A filter set on an element layer in QGIS -- the Query Builder in Layer Properties -- makes the next Save delete every tile it hides from the saved GeoPackage, permanently, under the word "Saved"; across a re-tile, which carries the filter onto the new layer, it empties that element's table outright. | `dialog._save_the_map`, whose writer iterates `getFeatures()`, and the already-saved question comparing a source's whole tail | 2026-08-27, `0ecdf7c`, the commit that added both the skip and the subset capture | `test_a_filter_is_a_view_and_is_never_written_into_the_file`, entry `a-filter-is-a-view-not-the-map` |
| 12 | Opening a self-contained GeoPackage somebody sent you leaves its layers stamped with the SENDER'S region, so the dialog lets go of the map it has just opened: the next Generate builds a rival group beside it and the next Save writes that rival into the opened map's own tables, with nothing said. | `dialog._resume_from_gpkg`, both branches, which stamp the group's record and never the layers `_our_groups` asks | 2026-08-28, `8db74ea`, when a resume first counted as this session's work and the binding could be reached at all | `test_a_resumed_map_tells_its_layers_which_region_it_found`, entry `a-resumed-map-stamps-its-own-layers` |
| 13 | A person who draws a map, then opens a saved one with Load, then presses Save gets a file whose element table is NAMED for the drawn map's variable while holding the opened map's data -- and the table the sender wrote is dropped. The file contradicts its own record. | `dialog._save_the_map`, taking names from `_element_tables` and asking the layer's own source only where that record was silent | 2026-08-27, `0ecdf7c`, which added the record, with the witness beside it covering only the empty case | `test_a_save_after_a_load_names_the_opened_maps_own_tables`, entry `the-witness-answers-for-every-element` |
| 14 | Deleting one element's row in the layers panel and pressing Save into a GeoPackage a colleague has since changed says "the file holds 3 of 4 elements" over a file holding TWO, so the sender sends a map short of an element they were told was in it. | `dialog._save_the_map`, `kept = len(order) - len(left_out)` | 2026-08-28, `01a121d`, the commit that added the sentence, its arithmetic unchanged since | `test_the_saves_count_is_asked_of_the_file`, entry `the-saves-count-is-asked-of-the-file` |
| 15 | A first save that was cancelled or failed leaves a GeoPackage holding no layer, so an ordinary second save at the same path -- in a session that does not remember the file -- is warned that it "was not written by this map" and will "replace the tables this map needs", of which the file has none. The safe button is No, so a stray Return declines the save. | `dialog._may_overwrite`, `os.path.getsize(path) == 0` | 2026-08-27, `0ecdf7c`, the commit that made saving a positive act and wrote this question | `test_the_overwrite_question_is_about_what_a_file_holds`, entry `an-empty-shell-is-not-somebody-elses-file` |
| 16 | Glancing at another dataset in another coordinate system and coming back to your map through the group chooser leaves the group's record naming YOUR region beside THEIR system; the Save carries both into the file, and a recipient's resume forces that system onto a region which declares its own correctly, so the region lands a long way from its own tiles. | `dialog._stamp_working_state`, carrying the design and the two edges while `region_crs` fell through to the live reading | 2026-08-28, `6ad7b13`, the commit that added `region_crs` and gave it to the capture and the Save's carry but not to the stamp's | `test_a_records_region_and_its_crs_name_one_dataset`, entry `a-regions-system-travels-with-the-region` |
| 17 | The Load button is live for the whole of every write, so a click delivered by the save's OWN pump repoints every element layer mid-loop: the other map's tiles go into this file's tables and the record written afterwards names the OTHER FILE as this one's output path, so a resume from the saved file points at a file its owner may not have and their next Save writes over that other map. | `dialog._save_the_map` and `_resume_from_gpkg`, which took Save and Generate down by hand and never Load | 2026-08-27, `0ecdf7c`, the commit that added the Load button, on the row beneath a sentence naming two controls | `test_no_acting_control_is_live_while_a_save_writes`, entry `a-pumped-act-takes-every-acting-control-down` |
| 18 | Reopening the plugin over somebody's self-contained GeoPackage and pressing Save removes the copy of the data they deliberately included, so the file can no longer be redrawn by anybody and the map's own layers are left reading a table that has gone. | `dialog._recover_the_source`, the only writer of `_embedded_when_resumed`, which the two RESUME branches call and adoption does not | 2026-08-28, `132713b`, the repair that gave the resume door that memory and not its twin | `test_both_doors_remember_the_file_carried_the_data`, entry `adoption-remembers-what-the-file-arrived-with` |
| 19 | A topology edit made in one project is replayed onto the same design in the NEXT project, reshaping its map and riding into its GeoPackage: the record that shelves edits is not emptied when QGIS clears the project, and neither is the tab that lists them. | `dialog._forget_the_last_project`, whose list omitted `_topology_shelf` | 2026-08-30, `5570115`, the commit that added the shelf and did not join it to that list | `test_a_new_project_leaves_no_topology_edits_behind`, entry `a-new-project-empties-the-topology-shelf` |
| 20 | Drawing a map, opening a colleague's with Load, and saving it somewhere ELSE gives a copy whose element tables are named for the variables of the map this session drew while holding the opened map's data, so the file contradicts its own record and anybody reading it without the plugin reads the wrong variable off the name. | `dialog._detach_from_the_group`, whose clear list omitted `_element_tables` | 2026-08-27, `0ecdf7c`, which added the record, with the witness beside it unable to answer on a Save As | `test_a_save_as_names_the_tables_of_the_map_it_saves`, entry `a-taken-over-map-forgets-the-drawn-ones-tables` |
| 21 | Choosing a class in the Topology tab's chooser or tick list moves the selection an edit is recorded against while the three drag handles stay on the thing last clicked -- so a drag's parameter, which is a polar coordinate about the handle's own edge, is measured on one edge and recorded against another. | `topology_tab.set_chosen`; `_chosen_thing` had one writer, in `mousePressEvent` | 2026-09-01, `764634f`, when multi-class selection gave the panel an owner every other reader asks and the handles went on asking the click | `test_the_handles_follow_a_class_chosen_from_the_list`, entry `the-handles-follow-the-class-that-is-chosen` |
| 22 | Opening a map whose data cannot be found -- a colleague's GeoPackage, or your own after the region has moved -- and pressing Save records THEIR region beside the coordinate system of whatever unrelated layer your project happened to hold, and a later resume forces that system onto a region which declares its own correctly, landing it thousands of kilometres from its own tiles. | `dialog._resume_from_gpkg`, both stamps, handing over `region` and `output_path` while `region_crs` fell through to the live reading | 2026-08-29, `878f983`, the commit that gave the resume its launch state, with `region_crs` two days old and not in it | `test_a_resume_records_the_system_it_landed_on`, entry `a-resume-records-the-system-it-landed-on` |
| 23 | THE REPAIR FOR ROW 22'S OWN DEFECT, found within the hour by the hunt aimed at it. Opening a saved map whose layers are still in the project, where the file's record is older than `region_crs` or carries what row 22 wrote before it was mended, stamps that absence or that wrong value over the group's own record -- which the landing that DREW the map wrote -- so a later resume declares the region into a system 9,813 km from its own tiles. | `dialog._crs_the_resume_landed_on`, whose answer went into the launch state, and a launch state beats `_stamp_working_state`'s carry | 2026-09-02, `367248c`, the repair for row 22, forty minutes earlier | `test_a_resume_records_the_system_it_landed_on`'s third and fourth arms, entry `the-groups-own-record-outranks-the-files` |
| 24 | Ticking "Map the dual instead", shaping one of the dual's edges and unticking the box leaves the ORIGINAL design bent on a different edge -- silently, with nothing in the change list to say so, and that is the map drawn and the map saved. The reverse too: edits made on a design are replayed onto its dual. | `topology_edits.shelf_key`, the family and the element count, which "Map the dual instead" moves neither of; and `dialog._topology_stamp`, whose landing check could not tell one build from the other | 2026-09-01, `071ad0f`, the commit that made the dual a design in its own right without widening the keys | `test_an_edit_on_the_dual_is_not_an_edit_on_the_design`, entry `an-edit-belongs-to-the-design-it-was-made-on` |
| 25 | RULING, not a defect: closing the plugin panel while a map was being written STOPPED the save outright, on an answer given about a promise rather than about the write. | `dialog.closeEvent`, where one question covered two states -- a promise not yet kept and the keeping of it | 2026-09-02, the repair for row 5 earlier the same day, which taught that branch to stop a write | `test_closing_during_a_write_asks_before_interrupting_it` (three arms), entry `closing-asks-before-it-interrupts-a-save` |
| 26 | Pressing Load while a save stood PROMISED threw the promise away: the map the person had asked to save was never written, silently, after the plugin had told them it would be saved once redrawn. | `_resume_from_gpkg` had no guard for a promise, and `_honour_a_queued_save` re-reads the output chooser at the write -- which a Load moves | 2026-08-29, the ruling that made a deferred press a promise; the Load door arrived 2026-08-27 and never learned about it | `test_a_load_waits_for_a_promised_save` (both halves), entries `a-load-waits-for-a-promised-save` and `a-deferred-load-is-still-performed` |

### 1. The cancel that was cleared under the writer

**The mechanism, and why it could not be seen by reading the line.**
`write_gpkg_layers` asks `should_stop` between tables and answers True
with a rollback, so the flag is the whole of the mechanism. The hold
that offers the button cleared that flag on its way out, under a
comment giving its reason: "a write that DID read it has already
returned by the time this line runs -- the pump that delivered the
click sits inside that write". That is TRUE of the ordinary deferred
press and FALSE when the close or the quit is delivered BY THE WRITE'S
OWN PUMP, which turns the event loop once per element behind its
progress bar. The hold then runs in a frame NESTED BELOW the writer,
so the clearing happened while the writer was still suspended above
it.

**Measured at the seam rather than deduced.**
`tools/probes/what_a_cancel_during_the_write_does.py` drives three
arms in one process and wraps the `should_stop` argument the dialog
hands the real writer, so what is recorded is what the PRODUCT was
told:

    CONTROL   nothing interrupts     four tables, "Saved to control.gpkg."
    WRITING   cancel while writing   four tables, "Saved to writing.gpkg.",
                                     should_stop asked 4 times, told
                                     [False, False, False, False]

    after the repair                 no tables, "The save was stopped,
                                     so the map was not written.",
                                     should_stop told [True]

**Two doors, two readers.** The hunt drove QGIS's QUIT through the
event filter and read the file with stdlib sqlite3; the verification
drove the PLUGIN WINDOW's own close, which reaches the same hold from
`closeEvent` after its question is answered Save, and read the file
through OGR.

**The repair is one condition**, and the reason is written at the
line: the clearing runs only where no write is in progress. A write
already under way is left to its own branch, which clears the flag,
rolls the transaction back and says the map was not written. Both
directions are asserted in the guard, because the obvious repair --
never clearing -- passes everything except the person's NEXT save,
which is the fault this same flag caused on 2026-09-01.

### 2. The handle the table did not grow for

`_drag_moved` answers one question -- did this gesture ask for
anything -- and it answers it PER MANIPULATION, because each has its
own idea of nothing: zero travel for a nudge, half a degree for a
rotation, one per cent for a scale. It was written on 2026-08-30, when
a vertex carried ONE handle. The push rail arrived the next day with
the ruling that every manipulation is reachable on the drawing, and
the fall-through answered False for it.

MEASURED THROUGH THE WIDGET'S OWN MOUSE EVENTS
(`tools/probes/does_a_push_drag_reach_the_record.py`), with the nudge
handle on the same vertex as the control:

    push_vertex   previewed, asked for push_d=0.153, record 0 -> 0
    nudge_vertex  previewed, asked for dx=0.105,     record 0 -> 1

    after the repair, push_vertex                    record 0 -> 1

THE GUARD IS THE SHAPE RATHER THAN THE INSTANCE. Every key in the two
handle tables is put to `_drag_moved` with a gesture past every
threshold and must be answered yes, so the SIXTH handle is covered by
whoever adds it; and the push is then driven end to end through the
widget, because a table answering correctly is not a gesture reaching
the record.

### 3. The repair's own defect, found within the hour

Row 1's repair narrowed the hold's clearing to "where no write is
running", which is right for the moment it was written for and leaves
a later one uncovered. The writer clears the flag only where IT read
one, between tables; the hold now leaves it alone while a write runs;
and a cancel landing AFTER the last table -- during the repointing or
the styling, 13.0s of a 256-element save -- falls between the two. The
flag then stands for ever and the next save is rolled back.

IT WAS FOUND BY A HUNT AIMED AT THE ROUND'S OWN REPAIRS, which is now
TEN ROUNDS FOR TEN in this project's record, and the repair it read
was ninety minutes old. The flag is cleared where the act ENDS now,
in the same `finally` as `_saving_now`.

AND ITS FIRST GUARD SURVIVED THE MUTATION, which is what sent the
entry to a second test: row 1's guard cancels BETWEEN tables, where
the writer's own branch clears the flag, so removing the line changed
nothing it could see. The journey has to reach the moment, and the
moment is staged by wrapping the seam -- a wrapper that calls the real
writer and then asks for the close -- rather than by timing a click.

### 4. A file that holds nothing is nobody's

The ownership question is asked once per press and CACHED for the
session, and it decided by `os.path.getsize(path) > 0`. A GeoPackage
OGR has created and nothing has written to is 65,536 bytes of header
holding no layer -- non-empty by size, empty by content -- and
`_this_map_owns_the_file`'s own docstring says an empty file "is
nobody's and is decided by the caller". Measured with a control: after
a cancelled first save, shrinking a four-element design to two left
`tiles_c_v3` and `tiles_d_v1` in the file where the same journey
without the cancel dropped them. It asks `bridge.gpkg_tables` now, and
the guard asserts BOTH directions, since a repair that made every file
ours would destroy a colleague's work.

### 5. A save that outran its own build

`_a_topology_is_owed` asks THE FILE, which is the cost ruling of
2026-08-30 and is right about whether to START a build: somebody who
has never opened the Topology tab pays none of it. It was the wrong
question about a build ALREADY RUNNING. Measured, both arms in one
run: hurried, no motif and `topology_written: False`; after a pause,
`weavingspace_unit_no_crs` and its dual. The press waits for work
already under way now, and the guard asserts the cost ruling beside
it, because the obvious repair overturns it.

### 6. A wait only an outer frame could end

`_save_the_map` turns the event loop once per element, so a close or a
quit arriving during a write is delivered by THAT WRITE'S OWN PUMP and
the hold runs nested inside it -- spinning until `_saving_now` clears,
which only the suspended frame beneath can do. Measured through both
doors with the ceiling shortened and said so: 5.1s against a control's
0.16s, and no tables at all once the escape pressed Cancel. The hold
declines where a write is already running: the save lands the moment
it returns, so the act is let through and the map is written.

AND IT PUT TWO OF THE DAY'S OWN ENTRIES TO SLEEP, which is this
project's recorded shape and is recorded again here. With the hold
unable to run beneath a writer, both of rows 1 and 3's conditions
became redundant -- the hold's branch now always executes after the
act has ended. Both entries are RETIRED with their measurements at the
catalogue, both lines KEPT as defence in depth, and what would reopen
each is written where it stood. The OUTCOMES are still guarded: a
cancel during a write rolls back, and a late one does not poison the
next save.

### 7. The commit that answered and was not asked

OGR reports this failure by RETURN VALUE rather than by raising, so
the `except` around the commit could not fire and its answer was
never read. The rollback branch beside it already clears `written`,
saying at the line that otherwise "the caller would repoint layers at
tables that went away" -- the commit branch is the same sentence for
the other way a transaction can end.

MEASURED THREE WAYS, one process, three arms, and the two failures are
NOT the same:

    control                    "Saved", four tables, layers 41/40/41/40
    a WRITE lock, held by a
      second process           fails at the first feature and says so;
                               the map on screen survives
    a SHARED READ transaction  "Saved to shared.gpkg", NO tables, and
                               all four layers invalid, yielding
                               nothing

The write lock fails early enough that the per-feature path reports
it; only the read transaction lets every table through and refuses the
commit, which is the state the claim is about. My first route was the
write lock and it came back honest -- so the claim would have been
filed as not reproducing by anybody who stopped there.

AND THE PROBE'S OWN FIRST RUN MEASURED NOTHING: the holder subprocess
inherited QGIS's `PYTHONHOME` and died at "No module named
'encodings'", which is this project's two-interpreters trap arriving
inside an instrument. The premise now says whether the lock is held,
and an unheld lock reports INCONCLUSIVE rather than clean.

### 8. The question that answered the wrong fact

`_a_save_is_outstanding` deliberately merges two things -- a promise
made and not yet kept, and the KEEPING of it -- because to the person
who pressed the button they are one act. Both doors ask it. The close
door then ASKS A QUESTION, and its Close arm was written about only
the first half: it cleared `_save_pending`, which during a write is
already False, and reported that nothing had been written while the
write ran on to completion.

TWO HUNTS FOUND IT INDEPENDENTLY, from opposite directions -- one
reading the day's own repairs, one asking which of the live path's
gates has a precondition nothing produces -- and both read the harm
off the FILE, one with stdlib sqlite3 and one with OGR. That
convergence is what made it safe to act on within the hour.

THE THIRD ROUTE READ THE PROJECT INSTEAD
(`tools/probes/what_a_close_that_declines_the_save_does.py`), because
a save that runs to its end repoints every element layer at the file:

    promised  saving_now False, save_pending True
              "Closed without saving; the map is still in the
              project."   0 of 4 layers reading from that file
    writing   saving_now True,  save_pending False
              "Closed without saving..." then "Saved to writing.gpkg."
              4 of 4 layers reading from that file

So the map in the person's project was backed by the file they had
just declined to write, which is a harm no reading of the file can
show. The promised arm is the control and is correct, which is what
says the sentence is right where it was written to be right.

WHAT STOPS A WRITE was already built and was not being used:
`_save_cancelled`, which `write_gpkg_layers` reads BETWEEN TABLES and
answers with a rollback, and which the waiting window's Cancel sets
for the same reason. The Close arm sets it now.

AND THE SENTENCE IS THE WRITER'S. Ours cannot be true here: past the
last table nothing reads the flag, so the save legitimately completes,
and the honest report is the one `_save_the_map` makes about what
actually happened. No new prose was needed, which is worth noting --
the repair uses two sentences a person has already approved.

### 9. The frame that answered for the writer

Rows 8 and 9 are one shape met at two doors, and they arrived within
an hour of each other from two different hunts. A frame reported the
outcome of an act it could not see.

`write_gpkg_layers` asks `should_stop` BETWEEN TABLES. That is right
and is not what changed: past the last table the transaction is
committed and nothing can undo it, so a press landing during the
styling or the repointing genuinely cannot be served. What was wrong
is what the person was told. The hold resumed, read `_saving_now` --
cleared by the writer that had just FINISHED -- and could not tell
that from a wait in which nothing was ever opened.

MEASURED AT A LATER SEAM THAN THE HUNT USED, which drove the styles
pump and read the file with sqlite3. This drove the REPOINTING pump,
one phase further on, and read the file through OGR, the sentences in
order, and the layers' own sources
(`tools/probes/what_a_cancel_after_the_tables_reports.py`):

    early  should_stop [False, True]   no tables, no layers reading
           "The save was stopped, so the map was not written."
           ...and then the hold said the same thing again
    late   should_stop [False]*4       4 tables, 4 layers reading
           "Saved to late.gpkg."
           "The save was cancelled, so the map was not written."

So the early arm had the plugin speaking twice about one outcome and
the late arm had it contradicting itself. The repair records whether a
write was under way AT THE PRESS -- the only moment that can be known
-- and leaves the report to the writer, which speaks in both cases.
The hold keeps its own sentence for the wait where nothing had been
opened, which is the journey the promise-only guard drives and which
would otherwise have gone silent.

### 13. The record that answered for a map it had never seen

`_element_tables` is written by a LANDING and cleared by nothing --
not by the Load door, not by a group switch. So a session that has
drawn any map carries THAT map's table names, and an opened map's
elements share their ids with it: the record answered for all of them,
and the layer's own source was consulted only where the record was
silent. The comment at that line already calls the source "the only
witness that has not been through this session", which is the tell --
the authority was subordinated to the record it exists to correct.

MEASURED WITH A CONTROL, two arms in one run
(`tools/probes/what_a_save_calls_the_tables_after_a_load.py`), the
file read through OGR and its own record read back beside it:

    control  nothing drawn first   tables unchanged
    treated  a map drawn first     tiles_b_landcover REMOVED,
                                   tiles_b_v2 in its place, while the
                                   record still says b: landcover

So the file contradicts itself, which is the ruling of 2026-08-26 --
the file shows the limit of what it contains -- broken from the
inside, and the sender's table is gone.

THE REPAIR IS AN ORDER, not a new rule: the witness is asked for
EVERY element, and the record answers only where it has nothing to
say. It costs the drawn map nothing, and that is why the order can
simply be reversed -- a map drawn here reads from MEMORY at its first
save, so the witness has no answer and the record decides exactly as
before; from the second save on it reads the very names the record
holds. A Save As is answered None by construction, the layer reading
a different file.

AND THE FIXTURE HAD TO STAGE TWO DIFFERENT NAMES. The guard's first
version drew both maps on their defaults, so the two agreed by
accident and the entry SURVIVED against a test that was otherwise
sound. One element is put on another variable now, and the premise
asserts the sent file carries a table named for it.

### 12. The record and the layers, disagreeing about one dataset

This project's commonest defect shape, met on the newest door: one
fact held in two stores and mended in one.

A resume stamps the GROUP's record with the region the recovery
LANDED ON, and it must -- a self-contained file records the SENDER'S
own path, and nothing on the recipient's machine answers to it, so
the record alone would file the resumed group under a dataset that
does not exist here. That was settled on 2026-08-29 and is right.
What it did not do is re-stamp the LAYERS, and `_our_groups` asks the
layers.

SO `theirs` CAME BACK EMPTY and `_bind_group_to_dataset` let go of
the map the person had just opened. A Generate then built a rival
group beside it, and a Save wrote that rival into the same tables.

MEASURED AT BOTH DOORS, one process, each on its own file
(`tools/probes/what_a_resumed_map_stamps_on_its_layers.py`):

    layers say    MultiPolygon?crs=EPSG:3857&uid={...}
    chooser holds <file>|layername=weavingspace_region
    binding       False
    after one Generate   TWO groups, four of our layers each

AND THE FRESH DOOR DOES IT TOO, which is what decides whose defect it
is. The hunt found it through the already-open door, hours after that
door gained `_landed_this_session`, and its own note said that if the
fresh branch shared the exposure the repair belonged on the stamp
rather than on the flag. It does, and it did. The flag decides only
whether the binding is REACHED; the stamp decides what it finds.

THE FIXTURE IS THE PREMISE, and my own first run failed it silently.
`_recover_the_source` has three routes and the first is a layer
already open, so with the sender's region layer still in the project
the recovery lands there, the two stores agree, and the probe reports
health about a journey it never drove. A recipient does not have that
layer -- which is what the copy inside the file is for.

NOTHING IS STAMPED WHERE THE RECOVERY LANDED ON NOTHING. Writing the
record's own region onto the layers would put the sender's path on
the recipient's map and turn a fiction into a stamped one.

### 11. The filter that was written as though it were the map

`write_gpkg_layers` iterates `getFeatures()`, which honours a layer's
subset string. So a filter -- a thing a person sets to LOOK at part of
their map -- was written into the file as the map itself.

TWO ROUTES REACH IT AND ONLY ONE INVOLVES THE SKIP. A layer already
reading from its own table is treated as saved already, and identity
was `same_source` over the WHOLE tail of a source string, which a
subset lives in; so a filtered layer failed to match the table it was
plainly reading from and went down the write path into that table. But
a RE-TILE gives every element a new memory layer and carries the
person's filter onto it deliberately, and no skip can fire there at
all -- which is the route the hunt did not drive and the worse of the
two.

MEASURED ON THREE ARMS IN ONE RUN
(`tools/probes/what_a_filter_does_to_a_save.py`):

    control   two saves, no filter          41/40/41/40 unchanged
    second    filter between two saves      tiles_a_v1  41 -> 3
    re-tiled  filter, then design changed   tiles_a_v1  41 -> 0

The zero is the sharp one: the filter names feature ids that the new
tiling never produced, so it selects nothing and the element's table
is emptied.

WHICH OF THE TWO A FILTER IS was decided by the plugin's own words
rather than by preference. The line that carries a subset across a
re-tile says a subset "says which features to DRAW". A view, then, so
it comes off for the write and goes back afterwards -- restored in the
save's own `finally`, because the cancel branch returns between the
two and an exception may leave by neither door. A save that silently
cleared somebody's filter would have traded one loss for another,
which is why the guard asserts the filter is still there at the end.

AND THE IDENTITY HALF IS HELD REDUNDANTLY FOR THE HARM, said here
because it has no entry. With the subset off, a filtered second save
writes the whole map back and loses nothing, so mutating that half
changes no count a test can see; its job is to stop the second press
rewriting what is already there, which is a cost rather than a loss.
The spec hunt measured the same day that OGR no longer refuses a
layer written into the table it reads from -- the reason the skip was
originally justified by, which did not survive the single-session
rewrite of 2026-09-01. The skip now rests on its other reason alone.

### 10. One button, two branches, one of them armed

The repair of 2026-08-28 is named in this branch's own twin, at
length, with the harm it cost: a map OPENED is this session's work,
and `switched_from_work = switched and self._landed_this_session`
reads a change of dataset as a first choice without it. It went to the
FRESH branch, taken where the map's layers are not in the project. The
ALREADY-OPEN branch is the door a person takes most -- the plugin
closed and opened again, or a project reopened, with the map still in
front of them -- and it was left unarmed.

MEASURED BY DRIVING THE TWO DOORS SIDE BY SIDE on one journey rather
than by driving one to its loss, which is the difference from the
hunt's route: the hunt read the tiles' extents back with sqlite3 after
a Generate and a Save, and said honestly that a control WITHOUT the
press reaches the same end state. The claim is about the PRESS, so the
comparison is between the presses
(`tools/probes/what_each_load_door_arms.py`):

    fresh          landed True   path cleared   "...was cleared..."
    already open   landed False  path KEPT      nothing said

THE FIXTURE IS THE WHOLE OF IT, and my first run got it wrong.
`_add_output_layers` sets the flag when a map LANDS, so a probe that
draws a map and presses Load in the same window measures nothing about
the press -- both doors came back armed and the control could not
fail. The plugin is reopened before the press now, which is the
journey the claim is about.

THE CONDITION IS THE TWIN'S, asked the shorter way.
`_recover_the_source` has just answered "did this find the data the
map was made from" and returns what it landed on; a recovery that
found nothing is still not a switch, which is the settled clause of
2026-08-21 and is why the flag is `bool(landed_on)` rather than True.

### 21. A selection with one owner, and handles that asked the click

`TopologyPanel._selection` is the one answer to "what would an edit
move", and Apply, the drag preview and the drop all ask it. The three
drag handles are seated on `_chosen_thing`, which had ONE writer, in
`mousePressEvent` -- so choosing a class in the chooser or the tick
list moved the selection and left the handles behind.

MEASURED WITH THE TWO READINGS TAKEN TOGETHER
(`tools/probes/where_the_handles_sit_after_a_list_choice.py`), which
is what makes the disagreement mean something:

    clicked        selection ('edge', 'b')  handles at
                                            (210,277) (180,277) (150,231)
    chose edge a   selection ('edge', 'a')  handles UNMOVED

WHAT IT COSTS IS NOT A WRONG-LOOKING PICTURE. A drag's parameter is a
polar coordinate about the handle's own edge -- the scale factor is
how far out the end now sits, the rotation is the angle it now makes
-- so grabbing a handle seated on `b` while the edit is recorded
against `a` measures the number on the wrong edge.

MEMBERSHIP IS ASKED THE WAY THE PAINT ASKS IT, `label in chosen`,
because a selection may name several classes and "every edge" always
does: its datum is the whole group, and an equality test there lit
nothing at all when the same mistake was made in the highlight.

AND THE THING ALREADY IN HAND WINS, which is the second answer the
guard asserts: a click chooses a particular edge, and the sync that
follows it must not drag the handles off onto the first member of
that class.

### 20. The same record, at the one door its witness cannot reach

Row 13 closed this shape for the SAME-file case by asking the layer's
own source for every element. A Save AS is the door where that witness
has nothing to say, and it says so deliberately: it answers only where
the layer reads from the file being saved to, because a map written
afresh should take the names THIS map would choose. It then took the
names of the last map DRAWN.

`_element_tables` is filled by a LANDING, and `_detach_from_the_group`
-- the clear site every take-over passes through -- emptied four
records and left this one. An opened map's elements share their ids
with the drawn one's, so it answered for all of them.

MEASURED WITH A CONTROL, two arms in one run
(`tools/probes/what_a_save_as_calls_the_tables.py`), the copy read
through OGR and its own record read back beside it:

    control  nothing drawn first   tables agree with the record
    treated  a map drawn with `b`  the copy holds `tiles_b_v2` while
             on another variable   its record says `b: landcover`

A HUNT CALLED IT HELD REDUNDANTLY THAT MORNING, and was right about
the case it drove: on the same file the witness corrects the record
for every element. This is the door where it cannot, which is this
project's own sentence about an omission that currently costs nothing
being a countdown rather than a defence.

AND THE FIXTURE'S PREMISE IS ASSERTED, because two maps on their
defaults agree by accident: the drawn map puts `b` on another
variable, and the record is required to hold that name before the
Load.

### 19. A shelf that outlived its project

`_forget_the_last_project` empties every record keyed by tile id when
QgsProject says `cleared`, which fires on File > New and immediately
before File > Open. `_scheme_memory` joined that list on 2026-08-25,
after a scheme shelved in the project you CLOSED redrew a column in
the project you OPENED, and the comment beside it says a cleanup that
works by side effect is a cleanup nobody has written down.
`_topology_shelf` arrived on 2026-08-30 and never joined it.

EDITS ARE SHELVED BY FAMILY AND ELEMENT COUNT, which is what makes
this bite: a key names the same design in every project, so the edits
were not merely remembered but REPLAYED.

MEASURED WITH THE SCHEME MEMORY AS THE CONTROL
(`tools/probes/what_a_new_project_leaves_on_the_shelf.py`), which is
what says the clear ran at all rather than that this probe missed it:

    before the clear   shelf 1  panel 1  scheme 1  ground 246,109.968
    after the clear    shelf 1  panel 1  scheme 0
    the next project   ground 246,109.968
    a control's        ground 250,000.0

AND THE SHELF ALONE IS NOT ENOUGH. `_restore_topology_edits` is what
puts the shelf back into the tab, and nothing calls it unless the
design MOVES -- so somebody who opens a new project and leaves the
design alone would keep the outgoing project's edits in front of them
whatever the shelf held. Both are emptied.

THE PANEL HALF IS HELD REDUNDANTLY, AND THAT IS MEASURED. Its entry
SURVIVED with the control run, and wrapping `_restore_topology_edits`
across the clear records three calls, the last with zero edits, with
the panel reading empty immediately after `QgsProject.clear()`. The
entry is RETIRED with that measurement, the lines KEPT as defence in
depth, and what would reopen it written where it stood.

AND MY FIRST FIXTURE COULD NOT EXHIBIT ITS CASE. The first index of
each chooser is a vertex class and `push_vertex`, whose displacement
is exactly zero on this design -- the incident unit vectors cancel --
so the probe recorded an edit, changed nothing, and reported that the
next project's map matched a control. An EDGE manipulation is chosen
now, and the ground is required to have moved before the clear.

### 18. A memory written at one door and not at its twin

`_embed_or_drop_the_source` asks the checkbox first, and where it is
untouched it asks a per-FILE memory instead, so a recipient who has
never had an opinion cannot strip a copy the sender chose. That
reasoning is right and the memory had ONE writer, in
`_recover_the_source`, which only the two RESUME branches call.

ADOPTION IS THE OTHER DOOR and wrote nothing. The box is back at its
default there and `_embed_touches` is zero, so the drop written for
the act of UNTICKING fired for the act's ABSENCE -- the
repair-aimed-at-an-act rule met from the side where the act never
happens.

MEASURED ON BOTH DOORS AND ONE FILE
(`tools/probes/which_door_remembers_the_embedded_copy.py`), reading
the decision's own inputs at the press beside what the file ended
with, which is what says whose defect it is rather than which journey
is unlucky:

    Load      box False, memory (True, 0)      copy kept
    adoption  box False, memory no entry       copy GONE

and the recipient's own layers then failed at the sqlite level,
reading a table that was no longer there.

IT IS THE SECOND TIME THIS TWIN PAIR HAS BEEN SWEPT FOR ITS GUARDS AND
NOT FOR ITS MEMORIES. When a repair gives one door a per-file memory,
the twin needs the memory as well as the guard.

AND THE RECORDS MOVED ABOVE THE INTERFACE, which is load-bearing
rather than tidy: `_adopt_existing_group` runs in `__init__` BEFORE
the tab holding that checkbox is built, so leaving the two records
beside the control meant the interface wiped what adoption had just
learned. The repair raised an AttributeError at the adoption site
rather than losing the answer quietly, which is the better of the two
failures.

AND THE THIRD ARM'S FIRST STAGING WAS MINE. The box is a standing
preference and shows unchecked after an adoption whatever the file
says, so `setChecked(False)` there clicks nothing and emits nothing --
an arm asserting a no-op, which failed honestly. Speaking about the
box from that state is a tick and then an untick, and the arm asserts
the touch count moved.

### 17. A sentence that named two controls where there were three

MAINTAINING.md says of the save's pump: "Turning the event loop is
exactly what would otherwise let somebody press Save or Generate into
a half-written file, so both controls go down for the duration." That
was true when it was written. LOAD arrived on 2026-08-27, on the row
beneath Save, and was never taken down at either of the two acts that
pump -- and `_resume_from_gpkg` guards `self._task is not None` and
nothing about a save.

THE REACHABILITY WAS MEASURED SEPARATELY FROM THE MECHANISM, which is
this project's own rule after a defect whose journey turned out to be
the suite's alone. Sampling what a person could press at each beat of
a write (`tools/probes/which_controls_are_live_while_writing.py`):

    beats 1..12   save_button False, generate_btn False,
                  load_button TRUE

That is a fact about the interface and would be worth reporting even
if no press cost anything.

WHAT ONE COSTS was then driven at a LATE beat rather than the first,
which is a different seam from the hunt's: the saved file's own record
came back naming the file that had been OPENED. The hunt's earlier
press also put the other map's tiles into this file's tables, the
already-saved skip having stopped recognising layers repointed
mid-loop.

THE REPAIR IS A LIST, NOT TWO MORE LINES. Both pumped acts take their
controls down through one owner now, so a fourth control joins
`CONTROLS_A_PUMP_TAKES_DOWN` rather than being remembered at two
sites. The two file choosers are deliberately not in it: a chooser
records what you WOULD save to or load from and does nothing on its
own.

AND THE ENUMERATION FOUND MORE THAN THE CLAIM NAMED, which is recorded
rather than repaired. The Topology tab's Apply, Undo and Clear, and
Auto, are also live during a write, and the record is captured after
the loop -- so whether any of them reaches it is a measurement nobody
has taken. A harm named by reading is a hypothesis, so it is on the
owed list.

### 26. The save happens first, then the Load

**(Maintainer's ruling, 2026-09-02, on being shown the measurement:
"the save should happen first. then the load".)** Claimed by the
asymmetry hunt of round seven, which reported it as the promised save
landing on the OTHER person's file. Verified here by a third route and
the harm came back NARROWER IN ONE HALF AND UNCHANGED IN THE OTHER,
which is this project's own rate for a hunt's harm: staged through the
LIVE REDRAW door rather than a topology build
(`tools/probes/where_a_promised_save_lands_after_a_load.py`, two arms,
control first), the other map's own record never moved -- and the
person's map was never written at all.

    control   mine 700 -> 520   theirs 900   promise kept
    treated   mine 700 -> 700   theirs 900   promise consumed, nothing written

**WHY IT HAPPENED, and it is two settled rules meeting.** A press kept
while a re-tile is coming is a PROMISE about this map, because most
people do not read a refusal (2026-08-29). And the promise
deliberately re-reads the output chooser at the moment of the write,
so somebody who changes their mind about where the map goes is obeyed
rather than written to a file they have abandoned. A LOAD moves that
same chooser and means nothing of the kind: it says where the map
being OPENED lives.

**THE RULING IS AN ORDER RATHER THAN A QUESTION**, which is what makes
it cost no new prose: the Load is DEFERRED behind the promise and
performed when it ends. Deferred rather than refused, for the reason
the whole family exists; and only the PROMISE is asked about, since
Load is in `CONTROLS_A_PUMP_TAKES_DOWN` and cannot be pressed during a
write at all -- so this door can never reach the nested wait that
froze the window in row 2.

**BOTH HALVES ARE GUARDED, AND EACH HAS ITS OWN ENTRY.** "The save
first, then the load" is two claims: a repair that protected the save
while swallowing the Load would satisfy any reading of the first and
leave a button that does nothing. Measured after the repair, treated
arm: our file 520, theirs 900, and the chooser sitting on their file
-- the save landed and the map then opened.

### 25. Closing asks before it interrupts a save

**(Maintainer's ruling, 2026-09-02: "a panel's close button shouldn't
stop a save, it should prompt whether to interrupt save".)** This is a
ruling rather than a defect report, and it is about a repair made the
same morning: row 5 taught the Close arm to set `_save_cancelled`,
because clearing a promise that was already kept answered nothing at
all. That was right about the mechanism and wrong about the person.

**ONE QUESTION WAS COVERING TWO STATES**, which is row 5's own lesson
one layer up. `_a_save_is_outstanding` merges a promise made with the
keeping of it -- deliberately, because to whoever pressed Save they
are one act -- and the question built on it asked "save it before
closing?" in both. So somebody who meant "never mind that promise"
was taken to have said "throw away the write you are half way
through". The predicate had been mended for exactly this merge and
the QUESTION still carried it.

**SO THERE ARE TWO QUESTIONS NOW.** A promise not yet kept keeps the
old sentence and is simply dropped: nothing has been opened, so there
is nothing on disk to lose. A write under way is asked about in the
maintainer's own words -- "Interrupt the save when closing?" -- with
**No as the default**, on the dependency-consent precedent that the
safe answer is what a stray Return gives you.

**WHAT No DOES, said plainly because the wording leaves it open.** The
write is not interrupted and the window closes; the save finishes in
the frame beneath and reports for itself. It cannot WAIT there: the
close arrives on the write's own pump, so holding would suspend the
only frame that can clear the flag, which is the freeze row 2 closed
this morning.

**GUARDED WITH THREE ARMS**, because the ruling is about which answer
does what: the promise arm (nothing written, the old sentence said),
No during a write (the file holds its tables and every element layer
reads from it), and Yes during a write (no tables, no layer reading
from it). Either write arm alone is satisfied by a repair that is
wrong in the other direction -- a close that never interrupts, or one
that always does, which is what this ruling overturned.

### 24. One shelf for two designs

**THREE hunts, three directions, one defect.** Backwards from harm
ranked it ninth on a list written before any source was opened; the
specification hunt asked whether `shelf_key`'s own justification --
"edge classes differ between FAMILIES" -- is true of its SCOPE; the
stochastic hunt ran 81 settled sessions that could not see it and then
found it by asking which two stores held "what an edit is about". That
is the strongest confirmation this method produces, and it is one
defect.

**VERIFIED FROM THE RECORD, which is what none of the three did.**
Each took its verdict from geometry -- the unit's ground, or the tiles
read back out of a saved GeoPackage with OGR. This reading asks the
shelf itself
(`tools/probes/what_the_shelf_key_cannot_tell_apart.py`), with an
ordinary design change as the control:

    design's own edit     key hex-slice 4#4        shelf 1 edit
    another family        key laves 3.3.4.3.4#4    shelf 0    <- control
    dual ticked           key hex-slice 4#4        shelf 1    <- the key
    edited the dual       key hex-slice 4#4        shelf 2       cannot
    dual unticked         key hex-slice 4#4        shelf 2       see it

The control is what makes the treatment readable: a change of family
moves the key and the edits go quiet, which is exactly what the shelf
is for. The dual moves nothing, so one key holds two designs' edits,
and an edit is replayed by LABEL.

**AND MY OWN FIRST CONTROL WAS THE INSTRUMENT.** It changed the
ELEMENT COUNT, which repopulates the family list and lands on whatever
that count offers -- `hex-slice 4#4` became `square-colouring 5#5`, and
every later reading was about a design nobody had chosen. This
project's own rule about adding a step to a sequence, met inside the
probe; the correction is written at the line.

**THE REPAIR IS THE KEY, AND IT IS A SUFFIX.** `shelf_key` takes the
dual as a third term and appends `#dual`, so nothing already saved
moves: a record written before this carries the plain key and restores
under it. Seven call sites pass it, six from one owner
(`_mapping_the_dual`) and the seventh -- the RESTORE -- from the
RECORD, for the reason the family and the count already come from
there. `_topology_stamp` gains the same term, which is the defect's
second face: a build about the design and a build about its dual were
indistinguishable to the landing that decides whether a build still
describes what is on screen.

**WHAT IS DELIBERATELY NOT REPAIRED**, and it is the maintainer's:
every OTHER design term is still outside the key. The specification
hunt measured a scale modifier turning two edge classes into four --
`ab` becoming `abcd`, the chosen class now naming a disjoint set of
edges -- while the key stood still. Widening the key to the whole
design would mean an ordinary spacing or modifier tweak putting
somebody's edits away and bringing them back, which is a ruling about
when edits go quiet rather than a defect to mend in passing. Recorded
in ROADMAP.md.

### 23. The repair that took the carry away

**Found by the hunt aimed at the repair, which is what that direction
is for.** Row 22's repair handed `region_crs` to `_stamp_working_state`
as part of the LAUNCH STATE, and a launch state is merged OVER the
carried record -- so the sentence written at the already-open door,
that the door was "held by the carry", was made false by the line
directly beneath it. The commit's own probe could not see it, because
all three of its arms had a file whose record was correct.

**MEASURED HERE ON TWO ARMS THE FIRST REPAIR DID NOT HAVE**, added to
the same probe rather than a new one
(`tools/probes/whose_crs_a_resume_writes_at_both_doors.py`, five arms
now, control first):

    older file, already-open   file 3857 -> None      before: 3857
    wrong file, already-open   file 3857 -> 27700     before: 3857

Both are the ordinary state of a file this project has already saved:
every GeoPackage written before 2026-08-28 lacks the key, and every one
written between then and this morning may carry what rows 16 and 22
wrote. The old code HEALED both, by the accident of not writing the
key at all; the repair took the healing away.

**THE ORDER IS NOW A QUESTION WITH THREE ANSWERS AND ONE OWNER.**
`_crs_the_resume_landed_on` asks the RESOLUTION first (the layer the
recovery landed on, checked against `landed_on` before the chooser is
believed), the GROUP's own record second, and the FILE's record last.
The middle step is the one this cost: a record written by the landing
that drew the map outranks a copy of it that may be older than the key.

**AND ITS SIBLING AXIS WAS DEAD, which the same hunt found.** The
first guard's two arms could both be satisfied by a helper that read
the record first -- the exact inverse of its own docstring's ruling.
The two arms above kill that mutation, and the catalogue carries it as
its own entry rather than resting on the sibling.

**WHAT IS NOT REACHABLE IS SAID AT THE CODE.** The `same_source` check
between the chooser and what was landed on cannot fail on any journey
measured: both doors point the chooser as part of landing and nothing
between moves it. It is kept, with that measurement written at the
line, because the chooser is the one thing this method reads that
other code moves -- and because the alternative, walking the project
for a layer whose source matches, is the lookup this project already
records answering with the WRONG layer where an overlay was built on
the region's own source.

### 22. The same fact, at the two doors row 16 did not visit

**Two hunts, from opposite directions, in one evening.** The
write-only hunt aimed at the round's own repairs asked what rows 14 to
21 write that nothing reads back; the two-stores hunt asked which of
two records wins when they disagree. Both arrived at
`_resume_from_gpkg`'s launch state, which hands `_stamp_working_state`
`region` and `output_path` and no `region_crs` -- so on the journey
that branch exists for, a recovery that found NOTHING, the region is
taken from the file's own record deliberately while the system falls
through to `_capture_working_state`'s reading of whatever the chooser
holds. That is row 16's sentence at a third writer, and row 16's
repair visited the switch-out stamp and the carry and not these two.

**VERIFIED BY A THIRD ROUTE, and it differed from both hunts on
purpose.** One hunt read raw GDAL metadata and the other went to
`gpkg_metadata` with sqlite3, both deliberately around the plugin;
this reading asked `bridge.read_working_state`, which is what every
other consumer asks. The failed recovery was staged with a MEMORY
region rather than by moving a file, so nothing on disk was touched
and the case arises from the ordinary fact that memory does not
survive a session. The stranger was EPSG:27700, neither hunt's
primary arm, so a record following it is reading the chooser rather
than defaulting to anything.

**AND ONLY ONE OF THE TWO DOORS CAN FAIL**, which neither hunt could
have known, because neither drove the other branch. Three arms in one
run (`tools/probes/whose_crs_a_resume_writes_at_both_doors.py`):

    control, fresh door        chooser alpha EPSG:3857    file 3857 -> 3857
    treated, fresh door        chooser stranger 27700     file 3857 -> 27700
    treated, already-open      chooser stranger 27700     file 3857 -> 3857

The already-open branch finds a group that ALREADY carries a record,
and `_stamp_working_state` prefers a carried `region_crs` to the live
reading -- so that door is held by the repair of row 16 rather than by
being immune. Both doors are written the same way regardless, because
this project has already watched a masked omission go live three hours
after a fix removed the mask; and the catalogue entry stands on the
FRESH door alone, since an entry on the twin could only ever survive.

**The repair is a question rather than a value.**
`_crs_the_resume_landed_on` asks the resolution first and the record
second, and never the chooser on its own: where the recovery landed on
a layer, that layer is the region this map now has; where it landed on
nothing, the file's own record is the only honest answer. The chooser
is CHECKED against what was landed on with `same_source` before it is
believed, which is this project's own rule that a record describes
where something came from and only the resolution describes where it
IS.

**Both answers are asserted**, and the second is what stops the lazy
repair: always taking the file's record would satisfy the failed
recovery and lose everything an older file cannot tell it, since a
file written before `region_crs` existed carries none and a resume
that LANDS must record the system the layer in force actually has.

### 16. A region and a system naming two different datasets

`WORKING_STATE_EDGES`' own comment says `region_crs` "is carried
wherever `region` is carried and asked for nowhere else". It was not.
`_stamp_working_state` carried the design and the two edges, so
`region_crs` fell through to the LIVE reading in
`_capture_working_state` -- and the switch-out stamp hands over the
OUTGOING dataset's source precisely because the chooser already holds
the incoming one. That writer therefore took `region` from the dataset
being left and `region_crs` from the dataset being arrived at.

FOUND BY TWO HUNTS INDEPENDENTLY, from opposite directions -- one
asking what a Save writes that nothing reads back, one asking which of
three stores wins when they disagree -- which is the strongest
confirmation this method produces and is what made it safe to act on
within the hour. Both read the harm off the FILE.

MEASURED HERE AT THE GROUP'S OWN RECORD, step by step, which is
upstream of the file and says WHICH writer is the wrong half
(`tools/probes/which_crs_the_record_carries.py`), driving the return
through the GROUP chooser rather than a bare switch:

    after the first save   region=alpha  crs=EPSG:3857
    after coming back      region=alpha  crs=EPSG:2193
    the file says          region=alpha  crs=EPSG:2193

OF ONE FACT WRITTEN TWICE, WHICH WRITER HAD A REASON. `region` is
handed over deliberately, with its reason at the call site;
`region_crs` was whatever the chooser happened to hold. So the record
is the wrong half and the carry is the repair -- and it is TWO
writers, because the carry covers every group that already has a
record and the switch-out stamp covers the first, where there is
nothing to carry.

AND THE SECOND ANSWER IS ASSERTED BESIDE IT, because a repair that
simply stopped writing the key would satisfy the first arm and destroy
the reason the key exists: a system a person ASSIGNED lives on the
layer and not in the source string, so a recovery rebuilt from the
string alone brings the region back in the file's own coordinates.

### 15. An empty shell read as somebody else's work

`_may_overwrite`'s own Returns block promises that a file which "does
not exist, HOLDS NOTHING, or is one of ours is never asked about". It
asked `os.path.getsize(path) == 0`. A GeoPackage OGR has created and
nothing has written to is 65,536 bytes of header holding no layer --
which is exactly what a cancelled or failed first save leaves at the
path somebody named, and exactly the reading its sibling
`_this_map_owns_the_file` was mended for in row 4 that morning.

THE JOURNEY NEEDS A SESSION THAT DOES NOT REMEMBER THE FILE, because
the ownership answer is taken before any write and a file that did not
exist counts as ours. So: the save is stopped, the plugin is reopened,
the same path is chosen again -- and the person meets a warning that
their own empty stub "was not written by this map" and that saving
"will replace the tables this map needs and leave the rest of the file
alone", of which it has neither. Its safe button is No.

MEASURED ON FOUR ARMS IN ONE RUN
(`tools/probes/what_the_overwrite_question_asks.py`), the last two
being the controls:

    absent   no file            not asked
    stub     65,536 bytes, no
             tables             ASKED
    real     somebody's map     asked, and must be
    foreign  not a GeoPackage   asked, and must be

AND THE OBVIOUS COMPOSITION WAS MEASURED WRONG THE SAME HOUR, which
is why the repair is a new reader rather than a pair of old ones. GDAL
returns None for a GeoPackage with NO LAYERS exactly as it does for a
file that is not a GeoPackage, so `why_a_file_will_not_open` calls
both "unreadable" and `gpkg_tables` answers empty for both: the
composition would have waved a save straight over a file this question
exists to protect. `bridge.gpkg_holds_nothing` reads `gpkg_contents`,
which a GeoPackage carries by definition and which LISTS its layers,
so the two cases separate exactly -- measured on four files, including
a plain sqlite database that is not a GeoPackage at all.

### 14. A count that was wrong in both directions at once

The sentence exists so the SENDER learns what the recipient will
find: delete an element's row in the layers panel, press Save, and
the file holds the rest. It counted `len(order) - len(left_out)`,
which is arithmetic over one of the three ways an element can fail to
reach the file.

TWO ERRORS, AND THEY CANCEL ON ONE ROUTE. An element whose table
somebody ELSE removed is `vanished` rather than left out, so it was
counted as held. And an element left out whose old table the drop
SPARED -- which is what happens once a file has changed under us,
since nothing is deleted on a guess -- really is in the file, so
subtracting it as well would have understated by one.

MEASURED ON THREE ARMS IN ONE RUN
(`tools/probes/what_the_success_sentence_counts.py`), the file read
with stdlib sqlite3 once everything had let go of it:

    control  a row deleted, nobody else acting   said 3, holds 3
    treated  ...and a colleague's save took a
             SECOND element's table              said 3, holds 3
    sharper  ...and took the deleted element's
             table too, as reducing a shared
             design does                         said 3, holds TWO

So the middle arm is why no arithmetic over those lists is right, and
it is also what makes the claim's own harm reachable rather than
theoretical: the two errors cancel until the colleague's save happens
to take both, and then the sender is told the file carries an element
that is not in it.

THE COUNT IS ASKED OF THE FILE NOW -- which of the names this save
decided the tiles live under does the file actually hold, read after
the write, the drop and the record, since every one of those moves the
answer. One open, and only inside the branch that says the sentence,
so the ordinary save pays nothing. That is the shape three of the same
day's repairs took: an "empty" file is a question about content rather
than bytes, and a layer's own source is the witness for what its table
is called.

BOTH ANSWERS ARE ASSERTED IN THE GUARD, and the third assertion is
what stops the fixture drifting: the two arms must quote DIFFERENT
numbers, or a count that always answered the same one would satisfy
them both.

## Judged and NOT reproduced, with the route that settled it

**The other acting controls live during a write.** Taking the Load
button down (row 17) closed the door the claim named, and the
enumeration beside it found more buttons live while a write pumps: the
Topology tab's Apply, Undo and Clear, and Auto. The record
`_save_the_map` writes is captured AFTER the element loop, so the
question was whether any of them reaches it. It was recorded as owed
rather than repaired, because a harm named by reading is a hypothesis.

MEASURED, three arms in one run, each pressing from the write's own
pump at the second element:

    control   nothing pressed        file design spacing 700.0,
                                     topology_edits 0
    apply     a topology Apply       spacing 700.0, edits 0 in the
                                     FILE while the panel holds 1
    spacing   the box moved to 1300  spacing still 700.0

So none of them reaches it, and the reason is a rule already settled:
the record's DESIGN half is carried from the group's own record
because only a landing knows what was drawn (2026-08-26, rows 55-57),
and a save stands at no landing. The protection is the ruling working
rather than an accident.

WHAT IS STILL TRUE is that the buttons are live, so an Apply pressed
mid-write is recorded in the panel and applied afterwards -- which is
the ordinary deferred behaviour rather than a file that contradicts
itself. Whether they should go down with the other three is an
affordance question and the maintainer's.


**An Apply pressed while the next design's topology is building.** The
claim was that it lands on the like-named class of the NEW design,
reshaping the map and the saved file. The MECHANISM is real and was
confirmed: `_apply` records against the labels the panel is HOLDING
while `edits_changed` files the edit under the design in force NOW, so
between a family change and its build landing those are two designs.

WHAT DOES NOT FOLLOW IS THE HARM, measured on two arms in one run
(`hex-slice 4` and `laves 3.3.4.3.4`, whose edge classes are `a,b,c,d`
and `a,b`):

    a like-named class   applying 'a' during the build and applying
                         'a' after it give the SAME ground,
                         215,287.993 either way -- a label is a
                         string, and it is replayed against the design
                         in force
    a class the new
    design lacks         refused BY NAME: "Rotate edge on 'c' was not
                         applied: this design has no edge class 'c'.
                         Its edge classes are ab."

So the person is told, precisely, which is the 2026-08-31 repair
working. What is left is an affordance question -- the chooser offers
the outgoing design's classes until the build lands -- and the
maintainer has already ruled against greying the tab during a build
("it doesn't have to grey, that seems to make trouble", 2026-09-01),
so it is theirs rather than a defect to repair.

AND ONE INSTRUMENT FAULT OF MINE IS TALLIED HERE. A first reading
reported that the class list NEVER follows a design change, even once
quiet. It was the reading: `_the_topology_tab_is_quiet` answered
before the debounce had queued the build, so the list was read before
anything had been asked of it. With the queue and the landing counted
directly -- one `_queue_topology`, one `set_unit` -- the list follows.
That is the third instrument fault of the day, and every one was
caught by a control or a premise rather than by reading.

AND A FOURTH, WHICH SPENT rc13. CI's coverage leg -- the whole suite
three ways, which is the one step in `mutation.yml` that can redden it
-- failed `a build that lands mid drag does not wipe the gesture` on
the candidate's own commit `6e40574`, on its MAIN assertion rather
than on a premise: 256 passed and 1 failed on one shard of three, each
shard naming the same total of 772, while the same test passed in the
candidate's own local suite at 4.8s against the runner's 11.7s.
THE PRODUCT IS INNOCENT AND IT WAS STAGED RATHER THAN REASONED. The
test read the topology the panel holds BEFORE the clicks that find a
handle to drag; those clicks turn the event loop; and this test
deliberately does not drain the queued build, its subject being a
landing that arrives under a pointer. A landing in that window is
adopted correctly, no gesture being in progress, so the reading went
stale and the assertion reported correct behaviour in a sentence
saying "mid-gesture" about a landing that happened before the gesture.
`tools/probes/which_moment_the_drag_guard_reads.py` shows it in one
run, both arms, each clearing the project first: with a landing
delivered before the press the old reading FAILS and a reading taken
at the press HOLDS, and in both arms the mid-gesture landing is
refused -- so the hold was never at fault.
THE REPAIR IS A MOMENT RATHER THAN A WAIT, taken inside the helper
that presses, and all three catalogue entries standing on that test
were re-proved `caught` afterwards. rc13 is spent, which is the
documented trade: no later commit can make an earlier candidate
publishable.

## Open

Nothing yet; the claims still in the verification queue are listed in
the session's handover rather than here, because a claim that has not
been reproduced independently is a hypothesis and this file is for
what was measured.
