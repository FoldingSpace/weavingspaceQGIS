# Defects found and closed, 2026-09-12 (evening): round ten

Eight hunts at once as Opus subagents on worktrees at `d118d82`, aimed
at the weave topology added between rc19 and rc20, replenished as each
bug CLOSES, to the maintainer's target of sixteen. A row is closed when
the claim is reproduced by a route its hunt did not use, repaired,
guarded by a registered test, proved by a catalogue entry judged
`caught`, and committed. Faults in our own instruments are tallied at
the foot, apart from the product defects.

| # | What a user lost | Where | Reached by | Guard | Entries | Owes |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Under "Ignore, like an inset", an edit aimed at a glued weave class moved ONE side of each strand-width hole on the map and in the saved file, while the chooser named both -- the glued reading was a label on screen and nothing on the map | `dialog.py` replay called `topology_edits.apply` without the `glue` the build had returned; `apply` widened by `edit["target"]`, a key no record carries; the drag preview and the drop's refinement never widened at all | FIVE hunts independently: asym10, boundary12, spec10, harm15, stoch12 (and stores18 at the library). Verified here by enumerating every product caller of `apply`, none passing `glue`, against the three tests that pass it by hand | `test_an_edit_aimed_at_a_glued_class_moves_every_side_of_the_hole` -- the dialog driven through both choosers and Apply, the expected cloth computed by aiming the recorded edit at every library label spelled out from the gluing map with NO glue, a control aimed at the one label, and a vertex arm | `the-replay-carries-the-glued-reading`, `a-glued-selector-is-widened-by-its-manipulations-target` | the drag preview's widening is repaired and exercised by `test_the_drag_previews_the_move_the_drop_would_make`, but no entry stands on that one call |
| 2 | On any weave past twenty-six classes -- the default basket weave carries 62 -- an edit aimed at a two-letter class such as `aa` or `ab` also moved every edge of `a` (and `b`), marked applied and sound, with nothing said | the library selects with `label in selector`, a SUBSTRING test on the joined string the plugin handed it; `apply`'s missing-class check and `_expanded` split selectors a character at a time; the tab's highlight, ticks, subset count and symmetry gate did the same | THREE hunts independently: trigger10 (the drawn unit's edges), stores18 (the library's moved edges), unreach12 (the dialog, matched by shape). Verified here by reading `vendor/weavingspace/topology.py:1286` and every character iteration of a selector in the plugin | `test_an_edit_aimed_at_a_two_letter_class_moves_that_class_alone` -- a zigzag aimed at `aa` on `basket weave ab|cd`, each edge's own geometry read by label before and after, `b` as control, plus a table of how a selector is read | `the-library-is-handed-labels-not-a-joined-string`, `a-selector-string-that-is-a-label-is-that-label` (first form SURVIVED as inert, two limbs answering; re-aimed at the whole decision and caught) | the tab's own reading (`_held_labels`, the highlight) is exercised by the selection tests but no entry stands on it |
| 3 | After ANY topology edit on a thin weave, Generate drew the scaffolding's filler as nine extra `w0..w8 (no data)` element layers in the weave's daylight and Save wrote them into the GeoPackage as tables and styles, with nothing said | `dialog.py`'s landing adopted the edited SCAFFOLD as the map's unit; `topology_edits.strands_of` said what to keep and had no caller | stoch12 (layers, then the file read through sqlite, against an unedited control) and stores18 independently. Verified here by reading `_adopt_edited_unit`, which takes the replay's tileable whole, and the worker storing `kinds` that nothing read | `test_an_edited_weave_draws_its_cloth_and_not_its_scaffolding` -- the filler ids from `weave_topology` for the same settings, asserted to exist; the dialog's unit after an edit, and the element layers a Generate then draws | `the-map-adopts-the-cloth-not-the-scaffold` | the file's DUAL is still taken from a topology rebuilt of the scaffold rather than the cloth -- a question of what a weave's dual means, not yet asked; and the guard's first run met the intermittent stall of R-93 once |
| 4 | With a topology edit standing on a weave, switching "Gaps from strand width" (or "Warp and weft classes") changed the design on the tab, but Generate drew the map exactly as it stood under the old reading, from tiles kept for the previous choice, with nothing said | neither chooser was a term of `_topology_edit_key`, so the geometry signature, the tiled-frame cache and the edited-unit key all read the switch as no change; and a build launched under the old reading could land after the switch, its stamp blind to the reading | THREE hunts: harm15 (the cache hit, and a miss with caching off), stores18, repairs23 (the layers byte-identical after Generate). Verified here by the guard's own first red: the unit after the switch sat exactly the glue difference, 1562.5, from the new reading's replay and 0.0 from the old | `test_a_reading_changed_under_a_standing_edit_redraws_the_map` -- a second Generate with nothing switched as the control, the unit waited on to AGREE with the record replayed under the new reading computed from the settings, and the layers required to move | `a-standing-weave-edit-keys-on-its-readings` | the landing's discard of a build launched under a moved reading SURVIVED its entry, held redundantly by the rebuild the chooser queues; the window between the two landings, where a Generate could draw the stale unit, is unguarded |
| 5 | On a thin weave with Rotate (or Scale, Skew) set, one topology edit drew the map and wrote the file UN-rotated -- 90 degrees where the control drew 120 -- while the Rotate box still read 30, with nothing said | `topology_edits.build`'s weave branch rebuilds the scaffold from the design's SETTINGS through `weave_topology`, which never applied the modifier chain `dialog._build_unit` had given the unit | stoch12 (the unit, the map's layer through QGIS and the saved table through sqlite, against a rotated control; seeds 2005 and 3200 also reached it). Verified here by reading `build`'s weave branch, which takes `spec`, `spacing` and `aspect` and nothing of the modifiers | `test_an_edited_weave_keeps_its_rotation` -- the strands' own directions read off the dialog's unit before an edit, asserted to be rotated, and again after it | `a-weave-topology-is-built-on-the-modified-scaffold` | the weave's TILE INSET, now applied to the cloth after the replay, has no guard of its own |
| 6 | A strands code whose parentheses crossed a direction or ran out of order -- `a(b|c)`, `ab(|)c`, `()a|b` -- passed the box unmarked and built an element called `)` in place of the `b` the person typed, into the map and the saved file; `()a|b` left the unit empty | `catalog.strands_problem` counted brackets over the WHOLE code, while the library's `get_strand_ids` splits on `|` before it reads a bracket | TWO hunts independently: spec11 (C-348's "every refusal is ours" checked against the library's parser; the file read through sqlite) and prose12 (the guide's promise that the letters typed are the elements drawn). Verified here by reading `tiling_utils.get_strand_ids`, which splits first | `test_a_strands_code_the_box_accepts_builds_exactly_its_letters` -- every bracketed code over `a`, `b`, the brackets and `|` up to five characters that the box accepts is built through `catalog.make_unit`, its tile ids required to be its letters; plus four codes added to the refusal test | `strands-brackets-match-within-each-direction` (first judged HUNG, the sweep too large for the judge; shrunk and made to stop at twelve failures, then caught) | the new sentence "A pair of parentheses in a strands code must hold a strand." awaits the maintainer's text review |
| 7 | Under "Ignore, like an inset", a topology that finished while a handle was pressed -- a press that then committed nothing -- was applied without its gluing: the tab's class list grew from the glued classes to the library's (35 rows against 18) and every later drag previewed a different edit from the one the drop put on the map | `TopologyPanel.set_unit` HELD a landing arriving mid-press as a tuple of four -- unit, topology, message, ghost -- written before `glue` was added, and replayed it with `set_unit(*held)` | asym11 (standalone panels given the landing directly and through the held path; then the drag preview's geometry against `apply(glue=...)`, 135,000 map units squared apart). Verified here by reading the tuple at the hold and the replay | `test_a_landing_held_under_a_press_keeps_the_glued_reading` -- the same landing given directly as the oracle, the press staged on the store `gesture_in_progress` reads, and premises that the landing was held and that a lost gluing changes the rows | `a-held-landing-keeps-its-glue` | the dual button's chain also builds a weave with no weave terms at full width, which is the queued dual claim |
| 8 | With live update off and an edit standing on a weave, a Save pressed just after switching a weave reading wrote the UN-EDITED unit as the file's motif, beside the edited tiles, the old dual and a record listing the edit, saying only that it had saved | row 4's repair made the switch rebuild the unit, but `_topology_stamp` carried no reading, so `_a_topology_is_owed` found the motif current and did not defer the press, and the held dual still counted as this design's | repairs26, aimed at row 4's repair (the file read with sqlite and shapely, the motif matching the plain unit exactly, against a patient control). Verified here by reading the stamp's terms beside the key row 4 widened | `test_a_save_just_after_a_reading_switch_writes_the_new_motif` -- the window staged by running the rebuild the debounce would, a premise that a build was outstanding at the press, and the file's unit table read through QGIS against the record replayed under the new reading | `a-weave-topology-stamp-carries-its-readings` | row 4's separate landing discard is removed as redundant, the stamp now answering that question; the guard's first staging never opened the window and was caught by its own FIXTURE assert |
| 9 | Every landing of a thin weave on the Topology tab -- opening it, moving a chooser, an edit's rebuild -- froze QGIS for about 13 s on `plain weave a|b` and 47 s on `basket weave ab|cd`, and on the basket weave the dual button then came out disabled, so the freeze bought nothing | the dual's corners are tile centres found by `polylabel` at a thousand-millionth of the tile's size (`_exact_centre`), and a rectangle's pole of inaccessibility is a whole segment the search subdivides for ~100 ms a tile; a scaffolded weave is rectangles, and the search ran once per patch copy, on the main thread | perf1 (a 50 ms main-thread heartbeat: a 47,343 ms gap overlapping `set_unit`; cProfile, 56/192/392 searches for 13/41/85 base tiles; shapely alone, 73 ms for a rectangle against 0.1 ms for a kite). Verified here by counting the searches `complete_dual` asks on the scaffold -- 56, every one from `_consistent_centre` | `test_a_weaves_dual_asks_no_search_of_a_rectangle` -- the real `polylabel` wrapped and counted, the allowance the base tiles that are not their own half turn by the test's own check, the Cairo tiling as the control that must still be searched | `a-centrally-symmetric-tile-is-not-searched` (first SURVIVED on a loose allowance, tightened, caught) | the dual is still computed twice on the main thread at a landing, now cheaply; perf1's probe scripts were removed with its worktree before this row was written |
| 10 | On `cube weave a--|b--|c--`, typing the strands code `pq|r|s` moved the element count to 4, where no cube family exists, and landed on `plain weave ab|cd`: the box showed the code accepted, the map drew `a,b,c,d`, and the file recorded `pq|r|s` beside a family that refuses it, with nothing said | `catalog.strands_problem` never asked whether a three-direction code names a count at which a cube weave exists; the dialog checks the code against the OLD weave type before the count moves, and `_strands_in_force` then dropped it against the new family in silence | repairs28 (the layers, table rows and saved tables all `a..d`, the record keeping the typed code). Verified by the repair branch typing the code key by key and reading the unit, the kwargs and the saved design record | `test_a_cube_strands_code_the_box_accepts_is_the_code_the_map_draws` -- premises from the catalogue and the library, and an accepted arm (`ab|cd|ef` at six) so a repair refusing every cube code cannot pass | `a-cube-code-names-a-count-a-cube-weave-has` (re-judged caught on the merged tree) | two new refusal sentences await the maintainer's text review |
| 11 | On a cube weave a two-direction code such as `ab|c` passed the box unmarked; the unit was not built and Generate said only "The tile unit could not be built." | `strands_problem` refused a third direction on biaxial weaves but never required three on a cube (`TRIAXIAL_WEAVES`) | repairs28 (the dialog), and the repair branch by `catalog.make_unit` directly, where the library raises `ZeroDivisionError` | the same test | `a-cube-code-needs-all-three-directions` (re-judged caught on the merged tree) | -- |
| 12 | A weave edited under "Ignore, like an inset" (or with warp and weft together) came back from a reopen, a group choice or a Load under the defaults "Count" and "Apart", the edit replayed on other edges, and the next Generate or Save wrote that other map and rewrote the file's unit and dual tables, with nothing said -- also when the file was Loaded into a dialog already on the other reading | neither weave reading was in the working-state record: `_capture_design` did not write them and `_restore_recorded_topology_edits` had nothing to set | harm16 (Load; the file read with pyogrio and sqlite), stoch13 (Load into a dialog on the other reading), and the repair branch by the ordinary reopen, adoption reading the group's record | `test_a_weaves_readings_come_back_with_its_record` -- a reopen against the edit replayed from the settings under the recorded readings, with a premise that the defaults land elsewhere; a Save then Load into a dialog on the other readings; and a record with the keys removed restoring the defaults | `a-restored-record-puts-the-weave-readings-back`, `the-record-carries-the-weave-readings` (both re-judged caught on the merged tree) | no entry breaks the Load arm alone |
| 13 | After ANY topology landing -- including the one a person's own Apply or drop causes -- a selection of several classes was replaced by the first class in the chooser (vertex A), so the next Apply or drag edited a class nobody chose, and it reached the map | `TopologyPanel._refresh_classes` cleared the temporary "N of M" row before looking for the held selection in the combo | stores19 (the tick list, and modified clicks in the drawing reaching the change list and the shelf). Verified by the repair branch on a standalone panel on `hex-slice 2`, handing it a landing directly | `test_a_landing_keeps_a_selection_of_several_classes` -- with a single-class control and the weave choosers' deliberate drop as an arm | `a-landing-puts-back-every-class-held`; `a-rebuild-keeps-the-class-a-person-chose` re-anchored (both re-judged caught on the merged tree) | -- |
| 14 | Under "Ignore, like an inset", choosing glued class `a` lit only its own library label's edges while Apply also moved `g`'s -- 24 edges drawn against 42 moved -- and a click on a `g` edge selected a class the chooser does not list ("1 of 12 edge classes") | the view's paint, handle seat, hover and click compared LIBRARY labels with GLUED class names | repairs23 (the drawing's own hit test and its pixels). Verified by the repair branch on a standalone panel, choosing through the tick list | `test_a_glued_class_is_lit_and_clicked_as_the_class` -- the drawing's pixels sampled, a control of other classes' edges, and a real mouse click | `the-drawing-compares-labels-by-glued-class` (re-judged caught on the merged tree) | the symmetry gate still passes glued vertex class names to `directions_a_class_may_move`, unjudged; the drawn edge and vertex labels now show glued class names |
| 15 | At strand width 1.0, where a weave takes the plain topology path, "Warp and weft classes: Apart" silently kept the library's mixed classes, so an edit aimed at a class the toggle called one family moved warp and weft together, with nothing said | `topology_edits.strand_directions` read directions off the tiles' long axes, and a square has none: `plain weave a|b` read one direction ([90], a rounding tie) and five twills read [45], so `keep_warp_and_weft_apart` refused with a note `build` discarded | spec10 (13 plain weaves and 5 twills across the catalogue; the dialog offering the same classes under both settings). Verified by the repair branch calling the refinement directly, unrotated and at 30 degrees, where the tie broke the other way | `test_a_full_width_weave_keeps_warp_and_weft_apart` -- on `plain weave a|b` and `ab|cd`, no Apart class holding both horizontal and vertical edges, with premises that Together does mix them and that the plain path was taken | `a-full-width-weave-reads-its-directions-off-the-design` (re-judged caught on the merged tree) | the refinement's refusal notes are still discarded by both paths |
| 16 | A strands code TYPED onto a weave entry without a hyphen -- `a|b-` onto `twill weave a|b` -- had its hyphen's ground read as strand-width daylight and glued across as though it were cloth, where the entry that ships the same code keeps it dropped | `daylight_by_kind` read the CATALOGUE entry's strands code, not the one the person typed | spec10 (the typed code against the entry's own). Verified by the repair branch comparing the scaffolds' kinds: typed, strand 2 / aspect 12; the entry, strand 2 / dropped 7 / aspect 15 | `test_a_typed_hyphen_leaves_its_ground_dropped` -- its oracle the shipped entry `twill weave a|b-`, which builds under QGIS's own GEOS: kinds, dropped ground and glued holes must match | `a-typed-strands-code-names-its-own-hyphens` (re-judged caught on the merged tree) | -- |
| 17 | On a thin weave the Topology tab offered "Generate the dual and tile it", and pressing it made a "— dual" group whose map and saved tiles were the SOURCE design itself, its edits dropped, with only a quiet sentence; the file's dual table held the scaffold's 25-tile dual | the offer asked the tab's topology, which is the scaffold's, while `_build_unit`'s dual chain built the plain unit, which has no topology -- two answers to whether the design has a dual | repairs25 (OGR on the saved file). Verified by the repair branch by the library route: the plain build refuses while the scaffold's dual is offered | `test_a_thin_weaves_dual_is_not_offered_where_the_map_cannot_take_it` | `a-weaves-scaffold-offers-no-dual` (re-judged caught on the merged tree) | the new sentence `SCAFFOLD_HAS_NO_DUAL` awaits text review; what a thin weave's dual MEANS, and the scaffold's dual a Save writes, are a design question in ROADMAP.md |
| 18 | In the dual group of a weave that tiles at full width, the tab refined the dual TILING by warp and weft -- 8 edge classes where its symmetry gives 4 -- so a zigzag on one class moved half the edges it should | the dialog handed the build the weave's terms whenever the spec was a weave, a dual group included | unreach13 (the module, the dialog and the map). Verified by the repair branch reading the tab's classes against the dual built from the settings | `test_a_weaves_dual_group_takes_the_classes_of_a_tiling` -- its premise RESTAGED on the dual's own tile directions after row 15 merged | `a-dual-is-built-with-a-tilings-terms` SURVIVED on the merged tree, measured as HELD REDUNDANTLY: since row 15 the weave's terms no longer split a dual's classes at all (4 and 4) | the same harm is closed by two repairs, neither alone observable by this guard |
| 19 | In a dual group made from an edited design, every full run waited on a topology build it could not use and then said the Topology-tab changes were left off the map, though the map was the edited design's dual and the tab listed none | `_topology_edit_key` carries the dual's frozen source edits for the signatures and the cache, and the `_generate` replay gate read that as edits a build owed | unreach14 (452 and 280 landed tiles matching the edited dual). Verified by the repair branch timing whether a run launched at the press | `test_a_dual_group_of_an_edited_design_draws_without_waiting` | `frozen-edits-owe-no-replay` (re-judged caught) | -- |
| 20 | Switching a dual group's family (laves 3.3.4.3.4 with a zigzag, to hex-slice 4) drew and saved the dual of hex-slice 4 bent by the laves zigzag, replayed by label, the tab listing no changes | the dual chain replayed frozen edits onto whatever design was on screen and discarded `apply`'s refusals | stoch14 (the map against rebuilt duals; OGR on both files). Verified by the repair branch, including the switch back | `test_a_dual_groups_frozen_edits_follow_their_own_design` | `a-frozen-edit-replays-only-onto-its-design` (re-judged caught) | -- |
| 21 | DATA LOSS: pressing the dual button on a saved group A and then choosing a never-saved group B before the dual's topology was ready dropped the dual in silence and put A's GeoPackage path in B's Save box, and the next Save overwrote A's file with B's map with no question; from a dual group, B also inherited the dual box and chain | `_on_group_chosen` refused only while a tiling run was in flight, and `_settle_a_dual_request` restored its four stashed values -- the new-group flag, the dual box, the chain and the path -- over B's just-restored record | stores20 (the plugin's stores through sqlite, and A.gpkg's features and record through GDAL). Verified by the repair branch | `test_a_group_chosen_while_a_dual_press_waits_keeps_its_own_file` -- intermittently HUNG under load on the merged tree (the nested-pump stall), passing on a standalone rerun | `a-group-choice-waits-for-a-dual-press` (re-judged caught) | closing the window, File > New or a Load while a dual press waits may leave the request standing, unverified |

ROW 1'S REPAIR HAS ONE OWNER, `topology_edits.widen_selector(selector,
glue, how)`, which takes the target from the MANIPULATION; `apply`,
the drag preview and `_refined_towards_what_was_asked` all call it,
and the dialog hands `built["glue"]` to the replay. Its lesson is the
row every hunt that reached it wrote: when a function gains a
parameter, grep its PRODUCT call sites, not its tests.

ROW 2'S HAS ONE TOO, `topology_edits.labels_in(selector, alphabet)`: a
string that is itself a label is that label, an older joined record on
a one-letter design is its characters, and a list is its members;
`move_as_applied` is the one door to the library and hands it a tuple.
A multi-class selection is now recorded as a LIST, a shape "every edge"
already wrote, so no older record changes meaning.
ITS FIRST FORM WAS INCOMPLETE, and the hunt aimed at it said so within
the hour (repairs24): a record naming `aa`, replayed on a design with no
`aa` class, was still split into `a`, `a` and moved `a`. The reading now
takes the record's own `against` -- an alphabet that held two-letter
labels named classes as labels -- and accepts a split only into DISTINCT
known labels, a repeated character with no such split being one label.
Counted as the same row, since the same user loss through the same
door; `a-record-made-against-two-letter-labels-names-one-class` stands
on it.
AND A THIRD DOOR, THE GLUING (trigger11, the tests-that-cannot-fail hunt
on this round's guards): `_expanded` read the selector without the
record's `against`, so under "Ignore, like an inset" a record naming
`af` was split into `a` and `f` and moved both glued classes. The
alphabet now travels through `widen_selector`; still the same row, and
`the-glued-door-reads-the-records-alphabet` stands on it. The same hunt
reported two guard gaps -- the replay arm's `aa` refused by another
branch whatever `against` said, now joined by an `ab` arm through the
gluing, and the drag preview's widening, which no test mutates.

ROW 3'S REPAIR is `topology_edits.cloth_of(unit, kinds)`, run on the
worker beside the replay: the tab goes on drawing and aiming with the
scaffold its topology is of, and the dialog adopts the cloth.

ROW 4'S REPAIR: `_topology_edit_key` carries the two weave readings
WHERE EDITS STAND (without edits they move no geometry, and the key
stays empty so nothing waits for a replay); both chooser handlers take
the edit's own door, `_queue_preview`, when edits stand; and a landing
whose readings have moved is discarded as superseded.

ROW 5'S REPAIR: `topology_edits.modified(unit, modifiers)` applies
rotate, scale and skew to the scaffold before the topology is built, in
`_build_unit`'s order and skipping identities; the dialog hands the
controls' values in `weave_terms["modifiers"]`, and a weave's tile inset
goes on the cloth after the replay, an inset being gaps no topology can
be built on.

ROW 8'S REPAIR: `_topology_stamp` carries a weave's two readings (a
tiling's stamp and saved key are unchanged), so a landing, a held dual
and a Save's owed-topology question all see a switch; row 4's own
discard branch is deleted in favour of it.

ROW 9'S REPAIR: `_exact_centre` answers a centrally symmetric tile with
its centroid, which lies on the pole of inaccessibility wherever that is
a point or a segment, so the library's choice of centre is kept; and
`_consistent_centre` searches once per base tile, keyed by its shape.

ROWS 10 AND 11 were repaired on the branch `round10/strands` by a
repair agent working in its own worktree, merged and re-judged here:
`catalog.weave_type_counts` reads which counts each weave type exists
at off the catalogue, and `strands_problem` and `_strands_in_force` ask
it, so the box and the map cannot disagree. The claim that a
count-changing code resets a typed over-under was judged a DESIGN
QUESTION rather than built -- a family change resetting it is today's
documented behaviour -- and is put to the maintainer.

ROW 12 was repaired on `round10/groups`: `_capture_design` writes both
readings for a weave (not for a tiling, so older tiling records pass the
save's staleness check), and the restore sets both choosers through
`TopologyPanel.put_the_readings`, signals blocked, BEFORE any edit is
filed or replayed, whatever the dialog holds, a missing key restoring
the defaults; `WORKING_STATE_VERSION` is unchanged. The same branch
judged boundary13's group-shelf leak a DESIGN QUESTION -- the rulings
pull both ways on whether a session's idle edits belong to a group or a
design -- and it is put to the maintainer with a recommendation to clear
the shelf at the chooser door.

ROWS 13 AND 14 were repaired on `round10/tabsel`: `_refresh_classes`
reads what to put back from `_selection` and restores it only where every
class survives, compared as labels; and the view holds the gluing
(`show_topology(glue=...)`), one `_class_of` answering for the paint, the
handles' seat, the hover and the click.

ROWS 15 AND 16 were repaired on `round10/spec10fix`: a new
`design_strand_directions` reads a weave's directions off the same design
rebuilt at strand width 0.5 with its modifiers (all 77 catalogue weaves
reading the right number, rotated or not), passed to
`keep_warp_and_weft_apart` as `directions`; and `daylight_by_kind` takes
the typed `strands`, which `scaffolded_weave` hands it.

ROWS 17 TO 21 were repaired on `round10/duals`: `dual_on_offer` refuses a
topology that `stands_on_scaffolding`; `_weave_terms_at(depth)` gives no
weave terms above depth 0; `_edits_a_replay_owes` answers the replay
gate; frozen edits carry `frozen_for` and `_frozen_for_this_design`
filters them, the chain's refusals reported; and `_on_group_chosen`
waits while `_a_dual_press_is_waiting`. Row 18 is kept as a row, a real
defect at `94fafd5`, but its own entry does not stand: the harm is held
by row 15 as well. Twenty-one rows against a target of sixteen.

## Instrument faults, tallied apart

- The new guard's first run failed on `_wait_for_the_topology` giving
  up on a machine carrying seven hunts while the panel held a topology;
  the wait for the glued class itself is the one kept.
- Row 4's guard first waited for the unit to MOVE, which the plain
  rebuild between a design change and its replay also does; it waits for
  agreement now (T-132's shape).
- The preview repair stranded `the-preview-takes-the-commits-own-route`
  (C-288); re-anchored and judged `caught` again.
- Two entries re-anchored under row 4 --
  `moving-the-warp-and-weft-chooser-rebuilds-the-topology` and
  `a-file-holding-a-motif-gets-a-fresh-one` -- came back HUNG, not
  caught, from `mutation_check` on a machine carrying six hunts; the
  second has reported HUNG before and was judged by hand. Both are owed
  a judgement on a quiet machine.
- Row 9's ledger row and a docstring correction missed its own commit:
  the editing script stopped at a non-matching anchor while the gates
  and the commit, run after it in the same command, did not depend on
  it. Committed separately.
- The merged-tree verification of the duals branch hung for nearly seven
  hours at under two minutes of CPU on
  `test_a_group_chosen_while_a_dual_press_waits_keeps_its_own_file`, and
  again for six minutes; a faulthandler rerun passed. It is recorded as
  the nested-pump stall of R-93 under load, not as a defect.
- perf1's and repairs27's probe scripts went with their worktrees before
  their rows were written; their reports carry the recipes.
