# Defects found on 2026-09-06

Round eight's ledger: eight hunts as Opus subagents toward SIXTEEN
closed bugs, the maintainer's instruction of 2026-09-05 (late). The
campaign's rules are in `HUNT-RECORD.md` under "ROUND EIGHT"; what
matters here is what a person would have lost, where it lives, when it
arrived, and what each row still OWES. A row is CLOSED only when the
claim was reproduced BY A ROUTE THE HUNT DID NOT USE, repaired, guarded
by a registered test, proved by a catalogue entry judged `caught`, and
committed.

## Closed

| # | What a person loses | Where | Since | Guarded by |
| --- | --- | --- | ---: | --- |
| 1 | A zigzag set by eye on the Topology tab -- the handle dragged out until the ghosted wave looks right -- puts a wave 2.1 times SHALLOWER on every tile of the map and in the saved file, because the handle, the ghost and the drag's inverse all stood at the whole of `h` while the library's `h` is peak to peak and its crest sits at half of it. | `topology_tab._draw_the_zigzag_it_would_make`, `handles`, `_drag_argument` and `_amplitude_deadband`, now sharing `_CREST_OF_H` | 2026-09-05, `f13b5f5` and `a48f09e`, the handle-as-position and ghost repairs for field reports 3 and 4 | `test_the_zigzag_ghost_crests_where_the_library_does`, entry `the-zigzag-ghost-crests-where-the-library-does`; three existing tests moved their arithmetic. Found by `spec6`, verified here on the edited unit's own polygon |
| 2 | Dragging the zigzag handle records a wave several times deeper than the one dragged to -- by as many mouse-move events as the machine delivered, a 5px drag as 25 events recording h 0.288 for 0.068 asked -- and that amplitude is what the design is tiled with and the file carries. | `topology_tab._on_dragging`, seeding the drag's position from the LIVE boxes each frame had just rewritten, while `_drag_started_with` was written for it and read by nobody but `_drag_moved` | 2026-09-05, `27fb730` and `051a906`, the handle-as-position repairs | `test_a_drag_delivered_in_many_moves_records_one_position`, entry `a-drag-is-measured-from-where-the-handle-was-taken`. Found by `repairs16`, verified here through real Qt events, one move against twelve |
| 3 | A zigzag count typed as 3 and followed straight by a drag on the handle is recorded as 3 -- the drawing takes no focus, so `editingFinished` never settles it -- and the map is tiled with an odd count the library lays out with gaps, 4.5% of hex-slice 4's class b. | `topology_tab._on_grabbed`, which stamped the boxes' numbers without settling the count | 2026-09-05, `ee84699`, the even-count ruling | `test_a_typed_odd_count_is_settled_when_the_handle_is_taken`, entry `a-typed-count-is-settled-at-the-grab`. Found by `asym6`, verified here through real key and mouse events. SUPERSEDED THE SAME MORNING by row 8: the grab settle left every other door open and is deleted, `_arguments` settling the record instead |
| 4 | Pressing "Generate the dual and tile it" while Generate cannot start -- no region layer yet, which is how the Topology tab is first met -- latches the plugin into dual mode invisibly, so the next ordinary Generate draws the DUAL of the design into a new group with no control on screen to untick. | `dialog._generate_the_dual`, writing `_new_group_chosen` and `opt_map_dual` BEFORE the act with eight refusals below it; settled now in a `finally` round `_generate` | 2026-09-05, `92851d9`, the dual button | `test_a_dual_request_that_is_refused_does_not_latch`, entry `a-refused-dual-request-is-put-back`. Found by `unreach9`, verified here through a run refused at the spacing floor after the deferral and through the no-layer door |
| 5 | The zigzag handle and its ghost bulge to one side of the edge and every tile the map and the file receive bulges to the OTHER, so the two tiles sharing that edge swap the ground they cover and the colours move with it. | `topology_tab.handles` and `_draw_the_zigzag_it_would_make`, whose screen normal was the mirror of the library's first lobe once the view's y-flip is counted; the drag's inverse with it | 2026-09-05, `f13b5f5` and `a48f09e` | `test_the_zigzag_ghost_crests_where_the_library_does`, now sign-aware, entry `the-zigzag-handle-sits-on-the-side-the-wave-goes`. Found by `repairs17` at the round's own crest repair, verified here by mapping the handle back to unit space against the edited unit's first lobe |
| 6 | With topology edits standing, "Generate the dual and tile it" draws and saves the dual of the design BEFORE the edits, while the tab shows the edited motif and its dual: 452 landed tiles matching the plain dual and none the edited one. | `dialog._build_unit`, taking the dual of the catalogue unit; the source's edits shelve under the key without the dual term and the restore asks for the key with it; `_edits_of_the_source_design` replays them first now | 2026-09-01, `071ad0f`; reachable by button since `92851d9` | `test_the_dual_is_taken_of_the_design_as_edited`, entry `the-dual-is-of-the-design-as-edited`. Found by `harm12`, verified here off the landed layers' feature areas against both duals built from `topology_edits` |
| 7 | A second press of the dual button, standing on the dual's group, lands a third group named `-- dual -- dual` holding a byte-identical copy of the first dual, with nothing said, and its saved file's motif is that dual. | `topology_tab._offer_the_dual`, judging the offer on the DUAL's topology, against `_build_unit` reading a boolean and taking the dual once | 2026-09-05, `92851d9` | `test_the_dual_button_refuses_on_a_dual_group`, entries `the-dual-button-is-not-offered-on-a-dual` and `the-dual-act-refuses-on-a-dual`. Found by `stores16` and `stoch8` independently, verified here off both groups' feature geometry digests |
| 8 | An odd zigzag count typed without Return reaches the record through EVERY door but the one row 3 closed: a sibling handle grabbed (the chooser banks the boxes before anything settles them), the chooser switched away and back, and a plain Apply -- so the map is tiled with gaps, 3.87% of hex-slice 4 lost as holes. | `topology_tab._arguments`, which read the boxes as they stood; it settles the count now, being the one owner every door reads, and row 3's grab settle is deleted as redundant | 2026-09-05, `ee84699` | `test_a_typed_odd_count_is_even_at_every_door`, entry `the-count-is-even-where-the-record-reads-it`. Found by `repairs18`, verified here through the chooser door and the plain Apply |
| 9 | A map saved to a GeoPackage, then "Create new" in the group chooser or the dual button, then Save: the new map replaces the saved file's tables, with no question and nothing said, since the create-new door never cleared the output path the dataset door has cleared since 2026-08-21 and the overwrite question stays quiet for a file of ours. | `dialog._add_output_layers`, at the landing that creates a group on purpose; `_a_new_map_does_not_inherit_the_file` clears and says so | 2026-08-25, `18df97d`, the chooser | `test_a_new_group_does_not_inherit_the_previous_maps_file`, entry `a-new-group-clears-the-inherited-path`. Found by `boundary10`, verified here through the chooser's own `activated` signal and sqlite, the second save landing in the first map's file. COMPLETED LATER THE SAME DAY: the launch snapshot's `output_path` was still stamped on the created group's record, so a reopen put the file back in the box (`stoch10`); the snapshot is blanked with the box, entry `a-new-groups-record-does-not-carry-the-old-file` |
| 10 | A dual group reopened and re-tiled draws the dual of the UN-EDITED design, the person's edit gone in silence; and with the cache on, the dual of a design edited a second time was served from the frame of the dual before it. Row 6's repair read the source's edits off a per-dialog shelf that is empty after a reopen and that the record's one `topology_edits` slot never carried, and the edit key -- hence the geometry signature and the tiled-frame cache -- never saw them. | `dialog._generate_the_dual` (frozen at the press), `_capture_design` and `_restore_recorded_topology_edits` (`dual_source_edits` in the record), `_topology_edit_key` (the key carries them), `_build_unit` (reads the frozen copy) | 2026-09-06, `4f4505c`, row 6's own repair | `test_a_dual_group_keeps_its_sources_edits_across_a_reopen`, entry `the-duals-source-edits-are-restored-from-the-record`; `test_a_second_edit_of_the_source_makes_a_different_dual`, entry `the-duals-key-carries-the-sources-frozen-edits`. Found by `repairs19` and `unreach10` (the reopen) and by `stoch9` and `harm13` (the key), verified here by reopening the plugin and re-tiling the dual group |
| 11 | Open a project holding a saved map, pick its group in the chooser, then point the plugin at another dataset: the output path stays aimed at that map's GeoPackage in silence, and the next Save replaces its tables with the other dataset's tiles. Both Load doors count a map as this session's work; the chooser, the third door, did not. | `dialog._on_group_chosen`, which never wrote `_landed_this_session` | 2026-08-25, `18df97d`, the chooser; the Load doors gained the flag on 2026-08-28 and 2026-09-02 | `test_a_group_chosen_in_the_chooser_counts_as_this_sessions_work`, entry `a-chosen-group-is-this-sessions-work`. Found by `doors7` (OGR on the file), verified here off the path box and the notice after the dataset switch |
| 12 | Set a zigzag amplitude, choose Rotate or Scale on the same edge, grab the zigzag handle where it is drawn and drag: the map gets a wave 1.5 to 1.6 times deeper than the one dragged to, the handle having jumped 44px on a 125px edge to the DEFAULTS' position while the bank held the person's numbers. | `topology_tab._push_zigzag_readout`, whose other-verb branch read `MANIPULATIONS`' defaults rather than `_argument_memory` | 2026-09-05, `f13b5f5`, the handle as a readout | `test_the_zigzag_readout_keeps_the_banked_numbers_under_another_verb`, entry `the-readout-reads-the-bank-under-another-verb`. Found by `repairs20` (the unit's geometry), verified here off the handle's drawn position against the bank |
| 13 | Press the dual button, close the window before the run lands, reopen the plugin and press Generate: the dual of the design lands in a new group unasked. The request was spent the moment a task existed; the close cancels the task and the dialog object outlives the window. | `dialog._settle_a_dual_request` (spent at the launch), `_finish_run` (spends it now), `closeEvent` (puts it back BEFORE the cancel, since the task's own end reaches the landing) | 2026-09-06, `777eec8`, row 4's own repair | `test_a_dual_request_whose_run_is_cancelled_is_put_back`, entries `a-cancelled-duals-request-is-put-back` and `the-request-is-spent-at-the-landing`. Found by `writeonly6`, verified here by closing mid-flight and reading the stores and the groups |
| 14 | Press the dual button, then File > New or File > Open while it is still tiling: the run is cancelled and the plugin stays in dual mode, so the first Generate in the new project draws the DUAL of the design, in a group named for the previous project's dataset. Row 13 closed one cancel door of two. | `dialog._forget_the_last_project`, the project door's cancel, which now puts the request back before it as the close does | 2026-09-06, `01791f2`, row 13's own repair | `test_a_dual_request_cancelled_by_a_new_project_is_put_back`, entry `the-project-door-puts-a-dual-request-back`. Found by `repairs21`, verified here by clearing the project mid-flight |
| 15 | A dual made of a design with NO edits follows its source's later edits on the next Generate: the frozen copy was `[]`, a truthiness gate wrote no key, the restore read None, and `_build_unit` fell back to the live shelf -- the derived group following its source, which ruling 4 refuses, and its record saying it has no source edits. | `dialog._capture_design` (written even when empty now), `_restore_recorded_topology_edits` (a list, empty or not, is a frozen copy) | 2026-09-06, `892a304`, row 10's own repair | `test_a_dual_of_an_unedited_design_does_not_follow_its_source`, entry `an-empty-frozen-copy-is-written`. Found by `stores17`, verified here through the dual group's landed profile after a source edit |
| 16 | Pick a saved map whose region layer has left the project: the binding returns silently, so the chooser names that map, the region combo stays on the OTHER dataset and the path box holds that map's file -- two stores naming two maps on screen; at the hunt's commit Generate landed the other dataset's tiles in that map's group and Save wrote its file. | `dialog._point_the_chooser_at`, which said nothing when no layer answered; it answers now and `_on_group_chosen` refuses in words, taking nothing over | 2026-08-28, `333ba1b`, the walk's consolidation | `test_a_group_whose_layer_has_gone_is_refused_in_words`, entry `a-group-whose-layer-has-gone-is-refused`; a sentence awaiting review. Found by `spec8` (sqlite on the file), verified here off the three stores after the chooser's own signal |

## A question for the maintainer, recorded rather than decided

The Amplitude box still reads in the library's `h` (peak to peak, 0.01
to 1.0), so a typed 0.4 draws a crest 0.2 of the edge out; the handle,
the ghost and the map now agree about that. Whether the box should
instead read in crest units -- so the number a person types is the
distance they see -- changes the meaning of every saved record's `h`,
and is left as it stands.

## Claimed when the sixteenth closed, judged and left open

`boundary11`: a map loaded through the fresh Load door is named from
the dialog's CURRENT dual term, `_a_name_for_a_new_group` being
called before the record's `map_dual` is applied (dialog.py, the
fresh resume branch), so a dual file opened in a fresh project loses
its `-- dual` name and a plain file opened while a dual is on screen
gains one. The loss is a label, the file's record is right, and the
frozen edits survive every crossing the hunt drove. Not repaired
under this mandate; the fix is to name after the record is applied.
`harm14`: a Save As of a self-contained map drops the embedded
source, since `_embedded_when_resumed` is remembered per FILE and a
new name has no memory, so the untouched box reads as unticked and
the passed-on file cannot be redrawn, its record naming the sender's
file. Claimed with a control at the same path; not judged here.
`trigger10` had not reported when the target was reached; its
findings, if any, are in `dev/hunts/round8-logs/`.

## Settled by a ruling rather than repaired

`harm13` and `stoch9` asked that Generate on the dual group FOLLOW a
later edit of the source design. Ruling 4 of 2026-09-05 says the dual
is one-shot and does not follow its source, so the dual group keeps
the edits it was made with (row 10's frozen copy); a dual of the
design as now edited is one press of the dual button away, and lands
in its own group. Their mechanism -- the edit key blind to the edits
the dual was built from -- is row 10.

## A question for the maintainer, from the specification hunt

`spec7` measured zigzag ruling 2's deadband against the spacing of
its own stops: the count's stops sit at `L / (2n)`, so from n=4 the
next stops lie 0.042 and 0.021 of the edge away while the deadband is
0.10 of it, and a drag past the deadband from n=4 or n=6 records only
2 or 8 -- 4 and 6 cannot be reached by dragging, only typed. The
ruling names a 12px glyph and the constant is a fraction of the edge.
Whether the deadband should be sized from the stops' spacing, or the
drag should step one stop at a time, is the maintainer's call; it is
recorded here rather than changed.

## Instrument faults, tallied apart

Two dead axes, found by `trigger8` mutating the guards written since
rc15 per assertion with every control run first, and owed a repair:
`test_the_zigzag_ghost_passes_through_its_handle` asserts the ghost
meets its handle under `if seat is not None`, and on its bare view
`handles()` is empty, so the agreement is certified by nothing (the
entry `the-zigzag-ghost-crests-where-its-handle-sits` is killed by the
peak assertion instead); and `test_a_cached_switch_draws_what_a_retile_draws`
passes with `return held.copy()` made `return held`, since its journey
serves the cache once and never sees the stripped frame again.
BOTH REPAIRED THE SAME DAY: the agreement is asserted on the drawing
by `test_the_ghost_meets_the_handle_on_the_drawing`, which the entry
now names, and the cache differential makes a SECOND hit, guarded by
`the-cache-serves-a-copy`, both proved caught.

Three more from `trigger9`, at the guards written today, and one of
them repaired at once: `test_a_dual_request_that_is_refused_does_not_latch`
read two of the three stores the repair puts back (it reads all of
them and the request now); its deferral branch --
`_settle_a_dual_request` keeping the request across a remembered
press -- is guarded by nothing, because two attempts to stage a
press remembered with no task in flight launched a task instead, so
that branch's reachability is a reading and is OWED a driven guard;
and its launch branch is held redundantly by the dual-lands test.
