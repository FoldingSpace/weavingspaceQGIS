# Every defect found on 2026-08-17, and what each one still owes

A LEDGER, not a narrative. It exists so that nothing found on this day
depends on anybody's memory or on a conversation surviving: each row
says what was wrong, where, when it arrived, how it was found, and
what it still owes. Read it beside ROADMAP.md, which carries only what
is OUTSTANDING; this file carries everything, closed included, because
"was that fixed?" is the question a ledger answers and a roadmap
cannot.

THIRTY-NINE defects. NONE are open in code; three prose corrections
await the maintainer's own reading, which is the only act an assistant
may never perform here. Fifteen were found by hunts aimed at code written
that day; **seven were older** -- 10 August, 14 August, 15 August and
v0.23.0 -- and surfaced only because a hunt was pointed at the new work
beside them. Several were inside FIXES for defects the same day's
earlier hunts had found.

**ALL OF THEM ARE IN THE PUBLISHED rc7** (`v0.24.3rc7`, tagged at
`1866338`), which is green on every local gate and on both CI jobs.
rc8 replaces it; rc7 must not be promoted or sent to a tester.

## How to read the columns

**Owes** is the honest state of the guard, and it is the point of the
file. `test + entry` means a regression test exists AND a mutation
catalogue entry was proved to catch its removal. `test` means the test
exists and the entry does not. `--` means neither, and the fix rests
on its hunt's reproduction alone, which is not what this project calls
fixed.

**MOVE A ROW IN THE COMMIT THAT LANDS ITS GUARD**, and the reason is
that this file failed to on the day it was written. Rows 8, 14, 15 and
18 were guarded in `522ad73` -- whose own message ends "Four of eleven
owed guards are in, the ledger's OWES column is the list" -- and the
NEXT commit updated this file without moving any of the four. They
read `--` for a day. A stale OWES column errs in the direction that
looks careful: it overstates the debt, so the next reader is sent to
write guards that already exist, and the four rows that genuinely owe
nothing are indistinguishable from the ten that do. ROADMAP.md
repeated three of the four as outstanding at the same time. Corrected
2026-08-17 by diffing every commit made after this file was written
against the suite and the catalogue, which is the check to repeat
rather than the mistake to remember.

## The ledger

| # | What a user lost | Where | Since | Owes |
|---|---|---|---|---|
| 1 | A pinned bound typed wider than the element's data was truncated -- 1200 kept 120 -- and the map drawn from it | `category_editor._bound_box` | 08-17 | test + entry |
| 2 | ...and the same in reverse: 0.5 on a column of square metres clamped to 0 decimals | `category_editor._bound_box` | 08-17 | test + entry |
| 3 | Nothing told a user a class was empty, though four documents and the approved changelog said something did | `dialog._legend_size_note` | 08-17 | test + entry |
| 4 | The spacing a refusal advised was rounded down as it printed, so typing it met the same refusal | `bridge.min_reasonable_spacing` | 08-07 | test + entry |
| 5 | The table never followed a renderer changed in QGIS: breaks, field, count, ramp | `dialog._on_layer_style_edited` | 08-10 | test + entry |
| 6 | A stale `disabled_by_deferring` mark left the Classes spinner live at a distinct-value count, clamped to 20 at the next rebuild | `dialog._refresh_deferring_rows` | 08-15 | test + entry |
| 7 | The Unclassed pin guard survived deleting the whole cell-rewrite loop; the strip's pin button was never clicked | `tests/run_tests.py` | 08-17 | test + entry |
| 8 | A ramp changed in QGIS never reached the exported GeoPackage (graduated) | `dialog._graduated_layer_edited` | 08-17 | test + entry |
| 9 | Every Unclassed row was warned a third of its fifty classes were empty | `dialog._classes_nothing_wears` | 08-17 | -- |
| 10 | A far pin destroyed a small one: 6e-10 read back 0.0 and moved nine of twenty features | `category_editor._bound_box` | 08-17 | -- |
| 11 | Leaving deferral for a hand-mixed fill lost it: `#0b1e2d` became `#3c8bc2` | `dialog._refresh_deferring_rows` | 08-15 | -- |
| 12 | Rotate, Skew and both angle boxes sat at ZERO decimals: 22.5 became 22 and the unit was built from it | `dialog._limit_the_figures_on_show` | 08-17 | test + entry |
| 13 | The spacing advice was hand-punctuated: under de_DE, typing it drew a map ten times too coarse in silence | `bridge.spacing_in_words` | 08-07 | test + entry |
| 14 | A break retyped in QGIS never reached the exported GeoPackage | `dialog._graduated_layer_edited` | 08-10 | test + entry |
| 15 | A style pasted while a run was in flight was destroyed by the landing | `dialog._on_layer_style_edited` | 08-15 | test + entry |
| 16 | A followed class count was written to the spinner and not `_class_counts`, so a rebuild reverted it | `dialog._row_follows_the_renderer` | 08-17 | test + entry |
| 17 | The colour editor printed class bounds with a full stop beside boxes parsing through QLocale: 4.052 pinned 4052, stamped | `category_editor._format_bound` | 08-10 | test + entry |
| 18 | A categorized dock edit -- a stroke, a label, a deleted class -- never reached the exported file | `dialog._on_layer_style_edited` | 08-10 | test + entry |
| 19 | The Ramp Display Range's percent boxes clamped each other: from (0, 40) typing 60 kept 6, recoloured and stamped | `category_editor` range section | 08-10 | test + entry |
| 20 | The follow ignored the row's Reverse, so a forward ramp "matched" a reversed row and the next edit flipped the map | `dialog._graduated_layer_edited` | 08-10 | -- |
| 21 | Deferral silently retired the No Data split: 28,828 unpainted pixels of 490,000 | `dialog._needs_a_no_data_split` | 08-15 | test + entry |
| 22 | A copied ladder was destroyed the instant it was copied, judged against the receiving column | `dialog._retire_an_undrawable_pin` | 08-16 | -- |
| 23 | Pins accepted against the live class count were retired by a guard handed the launch snapshot; the stamp removed | `dialog._retire_an_undrawable_pin` | 08-16 | -- |
| 24 | The deferral exit wrote the style combo without `touched`, so a rebuild discarded what the user set in QGIS to take the element back | `dialog._refresh_deferring_rows` | 08-15 | -- |
| 25 | A reversed element's ramp swatch went back to forward at every rebuild | `dialog._make_ramp_combo` | v0.23.0 | test + entry |
| 26 | `pin_problem`'s refusal printed a full stop while the cell beside it printed a comma; typing the refusal pinned 716 for 7,16 | `bridge._trim` | 08-14 | test + entry |
| 27 | Typing any scale between -1 and 1 was mangled; from a mirrored design, -0.5 silently un-mirrored it | `dialog._skip_zero_scale` | 08-15 | test + entry |
| 28 | `_restyle_only` stamped the layer and THEN retired the pin, so a reopened project restored a pin the map ignored | `dialog._restyle_only` | 08-16 | -- |
| 29 | One dedup set gated a per-COLUMN notice and a per-ELEMENT retirement, so a dead pin was kept or dropped by what another element had done | `dialog._restyle_only` | 08-14 | -- |
| 30 | The design view paints colours the map does not contain: 30,316 px of one, 0 of the other, after a QGIS-side restyle | `dialog._table_id_colours` | 08-15 | test + entry |
| 31 | Opening a saved project with the dialog open replaces every element's opacity with whatever the open dialog showed; one Generate paints it into the .qgz | `dialog._sync_row` / `_refresh_table` | 08-13 | test + entry |
| 32 | Moving the Classes spinner to 2 with both ends pinned destroys BOTH bounds, the stamp and the saved copy, and blames the data -- which has not moved | `dialog._retire_an_undrawable_pin` | 08-16 | test + entry |
| 33 | The constant-column notice is said on the first Generate and never on the restyle path, which is the only route a class-count change takes | `dialog._legend_size_note` | 08-17 | test + entry |
| 34 | The design view ignored the row's Reverse too, so a reversed element previewed in the forward ramp's colour -- on a diverging ramp, the opposite end | `dialog._table_id_colours` | v0.23.0 | test + entry |
| 35 | ...and hand-picked class or category colours never reached it either: five classes picked green previewed as `#e7342a`, 144/255 from anything the element wears, while the ramp cell beside it read Custom | `dialog._table_id_colours` | v0.23.0 | test + entry |
| 36 | A constant column previewed 53/255 from the ONLY colour it draws: the renderer colours a one-class element from the middle of the ramp window, the preview sampled at 65% | `dialog._table_id_colours` | v0.23.0 | test + entry |
| 37 | A column with nothing to classify draws no-data on the map and its ramp colour in the preview -- `seed_renderer`'s second door to no data, which the preview never had | `dialog._table_id_colours` | v0.23.0 | test + entry |
| 38 | MY OWN REGRESSION: the count re-ask suppressed retirement on every route while only one door refused, so a style excursion left the record and the stamp claiming 10 and 60 over a map drawing 0/21/121, in silence | `dialog._retire_an_undrawable_pin` | 08-17 | test + entry |
| 39 | A deferring element's opacity is honoured on the restyle path and DROPPED on the run landing: 30% set, map returns at full strength, table and preview still say 30%, nothing said | `dialog._add_output_layers` | 08-15 | test + entry |
| 40 | Deferring an element ERASES its pinned bounds and hand-picked class colours from the saved project: the stamp is removed, the session goes on showing work the file no longer has | `dialog._stamp_category_colours` | 08-15 | test + entry |
| 41 | The per-element emptiness notice is still deduped by COLUMN, so one element silences another and the sentence quotes a class count the silenced element does not have | `dialog._legend_size_note` call sites | 08-17 | test + entry |
| 42 | A deferring element's No Data layer loses hand styling to a restyle but survives a re-tile: `_restyle_no_data_layer` has no `kept_by_hand` gate where the landing has one | `dialog._restyle_no_data_layer` | 08-15 | test + entry |
| 43 | A pin on a CONSTANT column is accepted, stamped and drawn as one class: the copy door draws five, the pin door collapses to one, silently -- two settled rules, opposite answers | `bridge.make_graduated_renderer` | 08-17 | test + entry |
| 44 | The user guide tells Unclassed users the class list carries no control and the range strip is the only one -- reversed by the maintainer on 08-17, when Unclassed gained a Pin column | `docs/USER-GUIDE.md:184` | 08-17 | awaiting text review |
| 45 | "The catalogue runs to twenty and nothing stops you using them all" -- the ceiling is 26, and has been since 08-11 | `docs/USER-GUIDE.md:73` | 08-11 | awaiting text review |
| 46 | README and MAINTAINING claim the vendor is at commit 80e1dab; the stamp says c0f109c, and the gate that checks this splits on whitespace so it has NEVER compared the commit half | `tools/sync_release_content.py` | 08-10 | awaiting text review |

Forty-six rows. **Rows 44, 45 and 46 are corrected and sitting in the text-review
queue**, which is the maintainer's act and not an assistant's. Row 46's
GATE is fixed and proved: `check_vendor_claims` compares the COMMIT
half of the vendor stamp now, and its first run named both stale files
by itself. It had promised that comparison in its own docstring since
it was written, and did `stamp.split()[0]` -- the version alone.
A GATE THAT CHECKS HALF OF WHAT IT NAMES IS WORSE THAN NO GATE,
because the other half is then believed to be checked. Everything else is fixed.

ROWS 36-46 CAME FROM ONE ROUND OF SEVEN HUNTS on the evening of
2026-08-17, launched under the record's own portfolio rule: two
TRIGGERED by the day's work (a batch of tests written fast, and two
fixes shipped against one fixture), one backwards-from-harm on the
unshielded restyle and retirement paths, and two directions the record
listed as NEVER TRIED -- the specification itself, and the prose as
claims. Both untried directions paid, which is the portfolio rule's
own argument: hold a third of any round on directions that cannot
pattern-match.

FOUR OF THE ROUND'S FINDINGS WERE IN CODE WRITTEN THAT SAME EVENING,
including one regression of mine (row 38) and one dead axis in a guard
I had committed an hour earlier. That is the rate this project has
measured all week and it did not change for being known. Several
fixed defects were repaired in one edit and are listed separately
because they are separately reportable and separately testable.

**Rows 34 and 35 are what fixing row 30 turned up**, and it is the day's fifth
family arriving on cue: `_table_id_colours` built the preview from the
ramp NAME, so it missed the deferring layer, the display window AND
the row's Reverse -- three arms of one expression, two reported and
the third found only by reading the line beside them. It had been
shipping since v0.23.0.

AND THAT WAS STILL NOT THE WHOLE EXPRESSION. A hunt pointed at the
fix an hour later found a FOURTH arm, older than any of them: the
call never read `quant_colours` or `category_colours` either, so an
element whose classes were all picked by hand previewed in a ramp it
does not wear -- while the ramp cell in the same row already read
Custom and said, at its own branch, that "hand-picked colours really
do leave the ramp behind". Three of the six colour keys
`_assignments` carries were still unread after the commit that was
supposed to settle this.

SO THE RULE IS SHARPER THAN "READ THE WHOLE EXPRESSION". When a fix
widens a call so it stops ignoring X and Y, ENUMERATE EVERY KEY OF
THE RECORD THE CALLER COULD HAVE READ, not the ones the report
happened to name. A hunt names what it measured; it is not a census.

## Rows 30-33: what is known, and what was already tried

**30, the design view.** `_table_id_colours` asks only the ROW -- mode,
single colour, `ramp_swatch_colour(a["ramp"])` -- and has no deferring
branch, though `_ramp_cell_icon` grew one on 2026-08-15 for exactly
this reason. It also ignores `range_bounds`, though
`graduated_class_colours` already samples a windowed ramp. Probe:
`tools/probes/preview_paints_colours_the_map_does_not.py`. The second
arm (the display window) is a PREFERENCE rather than a defect if
"representative of the ramp" beats "representative of the window";
the first arm is not.
RULED 2026-08-17: **both arms are defects.** The preview samples the
same windowed ramp the map does. The design view exists so somebody
can judge whether the elements read as distinct by colour and form,
and after a window is narrowed they may not -- which is exactly the
judgement it is there to support, so a preview drawn from the full
ramp answers a question nobody asked.

**31, the opacity, AND A FIX THAT DID NOT WORK.** Probe:
`tools/probes/opacity_lost_when_a_project_opens.py`. `_adopt_category_
colours` recovers `layer.opacity()` into `_opacity_choices`; the
Opacity cell is CREATED and never updated (`if row_id and
self._row_opacity(row) is None:`), `setRowCount` clears no cell
widgets, so an open dialog's spin survives and `_refresh_table` writes
the record back FROM it.
FIXED 2026-08-18, on the fourth attempt, and the ledger's own
instruction is what made the difference: INSTRUMENT WHICH REBUILD
WRITES THE RECORD. A dump behind `WEAVINGSPACE_ADOPT_DUMP` inside
`_adopt_category_colours`, `_refresh_table` and
`_forget_the_last_project` -- committed, not thrown away -- printed
the whole sequence in one run: `FORGET`, then
`PREV a: table=100 dialog=<none>`, then
`ADOPT a: layer=40 dialog=100`. The clear works; the TABLE survives it
and refills the records from the outgoing project's cell widgets
before adoption is ever asked.

THREE THINGS HAD TO BE TRUE, each separately wrong: adoption outranks
a record left from the project being replaced; the previous table
fills a GAP rather than overruling the record; and the Opacity cell
follows the record, having been created once and never updated.

Two of the earlier attempts are worth keeping as shapes NOT to repeat.
An `_adopting` flag set in `_adopt_existing_group` was read too late,
because the refill happens before adoption. Clearing the table in
`_forget_the_last_project` re-entered and ran for ten minutes without
reaching the reopen; removing its cell widgets instead let adoption
win but left the cell rebuilt at 100.

The probe SEGFAULTS at exit and always has -- confirmed against a
reverted tree -- so exit 139 there is the probe's own teardown and not
a verdict on the plugin.

**32, the Classes spinner.** Probe:
`tools/probes/pins_killed_by_the_class_count.py`, three parts.
`_retire_an_undrawable_pin`'s own comment says retirement is "for a
pin the DATA MOVED OUT FROM UNDER", and a class count is not that. Its
settled twin two lines away -- a copied ladder meeting a new class
count -- KEEPS the bounds, on the rule that "a pin is a smaller and
more durable statement than an imported ladder". Same handler,
opposite answers. `pin_problem` even has the right sentence and it is
never shown. NEEDS A RULING: whether a refused class count may destroy
pins at all, and if so why only this half.
RULED 2026-08-17: **it may not. The COUNT is refused and the pins
stay.** The spinner reverts and the user is told a count that low
cannot carry both pinned ends. That is `pin_problem`'s own convention
-- its caller reverts the control and reports rather than clamping
quietly, and `_retire_an_undrawable_pin`'s docstring says as much
about a refused pin -- and it is the copied-ladder carve-out two lines
above, arriving from the other side. The general rule: RETIREMENT IS
FOR A PIN THE DATA MOVED OUT FROM UNDER. Where the thing that moved
is a control the user is holding, refuse the control; a spinner is
reversible and a destroyed stamp is not.

**33, the constant-column notice.** `_legend_size_note` was
deliberately added to the restyle path so a class-count change is not
silent, and `few_values_message` returns None at `distinct <= 1` --
so the constant sentence, which is that rule's n=1 instance, was left
behind. Low harm: the user heard it once, on the first Generate.

## The nine open rows, and the probe for each

COMMITTED, because the ledger claimed reproductions that lived in
hunt worktrees and would have gone with them. Run each with the QGIS
Python, from the repository root.

| Row | Probe |
|---|---|
| 31 | `tools/probes/opacity_lost_when_a_project_opens.py` |
| 33 | `tools/probes/pins_killed_by_the_class_count.py` (arm C) |
| 39 | `tools/probes/deferring_opacity_dropped_at_the_landing.py`, and `..._pixels.py` for the second route |
| 40 | `tools/probes/deferral_erases_the_pin_stamp.py` (`bytes`, `control`, `landing`) |
| 41 | `tools/probes/one_element_silences_another.py` (`pinned`, `control`) |
| 42 | `tools/probes/no_data_twin_loses_hand_styling.py` |
| 44 | `tools/probes/unclassed_pins_from_the_table.py` |
| 45 | `tools/probes/the_element_ceiling_is_twenty_six.py` |
| 46 | read `weavingspace_qgis/vendor/VENDOR-VERSION.txt` against README.md:140 and MAINTAINING.md:274; the gate is `sync_release_content.check_vendor_claims`, which does `stamp.split()[0]` and so has never compared the commit half |

`tools/probes/pins_claimed_after_a_reopen.py` belongs to row 38, which
is FIXED; it is kept because it reads the pins back off the layer
stamp after a save and reload, which is the route the next pin defect
will most likely arrive by.

WHAT ROW 40 COSTS, because it is the worst of the nine: deferring an
element removes `weavingspace_quant_style` from the layer, so the
pinned bounds and hand-picked class colours go out of the saved
project while the open session goes on showing them. Work destroyed
silently, which is this software's characteristic failure.

## The four families

Each family was found by fixing ONE member and hunting the rest, and in
three of the four the second member was OLDER than the first.

**A guard in front of an exit about something else** (8, 14, 15, 18).
A colour comparison standing in front of the GeoPackage write, twice --
once graduated, once categorized, the second found only because the
first was fixed. An in-flight gate in front of the table learning what
the landing was about to ask it.

**A number printed in one notation and parsed in another** (13, 17,
26). Each printed a hard-coded full stop beside a widget that parses
through QLocale. The sweep that settles the family is not "which
strings hold numbers" but WHICH WIDGETS TAKE NUMBERS: there are two.

**A control that eats a keystroke** (1, 2, 12, 19, 27). A range
clamped to 100x the data; two boxes clamping each other; `decimals`
lowered to tidy a display; a `valueChanged` handler rewriting its own
box mid-edit. Every guard on every one drove `setValue` and passed.

**A widget written without the record behind it** (16, 24). The class
count written to the spinner and not `_class_counts`; the style combo
written twice without `touched`, once in the follow and once in the
deferral exit -- the first was fixed and the second never grepped for.

## Process changed the same day, and where it now lives

None of this is in this file; it is here as an index, because a
procedure recorded only in a session is a procedure that did not
happen.

- **Hunt a batch of new tests, per assertion** -- docs/TESTING.md,
  "THE TRIGGER". The catalogue proves a test's primary axis and
  structurally cannot see the rest.
- **When a fix and its test are in, re-hunt that ground** -- near the
  fix, not on it. docs/TESTING.md, "THE SECOND TRIGGER".
- **Test a control by typing into it, not by `setValue`** --
  docs/TESTING.md, with the character-by-character `validate` walk and
  when `QTest.keyClicks` is needed instead.
- **A guard that asks about one thing must not stand in front of an
  exit about another** -- CLAUDE.md.
- **A refresh with one caller is a refresh that only works once** --
  CLAUDE.md. Grep the constructor as well as the updater.
- **When a fix is written into two paths in one commit, diff the two
  hunks against each other** -- CLAUDE.md. And: one dedup set must not
  gate two different things.
- **A guard that runs before a re-read must be asked with re-read
  values too** -- CLAUDE.md.
- **The plugin's table follows the layer's renderer**, and the scope
  of that -- CLAUDE.md, with the maintainer's ruling.

## What this day says about the apparatus

rc7 passed 481 functional tests across three shards, the visual
gallery, the colourspace comparison, standards, secrets, packaging and
both CI jobs -- and carries every defect above. Most are wrong maps or
destroyed work.

The suite and mutation testing measure whether KNOWN behaviour still
holds. Every one of these was behaviour nobody had named.
