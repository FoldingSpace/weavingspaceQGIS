# Defects found on 2026-08-28, round ten

The ledger for the round the maintainer asked for on the evening of
2026-08-28: eight hunts at a time, replenished as each reported, with
the consistency sweep rebuilt beside them under the standing rule of
2026-08-26.

It exists because the other three places cannot hold it. ROADMAP.md
carries only what is outstanding, so a defect fixed without a guard
drops out of it; `dev/state-of-play.md` is gitignored and rewritten
every session; and a conversation ends.

**OWES** is the honest column. `--` means the fix rests on its hunt's
reproduction alone, which is not what this project calls fixed.

## What a user gets

| # | What a user lost | Where | Arrived | Found by | OWES |
|---|---|---|---|---|---|
| 1 | A saved map came back carrying a design it was never drawn at, and the first Generate after opening it replaced the map -- 450 tiles across four elements became 98. | `dialog.py` `_save_the_map`, the record write | `0ecdf7c`, 2026-08-27 | `stores` and `harm`, independently, from opposite directions | test, entry |
| 2 | A project saved under one spelling of its folder and reopened under another stopped recognising its own dataset's output group, so the next Generate built a second group of the same tiles beside the first and the map being worked on was orphaned. | `dialog.py` `_bind_group_to_dataset` | `18df97d`, 2026-08-25, shipped in v0.24.3 | `crossplat` | test, entry |
| 3 | The user guide and the Help tab promise "Save asks first where the file holds data from another map". Save asks about another DATASET; a second map of the same dataset is replaced in silence, which is a settled decision of 2026-08-27. The sentence promises a question that is not asked. | `docs/USER-GUIDE.md`, `help_content.py` | `4196ef7`, 2026-08-27 | `prose` | the maintainer's approval of the wording |
| 4 | Opening somebody's self-contained map with Load and pressing Save stripped out the embedded region copy the sender had included, so the file could never be redrawn again by anyone. The recipient's box read False because nothing had told it what the file held. | `dialog.py` `_recover_the_source` / `_embed_or_drop_the_source` | `0ecdf7c`, 2026-08-27, with ruling 2 | `unreach` | done |
| 5 | The Canadian-spelling clause enforced one of the four spellings its own rule names: two of its three branches sat behind a dead `if wrong == "color":`, and `colormap` and `behavior` were never looked for at all. On its first run rebuilt it caught a real "categorised" in shipped text. | `tools/check_standards.py` | the clause as first written | `instruments`, with a plant as its control | done |
| 6 | The design view keeps painting an element in the plugin's old colour, one the map does not contain, after a dock edit installs a renderer the plugin cannot name -- and no later act heals it. A fifth door into the room round nine's repaint test walked. | `dialog.py` `_on_layer_style_edited` | `75bfc8c`, 2026-08-17 | `previewmap`, three mechanisms agreeing | done -- REPAIRED at `148b154` on 2026-08-28 with the measurement written at the site, and UNGUARDED until 2026-08-29, when the mutation was put on the line the repair added and SURVIVED. A fix nobody has watched fail is a fix nobody should count. It is door three of `test_every_restyle_door_repaints_the_preview` now |
| 7 | A landing's last preview repaint fires while the first element's old layer has gone and its new one does not exist, so that element gets the before-there-is-a-map fallback and nothing repaints after it. | `dialog.py`, the landing's repaint order | `75bfc8c`, 2026-08-17 | `previewmap` | NOT REPRODUCED on 2026-08-29: the preview was watched through a whole landing and every repaint saw all of its layers present. Left open rather than deleted, since one clean journey is not a proof about all of them -- but nothing is owed until it reproduces |
| 8 | `_forget_the_last_project` clears eight remembered-intent records and neither deferred-work flag, where its twin `closeEvent` clears both -- so a queued run is carried into the next project opened. Held redundantly today, since the press flag is not reachable through the interface; a countdown rather than a defence. | `dialog.py:7355` | today, `877706c` | `asym` | done in `8db74ea`, and this column was left stale: both flags are cleared there, before the cancel because `TilingTask.cancel()` reports synchronously, with a catalogue entry. The third deferred kind added on 2026-08-29 inherited the same site, which is what the row's own lesson asks |
| 11 | Open a saved map with Load, point the region chooser at other data, Generate, Save: the plugin wrote the new map into the file you opened, destroying the saved one, with no cleared path, no notice and no overwrite question. `_landed_this_session` had one writer -- the landing -- so a resume left it False and every protection a change of dataset carries was skipped. | `dialog.py` `_resume_from_gpkg` | Load arrived `0ecdf7c`, 2026-08-27 | `writeonly`, by a counterfactual arm | test |
| 12 | The legibility check converted every element's colours to Lab once for each element it was compared against, and the landing walked the whole assignment table once per element. At the 256-element ceiling raised the day before, together thirty-eight seconds of frozen QGIS on every Generate after the first. | `perception.py` `clashes`, `dialog.py` `_assignment_for` | harmless until `cbcd227`, 2026-08-27, made 256 reachable | `scale` | done |
| 13 | A Save recorded each element's VARIABLE live, so the file's record named a table the file does not hold and a colleague's first Generate replaced the sender's element. The defect was in the morning's own repair, which carried two keys of three. | `dialog.py` `_save_the_map` | `56cd0ec`, this evening | `repairs`, aimed at that repair an hour after it landed | done |
| 14 | A bound box silently replaced the number it held with three significant figures of its own abbreviated display, whenever Qt re-read the display on Return. On any column above about 1e5 or below about 1e-4, opening the editor and pressing Return with nothing typed moved every class break. | `widgets.py` `MarkableSpinBox` | `4663dd0`, 2026-08-19, shipped in v0.24.3 | `magnitude` | done |
| 15 | Reducing a design and saving to the same file left the dropped elements' tables, their columns and their values behind whenever there was no group to adopt. | `dialog.py` the stale-table drop | `0ecdf7c`, 2026-08-27 | `filebytes`, two processes, bare OGR | done |
| 16 | With live update at its default, pressing Load re-tiled the opened map into memory a second later, so the file's own layers were removed and the project reopened empty. | `dialog.py` `_resume_from_gpkg` | `0ecdf7c`, 2026-08-27 | `races`, after ruling out all four debounce windows | done |
| 17 | A category recoloured in QGIS and then set back to the colour it had left the DISCARDED colour in the record, silently, and the next re-seed painted it over the map. | `dialog.py` `_on_layer_style_edited` | `64cb0fa`, 2026-08-26 | `dockfollow` | done |
| 18 | Duplicating an output layer in the panel and pressing Generate left the copy on top of the new map, never updated again, with nothing said -- the guard exists at the adoption door and not at the landing door. | `dialog.py` `_add_output_layers`, and `_elements_claimed_twice` / `_say_a_layer_was_duplicated` beside it | `8c6d82e`, 2026-08-27 | `panelacts`; CONFIRMED here 2026-08-29 by the LANDING door rather than adoption -- duplicate the layer, re-tile, and the copy is still in the project claiming element `a` while the original has gone, with the row pointing at the new layer and nothing said | done. THE QUESTION AND THE SENTENCE HAVE ONE OWNER APIECE, so the two doors cannot disagree and the reviewed wording is not composed twice. AND THE NOTICE IS STASHED: a sentence pushed at the landing is wiped by `_on_generated`'s own `finally`, which the first draft of this repair did three lines below a comment saying so about its neighbour |
| 19 | Deleting the output group in the panel and pressing Save emptied the file and reported "Saved". The guard asks whether the dialog REMEMBERS element ids, not whether any resolve to a live layer. | `dialog.py` `_save_the_map` | `0ecdf7c`, 2026-08-27 | `notices` | done, with row 53 |
| 20 | A floor or ceiling and every colour picked after it were destroyed by a save-and-reopen before Generate: those stamps happen only as a side effect of a successful `_restyle_only`, and a limit is a geometry change, so the restyle declines. The change is recorded on the decline now, which is "preserve, do not repaint" read properly -- the map waits for the next Generate, and the CHANGE is not lost. | `dialog.py` `_apply_style_change`, `_stamp_a_change_the_map_has_not_drawn_yet` | `192ec1d`/`c5f3f88`, 2026-08-18/19 | `editor`; reproduced here through a real `.qgz` round trip, reading the LAYER'S own stamps rather than the dialog's dicts, against a control that picks a colour alone -- the control's layer carries its stamp and comes home, the arm's carried nothing at all | done |

**AND THE DECLINE HAS TWO MEANINGS, WHICH IS WHY THE REPAIR IS A
METHOD RATHER THAN A LINE.** `_restyle_only` says no for eight
reasons. Two of them mean "these records belong to this map and the
map has yet to catch up", which is row 20. Two mean the opposite:
"Create as new group" is a request for a SECOND result, so the map on
screen is not the one being changed, and a colleague's map opened
without their data is somebody else's map -- row 22 arriving from the
writing side, where stamping would put our floor and our colours onto
their layers and send them home in their file. Both are guarded, and
the second needed a leg of its own before its entry could catch: the
row-22 test presses Generate and never reaches the stamping path at
all, which is a survivor reporting the truth about a journey rather
than about a weak test.
AND THE SILENCE IS PRE-ANNOUNCED. A colour picked on a map whose data
cannot be found is now neither drawn nor recorded, and nothing new is
said about that -- because the Load has already said this map "can be
looked at but not redrawn. Choose the region layer to carry on", which
is exactly what happens.
| 22 | Open a colleague's map whose region data you do not have, with a polygon layer of your own already in the project. The recovery cannot find their data, says so, and leaves the chooser holding YOURS -- and the next Generate took the restyle fast path and recut every class from your layer. Their tiles, their file, our numbers: element a's top class went from 3.0-3.0 to 2.0-2.0 and c's from 4.0-9.0 to 2.0-4.0, so their high values fell outside every class, and the Save after it wrote those styles home. The fast path asks whose region it is now, and declines to a full run, which the landing's own refusal already protects. | `dialog.py` `_restyle_only`, and `_region_in_force_is` beside it | `6ea58e8`, 2026-08-25 | `firstminutes` claimed the map is REPLACED BEFORE YOU TOUCH ANYTHING; driven here with a control arm on an empty project, the map survives the Load intact and a Save changes nothing -- the mechanism it named is exact and the harm is one act later and quieter, which is this round's rate of three in fourteen met again | done |
| 23 | Open two maps sent by two people, go back to the first through the group chooser, and it is re-tiled from the SECOND sender's data; the next Save writes it over the first sender's file. The resume stamps the group with the sender's own unreachable path rather than the source recovery actually landed on. | `dialog.py:16529`, `14809` | `18df97d` and `64cb0fa`, 2026-08-25/26 | `sharing` | `test_returning_to_one_senders_map_re_tiles_it_from_their_own_data` + 3 entries, 2026-08-29 |
| 24 | The element carry added this evening carried the design and each variable and left the element LIST live, so lowering the count wrote a record claiming n=4 beside two elements against a file holding four tables -- and a recipient's Load brought the missing two back auto-assigned, destroying their tables on the next Save. | `dialog.py` `_save_the_map` | `56cd0ec`, this evening | `repairs2` | done |
| 25 | The first repair of the embed decision set the checkbox itself, which put a per-file fact on a session-wide control: a recipient's own next Save copied THEIR private region out unasked, and a deliberate tick was silently cleared by opening any file without a copy. | `dialog.py` `_recover_the_source` | `062552f`, this evening | `repairs2` | done |
| 26 | The widened CI-parity gate reads `run("...")` only: it misses a single-quoted name, an f-string, a name held in a variable, and `run_sharded`, which is how the functional suite is launched. It also DEMANDS a name written in a comment. | `tools/check_standards.py` | `df7cb35`, this evening | `repairs2`, nine planted shapes with the control first | done |

| 27 | Choosing "Create new" in the group chooser and pressing Generate took the restyle fast path: no second map, nothing said, and the request left ARMED to fork the map on a later unrelated run. | `dialog.py` `_generate` | the chooser, `18df97d`, 2026-08-25 | mine, driving the guard the catalogue triage had retired | done |
| 28 | That retirement was the one bad trade of the twelve: the assertion it leaned on cannot fail, because the test it named never touches the group chooser and so never arms the flag. Breaking every clear site passed. | `tools/mutation_check.py`, the triage | this morning | `deadaxes`, control-first on all twelve | done |
| 29 | A plugin the user DISABLED with a Customize window open kept a live colour editor repainting their layers and rewriting the project's group record. `retire()` never copied the line its twin has. | `dialog.py` `retire()` | `8cc5b4d`, 2026-08-27 | `twodialogs` | done |
| 30 | A CRS a person assigned to fix a shapefile with no `.prj` was lost by the outlines layer, which is rebuilt from the source string: tiles in the assigned system, overlay in the file's, fourteen thousand kilometres apart and zero ink pixels over the map. The resume's recovery had the same shape. | `bridge.py` `region_outline_layer`, `dialog.py` `_recover_the_source` | `3bd5f52`, the first commit | `crs` | done |
| 31 | The embed override was session-wide where the fact is per file, which is the lesson the same commit had just written into CLAUDE.md: one tick anywhere in a session made every self-contained file opened afterwards strippable. | `dialog.py` | this evening, `132713b` | `repairs3` | done |
| 32 | Setting the landed flag at every resume removed the settled clause that a RECOVERY is not a switch, so obeying the plugin's own instruction to choose a region layer cleared the path, let the group go and built a second identical group. | `dialog.py` `_resume_from_gpkg` | this evening, `8db74ea` | `repairs3`, by a counterfactual arm | done |
| 33 | A region edited between a Generate and a Save left the file holding tiles from the old numbers beside an embedded copy of the new ones, silently -- {7,17,27,37} travelling beside a map drawn from {0,1,2,3}. | `dialog.py` `_save_the_map` | `0ecdf7c`, 2026-08-27 | `editsessions` | done, as a notice |
| 34 | In icon mode the sentence naming which elements have no icon named the wrong ones: an element missing from three of the user's areas went unmentioned while one missing from none was named, because it subtracted a tile count from an area count. | `dialog.py`, the icon coverage loop | `5abf2f9`, 2026-08-16 | `iconmode` | done |
| 35 | If somebody else saves the shared file while your map is open, your Save reports success and leaves the GeoPackage with NO map in it -- the "already reading from this table" skip asks the layer's source string and never whether the table is still there, so every element is skipped and the drop then removes what is present. | `dialog.py:16054` | `844e6d4`, 2026-08-27 | `thirdhop`, two processes, bare sqlite | `test_a_save_leaves_a_shared_file_somebody_else_has_changed` + 3 entries, 2026-08-29 |
| 36 | With "Create as new group" TICKED, the group chooser goes on describing a landing that will not happen: it knows `_new_group_chosen` and not the checkbox, so a choice made through it is accepted, displayed, and then ignored. | `dialog.py` `_refresh_group_combo`, and the box's own wiring | `18df97d`, 2026-08-25 | `newcomer`; confirmed here 2026-08-29 -- ticked, the chooser went on reading 'WeavingSpace tiles — region' with `_new_group_chosen` still False | done. TWO HALVES: the chooser asked one of the two doors that arm a new group where the LANDING asks both (`force_new` reads the checkbox outright), and the box was connected to NOTHING -- deliberately kept out of the switches that queue a live run, since it changes where a map lands and not what it draws, which left it telling nobody. Both entries caught |

| 37 | The Save notice that says the data has moved since the map was drawn fired over a map nobody had touched: the reading was a single session-wide slot compared against whichever dataset the region chooser held. Draw two datasets, go back to the first through the group chooser, press Save, and somebody is told their file disagrees with itself and sent to re-Generate a map that was already right. | `dialog.py` `_save_the_map`, `_add_output_layers` | `d2afeec`, this evening, in my own repair | `repairs4`, and reproduced here by a counterfactual arm | done |
| 38 | The same notice could never fire at all for a map opened with **Load**: the readings are taken at landings and a resume is not one. Open a saved map, edit your region, press Save, and the file holds tiles cut from the old numbers beside a copy of the new ones with nothing said — the exact state the notice exists to announce, on the journey a shared file makes likeliest. | `dialog.py` `_resume_from_gpkg`, both branches | `d2afeec`, this evening | `repairs4`, by reading the file's tiles and its embedded copy back with bare OGR | done |

| 39 | The moved-data notice read a fingerprint that cannot see a VALUE edit -- the count, the extent, the field names and the CRS all stand still when somebody retypes numbers. So the case the notice was written for, and quoted in its own commit message, was the one case it could not report: a keystroke unrelated to whether the data had moved decided whether anybody was told. | `dialog.py` `_a_reading_of_the_data` | `d2afeec`, this evening | `undoredo`, by a counterfactual pair corrupting the data identically | done |
| 40 | Saving into a GeoPackage holding a colleague's map DELETED their element tables and their embedded region copy -- one line after a question promising to "leave the rest of the file alone". Ownership was decided by the table-name prefix `tiles_<id>`, and an element id is a letter every map shares, so their `tiles_a_*` and `tiles_b_*` went while their `tiles_zz_*` and their own tables stayed. | `dialog.py` the stale-table drop and the region drop | `43f0317`, 2026-08-27 | `underneath`, and reproduced here against a victim file built with bare GDAL and read back with stdlib sqlite3 | done |
| 41 | Opening a map whose layers are still in the project -- which is what happens when you open the file you have just saved -- took the already-open resume branch, which never recorded a run signature. With live update at its default a tick a second later re-tiled the opened map into memory, so the GeoPackage-backed layers Save had made were dropped and the project reopened empty. The repair for this had gone to one door of two. | `dialog.py` `_resume_from_gpkg`, the already-open branch | `36bbab7`, this evening, mended at the other door only | `pathspellings`, and reproduced here by comparing the two DOORS on one spelling rather than two spellings on one door | done |

| 42 | In icon mode the coverage notice compared a GEOGRAPHIC region against tiles the pipeline had reprojected to metres, so nothing intersected and every element was reported as reaching none of the areas. A person with an ordinary WGS84 download met, on every icon-mode run, a sentence naming all four elements while saying "which other elements still draw" -- false, self-contradictory, and impossible to act on. | `dialog.py` `_areas_no_icon_reaches` | `d4acafe`, this evening, in my own repair | `iconmode2`, and reproduced here by a metamorphic fixture in degrees | done |
| 43 | The dependency consent box named only the missing scientific packages while a provisioning run also fetched six support distributions -- packaging, python-dateutil, pytz, tzdata, six and certifi. Somebody who read "Missing or too old: geopandas" and approved had seven distributions fetched from pypi.org, against a hard rule and against metadata.txt's own promise that the plugin "shows exactly what it would fetch and asks first". | `deps.py` the SUPPORT loop, `plugin.py` the consent call | `3bd5f52`, the initial commit | `deps`, and reproduced here by stubbing `_fetch_dist` rather than urllib | done |
| 44 | A support package whose download failed was discarded in silence: the loop threw away `_fetch_dist`'s reason, so `LAST_FAILURES` stayed empty, provisioning reported success, and the user met "No module named 'dateutil'" from the library with nothing to say why. The reason machinery written for exactly that case was bypassed by one unbound call. | `deps.py:478` | `3bd5f52`, the initial commit | `deps` | done |

| 45 | With "Add map unit outlines layer" ticked, choosing your own map's group emptied the region chooser and dropped every element's variable, so the design the map was drawn at was gone and the next Generate would draw something nobody chose. The walk that recovers a record's region can land on the OUTLINES layer -- built on the region's own source, and excluded from the chooser -- and `setLayer` on an excluded layer is silent. Two doors carried the walk and neither carried the guards their twin has. | `dialog.py` `_on_group_chosen`, `_restore_the_adopted_design` | `18df97d` and `4f3c415`, 2026-08-25 | `chooserclaim`, and reproduced here with a control arm on file-backed data | done |

| 46 | Deleting ONE element layer from the group in the panel and pressing Save left the file holding that element's no-data twin with no element table beside it: a set of missing-value areas belonging to an element the map does not have. The plugin said "Saved", and a recipient's Load brought back three elements beside an orphan twin for a fourth. | `dialog.py` `_save_the_map`, the per-element write loop | `0ecdf7c`, 2026-08-27 | `absences` reported the MIRROR of this -- the twin dropped and the element kept -- and that direction did not reproduce here; driving the same panel act gave the opposite, which is the finding recorded | done |

| 47 | A Save As told the FILE where the map had gone and never told the group, whose record keeps `output_path` unless a landing moves it. Come back to that map through the group chooser and the box has reverted to the file you saved AWAY from: the next Save overwrites the older version with newer work, while the file just written goes stale, and nothing says so. | `dialog.py` `_save_the_map`, the group stamp it never made | `0ecdf7c`, 2026-08-27, when the write moved to the button | `colourpicks`; reproduced here through the chooser's own `activated` signal, reading the widget a person reads rather than any record | done |

| 48 | A colour picked in the editor never reached a map this dialog did not DRAW: the record took it, the map kept what it had, and nothing was said. `_restyle_only` returns at its first line while `_last_geometry_sig` is None, and only a landing set it. Two ordinary journeys reach that state -- opening a saved map with Load, and closing the plugin and opening it again -- and the second is the one `_adopt_existing_group`'s own docstring calls something users do constantly. It is set at every restore now, where the controls have just been written FROM the group's record and so describe the map its layers hold. | `dialog.py` `_apply_working_state` | `3bd5f52`, the first commit, for the adoption door; the Load door arrived `0ecdf7c` | `brokenfiles` reported the LOAD door; reproduced here at both, with a control arm on a map drawn in the same session and a before-and-after on a clean tree | done |

| 49 | "Take my classes from that layer" was dropped in silence by a Load, so two elements showing one column stopped agreeing the moment somebody moved the donor. The choice is held as `layer:<layer id>`, which survives a project save (the `.qgz` keeps ids) and cannot survive a GeoPackage, whose layers are made afresh on opening: the combo could not offer a token naming nothing, `_assignments` reads the widget, and the choice was gone. It travels as `element:<tile id>` now, translated back where the record enters the dialog -- one site, because the same list is armed as `_restoring_assignments` and the rebuild writes its own answer back. | `dialog.py` `_capture_working_state` / `_apply_working_state` | `0ecdf7c`, 2026-08-27, when a map first came back from a file | `classsource`; reproduced here with a control arm on the LINK rather than on today's colours -- move the donor and require the follower to move | done |

| 50 | A blend mode set in QGIS was thrown away at every re-tile. The landing carries the old layer's OPACITY across, and opacity's neighbours in the same Layer Rendering box -- the layer blend mode and the feature blend mode -- travelled nowhere, so an element set to Multiply came back SourceOver at the next spacing change, against the standing promise that hand styling survives unless the assignment changed. Carried on BOTH routes now, kept and re-seeded, and unconditionally: nothing in the plugin has ever set a blend mode, so whatever the layer wears is the user's. | `dialog.py` `_add_output_layers` | `3bd5f52`, the first commit | `layouts`; reproduced here by asking the layer rather than counting page pixels, with opacity as the control arm | done |

| 51 | A deferring element's ramp cell went back to naming a ramp over a map the plugin does not decide -- the control lying about the map that the Custom display exists to prevent. `_refresh_deferring_rows` sets that swatch on every styleChanged, and `_sync_row` decides the same cell from the row's own MODE, which for a deferring row is neither Categorized nor Graduated: it fell to the else branch and cleared what had just been set. Harmless until row 6's repair landed a preview refresh AFTER the deferring one in the same handler, and the second undid the first. `_sync_row` asks the question itself now, so the answer no longer depends on which refresh ran last. | `dialog.py` `_sync_row` | the clearing since the Custom display; reachable from `148b154`, this evening | THE FULL SUITE, run whole at `9046f3e` -- 666 passed, this one failed, and no targeted run of the repair's own neighbours could have found it | done |

| 52 | Opening a saved map whose region has a column called "no data" told the recipient that part of their map had not come back: four element layers opened and the sentence said two. Element tables are `tiles_<id>_<variable>`, so that column name sanitises into exactly the ending a paired twin's table has -- and the count asked the NAME while the line three above it asked the layer's own stamp for the same fact. Nobody need choose the column: the default cycle picks it, which is how the probe's own CONTROL arm reproduced the defect before the control was made honest. | `dialog.py` `_resume_from_gpkg`, the table loop | `0ecdf7c`, 2026-08-27 | `sentences`; reproduced here by naming a column the way a dataset does and reading the sentence, against a control without that column | done |

| 53 | A Save would not say what it had left out. Deleting one element's layer in the panel and pressing Save writes a file holding the rest -- right, and settled by row 46 -- and the press reported plain success, while the RECIPIENT is told the file gave back less than was saved to it. The sender was the only person not told. And at the limit the same guard was worse: deleting the whole group left every remembered id in place with nothing resolving, so the write loop skipped everything, the stale-table drop removed what the file held, and the press reported success over an emptied file (row 19). The guard asks whether any remembered element still resolves, and the success sentence names what did not travel. | `dialog.py` `_save_the_map` | `0ecdf7c`, 2026-08-27 | `sentences` and `notices`; reproduced here by the panel act, reading the file with stdlib sqlite3 and both sentences from the message store | done |

| 54 | With live update at its default, a Save pressed inside the live debounce wrote the map on screen -- the one the person had just changed away from -- and reported plain success. Measured: saved at 55 tiles an element, the file still held 55 after the press, and a second later the map drew 190. The in-flight guard asks `_task`, which a QUEUED run does not have yet. It asks whether a queued run would REDRAW now, and the output path is excluded from that question: the path is a term of the run signature, settled when a run wrote the file, so the guard's first draft refused choose-a-file-then-Save -- caught by its own probe's first press. | `dialog.py` `_save_the_map`, `_a_queued_run_would_redraw` | `0ecdf7c`, 2026-08-27 | `sentences`; reproduced here through the live timer with the premise asserted, and the guard's ORDER caught by the save matrix's own no-map cells | done |

**AND THE REPAIR FOR ROW 54 WAS THE WRONG SHAPE, RULED SO BY THE
MAINTAINER ON 2026-08-29.** It REFUSED the press in words, on the
reasoning that a press the dialog remembers is a promise about a map
nobody has seen yet and that the run lands in under a second. The
maintainer struck that out on a ground no measurement here would have
produced: **most people will not read the sentence.** A refusal that
depends on being read is a save that quietly did not happen, and the
person closes QGIS believing their map is on disk.
So the press is KEPT and performed once the new map has landed, the
notice says so rather than asking for a second press, and
`_honour_a_queued_save` is a third deferred kind beside the queued
press and the queued tick -- consumed by taking and clearing the
intent at the point of use, and asked from three places because a
LANDING alone cannot cover a run that declines: the live tick has ten
gates and `_generate` eight. The reasoning is in CLAUDE.md and the
mechanism in MAINTAINING.md under "Three queues".
THE GENERAL FORM, and it is the useful half: **ask of any refusal what
it costs the person who does not read it.** Where the answer is work
lost, the refusal is the wrong shape and the act belongs deferred and
completed. Where it is a WRONG MAP drawn, a refusal is still right --
which is why the in-flight refusal beside it is untouched.

| 55 | Generate, choose a file, Save, save the project, open it again: the Save box came back EMPTY and a press was refused -- "Choose a file in the box beside Save first" -- while every layer in the map was reading from that very file. Measured on `f407fbc` and CLOSED BY ROW 47, which gave the group's own record the output path a Save decides, so adoption restores it. What was owed was the guard at THIS door: the repair was written at the group chooser and tested there. | `dialog.py` `_save_the_map` / `_apply_working_state` | `0ecdf7c`, 2026-08-27 | `projectacts`; reproduced here on the tree the hunt read and measured clean at HEAD | done, test only -- the line is the one row 47's entry stands on |

| 56 | A number you set was thrown away by a look at another family. The families disagree about these ranges -- a slice offset runs -1 to 1 and a dissection's runs 0 to 1, a hex dissection's inner angle reaches 85 where a square's stops at 70 -- and `setRange` CLAMPS in silence, so an offset of -0.5 came back 0.0 after a visit to a dissection and the next Generate drew from the reset number. The re-ranging holds what it cannot wear and gives it back when a family that can comes round, which is the spacing's own rule that a number a person TYPED survives. | `dialog.py` `_on_family_changed`, `_re_range_remembering` | `3bd5f52`, the first commit | `modifiers`; reproduced here through the family combo with a control arm on a value both families hold, reading the box AND the kwargs the unit is built from | done |

| 57 | A pinned ladder printed "0 - 0" on a column spanning magnitudes, telling a reader the palest part of their map holds zero. `_widen_degenerate_labels` is the fix for exactly that, and it ran ABOVE the call that INSERTS the pinned classes -- so it described a ladder nobody sees. Measured on a column mixing 1e-9 with 1e9: unpinned the legend reads "0.000000001 - 0.0000006", and with a pin at 7.75e-06 it read "0 - 0" beside "0 - 2.35". The widening runs below the pins and the limits now, on the ladder that is drawn. | `bridge.py` `make_graduated_renderer` | `4663dd0`, 2026-08-19, when pins arrived beside the widening | `legend`; reproduced here through the shipped entry point, reading the LEGEND, with the unpinned ladder as the control arm | done |

| 58 | Five kinds of unopenable file were given one sentence -- "that GeoPackage does not carry a saved map... add its layers in QGIS to look at it" -- and only one of the five is what it says. A path that does not exist, an empty file, a file that is not a GeoPackage, a truncated one and a sound GeoPackage with no record all answer None from `read_working_state`, and for four of them the advice cannot be followed. The resume asks WHY first now: nothing there, or something there it cannot read. | `dialog.py` `_resume_from_gpkg`, `bridge.why_a_file_will_not_open` | `0ecdf7c`, 2026-08-27, when Load arrived | `brokenfiles`; reproduced here by driving all five through the Load button, with a real saved map as the control arm | done |

| 59 | The many-categories question was never asked for a TEXT column, which is the kind most likely to need it: `_distinct_values` counts through the geometry-less scratch layer, and that layer holds its column AS DOUBLES -- so every word arrived NULL and 120 named wards counted 0. A name column therefore handed QGIS one legend entry per area with nothing asked. A non-numeric column is counted on the region itself now, which is the same whole map the scratch layer exists to give the numeric path. | `dialog.py` `_distinct_values` | `18df97d`, 2026-08-25, with the question | `manyareas`; reproduced here with the same 120 values as numbers for a control arm | done |

| 60 | A floor or ceiling typed while live update is TICKED but no run can follow was accepted in total silence, and the map did not move -- which reads exactly like the control not working. The notice asked whether the checkbox was ticked; the live path declines for reasons of its own, and "Create as new group" is one of them. It asks whether a run WILL FOLLOW now, through one predicate that names the standing reasons and says at its own docstring which reasons it deliberately leaves to the live path. | `dialog.py` `_edit_quant_colours`, `_a_live_run_will_follow` | `c5f3f88`, 2026-08-19, with the notice | `livepath`; reproduced here through the editor's own floor box with three arms, the middle one requiring SILENCE where a run does follow | done |

| 61 | A follower's inherited colours were adopted as its OWN hand-picks when a self-contained map was opened, so it stopped following its donor and the two drew one column in two sets of colours from then on. A map saved with the source embedded travels with a copy of the region inside it; the recipient's recovery lands on that copy, so `same_data` compared the file against the sender's own path, answered False, and skipped the value-laden block -- which means NEITHER APPLIED NOR CLEARED, leaving what adoption had recovered off the layers standing as though somebody had picked it. The copy a file carries is now recognised as that record's own data, through `same_source`, and ruling 8 is untouched: it is one file's own record meeting the data that file brought. | `dialog.py` `_apply_element_records` | `0ecdf7c`, 2026-08-27, when a map first came back from a file | `classsource` reported the LOAD door; reproduced here at that door AND at the group chooser after a project reopen, which nothing can pass a path into, with the control arm on the LINK -- move the donor and require the follower to move | done |
| 62 | MINE, from row 54's own repair, and found by running that repair's neighbours a day late: refusing the press broke two registered tests outright -- `test_a_map_opened_and_then_edited_says_so_when_it_is_saved` and `test_the_moved_data_notice_is_about_the_map_being_saved`, both of which press Save with live update at its default. Measured red at `d809027` in a throwaway worktree, so they were red before any of today's edits and the targeted runs that verified row 54 never saw them. They are green under the ruling below, because a deferred-and-honoured save is exactly what they were asking for. | `dialog.py` `_save_the_map`, and the two tests | `f8f2c37`, 2026-08-29, with row 54 | mine, running row 54's neighbours after the ruling changed the same code | done |

| 63 | A SEGMENTATION FAULT, and not a wrong map: three callables outlived their dialog and reached it through bare lambdas, so an attribute access on a deleted sip wrapper took the whole process down. `cleared` and `readProject` reach every dialog a session has ever opened and an element layer outlives the dialog that made it, so in QGIS this is the application closing on somebody who opened the plugin a few times and chose File > New; in a sharded suite it is a shard stopping with no verdict. Qt drops a bound METHOD when its QObject dies and keeps a LAMBDA -- and the crash happens before any handler's own retirement gate can run. One of the three DID guard and wrote to `self` on the line above the guard, which is row 25 of 2026-08-27 at a second site. | `dialog.py` `_forget_the_last_project`, the queued restamp's closure, and three signal connections | the first two with the plugin; the lambdas since adoption | mine, running twelve tests in one process to check a repair's neighbours -- reproducible, and no one of the twelve fails alone | done, guarded by the SHAPE rather than the three sites |
| 64 | Every cell in the assignment table elided at any desktop font. The nine column widths were constants in pixels measured against the 9pt Sans Serif `QT_QPA_PLATFORM=offscreen` supplies -- which every runner and every CI job sets -- and at 13pt the same columns need 1096px against the 947 they were pinned to: "Style" needs 197 and had 152, "Categ colourmap src" 180 against 150. So the chooser whose job is saying which style a row wears read "Quant: Equal inter...". The columns grow to their content now, and the 1280 ceiling was re-derived to 1480 from what that needs. | `dialog.py`, the column constants and `_fit_table_width` | the widths, with the table | mine, measuring per column after the maintainer's decision -- and the decision's own premise, which I had quoted from the 9pt reading, was wrong in the opposite direction | done |

### A hazard rather than a defect

`weaves` drove all seventy-seven weave entries through Save, Load, a
group switch and the `.qgz` and found nothing -- strands character for
character, hyphens included, aspect and the aspect-scaled inset intact
to 0.1 m². The one thing it noted: a record naming a weave at a count
no weave is offered at resumes SILENTLY AS A TILING, because
`_write_control("text")` does nothing when the family combo misses.
Unreachable from this build, and reachable the day a weave above n=12
is added and withdrawn, or a file crosses builds.

## What the checkers claimed and did not do

| # | The claim | What it does | Found by | OWES |
|---|---|---|---|---|
| 9 | CLAUDE.md: `check_standards` reads `release.py`'s own stage list, and "nothing there is a hand-kept list, so the two cannot drift apart quietly". | CLOSED, re-measured 2026-08-29: a stage planted in `release.py` that no CI job runs was objected to, with the control silent. When this was written, `EXPECTED_STAGES` was hand-kept and the only thing read, with two live stages -- the release notes and the pre-commit secrets audit -- already outside it. A stage added and forgotten gets no CI job and no written exemption. | `instruments`, plant and control | -- |
| 10 | `check_standards`: every harness under `tests/` is run by the workflow or exempt with a reason. | A substring hit anywhere in `ci.yml` counts, comments included, so a line reading `# NOT run here: tests/parity_harness.py` satisfies the gate. The sentence that satisfies it can deny it -- the roadmap gate's own defect of 2026-08-26, in a sibling checker. | `instruments`, plant and control | CLOSED, re-measured 2026-08-29 by planting a harness named only inside a comment: the gate objected. Its control -- nothing planted -- was silent, which is what makes the answer mean anything |

## What the suite lost

| # | What was not guarded | Where | Arrived | Found by | OWES |
|---|---|---|---|---|---|
| 4 | The restyle leg written to replace a retired catalogue entry cannot fail: the follow has already brought the row to the layer's field, class count AND ramp, so a re-seed reproduces the layer exactly and the assertion holds either way. Its twin retirement left `test_taking_an_element_back_from_qgis_restyles_at_once` carrying no entry at all. | `tests/run_tests.py`, the legs added at `5b555f0` | 2026-08-28, the catalogue triage | `testbatch` | done 2026-08-29: the leg is restaged so the re-tile meets an element taken back in that same act (spacing moved FIRST, so the restyle declines and the layer still holds the dock's renderer when the run lands), and both paths have an entry -- `a-taken-back-element-is-not-carried-at-the-landing` and `a-taken-back-element-is-restyled-at-once`, both caught |

## A correction to the morning's own work

**THE QUEUED-PRESS DEFECT WAS THE SUITE'S, NOT A USER'S.** Fixed
earlier the same day and made the reason for candidate `0.24.4rc3`,
its harm was written into CLAUDE.md, docs/TESTING.md, ROADMAP.md and
the candidate's tester notes as something a person met. Measured on
the evening of 2026-08-28, three ways: `generate_btn.setEnabled(False)`
runs before the task starts and only `_finish_run` and the zombie
recovery re-enable it; a probe driving a run at a fine spacing reads
`task=True` with the button disabled, and a real `click()` sets
nothing while a direct `_generate()` sets `_press_pending` at once;
and the test that failed under load,
`test_two_generates_with_different_families_keep_their_elements_apart`,
calls `dlg._generate()` three times, as `test_race_double_generate`
has for far longer.

The mechanism was exact and the fix stands as defence in depth -- a
deferred press and a deferred live tick are different facts. What was
wrong was the sentence around it, and the cost of it was a candidate
whose notes opened by telling testers to press a button the plugin
greys out for the whole run. rc3 is not to be published with those
notes. Found by `unreach`, which reported it as an unreachable guard
rather than as a defect, and by my own measurement afterwards.

THE LESSON, and it is a new one for this project: a mechanism and a
JOURNEY are two claims, and only one of them was measured. Ask of any
defect whether a person can drive the journey that reaches it, and
measure that as separately as the mechanism -- one probe reading a
control's `isEnabled()` settles it in five minutes.

## Still to verify, claimed by a hunt

**LOAD A SHARED MAP, PRESS SAVE, AND THE EMBEDDED SOURCE IS DROPPED.**
Claimed by `unreach`: `_embed_or_drop_the_source` asks only the live
`opt_embed_source` control, which is never captured into or restored
from the working state, so the drop branch written for "the user
unticked it" fires identically for "the plugin never told the box".
A recipient who opens a self-contained GeoPackage and presses Save
strips out the region copy the sender ticked the box to include, and
the file can never be redrawn by anyone. Read back with stdlib
`sqlite3` against `gpkg_contents` as its second route. Not yet
reproduced here by a route the hunt did not use, so it is not in the
table above.

## Open, and for the maintainer rather than for me

**THE WINDOW KEEPS ITS 1280 PROMISE ONLY UNDER A FONT NOBODY HAS.**
Found 2026-08-28 by accident, launching the suite without
`QT_QPA_PLATFORM=offscreen` -- every other runner sets it, and this
one did not. Three layout tests failed, and the failure is not a
harness artefact in the direction it first looks.

MEASURED, same tree, same dialog, same nine columns at 947px:

    platform  font                        window
    offscreen Sans Serif 9pt              1279px   (cap 1280)
    cocoa     .AppleSystemUIFont 13pt     1334px

The rule settled 2026-08-09 puts the 1280 ceiling FIRST, then "the
table never scrolls horizontally", then the preview down to its
260px floor. At the system font those three cannot all hold: the
table's columns and chrome come to 972, the preview is already at its
floor, and the remainder is 54px over. A user on a 1280-wide screen
-- the machine the rule names -- meets a window wider than their
screen, and no runner here or on CI can see it, because all of them
measure a 9pt font that no desktop uses.

WHAT IS NOT KNOWN: whether 1334 is what a real QGIS shows. This
measurement is of the plugin's own dialog under the system font, not
of QGIS's own widget style, and QGIS may impose its own. That is one
screenshot on a real desktop.

WHICH OF THE THREE PRIORITIES GIVES IS THE MAINTAINER'S, and it is a
real decision rather than a defect to fix quietly: narrower columns
(the "Colour ramp" cell is 172px for a 64px swatch and a name), a
lower preview floor, or the ceiling itself, which was chosen as "the
narrowest screen still in use" in 2026-08-09 and could be
re-derived. What must NOT happen is the guard being left measuring
the font it can see.

**DECIDED 2026-08-29 AND BUILT: numbers stored as text are
classifiable.** The maintainer narrowed the rule to its own evidence
-- words are not classifiable, numeric strings are -- and
`_field_is_numeric` answers the wider question for all eight of its
readers, cached the way `_classification_values` is. It is STRICT: a
column of mostly-numbers-with-a-word stays categorical, because a
graduated renderer would drop those rows in silence, which is the
failure the old rule was about. The second half was never a decision
and is closed with it: the row and the assignment agreed once one
predicate answered both. What follows is the question as it stood.

**A QUANTITATIVE STYLE ON A NUMERIC-TEXT COLUMN.** The settled rule is
that a quantitative style never stands on a text field, and its stated
reason is that a graduated renderer over text comes back with no ranges
and the layer paints nothing. Measured on QGIS 4.0.3 by the `spec`
hunt: that is true of WORDS and false of NUMERIC STRINGS. A String
column running "10" to "120" classifies exactly as its integer twin --
five ranges, the same bounds, twelve of twelve features symbolised --
and the plugin's own `classification_source` already coerces such
values while `distinct_numeric_count` already skips text that is not
numeric. So a person whose numbers arrived as text through a CSV join
or a GeoJSON cannot draw a choropleth from them at all, and is given
one colour per value instead: at three thousand areas, three thousand
and one categories.

Whether a `str`-typed column SHOULD be classifiable is a decision, and
it is yours. What is not a decision is the second half: between a
graduated style built in QGIS's own dock and the next Generate, the row
reads "Quant: Quantiles" while `_assignments` returns Categorized, and
that Generate destroys the dock's work. The follow ruling of 2026-08-17
says the row follows the layer wherever the plugin can name what the
layer holds, and a graduated renderer is nameable. Two settled rules,
one act, two answers.

## Claimed by a hunt, reproduced by that hunt, NOT YET VERIFIED HERE

Two, at the close of the third wave and fourteen closed since -- rows
47 to 50, 52 to 60 and 61 above. Each was confirmed by its own
hunt with a second independent route, and none has been reproduced here
by a route the hunt did not use, which is what this project calls
confirmed. They are listed so the next session picks up a work list
rather than a memory, in rough order of the harm claimed.

(The follower's inherited colours, which stood first here, is CLOSED
as row 61: repaired at both doors, guarded, and its entry proved
caught. Two readings from it are worth keeping where the next person
will meet them. The claim named the LOAD door and the group chooser
after a project reopen was equally open, which is the one-extra-arm
rule again. And `_category_colours` must not be watched by rebinding
it: it is a view into a per-dataset bank that `_swap_dataset_memory`
rebinds, and a probe that did exactly that reported zero writes while
another reading showed all four colours.)

1. **Opening another project then pressing Save renames that project's
   tables after the one you left.** NOT REPRODUCED HERE on two routes,
   including one keeping a single dialog across the project change, so
   this is the one entry whose existence is still open. (`projectacts`;
   `_element_tables` absent from `_forget_the_last_project`.)
2. **Save and Load are quadratic in the element count and freeze the
   interface** -- 134s and 122s at the 256 ceiling, a 50 ms heartbeat
   recording zero beats, no progress bar. Every store was clean at
   256. A discarded OGR open has been removed; it is a term, not the
   cause. (`bigsave`.)
   MEASURED TO ITS EQUATION ON 2026-08-29, at 8, 16, 32 and 64
   elements, counting calls rather than timing two points -- which is
   the method `scale` used and the reason the answer names its own
   caller. **EVERY CALL COUNT IS EXACTLY LINEAR.** Nothing is called a
   quadratic number of times; what grows is the COST OF EACH CALL,
   because every one of them opens the GeoPackage and opening a
   GeoPackage costs time proportional to the layers already in it. So
   a save is n opens of an O(n) file, and four terms are 2.6s of the
   3.0s wall at 64 elements:

       term                            8      16      32      64
       the style drop               0.01    0.03    0.10    0.42
       the style save               0.04    0.09    0.23    0.66
       write_gpkg_layer             0.11    0.09    0.19    0.62
       point_layer_at               0.06    0.08    0.24    0.93

   ONE OF THE FOUR IS OURS AND IS DONE. The style drop opened the file
   per element to remove superseded rows; it opens it ONCE now, for
   the whole map, and its call count went from n to 1 -- a structural
   fact rather than a timing claim. It has fallen out of the cost
   table entirely (0.42s at 64 elements to below the 0.05s printing
   threshold) and the wall came 3.0s to 2.6s. The two implementations
   of that DELETE were collapsed into one at the same time, so the
   catalogue entry over it cannot be split by a second writer.
   THE OTHER THREE ARE QGIS'S AND OGR'S OWN PER-LAYER APIS -- a writer
   that takes a path, a style save that takes a layer, and
   `setDataSource` -- and none has a batch form. Making the save one
   OGR session would be a rewrite of the writer rather than a repair,
   and it changes what the file contains in ways the whole suite would
   have to re-answer. THAT IS THE MAINTAINER'S CALL, with the
   FREEZE beside it: the loop pumps nothing, so a 256-element save is
   an unresponsive window whatever it costs, and whether Save should
   report progress is a decision about the interface rather than about
   arithmetic.
AND ONE CLAIM'S DIRECTION WAS WRONG, corrected rather than dropped:
`absences` reported a no-data twin dropped while its element survived,
and the mirror is what reproduced. Row 46 is the mirror, fixed. What is
still owed is whether the hunt's own journey -- through adoption and
through a resume -- reaches the direction it reported.

## The round's own faults, counted


Mine, while verifying, and the third wave produced four more of the
same kind in one evening -- every one of them a shape this project has
already written down, made by somebody who had read the entry
describing it. That is the argument for `tools/probe_kit.py`, which
prevents each at the line.

- A probe let its temporary directory be swept away underneath it (the
  context manager entered without being held), so a Save failed at the
  sqlite level and read as the product refusing to write.
- A probe imported the suite without installing the modal shim, which
  `main()` installs, and hung offscreen for eight minutes on a real
  QMessageBox -- the fault the `manyareas` hunt had reported an hour
  earlier.
- A probe read `BAR_MESSAGES` after calling `press_save`, which blanks
  both stores before it clicks and keeps what it read in a local, so an
  empty store read as the plugin saying nothing.
- A probe cleared `_last_run_sig` by hand before a Load, which forces a
  re-tile on BOTH arms and destroys the control it was there to
  provide; and another measured a support-package loop that never ran,
  because this machine's Python already ships every support package,
  and reported a silent failure that was really nothing attempted.
- A three-armed probe gave every arm THE SAME COLOUR, and the control
  arm's pick travelled into the file the second arm opened -- so that
  arm compared a colour with itself, reported a repaint that had
  happened before it ran, and the claim was nearly recorded as not
  reproducing. The same probe first drove the RAMP where the act is the
  colour EDITOR's, and then picked a CATEGORY colour on a quantitative
  row; both read exactly like the defect, and the control arm caught
  all three. Written up in docs/TESTING.md.

Two of my verifications also came back NOT REPRODUCED and are recorded
as such rather than quietly dropped: the cross-project table renaming,
and the saving claimed for dropping a discarded OGR open.
