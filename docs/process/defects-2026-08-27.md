# Defects found on 2026-08-27

The 0.24.4 campaign. Six hunts were launched at once with the standing
instruction that each returning hunt is fixed and guarded before its
replacement goes out, so this ledger grows through the day rather than
being written at the end of it.

The OWES column is the honest part. `--` means the fix rests on its
hunt's own reproduction, which is not what this project calls fixed;
an entry is only finished when it names both a registered test and a
mutation-catalogue entry that has been RUN and reported `caught`.

Two of the six went to the mechanical consistency sweep rather than to
intuition, under the rule of 2026-08-26 that the sweep is proposed
beside the hunts with a reason for choosing one. The reason here is
that the ground is fresh — most of this code moved yesterday — which
the record says favours hunts; the portfolio keeps a third of the
round for the directions a sweep cannot pattern-match, and this round
inverts that deliberately to test the sweep's cost against a hunt's on
ground where hunts are supposed to do well.

## Fixed, with a guard and a proved catalogue entry

Numbered in the order they were FOUND rather than the order they
appear, because the rows were written as each hunt returned and the
open ones are gathered below; a number is a handle, not a rank.


| # | What a user lost | Where | Arrived | OWES |
|---|---|---|---|---|
| 1 | An element whose class-source file had gone kept its old renderer across a change of VARIABLE, so it was classed on a column its rebuilt layer does not carry. Every tile falls outside every class and the map draws as one flat sheet of the catch-all; the old column's value strings were then recorded under the new column's name, in the record, in the shadow that marks them held, and in the layer stamp a reopened project reads back. | `dialog.py`, the unreadable-class-source arm of the landing, and `_own_the_colours_of_an_unreadable_source` | 2026-08-26, with the kept-scheme ruling | test + entry, both proved |
| 2 | A group made by opening a saved GeoPackage was named from the bare base with a counter, not for its dataset — so two resumed maps gave the chooser two identical labels (it appends the dataset only where the name lacks it) while writing different files. | `dialog._resume_from_gpkg` | 2026-08-26, when the naming ruling was written into the run's door only | test + entry, both proved |
| 3 | A GeoPackage already open under another spelling of the same path was opened AGAIN, building a second group over the same tables. One ordinary Generate then hollowed out the first: the element whose table the run renamed points at a table that no longer exists, and its siblings quietly redraw the new run's data under the abandoned group's renderers. | `dialog._resume_from_gpkg`, the one-file-is-one-map door | 2026-08-25 (`b597861`), nine days after `same_destination` shipped for exactly this | test + entry, both proved |
| 4 | Removing the region layer leaves the assignment table blank because its columns went with it — a blank the plugin imposed — and the queued restamp wrote that over the group's good record. Every element's variable and style gone, so the next dialog opened in that project met a map plainly drawn from three columns beside a table describing none of them, with Generate refusing for want of a variable. | `dialog._queue_group_restamp` | 2026-08-26 (`64cb0fa`), when the restamp was added without the conditions its twin had been given | test + entry, both proved |
| 5 | A dialog that had never adopted or resumed the output GeoPackage knew nothing about what an earlier session wrote, so switching an element's variable left the previous variable's table behind. Resuming that file loads the orphan as an extra element — five layers for a four-element record, two of them claiming element `a` — and tables load in sorted order, so the abandoned variable sorts above the live one and paints over the map: 23.7% of sampled pixels. Its style row travels too, carrying the abandoned field's name, pins and hand-picked colours to whoever the file is sent to. | `dialog`, the stale-table drop, which read a record living on the DIALOG | 2026-08-25, when the variable joined the table name | test + entry, both proved |
| 6 | Two column names that sanitise to one table name (`Pop` and `Pop %` both give `tiles_a_Pop`) leave the earlier column's saved style in the file. The table is REPLACED rather than dropped, so the drop — which has taken styles with it since 2026-08-26 — never runs, and `layer_styles` is keyed by the style's name, so `a – Pop` sits beside `a – Pop %` with its whole QML. | `bridge.embed_style` | with style embedding | test + entry, both proved |

| 7 | Every exit where a dock edit is ADOPTED embeds the layer's new style into the GeoPackage, and none wrote the file's own record — only the landing and the restyle did. So the file disagreed with itself: its saved style drew a colour somebody picked in QGIS's panel, and its record said no colour had been picked. A colleague resumes the record, and their first Generate paints over the colour the same file is drawing. | `dialog`, six adoption exits; mended once, at the queued restamp, so a seventh exit cannot be written without it | 2026-08-26, when the restyle gained this write and its neighbours did not | test + entry, both proved |
| 8 | An opacity set in QGIS's own Layer Properties reached the layer, its renderer and the file's saved style, while the spin box and all four records still said 100 — and the dialog pushes its own number back, so one later restyle for any reason undid a choice still visible in the layer panel. | `dialog._on_layer_style_edited`; the layer is the authority, as adoption at reopen has said since 2026-08-13 | with per-element opacity | test + entry, both proved |
| 9 | Layers loaded by resuming a GeoPackage kept their internal table names, so a resumed map's panel and any legend printed from it read `tiles_a_v1` where a generated map reads `a – v1`. Ruling 5 exists so somebody can open a finished result without generating, which is the one journey where nothing afterwards corrects the name. | `dialog._resume_from_gpkg` | with the resume | test + entry, both proved |
| 10 | Paired no-data layers carried no record of which dataset they were made from. Held redundantly today, since every reader drops a falsy stamp — closed because a group left holding only twins gives the landing's refusal an EMPTY set of stamps, which it reads as output made before stamping existed, and the guard against writing over another dataset's map goes quiet. | `dialog._add_no_data_layer` | with the region stamp, whose commit anchored on the element's tile-id line and never touched the twin's identical one | test + entry, both proved |

| 14 | An element taking its classes from another element's layer names that layer by ID, and a re-tile makes a new layer — so one Generate after the choice, the reference named nothing. The donor read as an unreadable class source, the follower kept whatever colours it had under the keep-the-map promise, and the two went on drawing one column in two sets of colours for good. A dangling reference is not a disagreement, so nothing complained. The donor's content was also unstamped, where a QML class source has been stamped by content since 2026-08-13. | `dialog`, the landing (references repointed as the layers are replaced) and `_class_source_stamp` | with the layer class source | two tests' worth of assertions, two entries, all proved |
| 15 | A plugin the user has DISABLED goes on working: it adopts styling-dock edits, rewrites the project's group record, and pushes sentences into QGIS's message bar about controls in a window there is no longer any way to open. It lasts until QGIS is restarted. | `plugin.unload`, which closed the dialog and retired nothing; `dialog.retire` and the universal gate | with the plugin | test + entry, both proved |

| 17 | QGIS's own Duplicate Layer copies custom properties, so a copy of an output layer claims its element too. Adoption kept the LAST claimant in panel order, so a copy sitting below the original was adopted instead of it — and the next Generate replaced the copy and left the ORIGINAL standing: last run's tiling, permanently over the new map, under an identical name, never updated again. Nothing said so. | `dialog._adopt_existing_group`, the claim loop | with adoption; the 2026-08-16 fix counted the plugin's own twin as the producer of a second claimant and not the user's copy | test + entry, both proved; the sentence owes text review |

| 18 | Resuming a map made with the map-unit outlines layer ticked could recover onto THAT layer rather than the region: it is built on the region's own source and it carries `weavingspace_output`, which excludes it from the region chooser — so `setLayer` left the chooser empty and the walk returned in front of its own two fallbacks. The resume reported success beside a blank chooser, every element's variable lost, and a Generate that launched nothing and said nothing. Two of four ordinary trials, and certain once the raw layer is tidied out of the panel. | `dialog._recover_the_source` | with the outlines layer | test + entry, both proved |

| 19 | The group chooser names a map by walking the project for a layer whose source matches, taking the first — the same fault as row 18, where the outlines layer is built on the region's own source. Cosmetic here: the chooser could label a map after the plugin's own output. Closed anyway, because a guard missing at one of two doors is a countdown rather than a defence. | `dialog._dataset_label` | with the chooser | assertion + entry, both proved |

| 20 | Two elements drawing one column with different class counts produced ONE emptiness notice: true of one of them, quoting a class count the other does not have, while the second element's own emptiness was never mentioned. The notice has been PER ELEMENT since 2026-08-17 and the landing went on deduplicating it by COLUMN — the restyle path was given this fix on 2026-08-26 and the landing was not. Open as row 41 of the 2026-08-17 ledger for nine days. | `dialog`, the landing's notice loop, where one dedup set gated three sentences of two different kinds | 2026-08-17, when the notice became per element | test + entry, both proved |
| 21 | The no-data notice counted areas with NO GEOMETRY, which never became a tile and draw nothing whatever — so it promised more no-data areas than the map contains, while those rows already have their own sentence. True of the table, false of the map. | `dialog`, the missing-values count | with the notice | test + entry, both proved |
| 22 | Resuming a GeoPackage that gives back fewer elements than were saved to it said nothing: the count was honest about what came back, and only the case where EVERY table failed spoke. Somebody who saved four elements and was handed three was told "3 element layers" and no more, on the journey whose whole purpose is opening a finished result. | `dialog._resume_from_gpkg` | with the resume | test + entry, both proved; the sentence owes text review |

| 23 | Live update's tile-count gate measured the region's bounding BOX, not its ground — the fix of 2026-08-19 went to the estimator's arithmetic and to Generate's caller, and the live caller kept the old question by passing no geometry. So the path a user meets on every keystroke paused maps it can draw: two long islands occupying four percent of their extent read 64,098 tiles against 7,490 drawn, and the note quoted a number the run then disproved. Measured at 5.5x, 8.6x, 34x and 2,592x on four sparse shapes. | `dialog._maybe_live_generate`, and a new cached measure of the ground | 2026-08-19, when the estimator was mended and one of its two callers was not | test + entry, both proved |
| 24 | A layer whose CRS does not match its coordinates reprojects to infinite bounds — and the estimator checks its RESULT for finiteness, which a caller supplying the measured ground never reaches, because that caller discards the extent. The run scored 51 tiles, band "ok", was waved through, and reached the user as a raw `IndexError: index out of range` from inside the library. A sentinel guarded by `isfinite` is only as reachable as the term it watches. | `bridge.estimate_tile_count_bounds` | with the sentinel | test + entry, both proved |

| 25 | MINE, and found by the suite rather than by reading: the retirement check added for row 15 was put ABOVE the deleted-object check in `_dialog_is_gone`, on the reasoning that it is cheaper. Reading any attribute off a deleted wrapper raises from inside a Qt slot, so the one test that destroys a dialog and then pokes it took the whole suite down with `Fatal Python error: Aborted` — twice, at exactly 571 tests, which is what a reproducible crash looks like beside a flaky one. A guard whose whole job is being safe on a dead object may not touch that object before asking whether it is dead. | `dialog._dialog_is_gone` | 2026-08-27, hours earlier | the suite's own test, which now passes |

| 27 | MINE, on the saving branch, and found by its own new tests on their first run: a map could be SAVED ONCE and never again. The first press repoints every element layer at the file it just wrote — which is what keeps the map alive in a reopened project — so the second press asks OGR to write a layer into the table it is reading from, and OGR refuses: `Cannot overwrite an OGR layer in place`. `_save_the_map` reports the first element's failure and returns BEFORE the styles, the stale-table drop and the record, so the whole of "save, change something, save again" was unreachable, and the untick that ruling 2 exists for could never fire. A layer already reading from the destination needs no writing — the data is there — so it is skipped, counted as current so the stale-table drop spares it, and its style embedded. | `dialog._save_the_map`, the write loop | 2026-08-27, with the saving ruling itself | test + entry, both proved (`a-second-save-is-not-a-write-in-place`, caught) |
| 28 | MINE, on the same branch and found the same way: a Save pressed while the FIRST run was still drawing answered "There is no map to save yet. Press Generate first." -- false twice over, since they had pressed Generate and it was running. The mid-run guard was written below the no-map check, and on a first run there are no element layers yet, so the wrong one answered. It sits above now, and the two together say what is actually happening: still drawing, or nothing drawn. | `dialog._save_the_map`, the order of its two refusals | 2026-08-27, with the guard itself, hours earlier | test + entry, both proved |

Rows 1, 2 and 4 were each confirmed here by a route the hunt did not
use before anything was edited: the renderer's own `classAttribute()`
against the layer's fields and the layer stamp for 1; the chooser's
labels beside the panel's names for 2; the group record read before
and after for 4. Row 3 needed no second opinion of mine — two hunts
found it independently, one of them reading the file through GDAL
rather than QGIS, and they agreed on the site.

ROW 27 IS THE ONE THE CONVERSION PAID FOR. It is not a defect a hunt
would have reasoned its way to, because nothing about the code reads
wrongly: the repointing is right, the write loop is right, and each
is right in ignorance of the other. What found it was the ordinary
act — press Save twice — driven by a test written to a ruling, on the
first run of code that had been read several times and never
exercised.

## Ruled on 2026-08-27, and what became of each

THREE OF THE FIVE ARE BUILT AND PROVED, on
`for-0.24.4/copy-select-all`: rows 12, 13 and 16, each with a
registered test and a catalogue entry run and reported `caught`, with
one exception recorded at the entry itself -- the restyle half of row
16 is unproved and its entry is retired with the measurement rather
than left able only to be red.
TWO ARE BUILT AS PRODUCT CODE ONLY, on
`for-0.24.4/saving-is-an-act`: row 26 and the unnumbered Save row.
That branch has never been run, owes twenty-one orphaned catalogue
entries and fifty-six tests, and must not merge until they are
settled. Nothing on the merged line behaves that way yet.
ROW 11 WENT WITH THE SAVE WORK, since the gate it is about is deleted
on that branch and nowhere else.

Five findings were not fixes waiting to be typed. Each changed what is
stored in a file, or what a control means, or a rule other work leans
on -- so each went to a grilling rather than to whoever happened to be
reading the code. All five were settled by the maintainer the same
evening, and **none of them is built**: the work they became is in
ROADMAP.md under 0.24.4, written to be implementable from that
document alone.

Numbered 26 and 11-16 because a number here is a handle rather than a
rank, and because 5 was briefly used twice -- once in the table above
and once for the embed question, which is now 26.

| # | What a user loses | The ruling | Owed |
|---|---|---|---|
| 26 | Ticking "Include the source data", then unticking it and saving again, ships a GeoPackage that still holds a full private copy of the region layer — while the record beside it says `region_embedded: False`, so the plugin then tells the recipient the data cannot be found. They lose both the privacy the box promised and the resume the copy would have given. Measured on the file's own bytes through OGR, and by resuming it in a fresh session. | UNTICKING MEANS IT IS NOT IN THIS FILE. `weavingspace_region` is dropped at Save, and only that table — the one the plugin wrote itself. Follows the 2026-08-26 ruling that the file shows the limit of what it contains. | BUILT on `saving-is-an-act`, unrun: dropped at Save through `_embed_or_drop_the_source` |
| 11 | Live update is ticked, and setting an output path — the documented way to save — stops it. The design view goes on following while the map freezes, the box stays ticked, and nothing is said: no note, no message, no modal, while the user guide promises pausing comes with a note. | THE GATE IS DELETED RATHER THAN EXPLAINED. Under the Save ruling below, no run writes, so a gate that exists to stop a run rewriting a file on every keystroke can no longer fire — and a guard that cannot fire reads as protection. | BUILT on `saving-is-an-act`, unrun: the gate is deleted with its measurement at the site |
| 12 | Clearing the output path and generating again builds a SECOND group rather than replacing in place — silently, unattended under live update. Found independently by the return sweep and the live-update hunt, and it reproduces the panel of near-identical group names the ruling of 2026-08-26 was made to end. | AN OUTPUT PATH NEVER DECIDES WHICH GROUP A RUN LANDS ON. The chooser alone does, which is what the 2026-08-25 ruling gave it. The fork's own reason disappears anyway once a run writes nothing. | BUILT AND PROVED on `copy-select-all`: `test_model_based_dialog_states`, re-decided rather than bent, and the entry `an-output-path-never-decides-the-group` caught |
| 13 | A ramp picked and then picked back leaves a memory nobody can see: the ramp is filed under the family it BELONGS to, so trying a qualitative ramp on a quantitative row fills the categorical slot permanently. Every visible cell returns; the next variable change draws the abandoned ramp. | A RAMP IS REMEMBERED UNDER THE MODE THE ROW IS IN. A row remembers what it WORE in each mode, so the undo returns and neither slot is changed by the other side. | BUILT AND PROVED on `copy-select-all`: `test_a_ramp_is_remembered_under_the_mode_the_row_is_in`, entry caught |
| 16 | A donor that MOVES is followed one run late: the template is read from the donor's outgoing layer at the top of a landing, while the donor is being re-seeded in the same pass, so the follower draws what the donor drew last time. | DONORS ARE SEEDED BEFORE THEIR FOLLOWERS, reading the donor's new layer, with seeding order separated from panel order. Two elements taking from each other have no valid order and keep the lag, with the reason at the code. | BUILT on `copy-select-all`, three entries caught and one RETIRED unproved -- the restyle path's own half is covered by nothing this test can show |
| — | Setting an output path is the documented way to save, and it writes on every run — so a map is saved by a side effect of drawing it, live update has to be held back to stop it, and a path chosen for later is written to at once. | SAVING IS A POSITIVE ACT. A path chooser records what you WOULD save to or load from; a Save button writes and a Load button reads; Generate draws and auto-generate never writes. Save ASKS before overwriting a file the plugin did not write. | BUILT on `saving-is-an-act`, unrun: `_save_the_map`, `_may_overwrite`, and Generate writing nothing at all |

THE LAST ROW HAS NO NUMBER BECAUSE IT IS NOT A DEFECT. Nothing here
was broken; the maintainer was asked how live update should announce a
pause and answered by moving the ground under the question, which is
the outcome a grilling is for. It is recorded beside the four defects
because the work it becomes is what mends them.

## What the round has taught so far

**A GUARD WHOSE CONDITION IS RIGHT CAN STILL BE AIMED AT NOTHING.**
Row 4's guard reads `_fieldless_build`, and that flag was measured
True at exactly the moment the defect fires. Its first test passed
anyway with the guard mutated away, because the journey it drove — a
recolour to a SINGLE SYMBOL — never reaches the adoption that queues
the restamp at all. A single symbol is deliberately not followed (the
scope of the follow ruling of 2026-08-17), so the test was aimed at a
door the act never opens. What found it was the catalogue, not
reading, and the repair was to drive the recolour the way this suite's
other adoption tests drive it: clone the renderer the layer already
has, move one class's colour, install the clone.

**AND THE PREMISE CHECK LIED FIRST.** The rewritten test then failed
on its own premise, because that check read
`layer.renderer().ranges()[0].symbol()` — the freed-temporary pattern
this project has already paid for twice. A premise that cannot be
trusted is worse than no premise, since its failure is read as the
product's. Bind the list to a name before subscripting it, in tests as
much as in the plugin.

**A REPAIR AIMED AT AN ACT MUST BE RE-AIMED AT THE ACT'S ABSENCE.**
Rows 5 and 6 are one sentence apart. On 2026-08-26 the stale-table
drop was taught to take the saved style with the table, which mends
every case where something is deleted. Both of these are cases where
nothing is deleted at all: once because the record of what to delete
lives on a window rather than in the file, once because the table name
did not change and so nothing looked stale. Two hunts found them from
different directions on the same afternoon, which is what makes the
shape worth naming rather than the two rows.

**AND THE INSTRUMENT CHANGED WHAT IT MEASURED, TWICE IN ONE TEST.**
Row 5's guard first read the file through a `QgsVectorLayer` it left
alive, and the open handle made the NEXT run fail at the sqlite level:
zero tables, read as the product's fault. Row 6's guard then read the
file's raw bytes while the dataset was still open and found the
abandoned colour sitting in sqlite's freelist — present with the layer
open, absent once everything had let go, with the style row itself
gone in both readings. The file a colleague receives is the file after
it is closed, and that is the only moment a byte-level claim about it
means anything.

**AND THE SWEEP THAT FINDS A FAMILY MUST SWEEP FOR THE QUESTION.**
Row 3 is the ninth `same_source` site, and it survived the eight-site
sweep of 2026-08-26 for the reason the eighth did: the sweep searched
for the SHAPE that had just been fixed. Eight compared a stamp with a
stamp; the eighth compared a stamp with a live source; this one
compares a layer source with a bare path. Three different pairs of
operands asking one question. The lesson was already written down
after the eighth and it did not prevent the ninth, which suggests the
remedy is not another sentence but a check: anything comparing two
strings either of which has been through a filesystem is a candidate,
and that is greppable.
