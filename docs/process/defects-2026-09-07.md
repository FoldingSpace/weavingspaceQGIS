# Defects closed on 2026-09-07 (night): round nine

Round nine of the hunt campaign, eight Opus hunts at once on worktrees
under `dev/hunts/`, replenished as bugs CLOSE rather than as they are
reported. The round's aim, its slate and the reason the consistency
sweep was proposed beside it and held are in
`dev/hunts/briefs/ROUND-AIM.md`.

A bug is CLOSED when the claim has been reproduced BY A ROUTE THE HUNT
DID NOT USE, repaired, guarded by a registered test, proved by a
catalogue entry judged `caught`, and committed. Faults in our own
instruments are tallied apart, because a day whose findings are mostly
its own instruments is a day nobody should act on.

The ground is the freshest in the repository: everything between rc18
and `79ac943`, which is the honest preview, the zigzag clamp, the
selection rule and the release-asset namer.

| # | What a user lost | Where it lived | Found by | Guard | Entry |
|---|---|---|---|---|---|
| 1 | A rotate or scale drag drew a TORN design and said in red that it could not be laid out, for a move whose commit is gap-free — so a person eased back off an edit the plugin would have accepted in full | `topology_tab._on_dragging` called the library's per-edge `transform_geometry`; `apply` reroutes rotate and scale to the vertex-consistent move. Three callers, one guarded | hunt `spec` | `the drag previews the move the drop would make` | `the-preview-takes-the-commits-own-route` — caught |

## Row 1, in full, because the shape is the transferable part

C-341 reformulated `rotate_edge` and `scale_edge` to move each shared
vertex once, so the tiling stays edge-to-edge where the library's
per-edge versions tear it. That reformulation was written into `apply`
and into `apply` alone. It had THREE callers who must agree with it:
the drop, the drag PREVIEW, and `lays_out`, which is the predicate
every zigzag ceiling is found with.

MEASURED, `laves 3.3.4.3.4` class `a`, one fundamental cell:

    edit                    the preview drew        the drop records
    rotate_edge  15 deg     gap 1.7785% ovl 0.222%  gap 0% ovl 0%
    rotate_edge  60 deg     RAISED GEOSException    gap 0% ovl 0%
    scale_edge   sf 1.5     gap 0.8911% ovl 8.216%  gap 0% ovl 0%
    zigzag_edge  (CONTROL)  gap 0%      ovl 0%      gap 0% ovl 0%

THE CONTROL ROW IS WHAT MAKES THE OTHERS READABLE: zigzag is not
rerouted, so both routes are the same call and must agree — and they
do. A uniform verdict would have been the instrument.

THE REPAIR IS ONE OWNER, `topology_edits.move_as_applied`, which
performs the move a commit would perform; `apply`, `lays_out` and the
drag preview all go through it. The alternative — copying the dispatch
into the tab — is the state the code was already in, one site short.

AND `lays_out` CARRIED A FALSE CLAIM while this stood: its docstring
said it asks "through the same two steps `apply` uses ... so the two
cannot disagree", which was true of zigzag and false of rotate and
scale. Only `zigzag_ceiling` calls it today, so the harm was latent;
it is true now rather than merely written.

THE LESSON, and the hunt that found it said it best: when a
reformulation reroutes a library call, the CALLERS are the door list —
grep for the library's symbol, not for the reformulation's name.

## Instrument faults, tallied apart

None yet in this round's own probes. Two of my own harness traps were
met and cost a run each rather than a diagnosis: `tools/run_some.py`
takes the FUNCTION name and prints `MISSING` otherwise, and it ends
through `os._exit`, so a redirected stdout is block-buffered and the
verdict is discarded — an empty log and exit 1, which reads exactly
like a gate that answered no. `PYTHONUNBUFFERED=1` is the fix, and it
is the fault C-34 already records for a pipe, arriving through a file.

## Reported and NOT counted here

Hunt `assets` walked the release-asset namer end to end — the upload
argv for both uploaders, the generated prose against the namer, the
four documents that name report paths, and C-1's other half — and
found nothing. That is ground the next round need not walk.
