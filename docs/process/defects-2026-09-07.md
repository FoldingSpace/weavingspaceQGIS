# Defects closed on 2026-09-07 (night): round nine

Round nine of the hunt campaign, eight Opus hunts at once on worktrees
under `dev/hunts/`, replenished as bugs CLOSE rather than as they are
reported. The round's aim, its slate and the reason the consistency
sweep was proposed beside it and held are in
`dev/hunts/briefs/ROUND-AIM.md`.

A bug is CLOSED when the claim has been reproduced BY A ROUTE THE HUNT
DID NOT USE, repaired, guarded by a registered test, proved by a
catalogue entry judged `caught`, and committed. Faults in our own
instruments are tallied apart, because a day whose findings are mostly
its own instruments is a day nobody should act on.

The ground is the freshest in the repository: everything between rc18
and `79ac943`, which is the honest preview, the zigzag clamp, the
selection rule and the release-asset namer.

| # | What a user lost | Where it lived | Found by | Guard | Entry |
|---|---|---|---|---|---|
| 1 | A rotate or scale drag drew a TORN design and said in red that it could not be laid out, for a move whose commit is gap-free — so a person eased back off an edit the plugin would have accepted in full | `topology_tab._on_dragging` called the library's per-edge `transform_geometry`; `apply` reroutes rotate and scale to the vertex-consistent move. Three callers, one guarded | hunts `spec` and `harm`, independently | `the drag previews the move the drop would make` | `the-preview-takes-the-commits-own-route` — caught |
| 11 | Drag past what the design allows, meet the red refusal, ease back inside and let go — and the recorded edit was up to 3.5× the move the preview drew, while the box still showed what you released at | `_drag_reached` is written on the failing branch and was cleared only at the press and at the drop, never when a later frame laid out, so the drop refined toward a value abandoned mid-gesture | hunt `stores3`, on a repair minutes old | `the drop closes the gap a frame of travel left`, gained the forgetting arm | `a-frame-that-lays-out-forgets-what-was-held` — caught |
| 10 | Your SECOND push drag moved the design nearly twice as far as your pointer and recorded nearly twice the number you asked for — the exact loss row 9 had just closed | Row 9 froze the gain at the press but `_push_gain_now` read `_drawn()`, and the drop deliberately keeps a preview until the asynchronous rebuild lands, so the freeze captured the previous drag's preview | hunt `repairs5`, on a repair minutes old | `a push moves the ground as far as the pointer went`, gained the store arm | `the-push-gain-comes-off-the-held-design` — caught |
| 9 | A push rail stopped following the pointer as soon as a drag arrived as more than one move event — which is every real drag: the ground ran up to 1.9× ahead, then the tab turned amber, and what was recorded depended on how many events the machine delivered (45 px recorded 0.2805, 90 px recorded 0.1996) | `push_gain` asked `_drawn()`, which is the drag's own PREVIEW from the second frame on, and a push moves the very neighbours the gain is summed from | hunt `repairs4`, on a repair one hour old | `a push moves the ground as far as the pointer went`, gained the freeze arm | `the-push-gain-is-frozen-at-the-press` — caught |
| 8 | Change the design family or element count and the change list labelled the INCOMING design's edits with the OUTGOING design's verdicts, for 6.45 seconds until the rebuild landed | `set_edits` is a third writer of `_edits` and touched `_marks` not at all; tonight's repair for row 3 enumerated `_undo` and `_clear`, the two writers anybody would list | hunt `unreach2` | `a change never wears the verdict of the one it replaced`, gained the restore door | `a-restored-design-brings-no-marks-with-it` — caught |
| 7 | The ghosted wave showed 2n−1 lobes at twice the pitch of the one the map receives, and at the default n=2 its trailing lobe bulged to the WRONG SIDE of the edge — so the cue a person sets a zigzag by was not the wave they got | `_draw_the_zigzag_it_would_make` put a full-amplitude point at every multiple of `length/(2n)`; the library's sine crests at the ODD multiples and crosses zero at the even ones | hunt `spec2` | `the zigzag ghost crests where the library does`, gained the pitch axis | `the-ghost-crests-at-the-librarys-pitch` — caught |
| 6 | The change list printed the zigzag's amplitude at TWICE the number the Amplitude box showed for the same edit — and a clamped edit could put `h 1` on the same screen as a sentence saying it was drawn at 0.488 | `h` is peak-to-peak and `_CREST_OF_H` converts it at the handle, the ghost, the drag's inverse, the box and the clamp's sentence; the list was a sixth readout printing the raw record | hunt `boundary` | `the change list shows the amplitude the box shows` | `the-change-list-converts-the-amplitude` — caught |
| 5 | On five catalogue designs, EVERY edit was marked as having broken the tiling and every drag drew red "this would make the tiles overlap" — on designs that tile perfectly and moves the plugin accepts in full | `plane_coverage`'s overlap term is summed tile area minus the union's, and `get_local_patch` hands the same tile back twice on `square-colouring 3` and `chavey H`, `I`, `J`, `K`; the subtraction cannot tell two tiles from one counted twice | hunt `repairs2` | `an untouched design is never told it does not tile` | `one-tile-per-piece-of-ground` — caught |
| 4 | If a build happened to land while you were holding a handle, you were never told why your replayed edit could not move what it named — the mark reads sound and the row reads clean, so the sentence was the only witness | A landing is THREE calls — `set_unit`, `set_marks`, `report` — and only the first is held during a gesture; the held one replays at the drop and clears the note the third had written | hunt `stores2` | `a build that lands mid drag does not wipe the gesture`, gained the axis | `a-held-landing-keeps-its-sentence` — caught |
| 3 | The change you make after an Undo was labelled with the verdict of the change you took off — "from here the tiles no longer meet" over an edit that tiles perfectly, until the rebuild lands | `_marks` has one writer, a landing, and is read BY INDEX beside `_edits`; `_undo` and `_clear` change the edits alone | hunt `dragstore` | `a change never wears the verdict of the one it replaced` | `an-undone-change-takes-its-mark-with-it` — caught |
| 2 | A SOUND hex design was hatched as torn at two corners of the drawing — the red mark that means "the tiles no longer meet" drawn over ground that meets perfectly | `tears_in_the_patch` lays a 3×3 block of cells on the two shortest lattice vectors and read it against a patch at `r=2`; the patch is not that shape, so on a non-orthogonal lattice the block's corners hung outside the tiles | hunt `harm` | `the hatch shows the tear and not the edge of the drawing`, extended to a hex family | `the-hatch-block-stays-inside-the-patch` — caught |

## Row 1, in full, because the shape is the transferable part

C-341 reformulated `rotate_edge` and `scale_edge` to move each shared
vertex once, so the tiling stays edge-to-edge where the library's
per-edge versions tear it. That reformulation was written into `apply`
and into `apply` alone. It had THREE callers who must agree with it:
the drop, the drag PREVIEW, and `lays_out`, which is the predicate
every zigzag ceiling is found with.

MEASURED, `laves 3.3.4.3.4` class `a`, one fundamental cell:

    edit                    the preview drew        the drop records
    rotate_edge  15 deg     gap 1.7785% ovl 0.222%  gap 0% ovl 0%
    rotate_edge  60 deg     RAISED GEOSException    gap 0% ovl 0%
    scale_edge   sf 1.5     gap 0.8911% ovl 8.216%  gap 0% ovl 0%
    zigzag_edge  (CONTROL)  gap 0%      ovl 0%      gap 0% ovl 0%

THE CONTROL ROW IS WHAT MAKES THE OTHERS READABLE: zigzag is not
rerouted, so both routes are the same call and must agree — and they
do. A uniform verdict would have been the instrument.

THE REPAIR IS ONE OWNER, `topology_edits.move_as_applied`, which
performs the move a commit would perform; `apply`, `lays_out` and the
drag preview all go through it. The alternative — copying the dispatch
into the tab — is the state the code was already in, one site short.

AND `lays_out` CARRIED A FALSE CLAIM while this stood: its docstring
said it asks "through the same two steps `apply` uses ... so the two
cannot disagree", which was true of zigzag and false of rotate and
scale. Only `zigzag_ceiling` calls it today, so the harm was latent;
it is true now rather than merely written.

THE LESSON, and the hunt that found it said it best: when a
reformulation reroutes a library call, the CALLERS are the door list —
grep for the library's symbol, not for the reformulation's name.

## Row 1 closed a SECOND claim, which was checked rather than assumed

Hunt `preview` reported, separately, that a torn preview stops saying
so the moment the value freezes: the held arm of `_on_dragging` writes
`failed` as "nothing has laid out yet in this gesture" and discards
what `_status_of_a_drag` decides from `plane_coverage`, so red turns
amber while the same torn design is still on screen. It measured that
on a per-edge rotate of `archimedean 4.8.8` — which is the route row 1
removed.

MEASURED ON THE REPAIRED TREE, and the design the claim was made on is
in the sweep because a negative that omits it is not a negative: 155
combinations of design, class, manipulation and value across `laves
3.3.4.3.4`, `hex-slice 3` and `archimedean 4.8.8` produced NO move
that lays out and still tears. The held arm's own comment reserves
FAILED for exactly that state, so with the per-edge route gone there
is nothing left to reach it.

SO THE CLAIM WAS REAL BEFORE `a2f719a` AND IS INERT AFTER IT. What
survives is a LATENT disagreement rather than a live defect: the held
arm still invents its two flags instead of asking the twin that owns
the judgement, and the day something drawable can tear again, the two
will disagree. Recorded here rather than repaired, because a guard for
it would have to stage a state no geometry currently produces.

(A fixture trap on the way, worth the line: my first sweep asked for
`archimedean 4.8.8` at n=3 and got a KeyError it reported as "could
not build", so the claim's own design was silently absent from the
first negative. A FAMILY TYPED IS NOT A FAMILY CHOSEN — look the name
up in the catalogue, which is the third time this document's family
has recorded that.)

## Corrected rather than counted: a comment that promised what the code cannot do

Hunt `clamp` found that `_on_dragging` justified computing no ceiling
during a drag by saying "the COMMIT-time clamp then applies the true
ceiling at the drop, so a fast drag that stops a little short is
corrected on release". It cannot: `apply`'s clamp is entered only
where the edit produced nothing drawable, and a held value laid out by
construction, so the clamp only ever LOWERS. Measured: asked h=0.297
against a ceiling of 0.594, `apply` returns 0.297 untouched.

AND THE SHORTFALL IS A FRAME OF TRAVEL, NOT A PIXEL OF IT, which is
what decides whether this is a wrong comment or a defect: a frame
costs 158 ms and the default design's edges draw at about 94 px, so a
pointer at 250 px/s advances 0.42 of the amplitude between samples
against that 0.594 ceiling.

The comment is corrected. The BEHAVIOUR is ruling 2 working as
written — the value holds at the last one that laid out — so making
the drop seek the exact ceiling is a change to a ruling rather than a
repair, and it is the maintainer's to make. Not counted among the
twelve: no code changed.

## Row 2, and the fixture dimension that let it ship

`tears_in_the_patch` was written tonight to answer the wider question
`plane_coverage` cannot: not whether a tiling has torn, which one
fundamental cell decides, but WHERE, so the hatch can be drawn over
every torn piece. It lays a 3×3 block of cells on the two shortest
lattice vectors. The patch it reads that block against is not that
shape, and on a non-orthogonal lattice the block's corners fall
outside the tiles altogether — where ground with no tile over it is
indistinguishable, to this function, from ground a tear opened.

MEASURED, untouched designs straight from the catalogue, spacing 1000:

    design               coverage gap     tear reported     one cell
    laves 3.3.4.3.4       0.00000000               0.0    1,000,000
    hex-slice 3           0.00000000         144,337.6      866,025
    hex-slice 6           0.00000000         144,337.6      866,025
    archimedean 4.8.8     0.00000000               0.0    1,000,000
    hex-colouring 4       0.00000000               0.0      866,025

A sixth of a cell of pure fiction on two of five designs, on ground
whose coverage gap is exactly zero. At `r=3` every row reads 0.0, for
11.3 ms against 5.8 — nothing beside a 158 ms drag frame.

THE GUARD ALREADY ASSERTED BOTH HALVES and still could not see it,
because it drove `laves 3.3.4.3.4` alone and laves has an orthogonal
lattice. THE DESIGN A CLAIM IS DRIVEN ON DECIDES WHETHER IT CAN BE
SEEN, which this project has recorded before about a nudge laves
refuses; here it is a whole FAMILY of lattices. The guard runs the
sound half over a hex family now.

AND THE TEST HAD NO CATALOGUE ENTRY AT ALL, which is why nobody had
watched it fail. It has one.

## The repair of row 1 stranded four guards, and the replenished hunt found it

`repairs1` was launched at `a2f719a` the moment row 1 closed, which is
this campaign's rule, and it found no product defect and one
instrument fault that mattered more: giving the reformulation one
owner MOVED the lines four catalogue entries stood on, so
`rotate-stays-a-tiling`, `scale-stays-a-tiling`,
`an-edits-arguments-reach-the-library` and
`a-drag-previews-in-the-units-it-commits` all anchored on text that no
longer existed. Such an entry matches nothing, mutates nothing and
exits clean — it reports nothing, which is indistinguishable from
reporting success (C-288). The branch could not have passed its own
pre-push gate.

All four are re-anchored on the lines as they stand. THE LESSON IS THE
HUNT'S OWN: a repair that MOVES lines strands every entry standing on
them, and one read-only run of `check_standards` says so in seconds —
so run it before any probe, not after.

AND MY OWN NEW ENTRY SURVIVED ON ITS FIRST JUDGEMENT, which is the
same fault one level up. It broke the tab's routing while its test
called the owner directly, so the test never walked through the door
the mutation shut — "is the test still ARRIVING at the door", the
third question the catalogue asks of a survivor. The guard now asks
the source what `_on_dragging` calls, which is the routing axis and a
different claim from whether the reformulation is correct. That split
is the same one the release-asset entries already draw between naming
and routing.

## Two dead axes in tonight's own guards, repaired

Hunt `trigger2` mutated nineteen assertions of tonight's nine guards,
one at a time, with every control run first: seventeen died at the
intended line. The two that could not fail are instrument faults
rather than product defects, and they are counted apart — but both
are repaired, since a test that cannot fail occupies the ground so
nobody writes the real one.

THE CLEAR ARM WAS NOT ARRIVING AT ITS DOOR. `_clear`'s arm sits below
the restore arm added for row 8, and `set_edits` now empties the
marks — so Clear arrived with nothing to carry and passed however it
behaved. It is given a mark to drop first, and with that line the
mutation fails at its own assertion: "a change made after a clear
wears an older change's verdict".

THE DISCRETE-ARGUMENT ASSERTION HAD NOTHING TO EXCLUDE. `held` and
`asked` carried the same count and smoothness, and the refinement's
own value filter already drops anything equal, so removing the
exclusion changed nothing. The fixture now differs in both, which is
what a real zigzag drag produces — count and amplitude move together
— and the mutation then interpolates them: `{'n': 2.5, 'h': 0.6445,
'smoothness': 3.25}`.

BOTH REPAIRS WERE WATCHED TO FAIL, and the first attempt to watch
them proved nothing: the mutation was applied by a heredoc run AFTER
the QGIS environment was exported, so it died at `encodings` having
edited nothing, and the test then passed unmutated. That is C-35
exactly — edits under `env -u PYTHONHOME -u PYTHONPATH python3`,
tests under `$QGIS_PY` — met by somebody who had quoted the rule
earlier the same night.

## Instrument faults, tallied apart

None yet in this round's own probes. Two of my own harness traps were
met and cost a run each rather than a diagnosis: `tools/run_some.py`
takes the FUNCTION name and prints `MISSING` otherwise, and it ends
through `os._exit`, so a redirected stdout is block-buffered and the
verdict is discarded — an empty log and exit 1, which reads exactly
like a gate that answered no. `PYTHONUNBUFFERED=1` is the fix, and it
is the fault C-34 already records for a pipe, arriving through a file.

## Reported and NOT counted here

Hunt `assets` walked the release-asset namer end to end — the upload
argv for both uploaders, the generated prose against the namer, the
four documents that name report paths, and C-1's other half — and
found nothing. That is ground the next round need not walk.
