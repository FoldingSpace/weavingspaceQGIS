# The defects of 2026-08-26: a candidate that went red, and eight hunts aimed at its repairs

Written the day they were found, per the rule that a day passing
roughly ten defects gets a ledger and that the OWES column is the
record rather than any sentence above it.

THE DAY HAS THREE PARTS. The morning's candidate failed three tests,
on every platform, and NOT ONE was a product defect: four tests were
asserting a contract the maintainer retired on 2026-08-25, an oracle
was judging from before a legitimate replacement, and a test was
holding a widget across a debounce that had been shortened hours
earlier. The evening's FIRST round of eight hunts, aimed at the seven
fixes made that morning, found four real defects, three of them
confirmed by hunts that could not see each other. Its SECOND round of
eight, aimed an hour later at those repairs, found five more -- and
four of those eight hunts landed on a single line written in that
hour.

ELEVEN DEFECTS AND FOUR TEST FAULTS IN ONE DAY, sixteen rows in all
counting the ruling and the false claim. Nine of the eleven are in
code written on 2026-08-25 or 2026-08-26, and SEVEN are inside repairs
made the same day -- three of them inside repairs made in the same
hour. This project's own history: eleven of fourteen within hours on
08-16, four of five on 08-20, nine of eleven on 08-25. The claim that
a round aimed at the last round's repairs always finds something now
stands at SIX ROUNDS FOR SIX.

AND THE SUITE COULD NOT HAVE FOUND ANY OF THEM. The candidate they
were found in had passed 574 tests, the visual gallery on three QGIS
versions, the colourspace comparison against a reference renderer
that had just moved by thirty upstream versions, and six CI jobs. A
gate measures whether known behaviour still holds.

## The ledger

| # | what a user lost | where it lives | found by | owes |
|---|---|---|---|---|
| 1 | A colour hand-picked, a bound pinned, a ramp, a single colour, an opacity or a class count chosen on ONE output group stayed in force on another whose own saved record holds none — drawn on that second map and STAMPED onto its layers, so it reached the saved project and the GeoPackage a colleague opens. Against ruling 4's "selecting a group restores all of it, so nothing is inferred" | `dialog._apply_element_records`: every key was written only where the incoming record HAD it, so a silent record left the previous group's answer standing. Nothing else clears them — `_detach_from_the_group` empties the layer ids and the signatures, and the per-dataset banks swap only on a change of DATASET, where two groups of one dataset share a bank by construction | THREE hunts independently — the records hunt (write-only), the stamp hunt (two-stores) and the whitelist hunt (one-boundary) | **FIXED** — every key assigns or pops; the field-keyed records clear this element's entry for THIS variable only. Test `a group restores its own state and no one else's` + entry |
| 2 | Resume a saved map whose layers are already in the project and the group is never given the working state, so saving the project, reopening it and choosing that group hands back the layers and none of the design | `dialog._resume_from_gpkg`, the `already` branch added in `b597861`. Its twin ends with `_stamp_working_state` and says at that line why; the new branch was written from the twin and dropped what came after the take-over | TWO hunts independently — the doors hunt (asymmetry) and the losses hunt, which starts from what a person loses rather than from the code | **FIXED** — the branch stamps the group. Test `a file already open resumes completely` + entry |
| 3 | The same resume leaves the region chooser on whatever OTHER dataset was selected, while the plugin announces that this map "is the one being worked on" — so every element is rebound to the wrong dataset's columns | the same branch, which also omits `_recover_the_source(path, record)` | the losses hunt; the resume hunt reached it too and ruled it held redundantly, having walked it only as far as the next Generate | **FIXED** — the branch recovers the source. Cell in the same test + the same entry |
| 4 | After resuming, the region chooser OFFERS the map's own tile layers and can auto-select one, so the next Generate tiles the plugin's own output into a new file and reports it as a success — a wrong map that looks exactly like a right one | `dialog._resume_from_gpkg`, BOTH branches: the path registers element layers and never calls `_update_layer_exclusions()`, which construction, project-read and the run landing all do. `test_integration_region_layer_switch` asserts the invariant in words, on the Generate path only | the silence hunt (write-only), read back through GDAL and against a control leg | **FIXED** — both branches update the exclusions. Tests `a file already open resumes completely` and `a fresh resume keeps its output out of the region chooser` + entry |
| 5 | Nothing, by the maintainer's ruling. A group nested inside a folder of the user's own is invisible to every reader that walks `root.children()`, so resuming its file builds a SECOND copy of the map beside it and a later run drops tables the nested one is still drawing from — `tiles_a_v1` removed from the file while the tidied group went on pointing at it | `dialog._our_groups` and the take-over scan in `_resume_from_gpkg`, both non-recursive by design | the resume hunt (one-boundary), at medium confidence and naming the settled decision itself | **ACCEPTED, 2026-08-26.** The maintainer ruled non-recursive is fine. Recorded at `_our_groups` with what it COSTS as well as what it buys: "left alone" means unmanaged rather than protected |
| 6 | Nothing directly, but it is a false claim in a binding place: the comment governing the preview debounce said a rebuild's cost "scales with elements times features", and the roadmap and the handover repeated it | `dialog.py` above `PREVIEW_DEBOUNCE_MS`. Measured: one rebuild makes 30,529 primitive calls over sixteen areas and 30,529 over three thousand — identical. Cost tracks elements and FIELDS. The claim had borrowed the shape of the uncached-value defect of 2026-08-19, which really was about features and is a different mechanism on a different path | the wait hunt (unreachable), which found no defect and was worth its place for this | **FIXED** — the comment says what was measured, including the 871 ms rebuild at 26 elements over 100 columns that arms the timer at its ceiling |

## ROUND TWO: eight more hunts, aimed at round one's repairs

Launched the same evening, an hour after the six above were fixed, on
the maintainer's word. EIGHT HUNTS, FIVE DEFECTS -- and FOUR of the
eight landed on ONE LINE written in that hour, reaching four different
consequences from four different shapes. That is the strongest
convergence this method has produced here.

| # | what a user lost | where it lives | found by | owes |
|---|---|---|---|---|
| 11 | Resume a saved map whose layers are already open while the region chooser is on another dataset, and every element's variable is silently replaced by one derived from the WRONG dataset's columns -- the table describes a map the layers do not draw, and the group is stamped with that | `dialog._resume_from_gpkg`, the take-over branch. `_recover_the_source` was added to it in row 2's repair and placed AFTER `_apply_working_state`; the twin calls it BEFORE and says why at the line -- a variable cannot be restored to a column the region in force does not have | the takeover hunt (asymmetry) and the losses2 hunt (asymmetry), independently | **FIXED** -- the recovery runs first. Test cells + entry `a-resume-recovers-the-data-its-map-was-made-from` |
| 12 | The same journey skips the pins and the categorical colours outright, so a pinned ladder comes back re-derived: breaks moved from a pinned 0.5-2.5 to 0-3 | the same ordering. `_apply_element_records` computes `same_data` against the STALE chooser and `continue`s past those keys | the stamping hunt (two-stores), reading the file's own metadata through GDAL against the layers' stamps | **FIXED** by the same move. Cells in the same test |
| 13 | ONE MAP'S HAND-PICKED VALUE STRINGS REACH ANOTHER DATASET'S BANK AND ITS GEOPACKAGE -- ruling 8's cross-dataset leak, and the worst thing found tonight | the same ordering, one hop further: `_take_over_group` adopts the resumed layers' stamps while `_memory_layer_id` still names the OTHER dataset, so the bank swap comes too late | the banks hunt (two-stores), reading B's `.gpkg` as raw bytes for A's value strings | **FIXED** by the same move; guarded by a cell asserting no other dataset's fields are readable in the active bank |
| 14 | A pinned bound destroyed by switching that element to categories and then choosing your own group -- and stamped ABSENT on the layer, so a reopen could not bring it back | `dialog._apply_element_records`. The clearing added in row 1's repair read a record silent BY MODE as a record of a choice nobody made: `_assignments` reports pins, class colours and the ramp window as empty for any row not wearing Graduated. Against the ruling of 2026-08-20 that the records of the style a row is NOT wearing are kept, and kept silently | the pops hunt (one-boundary), which is the direction aimed straight at row 1's repair | **FIXED** -- the clearing is gated on the element's mode, which needed `mode` added to `WORKING_STATE_ELEMENT`, since that list is the record's real definition and the fix was inert without it. Test + entry |
| 15 | Nothing yet: a cell of mine could not fail. The exclusions cell in `test_a_file_already_open_resumes_completely` passed with the line it names deleted | the FIXTURE generates in-session, so the landing had already excluded those layers before the resume ran. The journey the branch exists for is a dialog that never landed them | the newtests hunt, mutating PER ASSERTION -- one dead axis in thirteen, against this project's standing one in five | **FIXED** -- the fixture clears the exclusions and asserts that it could |
| 16 | Change an element's ramp and press Generate -- a restyle, not a re-tile -- then open that GeoPackage anywhere without its project: the design from BEFORE the change is restored, the controls describe one map while the layers draw another, and the first Generate repaints the map back to the abandoned ramp | `dialog._restyle_only`'s tail. It stamps the GROUP's record and re-embeds each layer's style into the file, and never wrote the FILE's record: that write was added to the landing alone, hours after the group's, so it inherited the gap the group's write had just closed | the reopen hunt (one-boundary), reading `WEAVINGSPACE_STATE` straight out of GDAL and the file's map through a fresh layer | **FIXED** -- the restyle writes the file's record too, carrying the embedding flag rather than re-deciding it. Test + entry |
| 17 | A user who colours a categorized element from a QML loses it by selecting that element's own group: it reverts to automatic colours, the next Generate draws the wrong ones and stamps an empty class source, so the choice is gone from the project and the GeoPackage | `dialog._sync_row`. Where the class-source combo already exists it was repopulated and never RE-SELECTED from `_class_choices` -- the fix its ramp-cell twin was given on 2026-08-18. `_assignments` reads the widget, so `_refresh_table` wrote the stale answer back over the record | the widgets hunt (write-only), reading the layer's renderer rather than the dialog's dicts | **FIXED** -- the combo follows the record. Test + entry |

### What round two says, beyond the defects

THREE OF THE FIVE ARE IN REPAIRS MADE THAT HOUR. Rows 11 to 14 are all
inside row 1's and row 2's fixes. The claim in this project's record --
that a round aimed at the last round's repairs finds something every
time -- now stands at SIX ROUNDS FOR SIX.

AND THE TWO THAT ARE NOT are the same shape as each other and as row
16's cause: ONE FACT IN TWO STORES, mended in one. The group's record
and the file's record; the record and the widget. A repair that gives
one store a new write should enumerate every store that already held
that fact, which is now written into docs/TESTING.md.

FOUR HUNTS ON ONE LINE, and none of them could see the others. The
line was present and in the wrong place, which is why no reading of
"is the call there?" would have found it -- and why the entry aimed at
its POSITION could not be made to catch on three separate fixtures,
adoption covering the late recovery by accident through the layer
stamps. That entry is aimed at the call's ABSENCE instead, and the
order is recorded as guarded by these reproductions rather than by the
catalogue. A cover that is accidental is a countdown, exactly as this
project's ledger of 2026-08-25 says.

## ROUND THREE: no hunts at all, and three defects sixteen hunts walked past

Later the same night, on the maintainer's question of whether hunts
are the best use of a round's tokens. They were not, this time. What
ran instead was a CONSISTENCY SWEEP -- one oracle over many acts,
described in docs/process/HUNT-RECORD.md under "WHAT TO RUN INSTEAD OF
A HUNT" -- and it found three defects in ground sixteen hunts had just
finished reading, at a cost of one evening's building and about seven
minutes a run, with no verification queue at all.

WHY IT REACHED WHAT THE HUNTS DID NOT, which is the part worth
keeping. The hunts were aimed at the code written that day, and every
one of them was aimed WELL: rows 11 to 17 are all in that code. These
three are in code written on 2026-08-25 and in one case older still,
and they are reached not by an exotic journey but by the MOST ORDINARY
ONE THERE IS -- close the plugin window and open it again. A hunt
chooses where to look; a sweep enumerates what a person does. The two
find different things, and this round is the evidence.

| # | what a user lost | where it lives | found by | owes |
|---|---|---|---|---|
| 18 | Close the plugin and open it again -- same session, no save, no quit -- and every hand-chosen variable, style, ramp and class count reverts to the plugin's default cycle. The layers go on drawing the map the person made, the GeoPackage goes on holding the right design, and the TABLE shows a confident wrong one; the next Generate writes that wrong answer onto the map, so a categorical land-cover element comes back as a quantitative `v2` one. Two of four elements destroyed in the measured case, and the two that survived did so only because they sat on the plugin's own default. Against ruling 4 of 2026-08-25: selecting a group restores the whole working state "so nothing is inferred" | `dialog._adopt_existing_group`, which the CONSTRUCTOR calls and which `_on_project_read` calls after File > Open. It takes over the group's LAYERS and never reads the group's RECORD; `_on_group_chosen` reads it and applies it three lines apart. A restore added to one door and not the others, which is the shape CLAUDE.md calls the sharpest thing the group-unit build taught | the consistency sweep, as a BOUNDARY crossing that must change nothing and changed ten things. Confirmed by three routes: a new dialog in the same session, a new dialog after a project reopen, and -- the one that localises it -- CHOOSING that same group in the chooser, which restores everything instantly | **FIXED** -- `_restore_the_adopted_design`, called LAST in the constructor and from `_on_project_read`, recovering the source BEFORE applying the record per the ordering lesson of the same week. Test `a reopened dialog wears the design it left` (which drives the harm leg: the Generate after reopening) + proved entry |
| 19 | One ordinary session leaves TWO output groups. Live update draws a map into "WeavingSpace tiles"; the user then chooses a GeoPackage and presses Generate, and the plugin builds "WeavingSpace tiles 2" beside it rather than replacing in place. The first survives as four MEMORY layers holding a complete stale copy (153 features each), it survives the project save, and the group chooser then offers "WeavingSpace tiles 2 — first" beside "WeavingSpace tiles — first" for good. That is the colleague's own complaint of 2026-08-25 -- one dataset owning two groups with nothing to tell them apart -- arriving without any change of dataset at all, and against the settled rule that Generate replaces in place and "Create as new group" is the only route to a second | the landing, on the first run after an output PATH is chosen. Localised by printing the group list after each act: the second group appears at the Generate, not at the path being chosen | the consistency sweep's own baseline, which reported eight of the plugin's layers where four had been made; then a step-by-step probe, which put the second group's appearance at the Generate rather than at the path being chosen | **FIXED** on the maintainer's ruling (2026-08-26: replace in place -- the previous result IS this design's own draft). A destination is only "changed" if there WAS one: `moved_the_output` requires a non-empty `_last_path` before the comparison bites. Test `one session leaves one group` + proved entry |
| 20 | Delete the output GeoPackage in the Finder and press Generate. The file is rewritten, NOTHING in the project reads from it, the four element ids the dialog holds resolve to no layer, and the only map left on screen is the stale memory copy from row 19 -- at the OLD spacing. So a user changes the spacing, presses Generate, sees no change and no message, and is looking at a map that is no longer the one being saved | the landing. Traced by wrapping it: releasing the deleted file's old handles RECREATES a zero-byte file at the path (closing a GeoPackage makes sqlite touch it), the bare `os.path.exists` test then chose update mode, `write_gpkg_layer` raised on a file that is not a GeoPackage, and the Qt slot swallowed it -- old layers already removed, new ones never made. Measured at the write's own moment: `first=False exists=True size=0` | the four-corner probe's second leg; the site by three instrumented runs, the last a spy on `write_gpkg_layer` itself | **FIXED** -- a zero-byte file holds nothing to preserve, so it counts as absent; a NON-empty file keeps the update path that protects a colleague's tables. Test `the map survives its file being deleted` + proved entry. The file-lifecycle census then cleared all four variants -- deleted, moved away, moved away AND BACK (the Windows red's exact journey), and replaced by an empty file -- so the four-for-four Windows failure on `a project whose output geopackage has moved` is likely this same defect; the next CI run says |

**A THIRD LEAD, from the source-gone census after the fixes:** a
Generate pressed while the REGION file is deleted keeps the map (right)
and re-selecting through the chooser recovers the work (right, and it
is the answer to "can the user re-link" -- the chooser is the button),
but the refusal said NOTHING in this journey where the settled contract
says the user is told in terms of the file. Probe
`census2-source_gone`; unjudged, and it may be the probe's reading of
the message stores rather than a silence.

**TWO LEADS, recorded because an unjudged claim decays.** After
switching dataset and back, element a's ladder came back with five
classes where it had six -- real in the sweep, not reproducible in
isolation, and possibly an artefact of row 19's duplicate. And
`Median income ($)` and `Median income (%)` both sanitise to
`tiles_?_Median_income`, distinguishable only by the element letter,
which is legibility rather than loss but is exactly what the rename of
2026-08-25 existed to cure.

**THE SWEEP WAS RE-RUN ON `6d6ea2d` (the bulletproofing round) AND
ITS CLAIMS JUDGED THE SAME HOUR, each by a route it did not use.**
Four verdicts, two of them the sweep's own harness at fault:
- *save/quit/reopen turned a categorized Classes cell from 4 to 0*:
  REFUTED. The sweep's region is a MEMORY layer, and a memory layer
  round-tripped through a .qgz comes back valid with zero features --
  the suite's own documented trap, met by the instrument built after
  it was documented. Disk-backed, the cell reads 4 on both sides of
  the boundary (`judge_return.py`). Sweep harness fault.
- *four_corners door 4, "the map still classifies the OLD values"*:
  REFUTED, and the premise fault is subtler than last time: twelve
  values multiplied by ten can leave the column MAXIMUM unmoved
  (nonzero but max-preserving -- 0.5 x 10 = the old top of 5.0).
  Editing the LARGEST values and asserting the maximum moved, the
  settled ladder follows the data exactly, 3.0 -> 30.0
  (`judge_dataedit.py`). The premise assertion belongs on the
  quantity the code reads, said now for the second time.
- *close-and-reopen changed the map* (features 153 -> 112, ladders
  re-cut): NOT REPRODUCED on the plain journey -- deliberate ramp,
  k=6, generate, close, reopen changes nothing (`judge_reopen.py`) --
  and the plain A-B-A return holds all three stores at 6. The sweep's
  own record shows record k=6 against map k=5 BEFORE its crossing,
  so whatever diverged did so earlier in its staged session; OPEN,
  awaiting correlation with the round's doors hunt.
- *file_lifecycle*: all four output-file variants survived on this
  tree, which is the row-20 family measured sound on macOS; the
  "said nothing" lines match harness fault eleven's probe-side
  message reading and are not re-opened by this run.

**SEVEN THINGS CLEARED, which cost the same as the findings and are
worth as much.** Resume through its own door reproduces the sent map
exactly; a colleague's own table in a shared GeoPackage survives
untouched beside the new element tables; a user's subset filter reaches
the map and survives the run (measured on the tiles' own values, not on
an extent, which is what the first version got wrong); awkward column
names cost no element its data; one-colour-one-meaning holds across
acts and re-tiles; a second plugin window replaces in place as
designed; and a fresh unseeded profile behaves identically, which
retires the cheap arm of the cross-platform direction.

**TEN HARNESS FAULTS OF MINE, against three defects.** Counted here
for the reason a matrix's own failures are counted: a grid whose
failures are mostly its own is one nobody acts on. A freed temporary
answering `#000000` for a colour (this project's own documented trap,
met inside the instrument written to avoid it); a renderer wrapper
QGIS had taken ownership of after `setRenderer`; a spin box held
across a rebuild, which is the ledger's own row 9 arriving in a probe;
an oracle comparing against a snapshot older than a legitimate
replace-in-place; a view keyed by a group NAME, which a rename
legitimately moves; resume driven through the output-path widget
rather than its own door; an extent test too coarse to tell honest
tile overhang from a filter being ignored; a record read from the
wrong one of two groups, which row 19 had made ambiguous; a
line-based regex missing the multi-line `setCustomProperty` calls; and
an "explained at the list" check that looked for the wrong name. THE
PATTERN IS THAT EIGHT OF THE TEN WERE PROBE FAULTS THIS PROJECT HAS
ALREADY WRITTEN DOWN, met again by somebody who had read them that
same evening.

## ROUND FOUR: eight hunts at iterative exploration, five defects, all fixed

Launched on the maintainer's word at the core loop -- a person
exploring their data iteratively -- in eight directions chosen to be
unlike round three's ground: backwards-from-harm on a twenty-minute
session, A/B oscillation, the scheme shelf's kept-silently records,
preview-against-map under churn, classification churn on awkward
columns, the colour editor mid-iteration, monotonic growth over fifty
iterations, and seeded stochastic sessions. THREE CAME BACK EMPTY WITH
MEASUREMENTS (harm: destruction acts uniformly noticed; oscillate:
twelve oscillations all convergent across thirteen stores; growth:
nothing grows -- the GeoPackage plateaus at working-set size), which
retires those directions for a while. FIVE DEFECTS CONFIRMED, every
one verified here by a route its hunt did not use before it was
fixed, and every fix carries a registered test and a proved entry.

| # | what a user lost | where it lived | found by / verified by | owes |
|---|---|---|---|---|
| 21 | Every style change answered by the LIVE restyle left the design preview one act behind the map -- the pick-time repaint read the layers before the restyle landed, and nothing repainted after. A person iterating judged their design by colours the map no longer draws. Shipping since 2026-08-17 | `_maybe_live_generate`'s restyled exit, which never had the restyle-then-refresh pair `_apply_style_change` has | the preview hunt (three faces, two routes incl. a pixel scan of the widget's own grab) / my call-spy route could not stage the act, so the proof is the entry | **FIXED** -- the gate refreshes after a successful restyle. Test + entry `the-preview-learns-what-the-restyle-painted` |
| 22 | A legend printing "0 - 0" for a class of REAL width on a column mixing magnitudes (1e-9 beside 1e9): a reader concludes the palest quarter of the map holds zero | label precision derived from the whole span over k, blind to mixed magnitudes; the 2026-08-10 fix covered uniformly tiny columns only | the classification-churn hunt (which also measured churn/revisit determinism CLEAN over ~52 settled generates) / verified bridge-direct | **FIXED** -- `_widen_degenerate_labels`, a post-classification pass taking precision from the narrowest class actually cut, QGIS's own formatter still writing every label. Test + entry |
| 23 | A scheme or class-count change made while the row displayed v2 released the copied ladder kept for v1 -- so the return to v1 drew re-derived breaks under the surviving copied colours, and the two elements the copy aligned no longer agreed | `_release_copied_breaks` iterated EVERY field where its sibling `_clear_quant_customization` scopes to the current one, with a comment saying why | the shelf hunt (class-count door) / verified through the STYLE door, reading the dict | **FIXED** -- the release is scoped to the field the act is about, falling back to the old whole-element behaviour only when the row cannot be found. Test drives BOTH arms (cross-field spares, same-field still releases) + entry |
| 24 | A pin (or hand-picked colours, or the ramp window) made on one style DIED AT THE PROJECT BOUNDARY the moment the row generated wearing another -- whole in the session that made it, gone from every .qgz and GeoPackage it was saved into, against the kept-silently ruling of 2026-08-20 | BOTH persistence writers read the mode-filtered `_assignments`: `_stamp_category_colours` stamped absence-by-mode as absence-by-choice, and `_capture_working_state` wrote the same silence into the group record | the shelf hunt (stamps, with a control leg separating stamp fault from adoption fault) / verified by comparing the two stores against each other | **FIXED** -- both writers take kept work from the session dicts, where absence is real; row 14's apply-side mode gate still guards clearing. Test asserts BOTH stores carry the pin + entry |
| 25 | One Reverse click silently destroyed a hand-picked No data colour, and reversing back did not return it -- against the rule that Reverse CARRIES the customization and twice is home | `_mirror_quant_customization` rebuilt the picks dict from digit keys alone, and since 2026-08-15 the catch-all and infinity picks live in that dict under non-digit keys | the editor hunt (record + the twin layer's renderer) / verified by driving the Reverse control against a seeded record | **FIXED** -- absence keys have no mirror image and simply come along. Test + entry |

**THE FOUR LEADS WERE PUT TO THE MAINTAINER AND ALL FOUR RULED,
2026-08-26.** Three became fixes the same hour, each with a test and
a proved entry; the fourth was judged and refuted.

| # | ruling and fix | owes |
|---|---|---|
| 26 | RECLASSIFYING SPARES THE ABSENCE COLOURS. A class-count, scheme or ramp act retires positional picks as it always has; the catch-all and infinity colours name kinds of absence a class count says nothing about, and survive. The notice counts only what died | **FIXED** -- `_clear_quant_customization` keeps non-digit keys. Test + entry |
| 27 | A GRADUATED COPY CARRIES THE CATCH-ALL, extending the categorical ruling of 2026-08-20: a copy reproduces the classification's whole appearance, the twin's colour included | **FIXED** -- the copy merges the source's non-digit keys. Test + entry |
| 28 | A RAMP TWIN'S NAME NEVER MOVES UNDER THE USER. The shipped palettes hold byte-identical pairs ('gray'/'gist_gray'); when a renderer's ramp matches several names equally, the tie goes to the name the row already holds | **FIXED** -- `_ramp_match` takes `prefer`, threaded from the three dock-handler callers. Test asserts the twins are still byte-identical (its own premise) + entry |
| -- | THE SILENT SOURCE-GONE REFUSAL IS REFUTED. Judged by re-driving the journey and reading every message surface: the sentence "That layer's data is no longer available, so the map cannot be updated" reaches the bar, and `_said_source_gone` is set. The census's silence was its own reading -- harness fault eleven of this record | closed |

## ROUND FIVE: eight hunts at the seams, six defects, all fixed

Aimed by round four's own lessons at the seams the notices cannot
reach: acts injected mid-run, two dialogs interleaved, oscillation
crossed with persistence, every notice tested as a claim, the
class-source family iterated, the no-data twin's transitions, the
region data edited between acts, and drawn pixels read against the
records beneath. Two directions came back clean with measurements
(the twin's transitions; nothing at the pixel level beyond one
finding). SIX DEFECTS, each verified here by a route its hunt did not
use -- and TWO of my verification probes drove the WRONG DOOR first
(a graduated copy call for a categorical claim; a direct internal
call for a journey the fix closes at the window), which re-verifying
through the right doors settled.

| # | what a user lost | where it lived | owes |
|---|---|---|---|
| 29 | A text value recoded in QGIS (fix a typo, rename a class) never reached the legend: the digest's terms were all numeric, so the signature said unchanged and the stale renderer painted the new value catch-all grey under a legend still listing the departed class. The numeric half was fixed 2026-08-15; its guard drove a numeric column | `_value_digest` -- finite numbers only | **FIXED** -- word frequencies join the digest. Test + entry |
| 30 | A group chosen while a tiling ran repointed the records the landing read, landing the run in a rival group whose saved record named a variable nobody chose; resuming it later silently redrew the map. The FILE's record was measured sound, which narrowed the fault to this door | `_on_group_chosen`, the one door into the group machinery without the in-flight guard `_bind_group_to_dataset` has carried since 2026-08-25 | **FIXED** -- refused in words, chooser re-synced. Test + entry |
| 31 | A categorical copy from a pick-less source left the target's old hand-picks painting the map -- and hand-picks outrank the copied ramp and template, so "the elements now match" was false, stamped into every file | `_write_categorical_style`'s `if picks:` -- writing only when the source HAD picks | **FIXED** -- an empty source clears, per the overwrite ruling. Test + entry |
| 32 | A colour editor left open when a second plugin window opened went on acting for the RETIRED dialog: picks landed in a record nobody persists, or repainted shared layers with the retired dialog's stale design, which the live dialog adopted as a dock edit | `_retire_previous_instance` never touched `_open_editor`; the reachable route into a retired dialog is its surviving children, not its widgets | **FIXED** -- retirement closes the editor. Test + entry |
| 33 | A re-tile whose element named a class source that had GONE re-seeded automatic colours over the QML's, unrecoverably, while the identical journey through the restyle path kept them -- sub-second timing deciding which map a user got | the landing had no unreadable-source arm; the restyle has carried one since 2026-08-13 | **FIXED** -- the landing keeps the worn renderer, seeding alone conditional. Test + entry (see the fixture note below) |
| 34 | The quant discard notice named no field where its categorical twin does; and "1 of the 5 classes are empty" | notice strings | **FIXED** -- symmetry and number agreement; through text review |

**BOTH RULING REQUESTS BELOW WERE GRILLED AND SETTLED THE SAME NIGHT
(rows 37-38); the paragraph is kept as the record of what was asked.**

| # | ruling and fix | owes |
|---|---|---|
| 37 | THE FILE SHOWS THE LIMIT OF WHAT IT CONTAINS; THE PROJECT CARRIES THE MEMORY HOME. The group record's elements gain a `kept` map (other fields' pins and hand-picks, per field, same dataset), riding the .qgz and never the GeoPackage -- stamps stay one-field because they embed into the file's styles, and the file's record is stripped through `_file_safe_state` at every write. Resume-without-project restores the displayed design only, correct under the principle | **FIXED** -- capture, whitelist and apply widened in one commit; two tests (the reopen restores, the file's bytes carry nothing unworn) + two entries, the strip's anchored at the helper after the ambiguous-anchor refusal caught a two-site anchor |
| 38 | THE SWITCH DOOR SPEAKS, in the deleted-column door's sentence family, naming the NEW layer; quiet when every column survives by name; recovery and group selection stay silent | **FIXED** -- snapshot-before/compare-after around the synchronous rebuild. Test drives both legs (speaks on loss, quiet on survival) + entry. The two sentences await text review |

**A LEAD FOR THE MAINTAINER, corroborated twice:** records kept for
fields NOT in force at a save die at every persistence boundary --
project reopen and GeoPackage resume alike -- while the same journey
without the crossing restores everything (oscpersist hunt, byte-grep
of the .qgz as the second route; the twin hunt logged the same shape
independently). Ruling 6 of 2026-08-21 called the scheme memory's
session scope "an implementation choice to revisit if a reopened
project turns out to need it" -- this is that case arriving. And the
notices hunt's main claim is also a RULING REQUEST: a dataset switch
re-points elements whose column the new data lacks IN SILENCE, where
the deleted-column door speaks; rulings describe the speaking door
without deciding the others.

**ONE ENTRY TOOK FOUR FIXTURES TO PROVE**, and the ladder of reasons
is the lesson: ramp defaults re-seeded are ramp defaults again (the
worn colours must DIFFER from a re-seed's answer); the reconciliation
drain adopts QML colours as hand-picks, which keep the map whatever
the arm does (stage the race's LOSING side by emptying the record);
and the unchanged-assignment carry keeps the old renderer until a
second control moves the STYLE signature (the hunt's own journey had
the opacity in it, and the fixture had dropped it as incidental).
Nothing in a hunt's reproduction is incidental until proven so.

## THE BRANCH WENT RED FOR THREE PUSHES, AND THE SUITE'S OWN RULE SAYS WHY

Every suite leg failed on `4f3c415`, `f1eba21` and `b913da8` -- five
tests, none of them in any subset I ran, found only when CI (the
whole-tree answer) was read. The standing rule is "do not mistake a
green subset for a green suite", and this is its sharpest instance
yet: three pushes of green-locally work carried the same five reds.

| # | what went wrong | owes |
|---|---|---|
| 35 | MY ROW-18 FIX WAS A REGRESSION AT THE ADOPTION DOORS. `_restore_the_adopted_design` applied the group record over what adoption had just recovered from the layers' own stamps -- and at the constructor there is no rival group to clear against, so the clear-on-silence that is right at the group CHOOSER (row 1) destroyed same-group truth: hostile-property adoption, an older plugin's project, an imported scheme and a dock recolour all lost their recovered work to a record that predated it. Four tests red on every platform | **FIXED** -- `keep_adopted` threaded through both appliers: at the adoption doors a silent key leaves the adopted value standing; the chooser door still clears. All four tests pass, and the row-18 guard still passes beside them |
| 36 | THE STATE MACHINE HELD THE RETIRED FORK-ON-ANY-TARGET-CHANGE CONTRACT. Its memory-to-file transition expected a third group, which the ruling of 2026-08-26 (replace in place -- the memory layers are this design's own draft) retired; the file-to-memory transition's count moved with it | **RE-DECIDED** in the test, ruling cited at both counts. File-to-different-file still forks, which the same test still drives |

THE LESSON, which the file already carries and this batch re-paid
for: a fix at one door is not verified until the OTHER doors' tests
run, and the only list of those tests is the whole suite. The rc's
gates are where that happens next.

## The four test faults, which are the morning's half

None of these is a product defect and all four were red or latent on a
tree that had passed everything. They are listed because the ledger's
job is what a day found, and because three of them share one cause.

| # | what was wrong | why it passed until now | owes |
|---|---|---|---|
| 7 | `test_the_spacing_box_at_its_extremes` asserted that the guard REFUSES at the bottom of the range. The maintainer's ruling of 2026-08-25 made it a question with a safe default | the suite's shim answers `question` with **Yes** unless a test stages otherwise, so the suite consented on the user's behalf and then waited on the tiling. At 3.6e19 tiles that collapses in under a second, which looks exactly like a guard working; at 36,086,505 it grinds, and only that case outlasted the ninety-second ceiling — on Linux and Windows, not on either Mac | **FIXED** — both small-spacing cases stage the safe answer and assert what the bottom of the range now promises: the question is asked, it says what it will cost, declining produces nothing and leaves Generate usable |
| 8 | `test_a_large_region_is_handled` phase 3 carried the same retired assumption, in its docstring and in its settle message | it was passing on the fast-collapse accident described above | **FIXED** — stages the safe answer; the docstring says what is still refused outright (`UNTILEABLE`, `UNCOUNTABLE`) |
| 9 | `test_a_class_count_at_either_end_of_its_range_reaches_the_map` held a `QSpinBox` across a rebuild and died on a deleted C++ object | the preview debounce became a FLOOR of 150 ms rather than a flat 350 on 2026-08-26. Measured: the rebuild costs 23 ms on that fixture, so the wait sits at its floor and the widget dies 181 ms after the spacing change — inside the test's 200 ms tick, where at 350 it fell outside | **FIXED** — the rebuild is allowed to land before any cell widget is captured. Not a product fault: a rebuild landing sooner lands FURTHER from a person's hand, since nobody reaches a table cell in a sixth of a second |
| 10 | the switch matrix's `mid-run/touched-scheme/generate` cell required a snapshot of dataset A's layers to survive a journey whose second act is A's own second run | it was green BECAUSE OF the defect this branch fixed that morning: the landing used to take its region stamp from the chooser, so an A run landing under a B chooser built a rival group instead of replacing anything. Driven stage by stage: the in-flight run replaces A's map in place, and the Generate on B then leaves all four of A's layers alone | **FIXED** — the set to protect is re-read after the in-flight landing, with the premise asserted so the refresh cannot hide a real loss. The repaired cell would go red under the old stamp behaviour, where the one it replaces went green |

## The harness faults, counted because a grid whose failures are its own is one nobody acts on

Five of mine in one evening, every one reading like a result:

- a probe with no message-box shim, which hung for fifteen minutes on
  a size question that can never be clicked;
- `estimate_tile_count` handed a `QgsVectorLayer` where it wants the
  GeoDataFrame the dialog has already built;
- two controls reached for by names they do not have (`new_group_check`
  for what is an entry in the group chooser, `n_spin` for a combo);
- and twice, a fixture that could not exhibit the case: a probe whose
  group had been stamped by its own landing moments earlier, so the
  branch under test had nothing to prove and the probe reported
  REFUTED.

The last is the one to remember, because it is the same trap this
file's own product-side lesson names, arriving inside the instrument.

## What the round says about hunting

EIGHT HUNTS, SEVEN WITH FINDINGS, FOUR DISTINCT DEFECTS. Three hunts
converged on defect 1 and two on defect 2, from directions chosen
before any of them had read the code — which is this method's
strongest signal and the reason the briefs name each hunt's siblings.

THE ONE THAT FOUND NOTHING EARNED ITS PLACE. It established that the
adaptive preview wait's widening is genuinely reachable from real
state (871 ms at 26 elements over a hundred columns, arming the timer
at its 350 ms ceiling; 232 ms arming it at 231), which was an open
question rather than a settled one — and it caught the false claim in
row 6 while doing it.

AND THE AIMING PAID AGAIN. Every defect here is in code written within
two days, and three are inside repairs made the same morning. Five
rounds, five times.

## ROUND NINE: the bulletproofing round, eight hunts and the sweep re-run

Launched on the maintainer's charter of 2026-08-26 (late): everything
done in the last three days BULLETPROOF, in two senses -- the repairs
sound in themselves, and the original issues resolved AS THE USER
EXPERIENCES THEM. Eight hunts (doors, kept, switchdoor, livepreview,
picksdict, seams, testbatch, harm) plus the consistency sweep and two
censuses re-run on `6d6ea2d`. FIFTEEN product defects, four dead test
axes and a shared-fixture fault, two ruling questions -- and the
charter's second sense answered directly: the harm hunt re-drove all
five original complaints (the colleague's A-B-C-A demo, reopen keeps
the design, one session one group, resume completeness, the switch
door speaking) and measured every one resolved end-to-end.

Every claim below was verified here by a route its hunt did not use
-- `judge_*.py` and `verify_*.py` in the session scratchpad, plus the
regression test watched failing where noted -- before its fix landed.

| # | what a user lost | where it lived | owes |
|---|---|---|---|
| 39 | A cross-field graduated copy dropped the catch-all -- the ordinary case, since the chooser offers every other element -- or imported a stale kept colour nobody chose, painted onto the twin and stamped | `_copy_onto_one`'s row-27 merge read the source's absence picks under the TARGET's field; the categorical twin and the pin read three lines up both use the source's own | **FIXED** -- read under `source.get("var")`. Cross-field arm added to the catch-all test + entry |
| 40 | A manual Generate answered by the restyle fast path left the design preview resting on the previous act's colours (live OFF has no pick-time repaint at all) | `_generate`'s restyle exit never had the restyle-then-refresh pair | **FIXED** + test cell + entry |
| 41 | The same, at the live path's source-gone restyle exit, four lines from row 21's fix | `_maybe_live_generate`'s source-gone arm | **FIXED** + test cell + entry |
| 42 | A dock recolour adopted everywhere except the GROUP record, so a reopen restored the pre-edit colour and the next Generate painted the map backwards. Also the mechanism under the sweep's open reopen claim | dock adoption re-stamped layer and file; no writer re-stamped the group | **FIXED** -- `_queue_group_restamp`, coalesced, from `_stamp_category_colours`; guarded against in-flight runs, group selection, restyles and retired dialogs. Test + entry |
| 43 | A resume during a run repointed the records under the landing; one Generate then rewrote the saved file with the other dataset's tiling | `_resume_from_gpkg` was the one group-machinery door without row 30's in-flight guard | **FIXED** -- refused in words (sentence through text review). Test + entry |
| 44 | An at-rest resume was snatched back to the current dataset's group by its own tail -- exclusions churn after the window closed -- with the resumed group unclaimed and stamped as the wrong dataset | the `_selecting_a_group` window closed before the tail; the stamp captured the live chooser | **FIXED** -- the whole resume (recovery included) runs inside the window, and the stamp carries the FILE's region. Test + entries (both branches' shapes) |
| 45 | The recovery SPOKE: resuming A's file under dataset B cleared the output path out loud and announced a re-point of an intermediate table -- two spurious sentences on a journey ruling 38 keeps silent | `_recover_the_source` ran before the window opened | **FIXED** by row 44's window. The same test asserts the silence |
| 46 | `_project_is_being_replaced` leaked True through the adopt path's early return (and File > New never reaches adoption at all): banks inert, identity unbound, ruling 8's leak resurrected | the groupless early return skipped the clear its comment promises | **FIXED** -- the return clears, and a queued zero-delay clear backstops File > New. Test drives both shapes + two entries |
| 47 | A pick-less copy left the target governed by its imported class source while claiming the elements now match | `_write_categorical_style` only ever WROTE the token | **FIXED** -- a source with no class source clears the target's. Test + entry |
| 48 | The landing's re-examination adopted the unreadable-source arm's kept renderer as hand-picks and stamped them -- a template's colours became picks that outrank the template forever | the categorical adoption walk attributed by DELTA (adopt whatever differs from what the plugin would seed NOW), and a kept renderer over an unreadable source makes that delta lie | **FIXED** -- by ATTRIBUTION: `_painted_categories` records what the plugin itself painted, mirroring `_painted_ladders` (the settled 2026-08-20 design at its categorical twin), and the walk declines a colour the plugin painted or an element never seen. THREE narrower fixes preceded it -- a re-exam skip, a replay skip, a carry snapshot -- and the reworked test caught a fresh route past each, which is the four-fixture ladder arriving in a FIX: when the third narrow guard leaks, the question is not which route is missing but whether the walk should be asking delta at all. Test (file-token fixture; the layer-token draft could not exhibit its case) + entry aimed at the attribution condition |
| 49 | Opening the plugin again during the landing phase let the retired window's landing complete: live layers removed, a rival group built -- rows 18/19 broken at a fifth door | `_on_generated` had no retired-dialog gate; retirement cancels a task that has already reported | **FIXED** -- a retired dialog's result is discarded whole. Test + entry |
| 50 | A dropped element table's whole saved style survived in the GeoPackage -- the unworn field's name, pins and hand-picks, QML and SLD, surviving a WAL checkpoint -- against the file-limit ruling, through a door `_file_safe_state` cannot see | `bridge.drop_gpkg_layer` deleted the table; QGIS's `layer_styles` row is not GDAL's to manage | **FIXED** -- the drop deletes the style rows too. Byte-level test + entry |
| 51 | Parking a row on "---" and saving killed every pin and hand-pick it ever held, while the session dicts held everything | `_capture_working_state` skipped var-less elements whole | **FIXED** -- a parked element's kept map files every field. Test + entry |
| 52 | The switch notice covered an element left with NOTHING using a sentence about its neighbours; and the re-point path stranded rows on "---" that the deleted-column twin would have landed | the flattened gone/landed sets, and `preferred` missing the twin's `or fields` fallback | **FIXED** -- fallback aligned; each element gets the sentence true of it (both approved templates, unchanged wording). Test drives both legs + two entries |
| 53 | A variable picked and switched away from inside the live debounce was silently reverted on the return -- the notice's own sentence had just testified to the choice | the group record is stamped at landings only; the pick had no landing | **FIXED** -- leaving a built dataset stamps what you leave, region overridden to the OUTGOING dataset's (the two-writers rule) . Test + entry |

**THE TEST BATCH'S OWN FAULTS (the per-assertion hunt), all fixed:**
the retirement-editor test's closing axis was dead (`isVisible() or
True`, and a window the shim never showed) -- the editor is shown
now and both axes bite; the unreadable-source test intersected
before with after and passed on TOTAL loss -- `assert after` joined
it; `_a_disk_session` never asserted its landing, so a file that was
never written turned its dependants into tests of the memory draft
-- the helper asserts existence, size and `_reads_from` now; and the
group-choice test's substring match picked the CURRENT group, so its
deed axis could never fire -- exact-match on the label's name part,
with the staged-rival premise asserted. Plus the capture-side pin
axis flagged as entry-less: `the-capture-backfills-the-pin` guards it.

**THE SWEEP RE-RUN'S VERDICTS** are recorded above ("THE SWEEP WAS
RE-RUN ON 6d6ea2d"); its open reopen claim is CLOSED by row 42's
mechanism -- the sweep's session dock-edited before crossing, which
is precisely the journey row 42 repairs.

**JUDGED AND ACCEPTED, with reasons, rather than fixed:**
- the 2-layer removal's two sentences can arrive re-point-first: the
  combo's own emission precedes `layersRemoved`, so curing a
  one-beat legibility wart means re-plumbing signal order -- a new
  race surface on ground rows 29-33 just paid for. Both sentences
  are true and adjacent; put to the maintainer with the rulings.
- a consented design-floor recompose drops elements between the
  switch notice's snapshot and compare, so their columns' losses go
  unsaid -- by the ruling's own carve-out the recompose IS the
  user's explicit consent to exactly that loss, asked in a modal
  that names the element count.
- the "rows left '---' by a numeric-less first landing read as
  deliberate" lead is retired by row 52's fallback: rows land a
  field whenever one exists, and the zero-field build already
  carries `_fieldless_build`.

**FOR THE MAINTAINER, corroborated and unruled:**
1. A-B-A WITHOUT A GENERATE loses a hand-chosen variable that the
   same journey WITH a landed group restores (harm hunt, p5): the
   remembered-field branch is gated on `column_gone`, so when
   datasets share column names, keep-by-name keeps the RETURN leg's
   current field -- the composition of rulings 6 and 7 -- while
   ruling 4's group restore brings the choice back. Two settled
   rules, two answers, chosen by whether a run landed.
2. THE RECLASSIFY RULE DEFEATS KEPT-SILENTLY for quant picks on
   every A-B-A field journey (kept hunt, p1f/1g): the per-element
   mode memory forces a mode re-click on return, and the re-click
   clears positional picks by the 2026-08-09 rule -- session and
   reopen behave identically (parity measured), so this is the two
   rules colliding, not a persistence defect.
3. Mid-session, an un-checkpointed WAL can still carry the PREVIOUS
   file record's bytes (gone after checkpoint) -- SQLite lifecycle;
   at most a maintainer-facing line somewhere.

## ROW 59: THE EIGHTH SITE, AND WHAT THE FIRST SWEEP DID NOT LOOK AT

| # | what a user lost | where it lived | owes |
|---|---|---|---|
| 59 | On Windows, every reopened project dropped the pins and hand-picked colours its own record was carrying home for fields the map does not display -- ruling 8's protection refusing the very dataset it exists to protect. Three of the four Windows-only reds on `dd717e0` turned on it | `_apply_element_records`'s `same_data`, comparing the live layer's `source()` with the region a SAVED RECORD names, as raw strings. The seven sites mended with `same_source` that morning were all stamp-against-stamp; this one is stamp-against-live and a keyword sweep for the first shape does not turn it up | **FIXED** -- through `same_source`. Guarded HERE rather than on Windows: the symlink test gained a leg that hands the restore a record spelt the other way and requires the pin to survive. Test leg + entry |

**WHY IT SURVIVED THE SWEEP THAT FIXED THE OTHER SEVEN**, which is the
transferable part. The first sweep looked for the SHAPE it had just
found -- a stamp compared with a stamp -- and this site compares a
stamp with the source of the layer in force. Same question, same
boundary, different pair of operands, so it matched no search.
WHEN A FAMILY IS FOUND, ENUMERATE THE QUESTION RATHER THAN THE
PHRASING: every place that asks "are these the same dataset?" belongs
in the sweep, whichever two things it happens to be holding.

**AND TWO OF THE FOUR WINDOWS REDS WERE THE HARNESS, not the plugin.**
One test asked whether a resumed group's record named the file's own
dataset with `region in str(record)` -- a substring comparison of a
path inside JSON, which is the very defect the test was written to
guard, reproduced inside its own oracle. It asks through `same_source`
now. The other moved a GeoPackage out from under an open layer, which
Windows does not permit; it announces the missing door through
`_skip_loudly` and says which half ran, joining the four tests that
already do.

## ROWS 55-58: FOUR REGRESSIONS ROUND NINE SHIPPED, AND WHAT FOUND THEM

**THE FINDING BEFORE THE ROWS.** Round nine was verified by TARGETED
RUNS -- every fix's own test, every neighbour anybody thought of -- and
it shipped four regressions, three of them in one mechanism. What
found them was the mutation workflow's coverage leg, which runs the
WHOLE suite: `64cb0fa` red where `6d6ea2d` was green, four named
tests, reproduced here on the first attempt. Every one is a journey a
person takes. This project already says that a green subset is not a
green suite and that the candidate is where a core-path change gets
verified; what this adds is the cost of believing a subset TWICE IN
ONE DAY, since the same reading is what left the four alive.

| # | what a user lost | where it lived | owes |
|---|---|---|---|
| 55 | Move the design controls after a map lands -- a different family, a different element count, a spacing you have not generated yet -- and one adopted dock edit was enough for the GROUP to claim that design as its own. Choosing that group afterwards restored a design its layers were never drawn at, and the record went into the .qgz | `_stamp_working_state` takes the design, the output path and the region from the LAUNCH SNAPSHOT when it is handed one, and re-derives them from the live controls when it is not. Round nine added two writers that are never at a landing -- the switch-out stamp and the queued restamp -- so both wrote whatever the controls happened to say | **FIXED** -- the three map-shaped keys are CARRIED from the record already on the group; only a landing may move them. Test `an output group carries the whole working state` (the leg that reopens the project) |
| 56 | Open the plugin in a project that already holds a map -- the commonest journey there is -- and every row came up blank, on "Single colour", beside layers plainly drawn from a column. Generate then refused for want of a variable, saying nothing about why | the same switch-out stamp, running in a dialog the user had CLOSED, when a layer arrives in the project and the combo re-emits. Its own dataset had been removed, so the table in front of it was blank BY IMPOSITION -- and it wrote that blank over the group's perfectly good record | **FIXED** -- a dataset that has gone leaves nothing to stamp. Measured to the act: the record was intact through the close and the removal and blank the moment another layer arrived. Test `the layer changes without being edited` |
| 57 | A run on one dataset landing while the chooser is moved away and back filed the map under the OTHER dataset -- the single fact the landing's refusal to overwrite and the group binding both read, so the next run on that other dataset was free to replace a map it had never made | the switch-out stamp again: it stamps `_group_of_our_layers`, which answers "where this dialog's layers are" -- the group the last run LANDED in, not the group of the dataset being left | **FIXED** -- the group must be that dataset's own map, asked of the LAYERS' stamps, which is the authority for this question everywhere else here. Test `a map is filed under the dataset it was drawn from` |
| 58 | An element whose class-source file goes missing kept the colours on screen and recorded NONE of them, so the first Generate after a reopen re-seeded it from nothing and the map silently repainted | round nine's row 48 replaced a delta-based adoption walk with attribution (`_painted_categories`), correctly -- and the walk had been doing a second job nobody had written down: it was what put the kept colours into the element's record. The cure removed the job with the fault | **FIXED** by a maintainer's ruling (below), which settled the two halves together. Test `a kept scheme is held rather than owned` + three entries |

**ROW 58 IS A COLLISION, NOT A SLIP, and that is why it went to the
maintainer.** Two registered tests had come to demand opposite things
of one journey: the older one required the element to OWN the colours
of a file that has gone ("a missing file is a reason to stop
consulting the file, not a reason to repaint somebody's map"), and
round nine's required the record to stay EMPTY, because colours that
outrank a template mean restoring an edited scheme changes nothing.
Both harms are real and both were measured. The ruling of 2026-08-26
takes both: the colours are RECORDED, so they survive the next run,
restyle and reopen; they are recorded as HELD rather than as picks,
in a shadow record that travels in the layer's own stamp; and the
moment the file can be read again it takes them back, before anything
is seeded from it. Three entries, one per half plus the stamp that
carries the distinction across a project boundary.

WHAT THE COLLISION COST IS THE PART TO REMEMBER: round nine's test was
written and proved while the test it contradicted was never run.
Neither the author nor the catalogue can see that; only the whole
suite can.

## ROW 54: THE WINDOWS RED, SIX CI ROUNDS, DIAGNOSED AND FIXED

| # | what a user lost | where it lived | owes |
|---|---|---|---|
| 54 | On Windows, a reopened project whose output GeoPackage had moved could not be rebuilt: re-point the output at its own path, press Generate, and there is no file, no layers and NO MESSAGE. The same comparison decides which group a dataset owns, whether a landing may write over a group, and whether a resume finds a layer already open, so every group-unit ruling was affected on the platform most of this plugin's users are on | a layer source is a PATH plus a provider tail (`\|layername=`), and a project save respells the path half -- the plugin stamps `weavingspace_region` with backslashes, QGIS reads the layer back with forward slashes. SEVEN sites compared the two AS RAW STRINGS, so a project's own group read as another dataset's: `keeping` turned True and the keep-the-previous-result guard refused through a QMessageBox | **FIXED** -- `same_source`, the sibling of `same_destination`: the file half goes through it (samefile first, so the volume decides about case and Windows short names resolve), the tail is compared case-folded. All seven sites route through it. Test + entry |

**HOW WE KNOW, because the route matters more than the answer.** The
red had survived five rounds of being looked at, and what broke it
was instrumentation rather than insight, in three steps:

1. THE FAILURE MESSAGE WAS MADE TO SAY WHAT IT FOUND -- the file's
   existence and size at the path, the sqlite side files, every
   layer's validity and source, the chooser, the dialog's own
   records. Round one of the probe then reported the decisive fact:
   the region layer is VALID and the recovery run leaves NO MESSAGE.
2. AN EARLIER READING WAS WRONG AND SAYING SO IS WHAT MOVED IT. The
   "region layer was removed" warnings in that output were read here
   as the refusal; they are the fixture's own `project.clear()`,
   since BAR_MESSAGES accumulates across a test. Correcting that
   left the real question: which of `_generate`'s EIGHT exits took
   the run, none of which named itself.
3. EVERY EXIT WAS MADE TO NAME ITSELF, as live update's ten gates
   have since two diagnoses were lost to that silence, and the
   keep-the-result guard was made to print its whole decision. Round
   two answered in one line:
   `GEN-GATE would-overwrite-a-kept-result keeping=True
   would_replace=[] same_destination=True
   path_now=C:\WORKSP~1\...\map.gpkg
   last_path=C:/workspace/.../map.gpkg`
   -- two spellings of one path, side by side, and the modal store
   carrying the refusal the message bar never saw.

**AND THE FIX IS GUARDED WITHOUT WINDOWS.** A symlink gives one file
two names on any platform, so
`test_one_dataset_spelt_two_ways_is_one_dataset` stages the exact
condition here: it asserts `samefile` first (a filesystem that
cannot make the link skips loudly rather than passing vacuously),
checks the comparison both ways and against a genuinely different
dataset, then re-stamps a real group with the other spelling and
requires the next Generate to replace its own map in place rather
than refusing. Its entry mutates `same_source` back to `==`.

**AND ONE ENTRY WENT FROM CAUGHT TO SURVIVED UNDER THIS FIX, WHICH IS
OPEN AND MUST NOT BE READ AS SETTLED.**
`a-landing-never-writes-over-another-datasets-map` mutates `theirs`
to False -- "write into whatever group is there" -- and its test now
passes with that mutation applied, where it caught before
`same_source` landed. Nothing about the fix should weaken that
protection: in that test the two datasets are genuinely different, so
`same_source` answers False for every stamp and `theirs` is True
exactly as `!=` made it.

THE HYPOTHESIS, STATED AS ONE: `same_source` also owns the question
of which group a dataset OWNS, so the landing's candidate group may
now resolve to the dataset's own group and make `theirs` redundant --
the "behaviour held by two implementations" shape this project knows,
in which case the honest record is a note at the test and no entry.
IT HAS NOT BEEN MEASURED. Do that before believing it: break every
route at once and see whether the test fails. If it does, the axis is
live and redundantly held; if it does not, the assertion is the
problem and the protection needs re-proving. Until then this is an
open question, not a cleared one.

**THE FAMILY THIS JOINS.** CLAUDE.md already carries "identifiers
that differ only in case" and `same_destination`'s own account of
Windows short names. What is new is that the PROVIDER TAIL made
people reach for `==` again: a source looks like an opaque token
rather than a path, so the rule about paths was not applied to it.
When a string carries a path INSIDE it, it is a path.

**THE ENTRY PROOFS TOOK LADDERS OF THEIR OWN**, recorded because
the pattern generalises: 20 of 21 entries prove `caught`; one
(`a-groupless-adoption-drops-the-marker`) is RETIRED with its note --
the early-return clear is held redundantly by the queued zero-delay
clear on its only journey, and breaking BOTH fails the test at once
(proved by hand). Two anchors initially failed on a single backslash
becoming a line continuation inside the entry string (doubled now);
two tests needed their fixtures rebuilt to reach their cases (an
output path for the clearing to speak about; a landed map for the
rival to threaten); and one test's first fixture used a `layer:`
class-source token, which vanishes from the combo WITH its layer, so
the arm under test never fired -- the file-token fixture is the one
that can move.

**HARNESS FAULTS OF MINE THIS ROUND**, tallied as ever: the copy
probe called a two-argument method with a list and read the refusal
as the defect (premise assertion caught it); a preview probe grabbed
a never-shown dialog (the documented trap, met again) and then
matched the map's mid-class colour against a preview fed
`_table_id_colours`; the first cell-claim probe used a memory region
across a .qgz (the suite's own documented zero-features trap); and
the four_corners premise fault returned in subtler clothes --
max-preserving multiplication -- now asserted on the quantity the
classifier reads.
