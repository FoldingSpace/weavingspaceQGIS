# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

6 of 334 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Documents

The README, the project page and the user guide, one paragraph at a time. Edit inside the fences and leave the fence lines themselves alone. Markup is shown as it is written, so change the words and leave the tags where they are.

**`docs/USER-GUIDE.md:266`**

<!-- id:68231933304d224b -->
````
Drawing a map and saving one are separate acts. Nothing is written until
you press *Save*, which puts every element layer into a single file
with its symbology embedded: one `.gpkg` that a colleague can drop
into QGIS and see your elements as you styled them. The region
outlines stay in the project, since they are drawn from your own
layer, and until you press Save the map lives only with the project.
Nothing is opened until you press *Load*, on the row beneath, which
brings a saved map back into the dialog so you can carry on with it.
Save prompts you to confirm if the file selected holds a
map made from unrelated data. If somebody else has changed that file
since you opened the map, Save writes what it can, leaves their work
where it is, and tells you which element the file no longer holds.
````

**`weavingspace_qgis/metadata.txt:39`**

<!-- id:dc5839eb2565be3e -->
````
; Shown in the QGIS plugin manager. Keep the newest version first, and
; describe what a USER would notice rather than what changed in the code.
;
; EVERY VERSION OPENS WITH ONE SENTENCE saying what the release is
; FOR, and the detail that follows is GROUPED by category. Somebody deciding whether to upgrade
; can stop after the first sentence; somebody who has upgraded and
; wants to know what moved reads on. One text serving both is how a
; changelog becomes a list nobody finishes.
; (Maintainer's instruction, 2026-08-13.)
changelog=0.24.4 Saving a map is now something you do rather than
    something that happens to you, and a map you have saved can be
    opened again and carried on with.
    Saving: choosing a file says where a map would go and writes
    nothing. Nothing is written until you press Save, which puts every
    element layer into one GeoPackage with its symbology embedded,
    tidies away tables the design no longer has, and records the
    design so the file can be opened on its own later. Press Load on
    the row beneath to open one back. Generate draws the map; live
    update never writes at all, so a file you chose for later is
    still yours until you say otherwise. Save asks first where the
    file holds data from another map, and unticking Include the
    source data takes that copy out of the file rather than leaving
    it there.
    Designs: a tiling can now carry more than twenty-six elements,
    running a to z and then aa to zz, and every list you meet reads
    them in that order. A weave keeps twenty-six, its strands being
    named one character each. Select all beside the variable column
    assigns every element in one press. A column of numbers that
    arrived as text, through a spreadsheet or a join, can now be
    classed as numbers rather than getting one colour per value.
    What it puts right: an element taking its classes from another
    element followed it a run late, so the two drew one column in two
    sets of colours until you pressed Generate again. Clearing the
    output box quietly started a second map. A ramp tried on a row
    and taken off again came back on the next change of style. A
    plugin you had disabled went on running until QGIS restarted,
    writing into your project and posting messages about a window you
    could no longer open.
    A layer duplicated in the panel could be replaced instead of the
    original, and a copy of one was left standing over the new map
    after a re-tile, never updated again and with nothing said. And a
    map opened from a file could be lost by saving it.
    Sharing: with two maps open that two people had sent you, going
    back to the first re-tiled it from the second one's data, into
    the first one's file. Saving into a file somebody else had
    changed since you opened it could take an element out of it and
    say only that it had saved; it now writes what it can, leaves
    their work where it is, and tells you which element the file no
    longer holds.
    Also: with Create as new group ticked, the group chooser named
    the group the next map would not land in. A Save pressed while
    the map was about to be redrawn saved the map you had just
    changed away from, and is now kept and done once the new one has
    landed. Assignment table columns cut their text short at ordinary
    desktop font sizes. Saving a large map left the window looking as
    though it had hung. And the plugin could close QGIS outright
    after being opened several times in one session.
    0.24.3 You decide how your data is classed: which values are
    drawn at all, where the first and last classes end, and what
    becomes of the areas left out.
    The range drawn: an element's ladder has four ends you can set
    rather than two. The lowest and the highest values drawn say where
    the ramp starts and stops, so two elements given the same pair
    share a scale, and under Equal intervals or Unclassed they are cut
    into exactly the same classes. Bring either end inside your own
    data and the areas beyond it stop being classed: they are drawn in
    a colour you choose, with their own line in the legend, rather
    than left as gaps in the pattern. Leaving areas out changes which
    tiles the map holds rather than only their colour, so the map
    catches up at the next Generate and you are told so. An element
    set to Unclassed names where its ramp starts and ends in a strip
    above the class list.
    Missing values: an area whose value is absent used to come out as
    a hole in the map. So did one holding an infinity, which a
    classifier can no more place than a blank. Those areas are drawn
    now, and told apart: no data, infinity, negative infinity and the
    values your own range leaves out each get their own colour and
    their own line in the legend, and each sits in that element's
    colour editor to be changed. QGIS gains a second layer beside the
    element carrying those areas, while the dialog still shows one
    element.
    Class bounds: the first class and the last have editable boxes,
    and the classes between them have none, their breaks always being
    worked out for you. Move a bound off the value the classification
    arrived at and the relevant class break(s) are adjusted; a heavy
    outline round the box says which of the numbers in front of you
    were set by you rather than by the plugin. A cross inside the box
    returns the bound to plugin control, and typing the automatically
    computed break value in again does the same.
    Bounds that cannot be drawn are refused and the reason said,
    rather than quietly turned into different numbers.
    Copying: send one element's classes, colours, bounds and class
    count to as many other elements as you like, from the same window.
    Tick them and press Copy. The range travels with the rest, so
    holding several variables to one scale is one act rather than one
    trip through the window each. Where two elements have different
    attribute columns the ends are fitted to the receiving data, and
    any class its values cannot reach is kept rather than dropped, so
    the classification remains relatively intact. A bound the receiving
    attribute column cannot reach is left behind rather than carried,
    and said; an element
    holding no values at all inside the range is left out, and named,
    since the copy would leave it nothing to draw.
    Colour: an element is now classed from the whole map's values
    rather than from its own tiles alone, so two elements you have set
    up alike can be read against each other. They could not before:
    the same shade stood for different numbers in different parts of
    the pattern.
    Classes: a column with fewer values than the classes you asked
    for keeps every class you asked for, and each keeps the colour of
    its place on the ramp, so adding data later does not repaint the
    classes you already had. The classes left empty are counted in a
    notice. An element set to Unclassed says nothing
    about this, its fifty steps never having been a count you chose.
    Design: the scale factors take negative values, which mirror the
    design about that axis and scale it by the size of the number.
    The up and down controls step through zero rather than stopping
    there. Rotation turns a full circle either way rather than
    stopping at a quarter turn, so half a turn is one number rather
    than a puzzle.
    Maps that were refused to be drawn: the check on how many tiles
    a spacing asks for measured the box around your areas rather
    than the areas themselves, so a region that does not fill its
    own rectangle was told it had asked for far more than it had.
    Coastlines, islands, catchments and any scatter of places are
    all that shape. On one dataset of three thousand areas the count
    came out eight times the truth and the map was refused outright
    at a spacing that draws it in a few seconds. The check counts
    the ground you are tiling now.
    Icons: a design drawing one tile unit on each of your areas is no
    longer refused as though it were a tiling of many thousands, and
    live update no longer stops following you for the same reason.
    Both checks had counted what a tiling would draw, so a hundred
    icons came out as two hundred thousand tiles, and the advice to
    try a coarser spacing was wrong besides: spacing decides how large
    an icon is drawn rather than how many there are. Where areas of
    very different size sit side by side an icon can be drawn from a
    neighbouring area's value rather than its own, and you are told
    that; it used to be reported as areas missing from the map, which
    sent you looking for holes that were not there. You are also told
    when an element has no icon for some of your areas, and which
    elements those are. Nothing was said before, because the other
    elements still drew those areas.
    Speed: the optional warning about colours a reader may not be
    able to separate used to hold QGIS still for half a minute on a
    map of many classes. It answers in under two seconds.
    Changing your data: pointing the region chooser at a different
    dataset now starts the styling fresh where the content justifies
    it. Each element keeps its variable and its style where the new
    data has a column of that name, and otherwise takes a sensible
    default for a column the new data does hold; the scheme, ramp,
    class count and Reverse reset together, while the element's
    opacity stays. No styling is permanently lost: what a switch in
    dataset puts aside comes back when you return to that dataset.
    The saved-file path is cleared so the map you saved
    from the previous dataset keeps its file, and the next dataset
    appears in its own QGIS layer group rather than replacing the
    last one. A dataset with fewer usable columns than the design
    has elements asks before the design is changed, and a kept
    categorical scheme meeting more than a hundred distinct values
    asks before drawing a colour for each.
    Changing dataset: point the plugin at another region layer and any
    element whose column the new data does not have is re-pointed to one
    it does. That was already true and it happened in silence. The
    plugin now says which elements moved and what they show instead,
    naming the layer you have just chosen, in the same words it uses
    when a column is deleted in QGIS. An ordinary switch, where every
    column survives by name, stays quiet.
    Switching variables: an element's style now belongs to the column it
    draws rather than to the element. Send a variable away and bring it
    back and it returns with the style it had before, its class colours
    and pinned bounds where you left them. You are no longer obliged to
    re-click the style chooser, which counts as a fresh classification
    and discards the colours you picked by hand. Where you chose a
    variable and then changed dataset before generating, coming back
    restores your choice rather than a column the plugin picked for you.
    Reopening a project: an element whose ramp you reversed comes
    back reversed, naming the ramp you chose rather than reading as
    custom colours. A class you recoloured in QGIS's Layer Styling
    panel comes back too, where the next Generate used to paint over
    it, and so does a ladder you retyped there.
    Your files: asking for a result in a new group no longer rewrites
    the output .gpkg from the beginning, which had been losing the
    tables your earlier runs wrote into it.
    Reloading the plugin: reloading it and then restyling one of its
    layers in QGIS could bring QGIS down. It cannot now.
    Another project: opening one no longer carries the last project's
    per-element colours, class counts and bounds into it. Element
    names repeat between projects, so those settings could land on
    data they were never chosen for. The plugin takes over that
    project's own group of layers, so the next Generate rebuilds it
    where it stands rather than adding a second one beside it.
    Opening the plugin first: you can open the dialog before you load
    your data. The variables appear as soon as the layer does. Until
    now that order left the Variable list empty and Generate saying
    you had not assigned anything.
    Styling in QGIS: restyle an element in the Layer Styling panel
    and the plugin's table now follows it. Retype the class breaks,
    or copy a style from one element layer and paste it onto
    another, and the row comes to read the variable, the style, the
    number of classes and the ramp that QGIS is holding. The
    boundaries you typed are kept as your own, the two outer ones
    included, so the next Generate draws your ladder rather than
    working one out again. Until now the row went on describing the
    map the plugin had last drawn, and the next Generate replaced
    your work with it. Where the
    renderer is something the dialog cannot describe at all, the
    Style reads "Deferring to QGIS" instead of naming a style that
    no longer decides the map: the renderer survives Generate, the
    swatch shows the colours QGIS is actually drawing, and the
    controls that would overwrite it are greyed until you take the
    element back by choosing a style again. A single colour you
    mixed there is left exactly as you set it, as before.
    Losing the region layer: removing it from a project that holds
    several other layers is now noticed. Before, the dialog went on
    pointing at a layer that had gone, said nothing, and Generate
    did nothing at all.
    Numbers you type: several boxes changed what you typed as you
    typed it. A class bound wider than the element's own data was cut
    down to fit, so typing 1200 gave you 120 and the map was drawn
    from that, and a fine bound on a column of large numbers was
    rounded away. Rotate, Skew and the two angle boxes took no
    decimals at all, so 22.5 degrees became 22. A scale between minus
    one and one was reduced as you typed, so -0.5 quietly un-mirrored
    a design at a size that looked right. The two boxes of the Ramp
    Display Range each clamped the other, so from a window of 0 to
    40, typing 60 kept the 6. Each of those boxes now keeps what you
    type.
    In another language: on a computer that writes decimals with a
    comma, three of the plugin's own sentences printed numbers its
    own boxes then read differently. Following the spacing it advised
    drew a map ten times coarser; copying a class bound it printed
    pinned a number a thousand times out, and that number was
    accepted and saved. Those sentences now punctuate a number the
    way your computer punctuates it.
    Work you have set: a style pasted onto an element while a map is
    still drawing is no longer destroyed as that map lands, and
    neither is a classification the instant you copy it. Restyle an
    element in QGIS and its pinned bounds, its hand-picked colours,
    its opacity and its missing-value layer's own styling now stay
    with it; they used to leave the saved project while the window
    went on showing them. A class count too small to carry the bounds
    you pinned is refused, with the reason, rather than destroying
    them. Where such a count comes back from a reopened project it is
    raised to fit and you are told.
    What a colleague opens: a class break retyped in QGIS, a ramp
    changed there, a stroke, a legend label or a deleted category now
    reach the exported GeoPackage as you make the edit. They reached
    the map, the project and QGIS itself before, and stopped at the
    file, where Generate could not heal them. It was hard to notice,
    because the map was right.
    Class style files: an element coloured from a QML that later moves
    or is deleted keeps the colours it is drawing, and keeps them
    through a save and a reopen rather than only until the next
    Generate. Put the file back and it governs again, so an edited
    scheme reaches the map.
    The design view: the swatches in the data table are drawn from
    the colours the map is drawing. They used to be drawn from the
    ramp the row named, so an element restyled in QGIS, one whose
    colours you picked by hand, one with its display range narrowed
    and one with Reverse ticked were all previewed in colours the map
    did not hold. The table is where you judge whether your elements
    read as distinct from one another, and that judgement was being
    made from the wrong picture.
    Being told: a class no tile falls into is reported in words, and
    counted on the ladder actually drawn. An element set to Unclassed
    is no longer told that its fifty steps are empty. Drawing without
    classes is what that style is for. Where two elements each carry
    a column, both are now told about their own legend rather than
    the first silencing the second, and a column holding a single
    value says so whenever you change the class count, not only on
    the first Generate.
    Working on one map at a time: the dialog now names which map a
    run will land in. A chooser beside the region layer lists the
    groups of tiles you have made, each with the dataset it came
    from, and choosing one brings that map's whole design back --
    family, spacing, modifiers, and every element's variable, style,
    ramp, class count and colours. Choosing a dataset selects its own
    most recent map, and choosing a map selects the dataset it was
    made from, so the two always describe the same thing. Create new
    starts another beside it. Until now none of this was on screen:
    which group a run replaced was worked out by a rule you could
    neither see nor overrule, and coming back to a dataset gave you
    its colours with somebody else's design.
    Carrying on later: a map saved to a GeoPackage now remembers how
    it was made. Open a saved map and the plugin brings back its
    layers and its whole design, so you can pick the work up without
    the project you made it in. Where the data it was drawn from is
    still on your computer it is found again; tick Include the source
    data if you are sending the file to somebody who does not have
    it. A map saved by a newer version of the plugin is refused with
    the reason said, rather than half read.
    Smaller, clearer files: each element layer now carries the one
    variable it displays rather than every variable on the map, and
    its table inside the GeoPackage is named for that variable. One
    shared dataset of 800 KB had been arriving as 19 MB, and a file
    you sent on carried columns your map never drew.
    Large maps: a spacing that asks for a great many tiles is now a
    question rather than a refusal. Past a certain size the question
    says so in stronger terms, since a map that large may use all the
    memory on your computer and leave QGIS unresponsive while it is
    drawn, and it still names a spacing that would work. The decision
    is yours. What is still refused is a design that does not repeat
    across the plane, and a layer whose extent cannot be measured --
    neither of which any spacing would help, and both of which used
    to be reported as though they were about spacing.
    The pattern library: updated, bringing the tiling and weave
    families up to date with the work behind them.
    0.24.2 Bugfixes, one of them about not losing data.
    Your files: an existing .gpkg chosen as the output was rewritten
    whole, so every other table in it was lost, including the layer
    you were tiling if it lived there. Only the tables this plugin
    wrote are replaced now. Nothing else is touched. True since
    0.23.0, so upgrade before generating into a file you keep other
    work in.
    Colour: a ramp the dropdown offers is now the ramp you get, and a
    colour picked by hand survives a run landing, a lost column and a
    reopened project. What the plugin says it discarded stays
    discarded. Categorized layers record their ramp, so QGIS's own
    panel shows it and Classify starts from the right place.
    Class colour files: edit a .qml and Generate draws what it says
    today. Move one and the element keeps its colours, with the
    missing file named.
    Reopening a project: an element's ramp and class count come back
    from the layer rather than from a default. Reverse does not yet.
    Alongside QGIS: Classify no longer fails on a one-value column, a
    closed dialog no longer writes layers into your project, and an
    emptied region layer is reported rather than quietly stopping the
    plugin.
    Live update: a run that cannot finish says so on the note line
    instead of interrupting you with a message box.
    Correction: that live-update change reached you in 0.24.1, whose
    notes wrongly said nothing else had changed.
    0.24.1 Setting the plugin up is more forgiving where QGIS does not
    already carry the packages it needs, which mostly means Linux.
    Setup: when one of them cannot be downloaded the plugin says which
    package and what went wrong, rather than only that something is
    missing. It tries again once when the network was at fault, and it
    has a second version to fall back on.
    0.24.0 The map follows the layer, and data that does not suit a
    style is drawn sensibly rather than drawn wrongly.
    Following the layer: editing, deleting or reprojecting features in
    QGIS updates the map rather than leaving it out of date, and a
    column added there can be mapped straight away.
    Awkward data: areas with no value are left out of the class breaks
    and counted in a notice; a column with one value draws as one
    class; choosing a quantitative style for a column of text draws
    categories rather than nothing.
    Colour: an Edit colours button sets a colour per value for
    categorical elements.
    Warnings: you are told when a spacing leaves areas off the map,
    when a changed category count moves existing colours, and when two
    elements' colours are hard to tell apart.
    Preview: it shows one ring of neighbouring tiles by default, and
    colour-ramp swatches are larger.
    0.23.0 Per-element control over colour, two new pattern families,
    and a public home for the project.
    Data table: reverse-ramp switches and per-element opacity.
    Patterns: grid and stripe families, with their own row and column
    controls.
    Project: published as a public repository with a project page.

````

## Sentences

**`weavingspace_qgis/dialog.py:17740`** — in `_save_the_map`

<!-- id:50cb6f5784389769 -->
> Element {}'s data is no longer in the file: somebody else has changed it since this map was opened, so that element was not saved and nothing was removed. Open the file again to see what it holds.

**`weavingspace_qgis/dialog.py:17745`** — in `_save_the_map`

<!-- id:616a23a7528be793 -->
> Elements {} have data no longer in the file: somebody else has changed it since this map was opened, so those elements were not saved and nothing was removed. Open the file again to see what it holds.

**`weavingspace_qgis/dialog.py:17751`** — in `_save_the_map`

<!-- id:3c686e45aaf6f5eb -->
> Saved to {}. The data has changed since this map was drawn, so the file holds the map as it is and a copy of the data as it is now; press Generate and save again to make them agree.{}{}

**`weavingspace_qgis/help_content.py:7`** — in `module level`

<!-- id:65331509f552595b -->
> <h2>What this plugin makes</h2> <p>Mapping several attributes of the same areas at once is a long-standing challenge of thematic cartography, and it has partial answers already: multivariate choropleths (bivariate and trivariate colour schemes) blend two or three attributes into a single symbology, though their legends must be learned and their ceiling arrives quickly. This plugin takes a different route. It lays a small repeating pattern across your study area and colours each kind of shape in it (each <i>element</i>, labelled a, b, c&hellip;) by its own attribute, with its own ordinary symbology, so each variable stays individually legible while sharing the one map.</p> <h2>A way of working</h2> <p>Iteration is fast, and live update follows your changes. Begin with one polygon layer in the projected CRS you mean to publish in. Choose the number of variables you want to be able to display and choose a pattern. Rough in a design with the spacing kept coarse. Assign variables and colours, refine rotation and insets, and tighten the spacing last. A final spacing near the typical width of your polygons means the smallest polygons will miss some elements at any single spacing but may be a useful compromise.</p><p>The result is a group of ordinary QGIS layers: one per element, one more for any element whose column has blanks, and the region outlines if you asked for them. You can continue to edit here or return to the main part of QGIS to edit more.</p><h2>The switches</h2> <p><i>Join data using whole tileable</i> has every element in a unit take data from the same area, so units read as coherent local summaries; left off, each tile follows the area it overlaps most, which is more faithful tile by tile but lets a unit near a boundary mix its sources (for weaves we generally leave it off). <i>Clip by map units</i> gives a tidy outline at the cost of fragmented edge tiles and speed; the ragged default sits more comfortably with the pattern as a design. <i>Use tileable as icon</i> places one unit per polygon, a gentler multivariate symbol that pairs well with the outlines layer. <i>Warn about lack of legibility in colour choices</i> checks, after each map, whether any two elements use colours a reader may not tell apart, in ordinary vision and for the red-green deficiencies. It is off by default and changes nothing about the map; turn it on once the design is close to settled.</p> <h2>Regenerating and output</h2> <p>A first map appears of its own accord once a layer and variables are in place. Generate (and live update) then replace the previous result in place, and styling you have refined by hand is kept unless you change that element's assignment in the dialog. <i>Create as new group</i> keeps a previous attempt for comparison. A map lives in its own layer group, named for the dataset it came from, and the <i>QGIS Layer Group</i> chooser says where the next run will land; selecting a group restores the design that made it. A map saved to a GeoPackage can be opened on its own later and carries its design with it. Drawing a map and saving one are separate acts: nothing is written until you press <i>Save</i>, which writes every element layer with its symbology embedded, and nothing is opened until you press <i>Load</i>. Save prompts you to confirm if the file selected holds a map made from unrelated data. If somebody else has changed that file since you opened the map, Save writes what it can, leaves their work where it is, and tells you which element the file no longer holds. Layer identities change on regeneration, so re-pick layers in print layouts afterwards.</p> <h2>Further reading</h2> <p>The thinking behind the technique, with worked examples:<br> O'Sullivan, D., &amp; Bergmann, L. (2026). Using MapWeaver to make tiled and woven maps of multivariate thematic data. <i>Cartographic Perspectives, 108</i>, 41&ndash;52. <a href="https://doi.org/10.14714/CP108.2109">doi:10.14714/CP108.2109</a><br> O'Sullivan, D., &amp; Bergmann, L. Tilings and weaves for multivariate mapping. Manuscript under review.</p> <p>Library and examples: <a href="https://github.com/DOSull/weavingspace">github.com/DOSull/weavingspace</a>. If you would like to make maps like these outside QGIS, MapWeaver is a browser-based tool built on the same library: <a href="https://dosull.github.io/mapweaver/app/">dosull.github.io/mapweaver/app</a>. We welcome your examples, questions, and reports of anything that surprises you.</p>
