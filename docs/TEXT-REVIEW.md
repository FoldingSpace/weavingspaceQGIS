# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

27 of 408 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Sentences

**`weavingspace_qgis/topology_edits.py:327`** — in `_why_not`

<!-- id:09aac591d2da1479 -->
> This design's tiles meet, but the library could not work out its structure, so there is nothing here to edit.

**`weavingspace_qgis/topology_edits.py:330`** — in `_why_not`

<!-- id:4f0b4c6b29825808 -->
> This design has gaps between its tiles, and a topology can only be worked out for a design whose tiles meet. Set the strand width to 1.0, or the tile inset to 0, to work on its topology.

**`weavingspace_qgis/topology_edits.py:459`** — in `apply`

<!-- id:c2b75deb6dd17664 -->
> '{}' is not a manipulation this version offers, so it was left out.

**`weavingspace_qgis/topology_edits.py:472`** — in `apply`

<!-- id:203b26bbc698060b -->
> {} on {} could not be applied, because there is no topology to aim it with.

**`weavingspace_qgis/topology_edits.py:497`** — in `apply`

<!-- id:7a4152cbdb56ac66 -->
> {} on {} was not applied: this design has no {} class {}. Its {} classes are {}.

**`weavingspace_qgis/topology_edits.py:500`** — in `apply`

<!-- id:f074f169f527d8a6 -->
> none, so it cannot carry this change

**`weavingspace_qgis/topology_edits.py:508`** — in `apply`

<!-- id:be840459983c6bab -->
> {} on {} was applied to {} only: this design has no {} class {}.

**`weavingspace_qgis/topology_edits.py:548`** — in `apply`

<!-- id:c6540246f96fe95b -->
> {} on {} changed nothing about it, so the design is as it was. A different class, or a larger value, usually does something.

**`weavingspace_qgis/topology_edits.py:763`** — in `_refusal`

<!-- id:61ebebaf91d1a296 -->
> {} on {} left tiles that cannot be laid out, so it was not applied. A smaller value often works where a larger one does not.

**`weavingspace_qgis/topology_tab.py:293`** — in `__init__`

<!-- id:be10acf1b6ff8e21 -->
> Generate a map to see its topology.

**`weavingspace_qgis/topology_tab.py:371`** — in `show_topology`

<!-- id:f4bc46c9e12174e7 -->
> This design has no topology to show.

**`weavingspace_qgis/topology_tab.py:1815`** — in `__init__`

<!-- id:cceda2c5979f1b25 -->
> The structure of the repeating unit, before any inset or strand width is applied — not of the map on the ground.

**`weavingspace_qgis/topology_tab.py:1823`** — in `__init__`

<!-- id:6ed32abfbeca1697 -->
> Which class of edge or vertex to move.

**`weavingspace_qgis/topology_tab.py:1842`** — in `__init__`

<!-- id:d9b575ca0bb1856b -->
> Every class in this design. Tick more than one to move them together.

**`weavingspace_qgis/topology_tab.py:1858`** — in `__init__`

<!-- id:9b60138fa2c33f93 -->
> The symmetry group of each tile, and how many symmetries the whole design has.

**`weavingspace_qgis/topology_tab.py:1865`** — in `__init__`

<!-- id:f9d51f300babecca -->
> What to do to the chosen class.

**`weavingspace_qgis/topology_tab.py:1891`** — in `__init__`

<!-- id:6f5ce675ff4005f3 -->
> Tile the map with the dual of this design. Slower: the dual has to be worked out before anything can be drawn.

**`weavingspace_qgis/topology_tab.py:1897`** — in `__init__`

<!-- id:90f06a5752fef0fe -->
> Adds this change to the design.

**`weavingspace_qgis/topology_tab.py:1901`** — in `__init__`

<!-- id:944eb1032fc31cfb -->
> Removes the most recent change.

**`weavingspace_qgis/topology_tab.py:1905`** — in `__init__`

<!-- id:fe0eaac025cda848 -->
> Removes every change to this design.

**`weavingspace_qgis/topology_tab.py:1912`** — in `__init__`

<!-- id:643e4bf6882ed0dd -->
> Every change made to this design.

**`weavingspace_qgis/topology_tab.py:1964`** — in `__init__`

<!-- id:dbf91fed1f73e82d -->
> Redraws the whole map as you edit. The view above always follows your edits.

**`weavingspace_qgis/topology_tab.py:2087`** — in `say_the_build_has_not_started`

<!-- id:feeb9a0cdb34e5d1 -->
> QGIS has not started working out this design's structure. It may be busy with other work; the tab will fill in if the work begins.

**`weavingspace_qgis/topology_tab.py:2188`** — in `_say_what_the_symmetry_is`

<!-- id:5ecaa0956db0f97c -->
> {} rotation centre{}, {} mirror{}

**`weavingspace_qgis/topology_tab.py:2366`** — in `_grey_what_the_symmetry_holds`

<!-- id:c3de8852adfb3e79 -->
> The symmetry of this design holds this class in place, so a push along it has nowhere to go.

**`weavingspace_qgis/topology_tab.py:2491`** — in `_push_zigzag_readout`

<!-- id:df9c7a813870d639 -->
> Zigzags for this change. This edge is too short to place that many apart on the drawing, so the handle has stopped moving with the count -- this box is where the count is.

**`weavingspace_qgis/topology_tab.py:3160`** — in `_refresh_list`

<!-- id:b04406246519f71a -->
> — from here the tiles no longer meet
