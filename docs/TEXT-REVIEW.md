# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

9 of 182 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Sentences

**`weavingspace_qgis/bridge.py:706`** — in `constant_field_message`

<!-- id:222c24cbe80b578c -->
> Every area has the same value for '{}', so it is drawn as one class rather than a range of colours.

**`weavingspace_qgis/dialog.py:1142`** — in `_on_layer_changed`

<!-- id:833f0e7895b7a071 -->
> The region layer was removed from the project, so there is nothing to map. Choose another layer.

**`weavingspace_qgis/dialog.py:1379`** — in `_adapt_to_the_layer`

<!-- id:9bc197f228806dd1 -->
> The layer's coordinate system changed, so the spacing has been recalculated as {}

**`weavingspace_qgis/dialog.py:1492`** — in `_auto_spacing`

<!-- id:af0f5b5f974901ff -->
> That layer's data is no longer available, so a spacing cannot be suggested.

**`weavingspace_qgis/dialog.py:1507`** — in `_auto_spacing`

<!-- id:c92f28bce418b754 -->
> That layer has no usable extent, so a spacing cannot be suggested. Type a spacing instead.

**`weavingspace_qgis/dialog.py:1622`** — in `_maybe_live_generate`

<!-- id:a8c3672a23666329 -->
> That layer's data is no longer available, so the map cannot be updated.

**`weavingspace_qgis/dialog.py:1635`** — in `_maybe_live_generate`

<!-- id:2e20841dad92b6f9 -->
> This layer does not report how many features it has, so live update cannot tell when it changes. Press Generate to redraw it.

**`weavingspace_qgis/dialog.py:2783`** — in `_on_mode_chosen`

<!-- id:8218f03d677adfe4 -->
> '{}' holds text, so it is drawn as categories rather than a range of values.

**`weavingspace_qgis/dialog.py:3100`** — in `_generate`

<!-- id:603bf6416b61c41b -->
> That layer's data is no longer available. Its file may have been moved or deleted. Reload it in QGIS, or choose another layer.
