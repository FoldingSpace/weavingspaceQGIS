# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

11 of 258 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Sentences

**`weavingspace_qgis/category_editor.py:850`** — in `_build_clamp_strip`

<!-- id:2020ebaf6a837c09 -->
> Where the ramp starts; values below are not drawn

**`weavingspace_qgis/category_editor.py:852`** — in `_build_clamp_strip`

<!-- id:f0cba7b0896dc735 -->
> Where the ramp ends; values above are not drawn

**`weavingspace_qgis/category_editor.py:1102`** — in `_install_pin_row`

<!-- id:722a21c7a6119ff8 -->
> Set this bound; moving it off the computed value makes it yours

**`weavingspace_qgis/category_editor.py:1151`** — in `_install_limit_box`

<!-- id:c00ebe99752bb704 -->
> Where the ladder starts; values below are not drawn

**`weavingspace_qgis/category_editor.py:1153`** — in `_install_limit_box`

<!-- id:1b02d2763b609089 -->
> Where the ladder stops; values above are not drawn

**`weavingspace_qgis/dialog.py:7159`** — in `pin_changed`

<!-- id:192eeeec9c7c6729 -->
> Areas outside the limits stop being drawn on the next Generate, since leaving them out changes which tiles the map holds rather than only their colours.

**`weavingspace_qgis/dialog.py:7167`** — in `pin_changed`

<!-- id:8a1861ac1147771e -->
> Areas the old limits left out come back on the next Generate, since which tiles the map holds is decided when it is drawn rather than when it is coloured.

**`weavingspace_qgis/dialog.py:8452`** — in `_limit_problem`

<!-- id:a497381aef2d1319 -->
> The lowest value drawn must be below the highest; {} is not below {}.

**`weavingspace_qgis/dialog.py:8462`** — in `_limit_problem`

<!-- id:092bd95217c62864 -->
> The lowest value drawn ({}) is at or above the first class boundary ({}), which would leave that class with nothing to hold.

**`weavingspace_qgis/dialog.py:8466`** — in `_limit_problem`

<!-- id:1e97d1edcb502c46 -->
> The highest value drawn ({}) is at or below the last class boundary ({}), which would leave that class with nothing to hold.

**`weavingspace_qgis/dialog.py:9503`** — in `_generate`

<!-- id:d8fb754f8bebea18 -->
> Drawn as icons, this layer's {} areas ask for roughly {} tiles, which is more than the plugin will draw. Spacing will not help, since each area takes one tile unit however large it is drawn, so try a layer with fewer areas or a tileable with fewer elements.
