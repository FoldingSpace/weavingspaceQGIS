# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

6 of 460 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Sentences

**`weavingspace_qgis/topology_edits.py:1438`** — in `scaffolded_weave`

<!-- id:cbda5205b1d98411 -->
> the weave could not be built: {}

**`weavingspace_qgis/topology_edits.py:1443`** — in `scaffolded_weave`

<!-- id:ff426542b4059199 -->
> the weave has no strands to build from

**`weavingspace_qgis/topology_edits.py:1464`** — in `scaffolded_weave`

<!-- id:bdbc3badfba1456d -->
> the library would not rebuild the unit from the scaffolded tiles

**`weavingspace_qgis/topology_edits.py:1468`** — in `scaffolded_weave`

<!-- id:88b067bf51d58913 -->
> the scaffolding leaves a gap of {} and an overlap of {} of a cell, so it is not a tiling

**`weavingspace_qgis/topology_tab.py:2540`** — in `__init__`

<!-- id:a0aa60c2f4612ef9 -->
> Count, like a dropped strand

**`weavingspace_qgis/topology_tab.py:2543`** — in `__init__`

<!-- id:6aadb07fc44d2232 -->
> Whether strand-width gaps count in the structure, like a dropped strand.
