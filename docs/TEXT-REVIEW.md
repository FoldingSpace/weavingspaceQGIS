# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

90 of 90 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Documents changed

- `README.md` (160 lines)
- `docs/index.html` (245 lines)
- `docs/USER-GUIDE.md` (198 lines)

## Sentences

**`weavingspace_qgis/bridge.py:240`** — in `layer_to_gdf`

<!-- id:4bd3c5b8de418d67 -->
> The selected layer has no (non-empty) polygon features.

**`weavingspace_qgis/bridge.py:567`** — in `categorical_shift_message`

<!-- id:4e147e60885cd26e -->
> '{}' now has {} categories where it had {}, so the colours of the existing classes have changed: they are sampled across the palette by position. Import a colour mapping (QML) for that element to keep colours the same from one map to the next.

**`weavingspace_qgis/bridge.py:602`** — in `coverage_message`

<!-- id:e992f02b6b48802a -->
> At {} {} spacing, {} of {} areas received no tiles and appear nowhere on the map.

**`weavingspace_qgis/bridge.py:715`** — in `load_categorized_template`

<!-- id:3f9d74d0fa8823d8 -->
> No symbology found in the style file.

**`weavingspace_qgis/bridge.py:719`** — in `load_categorized_template`

<!-- id:e788b00d381f6796 -->
> The style file does not contain categorised symbology.

**`weavingspace_qgis/bridge.py:726`** — in `load_categorized_template`

<!-- id:75627c684c96b9d3 -->
> The style file defines no classes.

**`weavingspace_qgis/bridge.py:735`** — in `template_from_layer`

<!-- id:022d23e6bf0cd046 -->
> that layer has no categorized symbology

**`weavingspace_qgis/bridge.py:742`** — in `template_from_layer`

<!-- id:551ac361649e7925 -->
> that layer's symbology defines no classes

**`weavingspace_qgis/bridge.py:932`** — in `write_gpkg_layer`

<!-- id:a4498f670117f800 -->
> Writing {} to {} failed: {}

**`weavingspace_qgis/bridge.py:935`** — in `write_gpkg_layer`

<!-- id:ebd1b4358df2f4b7 -->
> Could not re-open {} from {}

**`weavingspace_qgis/dialog.py:265`** — in `__init__`

<!-- id:ad2e9b0a6b25716b -->
> Pick a design to preview it.

**`weavingspace_qgis/dialog.py:455`** — in `__init__`

<!-- id:0eec116b706692e6 -->
> WeavingSpace – tiled & woven multivariate maps (v{})

**`weavingspace_qgis/dialog.py:550`** — in `_build_ui`

<!-- id:7ea4315f91c82baf -->
> 0 shows the tile unit on its own; higher values add rings of neighbouring copies for context

**`weavingspace_qgis/dialog.py:572`** — in `_build_ui`

<!-- id:eaebe6754f9878b4 -->
> The polygon layer whose attributes will be mapped. A projected CRS is best; geographic layers are reprojected to Web Mercator (fine for exploring, rarely for publishing).

**`weavingspace_qgis/dialog.py:583`** — in `_build_ui`

<!-- id:c70ab16ea09fabb6 -->
> How many variables the pattern can carry. Two to four supports value-by-value reading; with more, the map reads as a texture in which exceptions stand out.

**`weavingspace_qgis/dialog.py:592`** — in `_build_ui`

<!-- id:e9fcfcbd57370b1c -->
> Tilings give compact side-by-side patches (and support the icon and glyph options); weaves interlace strands the eye can follow between distant places. Both carry the same information.

**`weavingspace_qgis/dialog.py:600`** — in `_build_ui`

<!-- id:d1b8be31b0c12eef -->
> The pattern catalogue for the chosen element count.

**`weavingspace_qgis/dialog.py:609`** — in `_build_ui`

<!-- id:8c2e86204592853b -->
> The pattern's grain, in map units: unit size for tilings, strand-to-strand distance for weaves. Aim near the typical polygon width (for weaves, divided by strands per direction); keep it coarse while iterating.

**`weavingspace_qgis/dialog.py:617`** — in `_build_ui`

<!-- id:dd5c5c65cf8a4490 -->
> A coarse value from the layer extent, meant for iterating rather than publication

**`weavingspace_qgis/dialog.py:629`** — in `_build_ui`

<!-- id:f297bd31c128e7bf -->
> Where the slices/dissection cuts start on the base shape: 0 cuts from the corners, 1 from the edge midpoints (fractions blend between).

**`weavingspace_qgis/dialog.py:639`** — in `_build_ui`

<!-- id:eb089e22d053f4cc -->
> Rotates the dissection's internal cuts within each tile.

**`weavingspace_qgis/dialog.py:648`** — in `_build_ui`

<!-- id:e63253361c79b332 -->
> Sharpness of the star points, in degrees.

**`weavingspace_qgis/dialog.py:659`** — in `_build_ui`

<!-- id:364344d670831204 -->
> Strand width as a fraction of spacing: 1.0 fills all space; smaller values open gaps between strands and strengthen the woven look.

**`weavingspace_qgis/dialog.py:669`** — in `_build_ui`

<!-- id:83c513a82e465f62 -->
> The passing pattern: how many crossing strands each strand goes over, then under (2, or 1,2, or 1,2,2,1). Long patterns produce very large repeating units, usually too coarse to map well.

**`weavingspace_qgis/dialog.py:687`** — in `_build_ui`

<!-- id:ef46c42e84d4d5a9 -->
> Rows of squares in the unit

**`weavingspace_qgis/dialog.py:689`** — in `_build_ui`

<!-- id:f7f089a2b5a45237 -->
> Columns of squares in the unit. Rows x columns beyond the element count leaves regular openings in the grid.

**`weavingspace_qgis/dialog.py:720`** — in `_build_ui`

<!-- id:55c94fd469d6f825 -->
> Rotating the pattern usually helps: roughly 15–75° suits two-direction weaves; hexagon-based patterns repeat their own symmetry every 30°, so those angles change little.

**`weavingspace_qgis/dialog.py:732`** — in `_build_ui`

<!-- id:4386757262e57cf9 -->
> Pulls each repeating unit apart from its neighbours, showing which elements belong together (tilings only: a weave's strands would be severed).

**`weavingspace_qgis/dialog.py:737`** — in `_build_ui`

<!-- id:f2e461246b9ddd4d -->
> Opens a thin gap around every tile or strand, a legibility device that also strengthens the woven appearance.

**`weavingspace_qgis/dialog.py:740`** — in `_build_ui`

<!-- id:8242b395903dbba5 -->
> Scale as glyph (independent of tiling)

**`weavingspace_qgis/dialog.py:757`** — in `_build_ui`

<!-- id:b82031d17558e246 -->
> Each element/row below becomes its own QGIS layer, initially symbolized using the options available under the 'Style' dropdowns; refine it afterwards in QGIS's built-in Layer Styling panel. A plausible style is picked from each field's type and follows the variable until you choose one yourself.

**`weavingspace_qgis/dialog.py:789`** — in `_build_ui`

<!-- id:037c76945b76eeef -->
> Join data using whole tileable

**`weavingspace_qgis/dialog.py:791`** — in `_build_ui`

<!-- id:a3c68fab740e7a54 -->
> Every element in a unit takes its data from the same area, so units read as coherent local summaries. Off, each tile follows the area it overlaps most (usually better for weaves).

**`weavingspace_qgis/dialog.py:794`** — in `_build_ui`

<!-- id:a7b69f13a9349cdd -->
> Retain complete tileables at edges

**`weavingspace_qgis/dialog.py:796`** — in `_build_ui`

<!-- id:d8dd17888ea57759 -->
> Keeps whole units that touch the region, letting the pattern spill outward. Mostly of interest alongside the whole-tileable join.

**`weavingspace_qgis/dialog.py:799`** — in `_build_ui`

<!-- id:cdd9c3fccc1946fa -->
> Clip by map units (no ragged edges)

**`weavingspace_qgis/dialog.py:801`** — in `_build_ui`

<!-- id:ee0f50e2d3bb79f6 -->
> Trims the pattern to the region outline. The slowest step, and it fragments edge tiles: leave it off while iterating.

**`weavingspace_qgis/dialog.py:803`** — in `_build_ui`

<!-- id:50af5cd426307348 -->
> Use tileable as icon (one per map unit)

**`weavingspace_qgis/dialog.py:805`** — in `_build_ui`

<!-- id:653f599980a22167 -->
> One unit per polygon, at its centre, instead of a continuous pattern: a gentler multivariate symbol. It pairs well with the outlines layer.

**`weavingspace_qgis/dialog.py:808`** — in `_build_ui`

<!-- id:6f8af792281b893c -->
> Add map unit outlines layer

**`weavingspace_qgis/dialog.py:810`** — in `_build_ui`

<!-- id:8a426e7dd6a4dc4a -->
> Adds a copy of the region layer, drawn as outlines on top, so the geography stays readable under the pattern.

**`weavingspace_qgis/dialog.py:813`** — in `_build_ui`

<!-- id:9a46c8497b5ce2cd -->
> Create as new group (keep the previous result)

**`weavingspace_qgis/dialog.py:815`** — in `_build_ui`

<!-- id:eb7d28c0460c4903 -->
> Keep the previous result and add this run as a separate group for side-by-side comparison. When saving to GeoPackage, use a different file for each kept run.

**`weavingspace_qgis/dialog.py:827`** — in `_build_ui`

<!-- id:db669f6c7c16d29b -->
> Save tiled map to GeoPackage

**`weavingspace_qgis/dialog.py:828`** — in `_build_ui`

<!-- id:887f012f004af6d1 -->
> Save to GeoPackage (empty = temporary layers)

**`weavingspace_qgis/dialog.py:841`** — in `_build_ui`

<!-- id:17a7f36e2944dfe2 -->
> <p><small>WeavingSpace plugin version {}</small></p>

**`weavingspace_qgis/dialog.py:850`** — in `_build_ui`

<!-- id:582906f2e55066f4 -->
> Renders a first map as soon as a layer and variables are in place, then regenerates as you change settings (temporary-layer output only, and only while the estimated tile count stays manageable)

**`weavingspace_qgis/dialog.py:1177`** — in `_maybe_live_generate`

<!-- id:130b8a7848a184af -->
> live update paused (about {} tiles); press Generate

**`weavingspace_qgis/dialog.py:1266`** — in `_rebuild_unit`

<!-- id:102e7ccb57665571 -->
> Could not build tile unit: {}

**`weavingspace_qgis/dialog.py:1269`** — in `_rebuild_unit`

<!-- id:ad2e9b0a6b25716b -->
> Pick a design to preview it.

**`weavingspace_qgis/dialog.py:1386`** — in `_make_class_source_combo`

<!-- id:aadbaa3fa0137d8a -->
> Where this element's class colours come from: automatic, another layer's categorized symbology, or a QML style file (a browsed file is offered to every categorized element).

**`weavingspace_qgis/dialog.py:1396`** — in `activated`

<!-- id:435f0273f756cd4a -->
> Class colours from a QGIS style file

**`weavingspace_qgis/dialog.py:1444`** — in `_make_reverse_box`

<!-- id:d4a7524d0d47a768 -->
> Run this element's ramp the other way round

**`weavingspace_qgis/dialog.py:1482`** — in `_make_opacity_spin`

<!-- id:90da9e347ba4b045 -->
> How solid this element draws, as QGIS layer opacity: 100 is opaque, lower values let whatever lies beneath show through. Stored separately from the colours, so changing the ramp keeps it, and it travels into a GeoPackage with the rest of the styling.

**`weavingspace_qgis/dialog.py:1576`** — in `_make_colour_button`

<!-- id:18c353b62194e54e -->
> Fill colour for this element

**`weavingspace_qgis/dialog.py:1815`** — in `_refresh_table`

<!-- id:fbfec607876e49fe -->
> How this element is symbolized; a plausible style follows the field's type until you choose one yourself. Quantiles equalize feature counts, equal intervals equalize value ranges, natural breaks find clusters; Quant: Unclassed gives the look of a continuous ramp (50 linear steps); Single colour flattens the element to one colour. All adjustable later in the Layer Styling panel.

**`weavingspace_qgis/dialog.py:1843`** — in `_refresh_table`

<!-- id:a559943a1519e0cb -->
> Number of classes for this element's Quant symbology; categorized rows show the number of detected categories.

**`weavingspace_qgis/dialog.py:2146`** — in `_restyle_only`

<!-- id:e28d4124937d4ab7 -->
> restyled {} (no re-tiling needed)

**`weavingspace_qgis/dialog.py:2237`** — in `_generate`

<!-- id:aa2a80e206a7838e -->
> The tile unit could not be built.

**`weavingspace_qgis/dialog.py:2244`** — in `_generate`

<!-- id:da78c8551b2e5967 -->
> Assign at least one variable in the Data & colours tab.

**`weavingspace_qgis/dialog.py:2252`** — in `_generate`

<!-- id:64a557b2a65d6c56 -->
> You asked to keep the previous result as its own group, but writing to the same GeoPackage would overwrite its data. Choose a different file for this run.

**`weavingspace_qgis/dialog.py:2277`** — in `_generate`

<!-- id:a3f356354f18c62d -->
> A spacing this small asks for roughly {} tiles, more than QGIS can survive. For this layer a spacing of about {} map units or more will work.

**`weavingspace_qgis/dialog.py:2285`** — in `_generate`

<!-- id:98e7c7ce4e0397db -->
> This will generate roughly {} tiles and may take a while. Continue?

**`weavingspace_qgis/dialog.py:2409`** — in `_generate`

<!-- id:ee4e54ecbcf3e695 -->
> WeavingSpace: tiling with {}

**`weavingspace_qgis/dialog.py:2414`** — in `on_progress`

<!-- id:4c59c799e27ed06e -->
> laying out the tiling… %p%

**`weavingspace_qgis/dialog.py:2416`** — in `on_progress`

<!-- id:d244536d33a684dc -->
> joining data to tiles… %p%

**`weavingspace_qgis/dialog.py:2561`** — in `_on_generated`

<!-- id:8ad335e8aa4ad916 -->
> Ran out of memory while tiling; a larger spacing will work for this layer.

**`weavingspace_qgis/dialog.py:2574`** — in `_on_generated`

<!-- id:f6627eb64fc59ba3 -->
> The tiling produced no tiles.

**`weavingspace_qgis/dialog.py:2580`** — in `_on_generated`

<!-- id:f553711493c84810 -->
> adding {} tiles to the map...

**`weavingspace_qgis/dialog.py:2595`** — in `_on_generated`

<!-- id:03fdf9b6056d9a4a -->
> The region layer was removed while the map was being made, so there was nothing to add it to.

**`weavingspace_qgis/dialog.py:2599`** — in `_on_generated`

<!-- id:ce05335606708960 -->
> Could not add the result layers:

**`weavingspace_qgis/dialog.py:2604`** — in `_on_generated`

<!-- id:ce05335606708960 -->
> Could not add the result layers:

**`weavingspace_qgis/dialog.py:2854`** — in `_add_output_layers`

<!-- id:36063a2ddcb64d78 -->
> '{}': {} tiles across {} element layers

**`weavingspace_qgis/dialog.py:2865`** — in `_add_output_layers`

<!-- id:fef807ccec8b9be2 -->
> Categorized styling produced a very large number of classes for:

**`weavingspace_qgis/dialog.py:2867`** — in `_add_output_layers`

<!-- id:78745481edf68bd8 -->
> . Is that field really categorical?

**`weavingspace_qgis/dialog.py:2871`** — in `_add_output_layers`

<!-- id:54dca3e5daced86f -->
> Some class style files could not be used (

**`weavingspace_qgis/dialog.py:2873`** — in `_add_output_layers`

<!-- id:9429cfb4275d4bef -->
> ); automatic colours were applied instead.

**`weavingspace_qgis/help_content.py:7`** — in `module level`

<!-- id:3b6ec35f3cb97bbd -->
> <h2>What this plugin makes</h2> <p>Mapping several attributes of the same areas at once is a long-standing challenge of thematic cartography, and it has partial answers already: multivariate choropleths (bivariate and trivariate colour schemes) blend two or three attributes into a single symbology, though their legends must be learned and their ceiling arrives quickly. This plugin takes a different route. It lays a small repeating pattern across your study area and colours each kind of shape in it (each <i>element</i>, labelled a, b, c&hellip;) by its own attribute, with its own ordinary symbology, so each variable stays individually legible while sharing the one map. With two or three variables the result can be read value by value, much as a choropleth is; with more it reads as a texture in which agreement forms smooth tone and exception announces itself. Nor are those the only readings such maps repay (we take up others, and the design choices that serve them, in a paper presently under review); it is still worth deciding early which reading you are designing for.</p> <h2>A way of working</h2> <p>Begin with one polygon layer in the projected CRS you mean to publish in, attributes as fields. Rough in a design with the spacing kept coarse; iteration is fast, and live update follows your changes. Assign variables and colours, refine rotation and insets, and tighten the spacing last. The result is a group of ordinary QGIS layers, one per element, so the finishing moves are ones you already know: QGIS's built-in Layer Styling panel, GeoPackage export, a print layout.</p> <h2>Choosing a design</h2> <p>Tilings and weaves carry the same information. Tilings give compact side-by-side patches and suit the icon and glyph options; weaves let the eye follow a strand between distant places. Two plainer families round out the catalogue: stripes runs the elements as parallel bands, and grid arrays them as squares, with rows and columns adjustable (fewer elements than cells leaves regular openings in the pattern). A sensible final spacing sits near the typical width of your polygons (divided, for a weave, by the strands per direction); the smallest polygons will miss some elements at any single spacing, the familiar compromise of mixed-size choropleths. Rotation usually helps, with something between fifteen and seventy-five degrees congenial for two-direction weaves, while hexagon-based patterns change little at multiples of thirty. A hyphen in a strands code leaves a deliberate gap so identical-looking strands can be told apart, and the two insets open gaps that show which shapes belong together.</p> <h2>Colour</h2> <p>Each element layer arrives with its initial symbology, a standard QGIS renderer built from your Style choices, so nothing here is final. The Style dropdown fixes each element's classification method (Quant: Unclassed gives the look of a continuous ramp, in fifty linear steps), and the Classes column beside it (present while any element is graduated) its number of classes. Distinct ramps per element make it easy to tell variables apart (the default). One shared ramp across related variables turns the unit into an instrument for noticing disagreement: agreement reads as smooth tone, and an element out of step with its neighbours shows as speckle. Categorical fields (land cover, period labels) receive a categorized renderer; each categorized element's Categ colourmap src cell chooses where its class codes, names, and colours come from: automatic assignment, another loaded layer's categorized symbology, or a QML style file. A file chosen once is offered to every categorized element, and the scheme travels through regeneration and GeoPackage export alike.</p> <p>An element left on <i>---</i> takes no variable and draws as plain fill; that is a design choice like any other, and it stays put as you change the pattern around it.</p> <h2>The switches</h2> <p><i>Join data using whole tileable</i> has every element in a unit take data from the same area, so units read as coherent local summaries; left off, each tile follows the area it overlaps most, which is more faithful tile by tile but lets a unit near a boundary mix its sources (for weaves we generally leave it off). <i>Clip by map units</i> gives a tidy outline at the cost of fragmented edge tiles and speed; the ragged default sits more comfortably with the pattern as a design. <i>Use tileable as icon</i> places one unit per polygon, a gentler multivariate symbol that pairs well with the outlines layer.</p> <h2>Regenerating and output</h2> <p>A first map appears of its own accord once a layer and variables are in place. Generate (and live update) then replace the previous result in place, and styling you have refined by hand is kept unless you change that element's assignment in the dialog. <i>Create as new group</i> keeps a previous attempt for comparison. <i>Save to GeoPackage</i> writes all element layers, with their symbology, into one shareable file. Layer identities change on regeneration, so re-pick layers in print layouts afterwards.</p> <h2>Further reading</h2> <p>The thinking behind the technique, with worked examples:<br> O'Sullivan, D., &amp; Bergmann, L. (2026). Using MapWeaver to make tiled and woven maps of multivariate thematic data. <i>Cartographic Perspectives, 108</i>, 41&ndash;52. <a href="https://doi.org/10.14714/CP108.2109">doi:10.14714/CP108.2109</a><br> O'Sullivan, D., &amp; Bergmann, L. Tilings and weaves for multivariate mapping. Manuscript under review.</p> <p>Library and examples: <a href="https://github.com/DOSull/weavingspace">github.com/DOSull/weavingspace</a>. If you would like to make maps like these outside QGIS, MapWeaver is a browser-based tool built on the same library: <a href="https://dosull.github.io/mapweaver/app/">dosull.github.io/mapweaver/app</a>. We welcome your examples, questions, and reports of anything that surprises you.</p>

**`weavingspace_qgis/perception.py:232`** — in `clash_message`

<!-- id:5ff68eb9e61a3a43 -->
> for a reader with deuteranopia

**`weavingspace_qgis/perception.py:233`** — in `clash_message`

<!-- id:1269baae3fb8f5cb -->
> for a reader with protanopia

**`weavingspace_qgis/perception.py:236`** — in `clash_message`

<!-- id:1c2e89bbd04a4727 -->
> ({} other pair(s) are close too)

**`weavingspace_qgis/perception.py:237`** — in `clash_message`

<!-- id:68022acfc248f883 -->
> Elements '{}' and '{}' use colours that are hard to tell apart {}{}. Readers may not be able to say which variable a tile belongs to. A shared ramp, or ramps that differ in lightness as well as hue, separates them more reliably.

**`weavingspace_qgis/plugin.py:42`** — in `initGui`

<!-- id:9124689adc58e115 -->
> Create tiled and woven multivariate maps (mapweaver)

**`weavingspace_qgis/plugin.py:85`** — in `_ensure_dependencies`

<!-- id:dd3edb0d6a4bbc1b -->
> WeavingSpace – one-time setup

**`weavingspace_qgis/plugin.py:86`** — in `_ensure_dependencies`

<!-- id:4f67d46c1c8b2316 -->
> This QGIS installation is missing (or has outdated versions of) Python components the plugin needs: {} WeavingSpace can download them from PyPI (python.org's package archive) into the plugin's own folder; nothing else in your QGIS installation is touched, and this happens only once. Download them now (about 20 to 60 MB)?

**`weavingspace_qgis/plugin.py:117`** — in `_ensure_dependencies`

<!-- id:04e40fd04661a010 -->
> Check your internet connection and try again, or install these packages into QGIS's Python yourself.

**`weavingspace_qgis/plugin.py:137`** — in `open_dialog`

<!-- id:42562fd4c77de6c3 -->
> The weavingspace library failed to load: {}
