# Text awaiting review

Generated by `tools/text_review.py` — and **you can edit the quoted sentences here directly**. When the wording is right, `--apply` writes your edits back into the source and records them as reviewed. Leave the `<!-- id -->` comments alone; they are how each sentence finds its way home.

7 of 337 pieces of user-facing text are new or changed since they were last approved.

Edit any sentence below, then:

```bash
python3 tools/text_review.py --apply    # edits -> source
```

Sentences you are happy with as they stand need no edit; `--apply` records everything here as reviewed either way. To accept the lot untouched, use `--approve`.

## Documents

The README, the project page and the user guide, one paragraph at a time. Edit inside the fences and leave the fence lines themselves alone. Markup is shown as it is written, so change the words and leave the tags where they are.

**`README.md:32`**

<!-- id:473ae096824cf5b4 -->
````
1. Go to the
   [latest release](https://github.com/FoldingSpace/weavingspaceQGIS/releases/latest)
   and download the zip file under the heading *Assets*. Its name
   carries the version, so it will look like
   `weavingspace_qgis-0.24.4.zip`; save it somewhere you can find
   it again, such as your Downloads folder.
2. In QGIS, choose **Plugins ▸ Manage and Install Plugins… ▸ Install
   from ZIP**, click the **…** button to pick the file you just
   downloaded, and click **Install Plugin**. QGIS may warn you that the
   plugin is experimental, which it is.
3. Open the dialog from the toolbar button, or from **Plugins ▸
   WeavingSpace**.
````

**`docs/index.html:197`**

<!-- id:a6a53315fb6e79c5 -->
````
<div class="obtain">
  <p><a href="https://github.com/FoldingSpace/weavingspaceQGIS/releases/latest">Download
  the plugin</a> (the zip under <em>Assets</em>, whose name carries the
  version), then in QGIS choose <strong>Plugins &#9654; Manage
  and Install Plugins&hellip; &#9654; Install from ZIP</strong> and pick
  the file.</p>
  <p>Source, issues, and documentation:
  <a href="https://github.com/FoldingSpace/weavingspaceQGIS">github.com/FoldingSpace/weavingspaceQGIS</a>.</p>
</div>
````

## Sentences

**`weavingspace_qgis/dialog.py:2514`** — in `_build_ui`

<!-- id:0caeb6598646e4dc -->
> Type an exact element count.

**`weavingspace_qgis/dialog.py:2784`** — in `_build_ui`

<!-- id:6e0dbac5b6fc92d1 -->
> Stretch the pattern left-right; negative mirrors it.

**`weavingspace_qgis/dialog.py:2798`** — in `_build_ui`

<!-- id:0cb31e87fecfb16e -->
> Scale Left-Right / Up-Down

**`weavingspace_qgis/dialog.py:2800`** — in `_build_ui`

<!-- id:29fd3d01135e939e -->
> Slant the pattern left-right.

**`weavingspace_qgis/dialog.py:2803`** — in `_build_ui`

<!-- id:90c5075b0972ab20 -->
> Skew Left-Right / Up-Down (°)
