# What the test suite covers

Generated by `tools/test_map.py`; do not edit. Regenerated at every release, so it cannot drift from the suite it describes.

642 tests across 10 areas.

`guards` marks a test whose docstring carries a `Regression:` line: it was written because that defect actually happened here. A test without it guards ground we imagined rather than ground we fell through, which is worth knowing when reading an area that looks well covered.

| area | tests | guarding a real defect | visual | family |
|---|---:|---:|---:|---:|
| Agreement with the library | 18 | 2 | 2 | 0 |
| Colour and symbology | 147 | 98 | 22 | 0 |
| Design: units, families, modifiers | 58 | 28 | 8 | 4 |
| Other | 178 | 118 | 14 | 4 |
| Output: layers, groups, GeoPackage | 79 | 44 | 7 | 1 |
| Robustness and the world outside | 31 | 12 | 3 | 1 |
| Runs, races and concurrency | 42 | 17 | 4 | 2 |
| The categorical colour editor | 34 | 14 | 2 | 0 |
| The preview widget | 23 | 15 | 3 | 0 |
| What the user reads | 32 | 22 | 2 | 1 |

## Agreement with the library

- **(visual gallery)** *[visual]*  
  The grid family (a library extra beyond the web app's catalogue): five elements on a 2 x 3 array, so one cell per unit stays open — the punctured form is deliberate library behaviour (the first n cells are filled) and the openings must show as background.
- **(visual gallery)** *[visual]*  
  The stripes family (the other library extra): four parallel bands per unit.
- **UI vs library: clipped edges**  
  Clipping to the region outline, the switch whose sense is inverted on the way to the library (the checkbox says clip, the argument says ragged_edges).
- **UI vs library: dissection inner angle**  
  The two angle controls that no other test drives to an effect: a dissection's inner angle, and a star's point angle.
- **UI vs library: grid rows and columns**  
  The grid family's row and column spinners, the newest controls and the ones most likely to be mis-plumbed: a 1 x 4 array is a very different map from 2 x 2, and both carry four elements.
- **UI vs library: icons and whole-tileable join**  
  Icon mode: one unit per polygon rather than a continuous pattern, which changes the tiling call rather than the unit.
- **UI vs library: rotate/scale/skew/inset chain** *[guards]*  
  The affine chain in the order the dialog applies it: rotate, then scale, then skew, then the two insets.
- **UI vs library: slice offset, rotate, inset**  
  Family option (slice offset), rotation and tile inset together.
- **UI vs library: star point angle**  
  A star tiling's point angle: sharper points, different tiles.
- **UI vs library: weave over-under, strand width, inset**  
  Weave controls: over-under pattern and strand width, plus the subtlety that a weave's tile inset is scaled BY the strand width (insetting a weave in raw per cent of spacing would eat thin strands entirely).
- **an inset percentage is a percentage of the spacing** *[race, guards]*  
  10% at a 1000-unit spacing insets by 100 units.
- **metamorphic: full rotation and the spacing law**  
  Rotating a design by 360 degrees reproduces it; halving the spacing multiplies the tiles by about four.
- **metamorphic: translation invariance**  
  Moving the region moves the map with it, and changes nothing about its content.
- **random designs keep their views in agreement**  
  The pairs, swept across designs instead of one fixture.
- **random designs match the library**  
  Draw designs at random and require each to match the library.
- **the catalogue offers only designs that build**  
  A family appears at a count exactly where the library can build it.
- **the library works without matplotlib or scipy**  
  The vendored library must import and tile with plotting absent.
- **weavingspace unit construction**  
  The vendored weavingspace builds the units we rely on.

## Colour and symbology

- **(visual gallery)** *[visual]*  
  The paper's favourite: Laves 3.3.4.3.4 (Cairo-adjacent), four variables on four distinct sequential ramps.
- **(visual gallery)** *[visual]*  
  The paper's 'out-of-step detector': hex-colouring 7 with every element on ONE shared ramp (Reds), so agreement reads as a single hue family; the check is that nearly all non-background pixels are red-dominant.
- **(visual gallery)** *[visual]*  
  Categorical symbology (the plugin's ground the web app lacks): land-cover classes on one element of a square slice, checked for one colour per class.
- **(visual gallery)** *[visual]*  
  A basket weave (the other biaxial weave type the catalogue leans on): 2x2 over-under with slight gaps; checks the basket matrix path and that gaps and ramps behave as in the twill case.
- **(visual gallery)** *[visual]*  
  Diverging ramps (RdBu, PiYG): the palette family for values with a meaningful midpoint; checks the diverging entries of the palette set survive the ramp installation and seeding paths.
- **(visual gallery)** *[visual]*  
  A class scheme supplied as a template (the QML/donor-layer path) must put its EXACT colours on the map: build a template with three unmistakable colours, seed a categorized element from it, and check each appears in the render within a tight Delta-E.
- **QGIS still mishandles non-finite class breaks (canary)** *[guards]*  
  A canary.
- **QML class template round trip** *[guards]*  
  A QML with categorized symbology should drive class colours.
- **UI vs library: categorical sources and unassigned element** *[visual, guards]*  
  Categorical plumbing end to end against a direct library call: one element categorized from an imported QML mapping, one categorized automatically, one graduated, one left unassigned.
- **UI vs library: categorical to GeoPackage and back**  
  Categorical colours from an imported mapping, taken all the way to disk and back: the file-backed layers must carry the same classes, colours and labels as the memory layers did, and the same tiles as a direct library call.
- **UI vs library: categorical weave, icons, layer source** *[visual]*  
  Categorical plumbing on the other side of the catalogue: a weave in icon mode, joining data on whole tileables, with its class colours taken from ANOTHER LAYER's symbology rather than a file.
- **Unclassed opens locked with a live range** *[race, guards]*  
  Unclassed rows open the editor locked, with the range alive.
- **a QGIS symbology edit reaches the plugin on every shape** *[guards]*  
  Every route a person can take in the Symbology panel, on awkward data.
- **a QGIS-side graduated restyle reaches the dialog** *[visual]*  
  Recolouring a graduated element in QGIS itself reaches the dialog.
- **a QGIS-side restyle reaches the dialog** *[visual]*  
  Recolouring an element layer in QGIS itself reaches the dialog.
- **a bound can be given back from every control** *[guards]*  
  All four ends, both ways of returning one to the classification.
- **a break retyped in QGIS reaches the plugin** *[guards]*  
  Retype a class boundary in the Symbology panel; the plugin follows.
- **a carried renderer never leaves an absence unpainted** *[guards]*  
  A kept renderer must still draw what the new layer holds.
- **a categorical scheme copies onto another element** *[guards]*  
  The control exists, and it overwrites all but four things.
- **a class added in QGIS is not a colour somebody picked** *[guards]*  
  QGIS clones OUR placeholder for a new class; nobody picked it.
- **a class count at either end of 2..20 reaches the map** *[visual, race]*  
  Two classes and twenty classes must both be twenty and two.
- **a class count is refused rather than destroying a pin** *[guards]*  
  The user's own control is refused; their pins are left alone.
- **a class source comes home to the dataset it was chosen on** *[guards]*  
  An imported QML returns with its own dataset, not another's.
- **a class source file that changes on disk**  
  The QML is rewritten after being chosen.
- **a class source file that goes away** *[race]*  
  The QML is deleted, moved or renamed after being chosen.
- **a class source follows the record under it** *[guards]*  
  Choosing a group restores the class source it was made with.
- **a class source that does not match the data**  
  A scheme whose classes are not the data's values.
- **a class source that moves after the map is drawn**  
  Idea 3: the QML an element's colours came from is tidied away.
- **a class that cannot be pinned says so in its cell** *[guards]*  
  An empty cell in a Pin column reads as a slot, not as a refusal.
- **a colour on a ladder we never saw is declined and named** *[guards]*  
  With no record of what we drew, a dock colour is left alone.
- **a colour picked after the file moved still reaches the map** *[race, guards]*  
  A restyle needs nothing from the region layer, so it must run.
- **a column deleted in qgis takes its ramp and count too** *[guards]*  
  Ruling 3 at the third door, driven through the controls.
- **a column of nothing but nulls invents no class**  
  A field that is entirely NULL: nothing to put a break between.
- **a copied classification carries the whole row** *[guards]*  
  Copying sends breaks, colours, count, style and PIN FLAGS.
- **a copied ladder is not reported as a reduction** *[guards]*  
  A copy's unreachable classes are kept, so nothing was reduced.
- **a copied ladder on one value still wears its ramp** *[guards]*  
  No class may be left on the colour it was built with.
- **a copy carries a class source the target has not met** *[guards]*  
  The reference travels, or the two elements do not agree.
- **a copy from unclassed leaves the chosen count alone** *[guards]*  
  Unclassed's fifty is the style's number, not the user's.
- **a copy reproduces a classification on every target** *[guards]*  
  Copy a classification to several elements, across shapes and states.
- **a count set in qgis releases a copied ladder** *[guards]*  
  A class count from QGIS degrades a copy, as one from the table does.
- **a data-defined fill is drawn as an unknown** *[guards]*  
  symbols() hands back the base symbol, not what is painted.
- **a dataset chosen after the dialog opened owns its colours** *[guards]*  
  The pre-identity merge fires for TWO routes, and one is a leak.
- **a deferring element keeps its no data layer** *[guards]*  
  Who picks the colours does not decide which rows can be drawn.
- **a deferring element keeps its renderer across a generate** *[guards]*  
  Deferring means deferring, until the variable makes it meaningless.
- **a design change and a ramp pick commute** *[visual, race]*  
  Colour choices made before a design change must survive it.
- **a dock classify on a constant column does not crash** *[race, guards]*  
  QGIS's own Classify button, on the column that collapses.
- **a dock edit that changes no colour is announced as nothing** *[guards]*  
  Changing a stroke width in QGIS must not be reported as a recolour.
- **a dock reclassification lands while a run is finishing** *[race]*  
  QGIS's own Classify, pressed while a tiling is in flight.
- **a dock recolour outlives a retile, a save and a reopen** *[visual]*  
  The colour you changed in QGIS is still there tomorrow.
- **a dock recolour survives the reopen** *[race, guards]*  
  An edit adopted from QGIS's panel is still there tomorrow.
- **a dock refinement survives the next restyle** *[guards]*  
  What you changed in QGIS alongside a recolour is not thrown away.
- **a dropped column's ramp goes even when the style was derived** *[guards]*  
  The reset is about the COLUMN, not about who chose the style.
- **a forward ramp does not match a reversed row** *[guards]*  
  A reversed row must not mistake the forward ramp for itself.
- **a geopackage carries the no data opacity it was given** *[guards]*  
  The map that TRAVELS must be the map that was made.
- **a geopackage carries the same colours it was given** *[race]*  
  The export boundary, compared by COLOUR rather than by kind.
- **a graduated copy carries the catch-all** *[race, guards]*  
  Copying a classification brings the No data colour with it.
- **a graduated dock recolour survives the plugin being shut** *[guards]*  
  The same promise, with nobody listening at the time.
- **a hostile class source leaves automatic colours**  
  A broken QML chosen as a class source falls back, and says so once.
- **a kept renderer must still draw the column it is classed on** *[race, guards]*  
  Losing a scheme file must not cost the element its next variable.
- **a kept scheme is held rather than owned** *[race, guards]*  
  Colours kept for a missing file survive it, and give way when it returns.
- **a limit keeps the colours the ramp gives** *[guards]*  
  A floor or ceiling must not repaint the element.
- **a long category value truncates but is recoverable**  
  A long category name elides, and hovering shows it whole.
- **a mixed-magnitude legend prints real bounds** *[guards]*  
  No class of real width may print both its bounds as one number.
- **a moved class source survives a restyle** *[guards]*  
  A file that has gone is a reason to stop consulting it, not a reason to repaint the map -- on BOTH styling paths.
- **a no data colour comes home beside a class colour** *[guards]*  
  Two colours on one element must both survive a reopen.
- **a palette is usable whatever case QGIS spells it** *[guards]*  
  A ramp the installer skipped must still be findable.
- **a pick-less copy clears the class source** *[guards]*  
  A copy overwrites the class source like everything else it owns.
- **a pin and a class source do not meet** *[guards]*  
  A style excursion must not resurrect a pin on the way back.
- **a pin is never adopted onto the ladder's own edge** *[guards]*  
  A `low` on the floor is a class of no width, and QGIS may die of it.
- **a pin leaves no class for a tile to miss** *[guards]*  
  The reduction must count the pool a PIN actually leaves.
- **a pin may sit outside the data it classifies** *[guards]*  
  Limits WIDER than the column are the point, not a mistake.
- **a pinned class bound reaches the map** *[guards]*  
  A bound set by hand decides that break, and unpinning gives it back.
- **a pinned element draws what the library draws** *[visual, guards]*  
  The pin, checked in PIXELS rather than in renderer numbers.
- **a ramp the library lacks still draws a map and says so** *[guards]*  
  A ramp name QGIS cannot resolve must not stop the map being drawn.
- **a ramp twin's name does not move under the user** *[race, guards]*  
  A dock Apply never renames a ramp to its byte-identical twin.
- **a ramp you are offered is the ramp you get** *[guards]*  
  Choosing a ramp the dropdown offers has to leave you with it.
- **a renderer the row cannot name defers to QGIS** *[guards]*  
  The row always describes the map, even when the plugin stopped.
- **a reopened project keeps an imported class scheme** *[guards]*  
  A scheme imported from a QML must survive a save and reopen.
- **a reopened project reaches its own colours and pins** *[guards]*  
  File > Open under an open panel must not bind the wrong identity.
- **a reopened project still hears a recolour made in qgis** *[guards]*  
  The commonest journey there is must not be the deaf one.
- **a restyle and a reseed agree**  
  The fast path and a full regeneration produce the same symbology.
- **a restyle arrives while the geopackage is written** *[race]*  
  A ramp picked while the output file is being written.
- **a retyped column reclassifies the map** *[guards]*  
  Values edited in QGIS must reach the legend, not just the tiles.
- **a row without classes says so**  
  A row whose style has no class count must show a dash, not a number.
- **a second project does not take the first one's opacity** *[race, guards]*  
  Opening a saved project must not repaint it from the one before.
- **a second reconciliation adopts no colour the plugin painted** *[guards]*  
  A later repaint does not claim our own ladder as somebody's picks.
- **a single category still gets a colour**  
  A field with exactly ONE distinct value must still render.
- **a value on a break belongs to the class below** *[guards]*  
  The nudge is scoped, and nothing else may feel it.
- **an Unclassed excursion leaves the count alone** *[guards]*  
  Fifty is Unclassed's number, not the user's, and must not stick.
- **an edited class source reaches the map** *[guards]*  
  Editing your scheme and regenerating has to give you the new one.
- **an element follows the layer it takes its classes from** *[race, guards]*  
  A class source that is a LAYER survives the next re-tile.
- **an element layer is deleted between a generate and a restyle** *[race]*  
  One element's layer is removed by hand, then a ramp is picked.
- **an empty class keeps its place and its colour** *[guards]*  
  Fewer distinct values than classes: kept, reported, reachable.
- **an in-place recolour is heard through its repaint** *[race, guards]*  
  A dock recolour that fires NO style signal still reaches the row.
- **an opacity set in QGIS reaches the table** *[race, guards]*  
  QGIS owns opacity, so the dialog reads it rather than deciding it.
- **an unclassed row pins from either control** *[race, guards]*  
  Two controls name one end, and neither may go stale.
- **an unreadable source keeps the map at the landing** *[race, guards]*  
  A missing QML keeps the worn colours through a RE-TILE.
- **class breaks ignore nulls** *[guards]*  
  Nulls must not be classified as though they were numbers.
- **classification survives inf, NaN and huge** *[race, guards]*  
  Infinities, NaN and 1e308 in a column must not poison the breaks.
- **classifying leaves the layer as it found it**  
  The null workaround must not leak its filter.
- **colours a reader cannot separate are reported**  
  Elements whose colours collapse must be named, including for readers with a colour-vision deficiency.
- **deferral keeps opacity and the twin's own styling** *[guards]*  
  Two more things that must survive handing an element to QGIS.
- **deferral survives a project round trip** *[guards]*  
  A reopened project asks the RENDERER, not a stamp.
- **design choices that leave nothing to colour**  
  Two legal choices that remove the thing the colour code works on.
- **editing a category colour reaches the map** *[visual, race, guards]*  
  The whole point: a colour picked here is the colour drawn.
- **element opacity (layer opacity, authority, gpkg)**  
  Per-element opacity: QGIS layer opacity, driven from the table.
- **equal intervals stay equal under a pin** *[guards]*  
  Every class the same width, and the same ladder on two columns.
- **every scheme excludes nulls**  
  The workaround covers all the classification schemes, not one.
- **installed palettes span their declared colours** *[guards]*  
  An installed ramp runs between the colours the palette declares.
- **integration: GeoPackage style round trip** *[visual, guards]*  
  Export to GeoPackage, then read the file back from disk as QGIS would on a later day: the embedded styles must rebuild the same renderers, with the same ramp and the same class colours.
- **integration: categorical colour-source session** *[visual]*  
  The categorical counterpart of the interleaved session, on the packaged parcels fixture: variables, spacing and colour sources interleaved, with elements shifting between the two imported colour mappings, automatic ramps, and another loaded layer's symbology.
- **metamorphic: opacity round trip**  
  Softening an element and restoring it returns the same picture.
- **metamorphic: permuting variables permutes the map**  
  Swapping which variable feeds which element swaps the map's colours correspondingly, and changes nothing else.
- **metamorphic: reversing twice is the identity**  
  Reversing a ramp twice must return exactly the original map.
- **no data is one more colour in the element's editor** *[guards]*  
  Two layers in QGIS, ONE element in the dialog.
- **no placeholder fill is ever a clash** *[guards]*  
  Every colour standing for an absent value, not only the grey.
- **no value is ever orphaned by a classification** *[guards]*  
  No value may end up belonging to no class at all.
- **no-data features still draw after classifying** *[race]*  
  Hiding nulls to classify must not hide them on the map.
- **one colour means one value across elements** *[guards]*  
  The categorical twin of ONE VARIABLE GETS ONE LEGEND.
- **one variable gets one legend wherever it appears** *[guards]*  
  Two elements carrying one variable must class identically.
- **opacity before a run equals opacity after** *[visual]*  
  Softening an element before Generate must equal softening it after.
- **per-row class file pickers**  
  Categorized rows grey the Classes cell to the detected count, get a categorical ramp, and gain a class-source cell; the column follows.
- **plugin lifecycle (menu, action, unload)**  
  The QGIS entry points themselves: classFactory, initGui, the toolbar action opening the dialog, and unload.
- **quant picks die when the ramp is asked anew**  
  The destruction list for quant picks, and the one non-destroyer.
- **ramp and reverse commute** *[visual]*  
  Ramp then Reverse must paint the same map as Reverse then ramp.
- **reclassifying spares the absence colours** *[race, guards]*  
  A class-count change kills positional picks and not the catch-all.
- **recolouring the catch-all alone is not a new ramp** *[guards]*  
  The "no data" colour is a pick, not evidence of a classify.
- **renderer seeding (graduated + categorized)**  
  Each style produces the QGIS renderer it should.
- **reverse runs a qualitative palette backwards** *[guards]*  
  Reverse works on a PRESET SCHEME ramp, not only on a gradient.
- **taking an element back from QGIS restyles at once** *[guards]*  
  Picking a plugin style must replace the dock's renderer NOW.
- **the Classes cell reports its own element** *[guards]*  
  A greyed report must describe the row it sits beside.
- **the categorical editor offers no pin but does copy** *[guards]*  
  One control cannot appear here, and the other must.
- **the class count changes under an open quant editor** *[race]*  
  The classification changes under an open graduated editor.
- **the no-data grey is never a clash** *[guards]*  
  The colour nobody chose cannot make two elements confusable.
- **the quant editor edits class colours** *[visual]*  
  A graduated row opens the one editor in its quant mode.
- **the ramp a row names is the ramp the map draws** *[guards]*  
  The one thing the colourspace comparison no longer asserts.
- **the ramp and scheme lists are what they claim**  
  The offered ramps and schemes, asserted as whole sets.
- **the ramp cell agrees with the map** *[guards]*  
  Two descriptions of one truth, required to agree.
- **the ramp display range reinterpolates**  
  The Ramp Display Range moves the classes along the ramp.
- **the ramp spans the classes a tile can wear** *[guards]*  
  Pins outside the data must not spend the ramp on empty classes.
- **the range editor repaints with its own element's colours** *[guards]*  
  Moving the display range repaints with THIS element's colours.
- **the release notes keep their categories** *[guards]*  
  One changelog, two renderers, and it has to read in both.
- **the same class source chosen twice changes nothing** *[visual]*  
  Re-choosing the class source already showing must destroy nothing.
- **the spinner outranks a value the dialog itself wrote** *[guards]*  
  A carried-over opacity is only the user's if the user set it.
- **the unclassed list fades without a graphics effect** *[guards]*  
  The Unclassed editor must not composite its table offscreen.
- **unclassed never announces a reduction** *[guards]*  
  Quant: Unclassed draws its fifty steps, and says nothing else.
- **unclassed over a constant column** *[visual, race]*  
  Fifty linear steps across a range of zero.

## Design: units, families, modifiers

- **(visual gallery)** *[visual]*  
  A twill weave with strand skips and aspect < 1: the weave look depends on the gaps, so the background must show through inside the pattern (this is what the aspect control and '-' skips are *for*).
- **(visual gallery)** *[visual]*  
  The notebook's modifier chain: rotate 30, inset the prototile and the tiles.
- **(visual gallery)** *[visual]*  
  The slice family with a non-default offset: a hexagon cut into six pie slices starting from edge midpoints (offset=1) rather than corners.
- **(visual gallery)**  
  What was learned the hard way: pathological spacings must be refused before they reach GEOS.
- **Generate uses the design on screen** *[guards]*  
  Generate immediately after a design change must tile the NEW design.
- **UI affordances are deliberate** *[race]*  
  The small numbers that shape how the controls feel.
- **a copy leaves one number in every control** *[guards]*  
  After a copy, the cell, the record and the map must agree.
- **a dataset that cannot fill the design asks first** *[guards]*  
  The design-floor question, with both answers driven.
- **a family excursion brings the map back and not the excursion** *[visual]*  
  Try another design, come back, and the MAP is yours again too.
- **a negative scale factor mirrors the design** *[guards]*  
  What a negative scale factor DOES, measured rather than assumed.
- **a pin may be typed far outside the element it pins** *[guards]*  
  A bound the guard allows, the CONTROL must let a person type.
- **a pin survives the table being rebuilt** *[race, guards]*  
  A design change rebuilds every row 350 ms later; pins must ride it.
- **a pinned bound can hold the numbers a column carries** *[guards]*  
  The control must be able to REPRESENT the data's domain.
- **a refusal names a number the box beside it accepts** *[guards]*  
  The third of one family, and the last to be guarded.
- **a refused pin reverts and says so** *[guards]*  
  A bound the map cannot draw puts the control back, and reports.
- **a region edit is undone while a run is in flight** *[race]*  
  Ctrl+Z in QGIS while the worker is tiling the edited data.
- **a reopened dialog wears the design it left** *[race, guards]*  
  Closing the plugin and opening it again keeps the design.
- **a scale control steps over zero** *[guards]*  
  A scale of zero is never reachable, in either direction.
- **a spacing a person typed outlives a change of dataset** *[guards]*  
  Whose number is in the spacing box, across the journey that lost it.
- **an inset that eats the design says so** *[guards]*  
  A tiles inset large enough to swallow elements must name itself.
- **an inset that swallows tiles leaves no half map** *[visual, race]*  
  Insetting the tiles until there is nothing left to draw.
- **an over-large inset empties the design visibly**  
  Insetting a tile by more than its own width must say so, and be undoable.
- **auto spacing offers a round number**  
  The spacing the Auto button proposes must READ as a number a person would choose.
- **catalogue sweep (every family, every count)**  
  Every catalogue entry at every element count must construct a unit whose distinct tile ids number exactly n.
- **catalogue values are what they claim**  
  The catalogue's OPTION VALUES matter, not just its element counts.
- **controls respond without being prompted** *[race]*  
  Every control must act through its own signal, not because a test called the rebuild afterwards.
- **design cascade (n/kind/family/options/fit)**  
  The Design tab's coupled controls: element count repopulates the family list, a count offering only one kind silently flips the tiling/weave toggle, family choice shows exactly its own option rows, those options actually reach the built unit, and the dialog re-fits its height when option rows appear.
- **design controls are usable as designed**  
  The numbers that decide what a control can express.
- **element state follows the element count** *[race]*  
  State for elements that no longer exist must not reach the map.
- **element state survives a family change and back**  
  Try another design, come back, and find your work intact.
- **every control accepts the range it should** *[family, guards]*  
  What each control will let a user ask for.
- **every control starts where it should** *[family, guards]*  
  The value a user meets in each control, before touching anything.
- **every declared offset is pinned** *[guards]*  
  Each slice or dissection entry's offset is the one intended.
- **every design control is reachable** *[family, guards]*  
  Each control the Design tab offers is really in the window.
- **every element count 2..52 is offered, 17 included**  
  Every element count from 2 to 26 is on offer, 17 included.
- **every element count still has its designs** *[guards]*  
  The catalogue offers a design for every element count it claims.
- **every family shows its own options**  
  Each family's option rows appear for it and for no other.
- **every per-row control keeps its declared range** *[family]*  
  The ranges of controls that live in TABLE CELLS.
- **family option ranges track the family**  
  A family's options must be bounded by what THAT family accepts.
- **icon mode is not counted as a tiling** *[guards]*  
  Drawing one tile unit per area must not be refused as a tiling.
- **integration: interleaved styling and design changes** *[visual, race]*  
  The long session: styling, then data, then design, then styling again, generating throughout.
- **no control is dead** *[race]*  
  Every control a user can reach must change SOMETHING by itself.
- **pins hold across the whole family catalogue** *[guards]*  
  A pin on several families, because geometry is not the pin's business.
- **repopulating the family list fires no handlers** *[guards]*  
  Switching tiling/weave must not fire family-changed midway.
- **restyle without re-tiling (fast path)**  
  A style change must repaint the map, not lay it out again.
- **table and layout at the largest element count** *[visual, race, guards]*  
  The dialog at 26 elements: 26 rows, and the layout rule holds.
- **the dialog is keyboard navigable**  
  Tab from the first control reaches every enabled focusable one.
- **the file carries the design the map is wearing** *[race, guards]*  
  A restyle updates the file's styles; its record must follow.
- **the size guard at its refusal boundary** *[visual, race]*  
  The finest spacing a user can type unasked, and the next one down.
- **the size guard does not refuse fine patterns**  
  The estimate that protects against runaway tilings must not reject small ones.
- **the table's headers read as designed** *[guards]*  
  The nine column headings, in order, and no row-number gutter.
- **the window fits its design tab when shown**  
  Showing the dialog sizes it to the controls it contains, and no larger.
- **tile-count size guard** *[guards]*  
  A spacing that would produce an unreasonable tiling is refused.
- **two generates with different families keep their elements apart** *[race]*  
  A seven-element run in flight, then a four-element one on top.
- **two pin controls agree across a run landing** *[race, guards]*  
  Pin from the table mid-run: the strip, the map and the record agree.
- **two pin controls survive qgis moving underneath** *[guards]*  
  The region goes, the scheme switches, the two pins are hammered.
- **typing reaches the design** *[race]*  
  Typing in a design field must reach the unit on its own.
- **unload with windows open and work in flight** *[race, guards]*  
  Unload the plugin with a window open and a tiling running.

## Other

- **(visual gallery)** *[visual]*  
  Icon mode (as_icons=True): one tileable per region polygon at its centre, so the tile count is bounded by polygons x unit size and plenty of background remains between icons.
- **PyPI downloads happen only with consent** *[guards]*  
  Shipped code reaches PyPI only after the user has said yes.
- **QGIS still counts nulls as zero (canary)**  
  A canary.
- **a Reverse tick survives a rebuild while it is greyed** *[guards]*  
  The choice underneath a disabled switch, across a rebuild.
- **a bound is readable and typable at every magnitude** *[guards]*  
  Whatever a bound box SHOWS, a person can type back.
- **a bound the editor prints can be typed into its own box** *[guards]*  
  A number printed beside a box must parse in that box.
- **a candidate is published only when it is gated** *[guards]*  
  Four refusals, each one a hand-published candidate's near miss.
- **a candidate number is never reused**  
  A number belongs to a candidate for good, deleted zip or not.
- **a closed dialog writes nothing into the project** *[race, guards]*  
  A window the user has shut does not go on making maps.
- **a column appearing in QGIS keeps hand styling** *[race, guards]*  
  A field added in QGIS must not discard styling done in QGIS.
- **a column deleted in qgis takes its scheme with it** *[guards]*  
  The THIRD door into one ruling, and the only one that says a word.
- **a column that keeps its name and changes its kind** *[guards]*  
  Same name is not the same thing, and the plugin asks.
- **a committed edit session reaches the map** *[race]*  
  Values committed through an edit session redraw the live map.
- **a constant column with gaps**  
  Constant and full of holes at once: the two rules must compose.
- **a copied ladder is fitted to the column it lands on** *[guards]*  
  The end rules, and the monotonicity they must not break.
- **a copy and its pins survive a project round trip** *[guards]*  
  A copied ladder comes home, and so do the flags that came with it.
- **a copy carries the range and refuses what it would empty** *[guards]*  
  One copy, two targets: one takes the range, one cannot and is told.
- **a copy leaves behind a pin the data cannot carry** *[guards]*  
  A pin is a claim about THIS element's data.
- **a dataset switch keeps its promises on every route** *[guards]*  
  The switch matrix: routes crossed with state crossed with what happens next.
- **a dataset switch says what it re-points** *[race, guards]*  
  Switching datasets announces re-pointed variables, as its twin does.
- **a deferring element moved to words still draws** *[guards]*  
  Every mode must be a mode the guards downstream recognise.
- **a disabled plugin stops talking** *[race, guards]*  
  Disabling the plugin stops it, rather than merely hiding it.
- **a discarded pick does not come back** *[race, guards]*  
  What the plugin SAYS it threw away has to be thrown away.
- **a dropped column takes its whole scheme and it returns** *[guards]*  
  Ruling 3 and ruling 6 of 2026-08-21, driven as one journey.
- **a dropped table takes its saved style with it** *[race, guards]*  
  What leaves the file leaves whole -- the style row included.
- **a failed stage is not remembered** *[guards]*  
  Only a stage that finished says how long the work takes.
- **a far pin does not flatten the box a small one needs** *[guards]*  
  One pin must not decide how finely the other can be read.
- **a field's return wears its own style and keeps its picks** *[guards]*  
  The mode follows the field, so no re-click retires the picks.
- **a graduated dock refinement survives the next restyle** *[guards]*  
  The graduated half of the same promise, at the sibling site.
- **a hanging test is named, not silent** *[guards]*  
  Every test runs under a watchdog that would name it if it stuck.
- **a landing never writes over another dataset's map** *[guards]*  
  Two maps stay two maps, whichever one you generate next.
- **a large region is handled** *[race]*  
  A region with thousands of polygons, not nine.
- **a limit edit that draws nothing new still says so** *[guards]*  
  Every limit edit is announced, in whichever direction it goes.
- **a limit that refuses a pin retires the pin and says so** *[guards]*  
  When a limit and a pin cannot both be drawn, the limit stands.
- **a long session accumulates no stale state** *[visual, race]*  
  A scripted long session leaves no stale or unbounded state.
- **a map is filed under the dataset it was drawn from** *[race, guards]*  
  Whose map is this? Asked of the tiles, not of the chooser.
- **a moved limit re-splits the tiles** *[guards]*  
  Raising a floor must move the newly excluded tiles across.
- **a new project does not inherit the last one's pins** *[guards]*  
  The dialog outlives the project; its beliefs must not.
- **a new region drops a setup whose column has gone** *[guards]*  
  A scheme cut for a column the new data lacks must not survive.
- **a parked element keeps its work at the boundary** *[race, guards]*  
  A row on "---" still has a memory, and the memory crosses a save.
- **a pick is not swallowed by the live path** *[race, guards]*  
  The live tick's no-op guard, and what it was blind to.
- **a pin belongs to an element and a field** *[guards]*  
  Switch the variable away and back; the pin must come back with it.
- **a pin kept silently still reaches the stores** *[race, guards]*  
  A pin on a row wearing another style survives the boundary.
- **a pin outranks the one-value collapse** *[guards]*  
  Both doors into a pinned ladder draw the same ladder.
- **a pin still works on a copied ladder** *[guards]*  
  The two claims in one record must work TOGETHER, not just alone.
- **a pin survives a dock edit to another boundary** *[guards]*  
  Retyping one boundary in QGIS must not demote a pin elsewhere.
- **a pin survives a project round trip** *[guards]*  
  A pinned bound comes home, and the next Generate honours it.
- **a pin that cannot be drawn is refused** *[guards]*  
  The guardrails, each of which is a bound nothing could draw.
- **a pin the data moved under is released and said** *[guards]*  
  A pin the map has stopped honouring must stop being shown as set.
- **a project opened under an open dialog is not drawn over** *[guards]*  
  Open a project with the plugin open, and Generate must not double it.
- **a project round trip changes nothing a user chose** *[race]*  
  The pair that crosses a SERIALISATION boundary.
- **a project saved by an older plugin still opens** *[visual]*  
  A project stamped by an earlier version opens, and says what it lost.
- **a queued restamp never writes a blank the plugin imposed** *[race, guards]*  
  Losing the data must not erase the record of the map.
- **a region of exactly one feature degenerates honestly** *[visual]*  
  One polygon: every per-feature statistic has nothing to divide.
- **a release needs a matching candidate**  
  Promotion happens on proof, not on hope.
- **a release publishes from the branch the page comes from** *[guards]*  
  Tagging one branch while the page is served from another stops.
- **a reloaded module retires the old dialog cleanly** *[race, guards]*  
  A reloaded module still retires the dialog it replaced.
- **a resume is not snatched by its own tail** *[race, guards]*  
  An at-rest resume ends ON the resumed map, quietly.
- **a retired dialog rebuilds nothing when the project moves** *[guards]*  
  A dialog the user has finished with does no work for a project.
- **a retired dialog stops watching** *[guards]*  
  Only the LIVE dialog reacts to a dock edit; its predecessor is deaf.
- **a retired dialog's landing is discarded** *[race, guards]*  
  A run belonging to a replaced window changes nothing.
- **a retired pin leaves neither a stamp nor a silent neighbour** *[guards]*  
  The restyle path retires a dead pin from EVERY element, and stamps after.
- **a row with no geometry is named** *[guards]*  
  A blank row is dropped, and now the user is told which and why.
- **a run in flight does not land in the project that replaced it** *[race, guards]*  
  A run launched for one project must not land in the next one.
- **a run started before a change lands after it** *[race]*  
  The launch snapshot decides the run; the table decides what rests.
- **a saved map can be opened and carried on** *[guards]*  
  One you made earlier, without the project that made it.
- **a saved project keeps the other fields' work** *[race, guards]*  
  Work for a field not on show at the save survives the reopen.
- **a scale between minus one and one can be typed** *[guards]*  
  A handler that rewrites its own box eats keystrokes too.
- **a second dialog after a manual cleanup**  
  After the tidy-up, the next dialog starts from nothing.
- **a second generate asked for nothing changes nothing** *[visual]*  
  Generate pressed twice with nothing touched between must be a no-op.
- **a session of pinning and copying holds together** *[guards]*  
  The integration shape: many steps, checked at every MOMENT.
- **a stage log never shows the previous run** *[guards]*  
  A stage's log says it is running, not what it said last time.
- **a style pasted between elements carries its pins** *[guards]*  
  A copy through QGIS brings the ladder AND whose ladder it is.
- **a style pasted mid run survives the landing** *[race, guards]*  
  A run in flight must not eat an edit made while it runs.
- **a style picked by hand still follows a paste in QGIS** *[guards]*  
  One click before the paste must not deafen the row to it.
- **a style state machine leaves no stale map behind** *[visual, family]*  
  Nine styling edits on one element, checked after every one.
- **a style switch is not consent to lose a pin** *[guards]*  
  A record silent BY MODE is not a record of a choice nobody made.
- **a switched variable leaves no orphan in the file** *[race, guards]*  
  The file a user sends on holds the map they made, and only that.
- **a test creeping toward its ceiling is reported** *[guards]*  
  A healthy test close to its stall ceiling has to say so.
- **a toggle switch shows which way it is set**  
  The switch's knob is visible, and sits at the end that says so.
- **a withdrawn equivalence stops excluding its mutant** *[guards]*  
  Withdrawing an equivalence must put the mutant back, not just say so.
- **an act about one field spares another's ladder** *[race, guards]*  
  Releasing a copied ladder is scoped to the field the act is about.
- **an adopted ladder is stamped for a reopen** *[guards]*  
  A ladder retyped in QGIS must survive the project being reopened.
- **an edit session rolled back changes nothing** *[race]*  
  An edit session abandoned with rollBack leaves the map alone.
- **an element table carries only what it displays** *[guards]*  
  The file a colleague receives holds this map and nothing beside it.
- **an infinity alone still asks for the split** *[guards]*  
  The question that DECIDES the split must match the split itself.
- **an unassigned element beside elements sharing one field** *[visual, race]*  
  Three elements on the same column, and a fourth on nothing.
- **an undone edit is followed back** *[race]*  
  Ctrl+Z in QGIS, and the map that must follow it back.
- **changing to a graduated style cuts the split it needs** *[guards]*  
  The split is GEOMETRY, however much it looks like styling.
- **compat helpers cope with other QGIS versions**  
  compat's fallback branches, which this QGIS never reaches.
- **concurrent dialogs in two QGIS windows** *[race]*  
  Two dialogs alive in one project: one owner, one deaf predecessor.
- **copying between two elements and back is stable** *[guards]*  
  Copy a to b, then b to a: the map must not drift.
- **defaults avoid identifier columns**  
  The variable offered by default must not be a row id.
- **deferral does not erase the work behind it** *[race, guards]*  
  Handing an element to QGIS must not throw away what you set here.
- **dialog structure (tabs, help, controls)**  
  The dialog has the parts it is supposed to have.
- **each column is scanned once however many elements carry one** *[guards]*  
  Several elements on several columns cost one scan each, not one per tick.
- **element ids survive a case-insensitive path** *[guards]*  
  No two elements share a name once case stops distinguishing them.
- **element-id alphabet caps the count (upstream canary)**  
  Upstream still runs out of element ids, silently, at 702.
- **every QGIS harness can reach the provisioned libs** *[guards]*  
  A harness that runs under QGIS looks where the wheels were put.
- **every ceiling widens for a slow machine** *[guards]*  
  The timing factor accounts for the MACHINE, not only for sharding.
- **every documented command still exists** *[family]*  
  The maintenance documents' commands are real commands.
- **every element is told about its own legend** *[guards]*  
  One element must not answer for another, and neither may be silent.
- **every expected stage is actually run** *[guards]*  
  A stage the release lists must be a stage the release runs.
- **every legend says what its map does**  
  Every legend the plugin can seed agrees with its own map.
- **every number box holds a value finer than its step** *[guards]*  
  A display rule must never decide what a box can HOLD.
- **every numeric input at its limits** *[race]*  
  Drive each spin box to its minimum and maximum, and generate.
- **every reader of unplaceable agrees with the split** *[guards]*  
  The map and the sentences about it must count the same things.
- **extreme magnitudes render readable legends** *[guards]*  
  A legend over 1e-9 or 1e12 values must still be readable.
- **hand styling survives an unrelated change** *[race]*  
  Restyle one element in QGIS, change another, regenerate.
- **hand styling yields when its own element changes** *[race]*  
  The other half: change THAT element and the plugin takes it back.
- **limits the data moved out from under are dropped** *[guards]*  
  Limits that exclude everything must not empty the element.
- **model-based: the dialog's state machine**  
  Walk the dialog's state machine, checking the model at each step.
- **moving a bound off its computed value pins it** *[guards]*  
  The pin follows the bound, in both directions.
- **no shard waits on a pipe nobody is reading** *[guards]*  
  Sharded stages run at the same time as each other, not in turn.
- **nothing defines the same name twice** *[guards]*  
  A second definition silently replaces the first, and says nothing.
- **null features draw as no data**  
  A feature with no value must be drawn as having no value.
- **numeric conversions are exact** *[race]*  
  The unit conversions between the dialog and everything else.
- **one dataset spelt two ways is one dataset** *[race, guards]*  
  A stamp and a source that name one file are one dataset.
- **one dataset's memory never steers another** *[guards]*  
  No residue of one dataset -- column names included -- reaches another.
- **one dialog instance per session**  
  Only one dialog may be live in a QGIS session.
- **one file is one map however its path is spelt** *[race, guards]*  
  A file already open is taken over, whatever route names it.
- **one file spelt two ways is one destination** *[guards]*  
  Two spellings of one path must not read as a changed destination.
- **pinning redraws the window it was typed into** *[guards]*  
  A pin moves every break between the pins, so the window must say so.
- **provisioning says why a package could not be fetched** *[guards]*  
  A failed download names its cause, not just the package.
- **real-world data end to end (Auckland IMD)** *[visual, guards]*  
  The whole pipeline on a real dataset, not synthetic squares.
- **reopening a saved project does not replace its map** *[race, guards]*  
  Open a saved project, open the plugin, touch nothing: the map stays.
- **repeated generates hold the project steady** *[visual, family]*  
  Thirty generates leave the project exactly as big as one does.
- **repopulating a chooser does not duplicate it**  
  Rebuilding a dropdown must replace its contents, not append them.
- **resuming skips only what still holds** *[guards]*  
  --resume reuses a stage's answer, and refuses to invent one.
- **stamped records round trip through a real .qgz** *[race]*  
  All three stamped records survive an actual project file.
- **swapping two variables re-cuts both splits** *[race, guards]*  
  A boolean cannot see a field move, and a swap is an ordinary move.
- **switches and the editor answer the keyboard** *[visual, race]*  
  Space works a Reverse switch; Esc closes the editor honestly.
- **taking an element back keeps the fill you mixed** *[guards]*  
  The row follows a hand-mixed fill, and keeps following it.
- **the absence fills are told apart**  
  Each kind of unplaceable value must be readable AS a kind.
- **the coverage count matches the map** *[race]*  
  The "areas received no tiles" count, recomputed independently.
- **the coverage record survives the suite exiting** *[guards]*  
  The per-test record is written even though the suite os._exits.
- **the dependency consent says what it will do**  
  The one dialogue that asks to download and unpack code.
- **the dialog closed partway through a run** *[race]*  
  Closed mid-run, at every stage of the run.
- **the display range keeps every digit a user types** *[guards]*  
  A window the slider can reach, the boxes must let you type.
- **the documents' numbers match the code** *[guards]*  
  Numbers in prose rot, and nothing here was checking them.
- **the editor copes with the data going away**  
  Opening the editor for a field that no longer exists.
- **the editor hides nothing at any size** *[family]*  
  Sweep the sizes a real field can produce, and check nothing is cut off.
- **the editor is laid out as specified**  
  The window's shape: alignment, fixed pitch, and no dead space.
- **the editor lists every value and the no-data row**  
  What the little window contains.
- **the editor scrolls only past fifteen values**  
  Fifteen rows show; a sixteenth brings a scroll bar, not a taller window.
- **the map agrees with the table at every step** *[visual, race]*  
  Six edits, six generations, and the same question asked six times.
- **the mutation guard scales with the diff** *[guards]*  
  The changed-lines guard samples in proportion to the change.
- **the new-code guard refuses a baseline it cannot find** *[guards]*  
  "Nothing changed" and "I could not look" must not read alike.
- **the new-code mutation guard reports rather than gates** *[guards]*  
  The incremental guard does not stop a candidate any more.
- **the plugin opened before the data still works** *[guards]*  
  The order a user works in, which no other test here follows.
- **the release chart estimates from previous runs**  
  The chart quotes what the last run took, and says so honestly.
- **the release digest watches what ships**  
  The fingerprint covers the artefact, and only the artefact.
- **the release refuses a tree it did not measure**  
  The receipt gate, driven the way a maintainer meets it.
- **the release watchdog ignores a sleeping machine** *[guards]*  
  The stall watchdog reads a clock that stops when the machine does.
- **the release watchdog leaves a busy stage alone**  
  A slow but working stage runs to completion.
- **the release watchdog measures the whole tree**  
  CPU is summed over a process and everything it starts.
- **the release watchdog stops a stuck stage**  
  A stage using no cpu at all is stopped, and says why.
- **the retirement guard is asked the right question** *[race, guards]*  
  Two ways `_retire_an_undrawable_pin` destroyed work it should keep.
- **the return leg restores the chosen variable** *[race, guards]*  
  A-B-A with no Generate gives back the variable you chose.
- **the roadmap gate reads a statement, not a mention** *[guards]*  
  The clearance phrase must be SAID, not discussed.
- **the row agrees with the map about what it shows** *[guards]*  
  The second pair: what the row SAYS against what the map DOES.
- **the row follows the dock back out of deferring** *[guards]*  
  Reconciliation is one question, asked in both directions.
- **the same settings twice give the same map** *[visual]*  
  The same settings, run cold twice, must give the identical map.
- **the shelf does not survive the project that made it** *[guards]*  
  A closed project's set-aside scheme reaches no other project.
- **the size guard measures ground, not the bounding box** *[guards]*  
  The tile estimate follows the region's AREA, not its extent.
- **the split tells the kinds of absence apart** *[guards]*  
  A hole and an off-the-scale value are not the same statement.
- **the style is saved through the current API** *[guards]*  
  Styles are written with the call QGIS has not deprecated.
- **the tenth candidate is named the tenth** *[guards]*  
  A candidate's dossier and receipt bear the zip's own number.
- **the vendoring tool reproduces the current vendor**  
  The vendor is rebuildable, which is what makes patches patches.
- **the window fits the narrowest screen**  
  The layout rule, asserted end to end on the assembled window.
- **the window survives being resized small**  
  A user-shrunk window clamps at its minimums and clips nothing.
- **two areas on the same ground do not double the tiles** *[visual]*  
  Duplicate geometry: one place, two rows, and one tile each.
- **two columns sharing a table leave one style of ours** *[race, guards]*  
  The file carries the map's own work, and nothing it abandoned.
- **two columns with one pair of limits draw one ladder** *[race, guards]*  
  Equal intervals are cut from the limits, not from each column.
- **two editors in one session do not leak** *[guards]*  
  Open one element's editor, then another; nothing crosses over.
- **two projects in one session** *[visual]*  
  Project A closed, project B opened, in the one QgsProject.
- **undo inside a session behaves like rollback** *[race]*  
  Edit, undo to clean, commit: equivalent to never having edited.
- **value-laden records never cross a shared name** *[guards]*  
  A shared column name moves no value strings and no numbers.

## Output: layers, groups, GeoPackage

- **GeoPackage export with an fid attribute**  
  Exporting a map whose data carries an attribute called "fid".
- **QGIS still calls a dead layer valid (canary)**  
  A canary.
- **QGIS still gives a bare memory layer 4326 (canary)**  
  A canary.
- **a Generate spares the rest of the user's GeoPackage** *[guards]*  
  The plugin does not destroy data it did not create.
- **a GeoPackage loses the elements a design dropped** *[guards]*  
  A session that SHRINKS, and the file somebody else opens.
- **a GeoPackage reopens with its styles** *[race]*  
  The GeoPackage a user keeps, opened fresh.
- **a QML exported here reimports here** *[visual]*  
  A style this plugin writes is a style this plugin can read.
- **a change of dataset starts a new file and a new group** *[guards]*  
  B's map must not land in A's file, nor in A's group.
- **a comparison group is left alone by the session** *[visual, family]*  
  The map you kept for comparison must stop changing.
- **a dataset that leaves the project is still a dataset left** *[guards]*  
  Removing the region layer must not defeat the switch protections.
- **a dataset's files never name another's columns** *[guards]*  
  The GeoPackage built from B holds not one byte of A's names.
- **a destroyed dialog cannot be reached by a layer it made** *[guards]*  
  Destroying the dialog must not leave QGIS holding a dead pointer.
- **a dock edit of any kind reaches the exported file** *[guards]*  
  What a colleague opens must be the map you made.
- **a duplicated layer does not displace the one it copied** *[race, guards]*  
  Copying a layer in QGIS must not cost the map it copied.
- **a file already open resumes completely** *[guards]*  
  Resuming a file whose layers are open does what the twin does.
- **a first-field variable is not a special case**  
  A variable that happens to be the layer's FIRST field must work.
- **a fresh resume keeps its output out of the region chooser** *[guards]*  
  The other resume branch owes the same exclusion, and paid it.
- **a geopackage path with spaces and accents** *[visual]*  
  The output file is written where a non-English user asked for it.
- **a group choice waits for the run** *[race, guards]*  
  Choosing a group mid-tiling is refused in words, not obeyed.
- **a group restores its own state and no one else's** *[guards]*  
  Choosing a group whose record is SILENT clears what was in force.
- **a groupless project drops the replacement marker** *[guards]*  
  The being-replaced flag never outlives the replacement.
- **a hand styled no data layer survives a re-tile** *[guards]*  
  The twin keeps what the user gave it, on its element's terms.
- **a layer whose file moved is refused before it is read** *[race, guards]*  
  The file goes while the layer is open, and NOTHING reloads it.
- **a pinned element exports and reopens from a geopackage** *[guards]*  
  The other serialisation boundary: what a colleague receives.
- **a project and its geopackage move together**  
  The whole folder is zipped, sent, and opened somewhere else.
- **a project opened under an open dialog is taken over** *[guards]*  
  The dialog adopts the incoming project's group, not a new one.
- **a project opened under an open dialog keeps its no data layers** *[guards]*  
  The THIRD place that clears per-element state, and the one missed.
- **a project that already has forty layers** *[visual]*  
  A busy project: the combo, the group adoption and the clock.
- **a project whose output geopackage has moved** *[race]*  
  The .qgz comes back; the file its OUTPUT was written to does not.
- **a project whose region layer has moved** *[race]*  
  The .qgz comes back; the file its region layer read from does not.
- **a read-only and a full-disk output path** *[visual]*  
  A GeoPackage the operating system will not let us write.
- **a region with no features is declined in words** *[race]*  
  An empty polygon layer: legal, loadable, and nothing to tile.
- **a renamed group is adopted when the plugin reopens** *[guards]*  
  The same rename, met by a dialog that was not there when it happened.
- **a renamed group is still the group the next run replaces** *[guards]*  
  Rename the output group in the layers panel; the next run REPLACES it.
- **a reopened geopackage is written into again** *[race]*  
  Idea 35: export, tidy up, reopen the file, and map into it again.
- **a reopened plugin adopts the group it last wrote** *[guards]*  
  "Create as new group" exists so a result can be KEPT.
- **a reopened plugin does not mistake a no data layer for its element** *[guards]*  
  A paired layer carries its element's id, and is not that element.
- **a reopened project cannot overwrite yesterday's geopackage** *[guards]*  
  "Create as new group" protects the file across a session, not just within one.
- **a resume waits for the run** *[race, guards]*  
  The third door into the group machinery refuses mid-run too.
- **a resumed group is named for its dataset** *[race, guards]*  
  Opening a saved map names its group the way generating one does.
- **a resumed layer is named as a fresh one is** *[race, guards]*  
  Opening a saved map does not show the plugin's own plumbing.
- **a row follows a style pasted onto its layer in qgis** *[guards]*  
  The table must describe THE MAP, not the map the plugin last drew.
- **a saved project with temporary output** *[race]*  
  Saving a project whose map is temporary layers.
- **a user subset filter meets the null workaround**  
  A user's filter and the NULL workaround share one layer, safely.
- **a user subset survives regeneration** *[guards]*  
  A filter set in QGIS is carried onto the regenerated layer.
- **an embedded style name fits its column** *[guards]*  
  The style saved into a GeoPackage keeps a name GDAL can hold.
- **an emptied region layer does not raise** *[guards]*  
  Deleting every feature is legal, and must not throw.
- **an exported GeoPackage is recognised as our own** *[guards]*  
  The stamps have to be INSIDE the style the file carries.
- **an output group carries the whole working state** *[race, guards]*  
  Choosing a group again gives back the map that group describes.
- **automatic first render on layer choice** *[guards]*  
  Choosing a layer should populate variables and render unaided.
- **both halves of an element fade together** *[guards]*  
  An element is ONE thing to a reader, however many layers it is.
- **deleting and undoing our group in the layers panel**  
  The plugin copes when its output is deleted, then resurrected.
- **dialog end-to-end (generate, regen, gpkg)**  
  One ordinary session: layer in, variables assigned, map out.
- **integration: region layer switch**  
  Switching region layer mid-session: variables repopulate from the new layer, a stale assignment does not survive into it, and the plugin's own outputs are never offered as a region (the bug where a second dialog tiled the first one's output and every assignment shifted).
- **integration: second dialog session** *[guards]*  
  Close the dialog and open a fresh one, as a user does across a work session: the new dialog must adopt the existing output group rather than starting a rival one, and generating again must leave a single group with the same element layers replaced in place.
- **integration: weave with icons and outlines** *[visual]*  
  A weave session with the map switches on: over-under and strand width reach the geometry, icon mode places one unit per polygon, and the outlines layer rides on top of the group.
- **keeping a result keeps both halves of every element** *[guards]*  
  "Create as new group" exists so a result can be KEPT, entire.
- **keeping a result keeps its file however it was kept** *[guards]*  
  The panel and the GeoPackage are protected by one question.
- **layer <-> GeoDataFrame roundtrip**  
  A QGIS layer becomes a GeoDataFrame and comes back intact.
- **output layers are named and grouped as specified** *[race]*  
  Layer names and the group carry meaning, so they are asserted.
- **output layers carry spatial indexes** *[race, guards]*  
  Every element layer the plugin creates is spatially indexed.
- **output management (outlines, selective reseed, groups)** *[guards]*  
  Regeneration behaviours around the layer group: the outlines layer appears and disappears with its checkbox, hand styling survives changes to OTHER elements (selective re-seed), and the comparison flow adds a second group while leaving the first intact.
- **region outlines are cased** *[guards]*  
  The outline is a black line over a wider white one.
- **removing the group leaves the project clean**  
  Deleting the output group leaves nothing of ours behind.
- **switching region layer counts as a change** *[race]*  
  Changing the region layer must force a re-tiling.
- **the GeoPackage opens cleanly in a fresh process** *[visual]*  
  The .gpkg is a whole map to a QGIS that never saw this session.
- **the file shows the limit of what it contains** *[race, guards]*  
  The GeoPackage never carries work for fields it does not display.
- **the file's record follows a dock edit** *[race, guards]*  
  The GeoPackage does not disagree with itself.
- **the group-unit rulings hold on every route** *[guards]*  
  The four boundaries the group-unit rulings cross, as one matrix.
- **the map group sits on top of the layers panel**  
  A freshly generated map must land at the TOP of the layers panel.
- **the map survives its file being deleted** *[race, guards]*  
  A deleted output file costs a re-save, never the map.
- **the outlines layer is drawn as specified**  
  The region outline: cased, and cased the right way round.
- **the output carries no working state**  
  A shipped layer holds what a reader needs and nothing of ours.
- **the output group is deleted while a run is in flight** *[race]*  
  The user tidies the layers panel with a tiling still running.
- **the output group is renamed while a run is in flight** *[race]*  
  The user renames the output group while a tiling is running.
- **the plugin never offers its own output as a region**  
  The region chooser must not list the layers the plugin itself made.
- **the plugin survives its own output as input** *[race]*  
  Our own element layers are never offered back as the region.
- **the region layer goes while the quant editor is open** *[race]*  
  The region layer is removed in QGIS with the editor still up.
- **the spacing follows the coordinate system**  
  Change the layer's CRS and the spacing stops meaning what it meant.

## Robustness and the world outside

- **QGIS changes around the plugin** *[race, guards]*  
  QGIS itself moving while the dialog is open.
- **a GeoPackage is rewritten and renamed** *[race]*  
  Writing over an existing GeoPackage, and awkward names in it.
- **a comma-decimal locale does not corrupt numbers**  
  The plugin must work where the decimal separator is a comma.
- **a layer that refreshes itself**  
  A layer QGIS reloads on a timer is followed.
- **a layer that will not say how big it is** *[race]*  
  A source the plugin cannot inspect is a snapshot, and admits it.
- **a reprojected layer is followed** *[race]*  
  The region reprojected in place: new coordinates, same ground.
- **a resume does not recover onto our own outlines layer** *[race, guards]*  
  Recovery finds the user's data, not the plugin's copy of its name.
- **an area with no value is drawn rather than left as a hole** *[guards]*  
  A missing value must READ as unknown, not as absent.
- **an element sitting wholly on missing values still draws** *[guards]*  
  `missing.all()` is a fact about an ELEMENT, not about a column.
- **attribute names survive the round trip**  
  Awkward column names reach the output, or the user is told.
- **awkward attribute values keep their meaning** *[race]*  
  Attribute mess that a fixed column type cannot prevent.
- **awkward geometry (invalid, holed, multipart, null)**  
  Geometry the bridge must cope with regardless of the packaged data: a self-touching (invalid) ring, a polygon with a hole, a multipart feature, and null attribute values.
- **awkward layers are handled or declined** *[race, guards]*  
  Data a stranger might hand the plugin: five awkward shapes.
- **bound columns format sanely in any locale**  
  _format_bound's contract, under a comma-decimal locale and abuse.
- **data changed in QGIS while the plugin is open** *[race, guards]*  
  A user edits the layer in QGIS while the dialog is open.
- **dependencies present / wheel logic**  
  Every package the plugin needs is present in this QGIS.
- **deps/compat support logic**  
  Pure-logic corners of deps.py and compat.py that no network or dialog is needed to exercise.
- **hostile data does not defeat the plugin**  
  Load the data users actually bring, each in its own process.
- **hostile numbers are handled or declined** *[race, guards]*  
  Layers whose arithmetic is hostile, not whose geometry is.
- **hostile stored properties never break adoption**  
  Malformed stamped records never stop a dialog opening.
- **pins hold against awkward columns** *[guards]*  
  Nulls, one value, two values, and the non-finite corpus.
- **release timings never break a release**  
  A broken or missing timings file costs an estimate, not a run.
- **the deps installer declines a corrupt wheel** *[guards]*  
  A wheel that will not open is declined, not raised.
- **the layer changes without being edited** *[race]*  
  A layer can stop matching the map without anyone editing it.
- **the plugin in another locale** *[race]*  
  The dialog under a locale where a comma is a decimal point.
- **the plugin under a non-English locale** *[visual]*  
  German decimals and an Arabic right-to-left layout.
- **the report generators survive hostile docstrings** *[visual, family, guards]*  
  The generated documents cannot quietly lose a test.
- **the spacing advice can be typed back in any locale** *[guards]*  
  The number a refusal prints must survive being typed back.
- **the spacing box at its extremes** *[race]*  
  The spacing spinner driven to both ends of its range.
- **the tile estimate is honest where shapes are awkward** *[visual, guards]*  
  The size guard's estimate never falls below the tiles produced.
- **the user changes the data underneath** *[race]*  
  QGIS-side changes while the dialog is open must not defeat it.

## Runs, races and concurrency

- **a boundary retyped beside a recolour is still recorded** *[guards]*  
  Two edits in one visit are two edits, not one that cancels both.
- **a cancel storm leaves a usable dialog** *[visual, race]*  
  Ten Generate/Cancel alternations, then one clean run.
- **a dock edit during a run is not lost** *[race, guards]*  
  A dock recolour made mid-run is adopted when the run lands.
- **a finished run leaves nothing armed** *[race]*  
  When live update is off, finishing a run must not queue another.
- **a new run always shows real progress** *[race, guards]*  
  A run reports a percentage, even after one that died mid-output.
- **a pick survives a switch inside the debounce** *[race, guards]*  
  A choice made and left within the live window is still a choice.
- **a pin reaches the map through the live path** *[race, guards]*  
  Live update must apply a pin, not merely record it.
- **a queued live rerun happens once and stops** *[race]*  
  A rerun remembered during a run is consumed, not left standing.
- **a ramp chosen during a run is not lost** *[race, guards]*  
  What a mid-run choice is owed, which is less than a repaint.
- **a sequence of edits under the plugin** *[race, guards]*  
  Several edits in a row, with maps drawn in between.
- **adversarial sequences** *[race, family]*  
  Every dangerous pairing of "start work" and "interfere with it".
- **cancelling frees the dialog at once** *[race, guards]*  
  Cancel gives the dialog back immediately, not eventually.
- **choice persistence and zombie recovery** *[race, guards]*  
  Two more state-keeping behaviours: a row's class-source choice survives a Design-tab rebuild (choices live in dicts keyed by tile id, not in the replaced widgets), and reopening the dialog recovers from a zombie task (a cancelled task left in _task must not block future runs).
- **editing colours never rebuilds the table** *[race]*  
  The chooser-race rule, applied to the new control.
- **exact overlap ties break the same way every time**  
  A tile split 50/50 between two areas must fall the same way twice.
- **fuzz: random interaction keeps the invariants** *[race, family]*  
  Random sequences of interaction, checked against invariants.
- **integration: cancel and recover** *[race]*  
  Cancelling a run mid-flight (what closing the dialog does) must leave the plugin able to generate again: no task stuck in _task, the Generate button enabled, and the next run completing normally.
- **integration: live update session**  
  A live-update session: switch live on, make a design change, and let the debounce fire.
- **live update gates (gpkg, comparison mode)** *[guards]*  
  Live update must decline to run when output goes to a GeoPackage (regenerating a file on every tweak would hammer the disk) and when the comparison checkbox is on (it would spawn a group per tweak).
- **live update is on by default** *[race, guards]*  
  A first map appears without anyone pressing Generate.
- **live update notices the data changing** *[race, guards]*  
  With live update ON, an edit to the layer must reach the map.
- **one session leaves one group** *[guards]*  
  Choosing a file after live update has drawn does not fork the map.
- **palette pick survives debounce (no chooser race)** *[race, guards]*  
  Regression for the 'race among choosers': picking a ramp must not schedule a table rebuild, so the row widgets remain the SAME objects after the debounce window and a pick made while another chooser was open can never land on a destroyed widget.
- **race: a class colour picked during a run is not lost** *[race, guards]*  
  The graduated twin of the race above, which was never written.
- **race: a colour picked during a run is not lost** *[race]*  
  A race: the editor is open, and the run underneath finishes.
- **race: change during the output phase** *[race]*  
  A change made while the LAYERS are being built.
- **race: close during a run** *[race]*  
  Closing the dialog mid-run must cancel cleanly: no output arriving after the dialog is gone, no wedged state, and a fresh dialog able to generate immediately afterwards.
- **race: every control changed mid-tiling** *[race]*  
  Change EACH control while a tiling is in flight, one at a time.
- **race: region layer removed during a run** *[race]*  
  The region layer disappearing mid-run must not take QGIS with it.
- **race: restyle during a run** *[race]*  
  A restyle must refuse while a tiling is in flight.
- **race: settings changed during a run** *[race, guards]*  
  Change settings while a tiling is in flight.
- **race: two Generate presses** *[race]*  
  Two Generate presses in quick succession must produce one run, then at most one rerun -- never two tilings racing each other into the same layer group.
- **run lifecycle: no overlapping runs**  
  One run at a time, including while its layers are being built.
- **staggered actions during a run** *[race]*  
  A second action DURING a run, at every stage of the run.
- **staggered dock edits during a run** *[race]*  
  Dock edits at every phase of a run converge to one honest state.
- **stress: fast interaction (race hunt)** *[visual, race]*  
  Fast clicking, deliberately: fire the sequence of changes an impatient user makes — spacing, family, variables, ramps, styles, switches — with no pauses, while live update is ON, then let everything settle and check the plugin is coherent.
- **stress: styling, rendering and sizing on complex data** *[visual]*  
  Styling, rendering and sizing fiddled with repeatedly on the categorical fixture: spacing swept from coarse to fine and back, styles cycled through every mode, class counts changed, ramps swapped, all with generation in between.
- **the dialog's chrome does its job**  
  The parts of the window that are not the design: the region chooser's filter, the idle progress bar, and the Close button.
- **the plugin is unloaded during a run** *[race]*  
  QGIS disables the plugin while a tiling is in flight.
- **the preview wait widens for a slow rebuild** *[race, guards]*  
  The preview debounce is a floor, and a slow machine widens it.
- **the project is cleared during a run** *[race]*  
  File > Close Project, with a tiling still in flight.
- **timers firing under the open colour picker** *[visual, race]*  
  Both debounces expire inside the colour picker's nested loop.

## The categorical colour editor

- **Custom follows the field and the class source**  
  Two consequences of the Custom rule, and the class-source case.
- **Edit colours column appears with categories**  
  The column exists only where there is something to edit.
- **Reverse carries the absence colours** *[race, guards]*  
  One Reverse click keeps a hand-picked No data colour.
- **Reverse permutes quant customization**  
  Reverse mirrors the window and permutes the picks; twice is home.
- **a categorical copy overwrites the target's picks** *[race, guards]*  
  A copy from a pick-less source clears the target's hand-picks.
- **a customized element reads Custom in the ramp cell**  
  The ramp cell of a categorized row reads Custom -- display only.
- **a dock edit while the editor is open stays sane**  
  A dock recolour landing under the open colour editor stays sane.
- **a ladder somebody else cut makes the scheme cell read custom** *[guards]*  
  The Style cell stops naming a scheme the map is not cut by.
- **a new ramp discards hand-picks and says so**  
  Choosing a ramp means starting that element's colours over.
- **a picked colour changes the rendered map** *[visual, guards]*  
  End to end: pick a colour, and the PIXELS change.
- **a pin set during a run is not lost** *[race, guards]*  
  The third thing written through the colour editor, and the race.
- **a renamed column does not destroy hand-picked colours** *[guards]*  
  A rename in QGIS is not a decision to throw work away.
- **a saved project brings back its colours** *[race]*  
  Hand-picked colours survive being saved, closed and reopened.
- **an Unclassed swatch reaches both ends of its ramp** *[guards]*  
  The Custom swatch for Unclassed shows the whole selected range.
- **an element count round trip restores customization** *[visual, race]*  
  n 6 -> 4 -> 6 brings the absent elements' work back, by design.
- **deferral closes the colour editor under it** *[guards]*  
  A window whose renderer has just stopped existing must not stay open.
- **every swatch in the ramp column is built the same way** *[guards]*  
  Named ramps and Custom are drawn by one construction.
- **hand picks survive a column appearing in QGIS**  
  Adding a field in QGIS refills the choosers and keeps the work.
- **hand-picked colours are written into the project**  
  They outlive the session, because the dialog does not.
- **hand-picked colours do not bleed across families**  
  A colour picked for one design must not appear in another's.
- **hand-picked colours survive a regenerate**  
  Pressing Generate again keeps them.
- **hand-picks are kept per variable**  
  Switching an element's variable away and back restores its work.
- **quant customization survives the project**  
  Picks and window are stamped on the layer and adopted back.
- **retirement closes the colour editor** *[race, guards]*  
  A second dialog closes the first one's open colour editor.
- **style follow, ramp memory, single colour**  
  The per-row style/ramp state machine: untouched styles follow the variable's type; a hand-picked style sticks except when impossible; the ramp cell remembers its last quantitative and last categorical choices across style flips; a picked single colour survives a Design-tab rebuild.
- **symbology edited in QGIS while the editor is open** *[race]*  
  QGIS restyles the output while the colour editor is open.
- **the colour editor opened partway through a run** *[race, guards]*  
  Open the editor and pick a colour at each stage of a run.
- **the colour editor opens on a column with no values** *[guards]*  
  Three clicks, no Generate, and the editor used to raise.
- **the colour editor opens on an element with infinities** *[guards]*  
  Every absence row is a row without bounds, not just the first.
- **the dock reapplying the same ramp discards the hand-picks** *[guards]*  
  A clean ramp applied in QGIS wins, even when the dialog already names that ramp.
- **the editor column appears for quant rows**  
  The Customize column serves graduated rows too.
- **the editor copes with thousands of categories**  
  The Categorical colour editor, given a field with 2,000 values.
- **the element count changes under an open colour editor** *[race]*  
  The design loses an element while its colour editor is open.
- **the field goes away under an open colour editor** *[race]*  
  The variable is deleted in QGIS while its colour editor is open.

## The preview widget

- **UI vs library: glyph scaling**  
  The glyph checkbox, which changes what scaling MEANS.
- **a class no tile wears is said out loud** *[guards]*  
  Emptiness is reported in WORDS, which is what the swatch stopped saying.
- **a copy asks before drawing a colour for every value** *[guards]*  
  A hundred swatches is a question, not a thing to discover.
- **a correction the dialog makes reaches the preview** *[race]*  
  A choice the plugin changes on the user's behalf reaches the design view, not only the chooser.
- **a deferring row shows the colours QGIS is drawing** *[guards]*  
  The swatch samples the map, and follows the styling panel.
- **a ramp swatch is drawn once and follows the library** *[guards]*  
  One draw per ramp and direction, and a fresh one when a ramp moves.
- **a reversed row keeps its reversed swatch through a rebuild** *[guards]*  
  The swatch a user reads an element by must match the map.
- **an unassigned element previews as it draws** *[guards]*  
  The preview's one job is to be the map, before you commit.
- **every restyle door repaints the preview** *[race, guards]*  
  The restyle-then-refresh pair travels to all four doors.
- **opacity in the preview (floored)**  
  The preview shows opacity, floored so the design stays readable.
- **preview colours follow the variable** *[race]*  
  Assigning a variable must recolour the preview, not just the map.
- **ramp swatches and palette installation**  
  The colour ramps a user picks from: installed once, and shown.
- **ramp swatches run the right way round** *[guards]*  
  A swatch shows the ramp in the direction the map will use it.
- **reverse ramp column (per element, no re-tiling)**  
  The Reverse column: present beside the ramp, greyed where there is no ramp to reverse, gone entirely when no element has one, and actually running the ramp the other way on the map (and on the row's swatch) when ticked.
- **the design view draws no tile outlines** *[visual, guards]*  
  The preview shows areas of colour, not a mesh.
- **the design view paints colours the map contains** *[guards]*  
  The preview stands for the map, and three things could stop it.
- **the pin shows which way it is set** *[visual, guards]*  
  The glyph's two states must be tellable apart, not merely differ.
- **the preview actually draws what it is given** *[visual, guards]*  
  The design view renders: filled, fitted, centred and smoothed.
- **the preview agrees with the map it predicts** *[guards]*  
  The third pair: the design view against the map it stands for.
- **the preview follows the live restyle** *[race, guards]*  
  The design preview rests on what the map draws, not one act back.
- **the preview follows the modifier chain**  
  A modifier that changes the map must change the design view.
- **the preview shows the design the map draws** *[race]*  
  The design view and the map must be the same design.
- **the swatch marks every end a person set** *[guards]*  
  Four ends, four edges, and none of them silent.

## What the user reads

- **a changed category count warns that colours moved** *[race]*  
  When a categorical field gains or loses a class, say so.
- **a constant column draws one class and says so** *[race, guards]*  
  A column with one value everywhere gets one class, and a notice.
- **a coverage notice quotes a spacing the box accepts** *[guards]*  
  Both coverage sentences punctuate their spacing for this QGIS.
- **a group is named for the dataset it was made from** *[race, guards]*  
  The layers panel says which map is which.
- **a no data layer says which dataset it came from** *[race, guards]*  
  The twin carries its element's provenance, as every output does.
- **a quantitative style never stands on text** *[race, guards]*  
  A quantitative style on a field of words must not paint nothing.
- **a recoded category reaches the legend** *[race, guards]*  
  Retyping a text value in QGIS reclassifies the map.
- **an unclassed row is not warned about its fifty steps** *[guards]*  
  Fifty faded slivers are a ramp, not fifty classes going unworn.
- **colour legibility warnings are opt-in** *[guards]*  
  The colour-legibility check runs only when it is asked for.
- **default ramp pairs against our own legibility bar**  
  The plugin's own defaults trip the plugin's own warning.
- **every control explains itself** *[family, guards]*  
  Each control carries a tooltip, and the tooltip says something.
- **every notice describes the map it came from** *[guards]*  
  Each notice's numbers and names match the map state it describes.
- **free-text inputs survive nonsense** *[race]*  
  The boxes that take prose, given prose that means nothing.
- **icon mode says when an element has no icon for an area** *[guards]*  
  The coverage notice asks a MAP-WIDE question, and icon mode hides in it.
- **icon mode says whose value an icon carries** *[guards]*  
  In icon mode the coverage notice must not claim areas are missing.
- **perception warnings point the right way**  
  Golden colour pairs: the clash check errs in neither direction.
- **preview fits unit and shells; labels at app type size** *[visual]*  
  The preview must show the WHOLE tile unit plus any context shells, rescaling as shells are added rather than letting the pattern run off the edge, and its element ids must be set in the application's own type size.
- **quietening the log hides nothing** *[guards]*  
  The swallowed Qt message is the one named, and only that one.
- **the cardinality warning fires on the side it should** *[visual]*  
  Sixty categories is fine; sixty-one is worth mentioning.
- **the constant notice counts the user's areas** *[guards]*  
  The sentence and the legend beside it must count the same thing.
- **the coverage notice counts what the map is missing** *[guards]*  
  The number in the sentence is checked against the map itself.
- **the help tab names real controls**  
  Every control the help text names exists in the dialog.
- **the legibility check agrees with its own distance** *[guards]*  
  The clash search computes what `distance` computes, and quickly.
- **the map says which areas it left out**  
  A coarse spacing drops small areas; the map must say how many.
- **the output group chooser binds to the dataset** *[guards]*  
  The dropdown says which map a run will land in, and both ways round.
- **the preview draws the middle of the patch** *[race]*  
  With context shells on, the preview must include the CENTRE unit.
- **the region layer removed from a project of any size** *[guards]*  
  The plugin follows its region layer being removed, at any size.
- **the removal notice survives the chooser moving first** *[guards]*  
  Two Qt handlers, no promised order, and only one right answer.
- **the size guard warns where it used to refuse** *[race, guards]*  
  A big map is a question; a broken design is still a refusal.
- **the switch notice owns every element** *[race, guards]*  
  Each element gets the sentence that is true of it.
- **two notices from one run both survive** *[guards]*  
  A single run can have several things worth saying about it.
- **two notices never share one slot**  
  Several things worth saying, said at once, none lost.
